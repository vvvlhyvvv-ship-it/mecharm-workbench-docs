# CR-2026-03 变更单：三维模型导入能力方案吸收（原生转换链／双缓存／碰撞两段制／离线预处理管线／P0 实测）

| 字段 | 内容 |
|---|---|
| **变更单编号** | CR-2026-03 |
| **提出日期** | 2026-09-15 |
| **提出方** | WorkBuddy（指挥方） |
| **批准方** | @user（项目经理） |
| **状态** | **待批准（书面确认）** —— 本单所含调整已按 2026-09-15 @user 指示（"就路线 B 根据最新方案调整前端包及分工执行方案，方案中需要的调整按你的建议修改"）**先行落入任务包文档**，本单为审计留痕；批准后补"已生效"标记 |
| **被变更文件** | `前端任务包\Tasks\T05_模型导入与视口显示.md`（整卡扩写）；`前端任务包\Tasks\T08_碰撞三态与禁发双阻断.md`（整卡扩写）；`前端任务包\03_架构设计_完全重构_V1.0.md`（§3 原则块、§8 组件准入、§9 修订记录）；`前端任务包\04_多会话并行分工指导_V1.0.md`（§6.4 T05 派单卡、§7.2 新增 G11–G15）；`前端任务包\99_进度与验收.md`（状态表备注、外部依赖节） |
| **技术附件** | **已归档进仓**（原始来源＝微信缓存与桌面临时文件，缓存会被清理，故入库留存）：`参考资料/三维模型导入能力综合技术方案_张晓东_2026-09-15.md`（下称《方案》）；`参考资料/综合技术方案-审阅意见_V1.0.md`（下称《审阅意见》） |
| **流转依据** | 《软件开发规则与合规交付要求》R-7.1.2；《大型工控软件开发规程》R-8.2（变更须走书面流程）；招标第 16 条实质性要求（16(1)(2)(3)） |
| **编号说明** | `CR-2026-02` 已被 G8 预案占用（热点文件包化留痕用，见 CR-2026-01 CR-4），故本单编号为 **CR-2026-03**，避免撞号 |

---

## 一、变更理由

1. **方案与既有路线的关系：一致，且互为补强。**《方案》的核心结论"解析归原生、渲染归 Web"（混合架构）与本项目既有技术路线（03 架构 §1 结论表：OCCT 原生几何内核 + PySide6 外壳 + 内嵌 three.js 视口）及《方案》§5.3 的 C2 外壳方案**同构**。因此本次吸收不是换路线，而是把《方案》对现场约束的发现与操作化路径并入既有任务包。
2. **口径澄清（防误读，必须在留痕中写明）**：《方案》所称"路线 B"指**全原生栈**（OCCT 原生 + AIS/VTK 渲染），与本项目路线 B（OCCT 原生 + **内嵌 three.js 视口**）**不是同一事物**；《方案》最终得出的混合架构结论恰好落在本项目既有路线上。引用《方案》时不得把其对"路线 B 渲染掉帧"的论证误读为对本项目视口方案的否定。
3. **《方案》带来的现场约束（本变更的直接动因）**：固定设备（压机/机械臂/加热炉/液压站/工装）可交付前离线预处理；**客户模型（模具/锻件/工件，单件可达 200MB 以上）由客户现场直接导入、无预处理环节** → 导入能力必须具备"运行时吃下 200MB 级单文件"的工程设计，且固定设备与客户模型走两条不同处理路径。
4. **附带登记一项待裁决缺口（G11）**：契约 §5.2 数据结构上限 8 轴，与实机 22 个受控运动轴不符（详见 04 §7.2-G11 与 §五 待裁决事项）。

---

## 二、变更项

### CR-1 T05 卡扩充（模型导入与视口显示）
- 全部文件走**原生转换链**（装配树→三角化→简化→LOD→轻量化网格→缓存）；**禁引入前端 WASM 直解路径**（occt-import-js 不引入，决策见 04 §7.2-G14）。
- **转换单次通过**：三角化完成后立即释放 XDE/B-Rep，峰值内存 = max 而非两者之和。
- **双缓存**：`.brep`（几何真身，拾取/精判按需重载）＋ 轻量化网格（显示），源文件哈希为键，**二次导入秒开**；缓存命中不跳过 is_brep 判定与日志。
- 三阶段进度条（解析/三角化/优化）；按工位按需加载，严禁整线模型全量常驻。
- **固定设备离线预处理管线**（新承接项，G12）：交付前一次性生成资产库（轻量化网格＋LOD×3＋碰撞代理＋缓存），随安装包交付；工具脚本放 `tools/`（新增文件名先报备）；暂定目标单件 ≤10MB、整线 ≤500MB。
- **P0 导入性能实测**（G15）：口径＝解析耗时／峰值内存／三角面数／几何面数／曲面类型分布／二次导入耗时；**不阻塞 T05 主体开发，但阻塞对外规格承诺——P0 完成前一切导入性能数字标"暂定"**。
- 弦高容差等参数进 `machine.yaml`（由指挥会话增补字段，ZCode 禁自改该文件）。

### CR-2 T08 卡扩充（碰撞三态与禁发双阻断）
- **权威判定保持 core 侧 OCC 几何级精判不变**；《方案》§3.4"前端加速结构精筛只作用于简化碰撞代理"的口径**否决为下发依据**（G13）。
- **补保守包络条款**（契约 §8.4）：碰撞检测时机械臂几何留保守包络 2–5mm（或按 PLC 轮廓偏差参数）；参数 `collision_envelope_mm` 进 `machine.yaml`（指挥会话增补）。——**原 04 §7.2-G4 挂账随本单一并落实**（此前仅 99 台账备注，任务卡正文未含）。
- **前端预检层列为可选增强**（T08 步骤 7，后置或 T10 收口期实施，不属必交付）：three-mesh-bvh 作用于简化碰撞代理，仅作交互即时提示；**不构成判定、不作为下发依据、不上屏为最终结论**。

### CR-3 03 架构增补
- §3 增"导入与碰撞的性能化处置原则"块（原生转换链／单次通过释放／双缓存／碰撞两段制／两条导入路径产物形态一致／性能数字暂定口径／按需加载）。
- §8 组件准入增：trimesh（MIT，随 T05）、Draco/meshopt（Apache-2.0/MIT，随 T05）、three-mesh-bvh（MIT，**仅当 T08 后置预检层启用时引入**）；登记 **occt-import-js 不引入（❌）**。均符合既有准入口径（宽松许可、本地分发、无收费 SDK、无 GPL/AGPL）。
- §9 修订记录加 V1.2。

### CR-4 缺口登记（04 §7.2 新增 G11–G15）
- **G11** 契约轴容量与实机不符（**待裁决**，见 §三）；**G12** 离线预处理管线承接（已定，落 T05 步骤 7）；**G13** 碰撞两段制（已定，落 T08）；**G14** 取消前端 WASM 直解（已定，落 T05/03 §8）；**G15** P0 导入性能实测（已定，落 T05 步骤 8）。

### CR-5 99 台账同步
- 状态表 T05/T08 行备注更新；外部依赖与挂账节登记本单；表头版本行追加 v11。

---

## 三、待裁决事项（提请指挥方）

1. **G11（已裁决，2026-09-15 @user 批准）**：契约 §5.2 `Pos = ARRAY[0..7]` 上限 8 轴 vs 实机 22 个受控运动轴。裁决＝**①＋② 并行**：①契约升版扩容（`Pos` ≥22 且留 ≥2 预留＋新增 AxisMask，随契约 V1.3 窗口处理，**不现在改契约**）；②编程范围＝轨迹级轴（本体 6 轴＋所挂臂专用轴，上限 9；YA~YE 只读）。三条结构要求已进参数体系（03 §4 / T03 卡）：工作模式→轴子集（modes）、轴分轨迹级/工序级（role）、X3 倍速链 1:2（coupling ratio 型）。**原"不裁决则返工"的风险已解除**（T03/T04/T07/T09 卡已按裁决口径同步）。
   - **裁决理由（指挥方建议，2026-09-15 @user 采纳）**：即使只算**轨迹级轴**（本体 6 轴 X1／Z1／Y1／X2／Z2／X3 ＋ 所挂多功能臂专用轴最多 3 个），已达 **9 轴**、超过 8 轴上界——**数据结构不扩容，任何"子集"方案都装不下**；范围界定（软件编程＝轨迹级轴、工序级轴 YA~YE 只读）与扩容互补，可显著降低校核与碰撞判定复杂度。扩容具体口径：段结构体 `Pos` 扩为 **≥22 且留 ≥2 预留（如 24）**，**并新增 AxisMask（轴使能掩码）**——现行"未使用轴位一律填 0"的规则在扩容后会变成**误动风险**（填 0 会被理解为"运动到 0"），必须改为"mask=0 的轴 PLC 忽略字段内容、保持当前值"。软件侧为零硬编码的配置驱动设计，扩容只改 `machine.yaml` 与契约，**不改代码**。
   - **职责边界补充（2026-09-15 22:43 @user 明确，②口径的最终落地）**：**主要动作由 PLC 编程；涉及轨迹编程的工况，由软件输出轨迹参数**（当前工作模式轨迹级轴的段序列：目标位置／速度／过渡方式）**交 PLC 执行**——软件不输出整机运动程序，轨迹参数由 PLC 接入其主程序。已写入 03 §4"实机轴系口径"块与 T07 卡产物口径。
   - **遗留待确认项**：工具臂专用轴是否**参与轨迹编程**（由软件输出轨迹参数），还是全部由 PLC 侧专用程序自控（如 ZA2 的自动打磨程序）——若全自控，轨迹轴收敛为本体 6 轴。列入 **9/16 踏勘确认清单**（连同各轴行程/最大速度/零点/正方向，即契约 Q-11 轴清单回执口径）。
2. **G11 附带三条进参数体系**：① 快接换臂 → 有效轴集合随所挂多功能臂切换（工作模式→轴子集映射）；② 轴分轨迹级联动轴与工序级设定轴（YA~YE 料架对位，按批次设定）；③ X3 倍速链 1:2 耦合（二级筒动 10mm → 三级筒相对一级筒动 20mm）用 `coupling` 字段显式表达，禁外部按两倍行程反推。
3. **契约轴名与实机不符项**（"立柱回转／腕部摆动／末端回转"在结构描述中未见对应机构）随 G9 的契约 V1.3 修订窗口一并处理；契约 §5.2 已声明"以附录 A 轴清单回执为准"，附录 A 填报时应覆盖实机全部受控轴。
4. **数字定级纪律（沿用审阅口径）**：《方案》含一条实测基线（2.13MB STEP→1.38s，RoboPath HMI 原型，**未经本项目复测**）与一组外推估算（130s@200MB、内存 1~3GB、350B/面、整线 2~6GB、10Hz 余量约 29 倍）；**P0 完成前均不得作为对外承诺**。
5. **V1.3 扩容影响测算（2026-09-15 @user 审计指出"只算了一半"，本表为按契约 V1.2 布局实算，属 V1.3 输入，勿漏）**：

| 对象 | V1.2 现状 | 扩容后（按 24 轴口径） | 依据 |
|---|---|---|---|
| 段结构体 `ST_Seg` | **56 B**（`Pos` = `ARRAY[0..7]` @4.0 → 8×4＝32 B；其余字段 24 B） | **124 B**（**`Pos` 原地扩为 `ARRAY[0..23]`、对齐口径不变（仍 @4.0）**：24×4＝96 B；＋ `SegAxisMask` `DWORD` 4 B；＋ 其余字段 24 B 不变 → 24＋96＋4＝**124 B**） | 契约 §5.2 |
| 下发块 `DB_SW_to_PLC`（10 段） | **584 B**（10×56 ＋ 24 头） | **1264 B**（10×124 ＋ 24） | 契约 §5.1 |
| 同上，若走 **`SegCount=50`** 方案（契约 L187："段数需求更大可提至 50"；亦即 **P-1 不成立**时规定的路） | — | **6224 B**（50×124 ＋ 24） | 契约 L187／L522 变更分支 |
| 回读块 `DB_PLC_to_SW` | **92 B 结构／96 B 声明**（`Pos[0..7]`、`Vel[0..7]` 各 32 B；`EnableMask`＝`BYTE`，L252 注明 bit0=J1…bit5=J6） | **228 B 声明**——分三步才算得出来：**结构 223 B**（`Pos`/`Vel` 各 24 轴＝96 B×2＝192 B；`EnableMask` 加宽 `DWORD` 4 B；其余字段 27 B 不变）→ **按 4 字节对齐 224 B** → **再套契约 `92→96` 那套"＋4 B 声明"惯例 → 228 B** | 契约 §6.1（含 L252 声明口径） |

   - ⚠️ **原 G11 只提了 `Pos`，"回读块不动"的说法不成立**——回读块同样是 8 轴／8 位口径；**即使只算轨迹级 9 轴也已 >8**。
   - **命名建议（防二次误引）**：新增的段级掩码建议命名 **`SegAxisMask`**，**不要**叫 `AxisMask`——它与回读块既有的 `EnableMask` 语义相邻，而 **V1.2 刚刚因一次 `EnableMask` 误引返工过**（见契约 V1.2 修订记录①）；`EnableMask` 由 `BYTE` 加宽为 `DWORD`（或 `ARRAY[0..2] OF BYTE`）。
   - ⚠️ **`EnableMask` 加宽会撞上现成的对齐预留——V1.3 须先明确处置，否则 220／223 之争说不清（2026-09-15 第六轮审计新增）**：契约 §6.1 **L252 `EnableMask` `BYTE`@77.0** 紧邻 **L253 `Reserved2` `WORD`@78.0**，而该字段的用途栏写的就是"**对齐预留**"。因此加宽为 `DWORD` 时至少有三种算法（差额 3 B）：**(a) 净增 3 B** → 结构 **223 B**（**本表取此口径**）；**(b) 吞并 `Reserved2`** → 结构约 **221 B**（223−2，**推算值，须按 V1.3 正式布局核算**）；**(c) 根本不加宽**（沿用 `BYTE`，只够 8 轴）→ **220 B**。**三者都成立且都被不同人算过**，所以 **V1.3 必须显式二选一声明：`EnableMask` 加宽是否吞并 `Reserved2`**；**声明前任何一方不得单方引数**（引 223 与引 220 会得出两套回读块与两套 DB 大小）。**建议：声明"吞并 `Reserved2`"并从表内删除该字段**——契约 §二"可单方变更"栏本就含"数据块内部预留字段（`Reserved*`）的用途"，即删除属契约授权范围内；采纳后 **228 B 的最终值须按声明重算后回填本表**。
   - **须提前告知电气**：50 段方案下 DB 达 **6.2 KB**，涉及 OPC UA 单次写的数据量与周期，属契约 V1.3 与 PLC 点表复核的要点。

---

## 四、影响面

| 项 | 影响 |
|---|---|
| 任务单 | T05 步骤扩为 9 步、T08 步骤扩为 8 步（含 1 个可选后置项）；完成标准相应增补 |
| 依赖与并行 | **不变**：T05 仍可按放宽项 R2 提前开工；`T05∥T06` 仍禁并行；不新增任务单编号 |
| 组件 | 新增 trimesh、Draco/meshopt（随 T05）；three-mesh-bvh 为条件引入；occt-import-js 明确不引入 |
| 排期 | T05 工作量增加（转换链＋缓存＋预处理管线＋P0），建议 W2-B 波次周期随 P0 实测结果评估 |
| 界面与文案 | 步骤①进度条由单段改三阶段（解析/三角化/优化），属实现细化，02 设计方案不冲突 |
| 对外承诺 | **冻结**：导入性能数字在 P0 实测锁定前一律标"暂定"，投标/汇报材料不得引用为承诺值 |

## 五、时序与生效

- 本单所含文档调整已先行落入（2026-09-15，依据 @user 当日指示）；待批准后补"已生效"标记。
- **G11 建议在 T03 开工前裁决**；附录 A 轴清单核对表的填报底稿可据 2026-09-15 结构描述整理（22 轴，已存当日工作日志）。
- 关联变更单：CR-2026-01（仓路径与并行编排，待批准）；CR-2026-02（包化留痕预案，条件触发）。

## 六、批准

| 角色 | 姓名 / 代号 | 意见 | 日期 |
|---|---|---|---|
| 提出 | WorkBuddy（指挥方） | 提请批准 | 2026-09-15 |
| 批准（文档调整追认） | @user（项目经理） | **对外承诺口径已确认：P0 实测前不对外承诺导入性能数字**（2026-09-15 23:04 口述确认）；§二 CR-1～CR-5 文档调整追认待书面签字 | 2026-09-15（待签） |

> 说明：本单的文档调整已按 @user 2026-09-15 指示**先行落入任务包**（T05/T08 卡、03、04、02、99），故本次批准为**书面追认**，批准后由指挥方在文件头"状态"字段补"已生效"标记并同步 99 台账。
> 已单独获 @user 批准的两项（不需要在本单重复决策，仅记录在案）：**G11 裁决（①＋② 并行，22:20 批准）**、**职责边界口径（主要动作归 PLC、轨迹参数归软件，22:43 明确）**。

## 七、修订记录

| 版本 | 日期 | 修订内容 |
|---|---|---|
| V1.0 | 2026-09-15 | 首次编制（草案），随任务包文档调整同步登记 |
| V1.1 | 2026-09-15 | G11 裁决落地（@user 批准 ①＋② 并行）：§三-1 由"待裁决"改记"已裁决"并补裁决理由与遗留待确认项；同步改动：03 §4 配置模式（role/modes/coupling 两型/envelope/deflection 字段＋"实机轴系口径"块）、T03 卡整卡改写（22 轴样例）、T04/T07/T09 卡挂口径、04 §6.3 T03 派单卡与 §7.2 G11（标已定）＋新增 G16（换臂工作模式界面入口）、99 台账 |
| V1.2 | 2026-09-15 | G16 落地（@user 指示按建议完成）：02 设计方案升 V1.1（顶栏工作模式选择器、§3 模式表增"工作模式"行、步骤②前置锁定、步骤③换臂作废规则）；T02/T06/T07 卡同步接线；04 §7.2 G16 标已定；99 台账 v13 |
| V1.3 | 2026-09-15 | ①补 §六 批准表并登记 **@user 对外承诺口径确认（P0 前不承诺导入性能数字，23:04）**；②因 CR-2026-01 的仓路径定案（代码并入 `D:\ZCode项目\多功能机械臂软件`），§四 影响面补注"全部产出落在该仓工作树内、`.workbuddy/` 不入库" |
| V1.4 | 2026-09-15 23:30 | @user 第五轮审计修正：①**技术附件改为仓内相对路径**（`参考资料/` 已归档《方案》《审阅意见》，原只存在于微信缓存／桌面）；②新增 **§三-5 V1.3 扩容影响测算表**（段 56→124 B／下发块 584→1264 B／50 段方案 6224 B／**回读块 92/96→约 228 B**——原"回读块不动"不成立）＋命名建议 **`SegAxisMask`** 与 `EnableMask` 加宽 `DWORD`（防二次误引）＋"50 段方案须提前告知电气" | WorkBuddy（@user 审计） |
| V1.5 | 2026-09-15 | @user 第六轮审计修正：**§三-5 表的"依据"列与结论数字不是同一套算式，已改正**——①段结构体依据列原写 `Pos = ARRAY[0..23] @8.0` ＋"4 B 对齐"，按此复算得 **128 B**（下发块 1304 B／50 段 6424 B），与结论 124／1264／6224 不符；**改按 124 B 的真实算式重写**（`Pos` **原地扩、对齐口径不变仍 @4.0**：24＋96＋4＝124），**选 124 B（PLC 侧改动最小）**；②回读块依据列原只写"按 4 字节对齐"，复算者只能得 224 B；**改为分三步写全**（结构 223 B → 4 字节对齐 224 B → 再套契约 `92→96` 的"＋4 B 声明"惯例 → 228 B）；③**新增 `EnableMask` 加宽与 `Reserved2`（L253 `WORD`@78.0「对齐预留」）的处置悬置说明**：220／221／223 三种算法并存，**V1.3 须显式声明"净增 3 B"还是"吞并 Reserved2"，建议吞并并删除该字段**；声明前不得单方引数 | WorkBuddy（@user 审计） |
