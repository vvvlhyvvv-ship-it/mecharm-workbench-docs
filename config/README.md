# config/ — 参数体系

现场参数的唯一落点。**配置与标定分离**：代码里不得出现轴参数数值字面量（04 §5.5-① 常驻复查项，由 T03 交付的 `tools/lint_no_magic.py` 自动化）。

| 路径 | 唯一写者 | 说明 |
|---|---|---|
| `machine.yaml` | T03 | ⚠️ **热点文件**，全部单只读；外部回执冻结后**只改此文件不改码**；任何单不得私改 |

yaml **禁中文键名**（注释除外）；字段结构以 03 §4 为准，**禁自增删**。

本目录当前仅有此占位 README，`machine.yaml` 随 T03 交付。
