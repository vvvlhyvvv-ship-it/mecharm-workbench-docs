# 接口契约书

## 三维编程软件 ↔ S7-1500 PLC 接口

| 项 | 内容 |
|---|---|
| 契约编号 | IC-2026-001 |
| 版本 | **V1.2（草案）** |
| 状态 | ☑ 草案　☐ 待签　☐ 已冻结 |
| 编制日期 | 2026-09-14 |
| 软件方（我方） | |
| 电气方（PLC／对方） | |
| 关联文件 | 《三维可视化软件与 PLC 接口沟通单 V1.0》《大型工控软件开发规程》S-1／门禁 G1 |

> **本契约是门禁 G1 的唯一交付物。** 双方签字后冻结，此后任何变更须走书面变更流程（见第 12 章）。
> **冻结前不得开始编写通讯层代码。** —— 依据《开发规程》§4.1。

---

## 第 1 章 契约范围与效力

### 1.1 适用范围
本契约约定"三维编程软件"（以下简称软件）与"PLC 控制系统"（以下简称 PLC）之间的全部数据交换、时序约定、坐标与单位约定、安全职责边界。

### 1.2 不在本契约范围内
- PLC 内部运动控制参数整定（伺服增益、滤波器、加减速曲线的最终整定）
- 安全回路的硬件构成与安全程序逻辑
- PLC 侧工艺联锁逻辑

### 1.3 效力
本契约经双方技术负责人签字后生效。双方各自的开发工作以本契约为准，**不得单方面变更数据结构或时序**。

### 1.4 术语与缩写

| 缩写 | 含义 |
|---|---|
| SW | 三维编程软件（本软件） |
| SW→PLC | 软件写入方向（下发） |
| PLC→SW | PLC 输出、软件读取方向（回读） |
| 段 | 轨迹的最小执行单元（一条直线/圆弧/点位运动） |
| 关节空间 | 以各运动轴位置描述的坐标空间 |
| 笛卡尔空间 | 以空间位置与姿态描述的坐标空间 |
| 工程值 | 以 mm / 度为单位的物理量 |
| 原始值 | PLC 内部的脉冲数或编码器计数 |

---

## 第 2 章 前置条件与待确认事项（门禁 G1 卡点）

### 2.1 本契约成立的前提

本契约以下列条件为前提。**任一项不成立时，第 5 章、第 7 章、第 8 章须按附录 B 的替换条款执行。**

| # | 前提条件 | 不成立的影响 |
|---|---|---|
| **P-1** | PLC **具备多轴插补能力**（运动控制工艺对象） | 第 8 章下发方式由方案 A 切至方案 B（见附录 B） |
| **P-2** | PLC 支持**连续路径过渡（blending）** | 密集小线段会逐段减速到零，节拍不达标，须调整路径细分策略 |
| **P-3** | PLC 侧提供 **OPC UA Server**（或另设网关） | 通讯方式须整体重新设计 |
| **P-4** | 可**新增独立数据块（DB）**，编号可指定 | 须改用既有 DB，地址须重排 |

### 2.2 待确认事项回执栏（由电气方填写）

> ⚠️ **本栏未填齐并签字，门禁 G1 不通过，S-1 不得结束。**

| 编号 | 待确认事项 | 对契约的影响 | 电气方答复 | 答复人／日期 |
|---|---|---|---|---|
| **Q-1** | PLC 具体型号：**S7-1500F 还是 S7-1500TF**？（F 为安全型，T 才有运动控制能力） | 决定 P-1，直接影响第 8 章 | | |
| **Q-2** | 运动控制由谁实现？（PLC 工艺对象／独立运动控制器／伺服驱动器自带定位） | 第 5 章段参数定义、第 8 章 | | |
| **Q-3** | 是否支持**多轴插补**（如 MC_MoveLinear）与**连续路径 blending**？ | P-1、P-2 | | |
| **Q-4** | 各轴位置数据的形态：**工程值（mm/度）还是原始值（脉冲/计数）**？ | ⚠️ 第 7 章核心，决定软件侧换算逻辑 | | |
| **Q-5** | **各轴零点定义与正方向**（机械零点位置、正方向指向、旋转轴逆／顺时针） | ⚠️ **最高危**。做错会导致实机反向动作且画面完全正常 | | |
| **Q-6** | OPC UA Server 位置：PLC 侧内置／独立网关？节点命名规则？ | 第 4 章、第 5 章、第 6 章 | | |
| **Q-7** | 可用的 DB 编号范围？可否新增独立 DB？ | 第 4.2 节 | | |
| **Q-8** | PLC 侧程序由谁编写？预计完成时间？ | 第 13 章联合调试计划 | | |
| **Q-9** | 是否为**龙门双驱同步结构**（两伺服驱动同一横梁且机械非刚性连接）？ | 若有，须启用双驱同步偏差监视（第 6.4 节） | | |
| **Q-10** | 目标工控机配置（CPU／内存／**独立显卡**）？ | 性能指标与三维显示可行性 | | |
| **Q-11** | 各运动轴清单、行程、最大速度 | 第 5 章段数据范围校验、第 7 章 | | |
| **Q-12** | 是否允许上位机在线写 PLC（运行时下发）？或仅允许离线生成程序后下载？ | 第 8 章 | | |

---

## 第 3 章 系统架构与职责边界

### 3.1 架构

```
┌─────────────────────────────┐
│  三维编程软件（上位机 / 工控机）  │
│  · 三维模型导入与拾取           │
│  · 轨迹生成与校核（含碰撞检测）   │
│  · 关节空间目标位置计算          │
│  · 下发与回读                   │
└──────────────┬──────────────┘
               │  OPC UA（SW = Client，PLC = Server）
┌──────────────┴──────────────┐
│  S7-1500 PLC                │
│  · 轨迹段校验                  │
│  · 运动控制与插补              │
│  · 软限位与工艺联锁            │
│  · 安全程序（Cat 3 / PL d）    │
└──────────────┬──────────────┘
               │  总线
┌──────────────┴──────────────┐
│  伺服驱动 + 电机               │
└─────────────────────────────┘
```

### 3.2 职责划分

| 功能 | 软件 | PLC | 硬回路 |
|---|---|---|---|
| 三维模型导入、面／边拾取 | ✅ | | |
| 轨迹生成（笛卡尔 → 关节空间） | ✅ | | |
| 轨迹离线校核（可达性、限位、碰撞） | ✅ | | |
| 碰撞路径禁止下发 | ✅ | | |
| 段数据校验（范围、轴数、限位） | 下发前校验 | ✅ 接收后再校验 | |
| 运动控制与插补 | ❌ **不承担** | ✅ | |
| 软限位（行程保护） | 提示 | ✅ 强制 | |
| 硬限位 | | 监视 | ✅ |
| 急停 | 显示状态 | 监视 | ✅ |
| 安全门／光栅联锁 | | ✅ | ✅ |
| Cat 3 / PL d 安全等级 | ❌ **不承担** | ✅ | ✅ |
| 速度／加速度上限 | 请求值（不超上限） | ✅ 强制钳位 | |
| 实时姿态跟随（模型随实机动） | ✅ | 提供实际位置 | |

> **核心原则：软件只"请求"，不"命令"。** 下发的目标位置是**请求值**，PLC 有权拒绝或钳位。

### 3.3 通讯方式

| 项 | 约定 |
|---|---|
| 协议 | **OPC UA**（二进制 TCP，非 WebService） |
| 角色 | SW = **Client**，PLC = **Server** |
| 访问方式 | ⚠️ **按符号名（NodeId）访问，不按字节偏移直接读写** |
| 订阅方式 | 回读数据采用 **Subscription（订阅）**，非轮询 |
| 发布周期 | **20 Hz（50 ms）**（招标要求 ≥10 Hz，留 2 倍余量） |
| 安全策略 | ⚠️【待确认：PLC 侧支持的 SecurityPolicy 与认证方式，默认 Basic256Sha256 + 用户名密码】 |

---

## 第 4 章 命名与地址约定

### 4.1 数据块规划

| DB | 名称 | 方向 | 长度 | 用途 |
|---|---|---|---|---|
| DB_ToPLC | `DB_SW_to_PLC` | SW → PLC | **584 字节**（按 §5.1 布局实算：头 8＋10×56 段＋预留 16） | 下发轨迹段与命令 |
| DB_FromPLC | `DB_PLC_to_SW` | PLC → SW | 96 字节 | 回读状态与实际位置 |

⚠️【待确认：实际 DB 编号，见 Q-3/Q-4。建议预留编号段，不占用既有工艺 DB。】

### 4.2 命名规范

| 对象 | 规则 | 示例 |
|---|---|---|
| DB 名 | `DB_<方向>` | `DB_SW_to_PLC` |
| 结构体 | `ST_<用途>` | `ST_Seg` |
| 变量名 | 大驼峰，不含中文与空格 | `SegCount` |
| 轴数组 | `Pos[0..n-1]`，索引 = 轴号 | `Pos[0]` = J1 |
| OPC UA 节点 | `ns=3;s="DB_SW_to_PLC"."SegCount"` | 符号名访问 |

### 4.3 数据类型与字节序

| 项 | 约定 |
|---|---|
| S7 数据类型 | `BOOL`/`BYTE`/`INT`/`DINT`/`REAL`/`ARRAY` |
| 位置／速度类型 | **`REAL`（32 位浮点）** |
| 字节序 | S7 内部为大端；**OPC UA 层由 Server 自动转换，双方均不手工处理字节序** |
| 数组对齐 | 按 4 字节对齐，`ARRAY` 前留足对齐字节 |
| 布尔量 | 以位（bit）表达于状态字／命令字中，**不单独占字节** |

---

## 第 5 章 下发数据块定义（SW → PLC）

### 5.1 数据结构总览

| 偏移 | 变量名 | 类型 | 单位 | 说明 | 取值范围 |
|---|---|---|---|---|---|
| 0.0 | `Cmd` | `BYTE` | — | 命令字（见 5.3） | 位定义 |
| 1.0 | `SeqID` | `INT` | — | 轨迹序号（0–32767 循环） | 0–32767 |
| 2.0 | `SegCount` | `INT` | — | 本批段数 | 1–10 |
| 4.0 | `SpeedOverride` | `REAL` | % | 速度倍率 | 1.0–100.0 |
| 8.0 | `Seg` | `ARRAY[0..9] OF ST_Seg` | — | 段数组（每段 56 字节） | 见 5.2 |
| 568.0 | `Reserved` | `ARRAY[0..15] OF BYTE` | — | 预留 | — |

> 段数组长度 10 为**建议值**。若连续轮廓作业段数需求更大，可将 `SegCount` 上限提升至 50，DB 长度相应扩展（每段 56 字节）。

### 5.2 段结构体 `ST_Seg`（56 字节）

| 偏移 | 变量名 | 类型 | 单位 | 说明 |
|---|---|---|---|---|
| 0.0 | `SegType` | `BYTE` | — | 0 = 点位(PTP)；1 = 直线(LIN)；2 = 圆弧(CIRC) |
| 1.0 | `MotionMode` | `BYTE` | — | 定位方式：0 = 绝对；1 = 相对 |
| 2.0 | `BlendMode` | `BYTE` | — | 0 = 精确停止；1 = 平滑过渡（blending） |
| 3.0 | `Reserved1` | `BYTE` | — | 对齐预留 |
| 4.0 | `Pos` | `ARRAY[0..7] OF REAL` | mm / deg | **各轴目标位置（关节空间）** |
| 36.0 | `Vel` | `REAL` | mm/s | 该段速度 |
| 40.0 | `Acc` | `REAL` | mm/s² | 加速度 |
| 44.0 | `Dec` | `REAL` | mm/s² | 减速度 |
| 48.0 | `BlendTol` | `REAL` | mm | 过渡容差（`BlendMode=1` 时有效） |
| 52.0 | `DwellMS` | `DINT` | ms | 到点停留时间（玻璃垫放置等工艺用） |

**轴数组与轴号映射（`Pos[i]` 的索引约定）**

| 索引 | 轴 | 运动类型 | 行程 | 说明 |
|---|---|---|---|---|
| `Pos[0]` | J1 大车行走 | 移动副 | ⚠️【待确认】 | |
| `Pos[1]` | J2 立柱升降 | 移动副 | ⚠️【待确认】 | |
| `Pos[2]` | J3 立柱回转 | **旋转副** | ⚠️【待确认】 | 单位 deg |
| `Pos[3]` | J4 横臂伸缩 | 移动副 | ⚠️【待确认】 | |
| `Pos[4]` | J5 腕部摆动 | **旋转副** | ⚠️【待确认】 | 单位 deg |
| `Pos[5]` | J6 末端回转 | **旋转副** | ⚠️【待确认】 | 单位 deg |
| `Pos[6]` | 预留／夹爪 | — | — | ⚠️【待确认：是否含夹爪轴】 |
| `Pos[7]` | 预留 | — | — | |

> ⚠️ **轴数与配置以附录 A 轴清单核对表回执为准（随 Q-11 图纸一并返回）。本表按 6 轴（+2 预留）实例化，数据结构上限支持 8 轴。**
> **未使用的轴位一律填 0，不得留空或填随机值。**

### 5.3 命令字 `Cmd` 位定义

| 位 | 名称 | 说明 | 生效条件 |
|---|---|---|---|
| 0 | `CMD_LOAD` | 装载轨迹（数据已写入，请求校验） | `SeqID` 已更新 |
| 1 | `CMD_START` | 启动执行 | PLC 已置 `ACK_LOAD_OK` |
| 2 | `CMD_PAUSE` | 暂停 | 执行中 |
| 3 | `CMD_RESUME` | 继续 | 已暂停 |
| 4 | `CMD_STOP` | 停止并取消当前轨迹 | 任意状态 |
| 5 | `CMD_RESET` | 复位（清报警、回待机） | 报警状态 |
| 6 | `CMD_HOME` | 请求回零 | 待机状态 |
| 7 | — | 预留 | — |

> **约定：命令字为"脉冲式"语义——软件置位后，收到对应应答即清零。不得长期保持置位。**

---

## 第 6 章 回读数据块定义（PLC → SW）

### 6.1 数据结构总览

| 偏移 | 变量名 | 类型 | 单位 | 说明 |
|---|---|---|---|---|
| 0.0 | `Status` | `BYTE` | — | 状态字（见 6.2） |
| 1.0 | `Ack` | `BYTE` | — | 应答字（见 6.3） |
| 2.0 | `AlarmWord` | `WORD` | — | 报警字（见 6.4） |
| 4.0 | `SeqID` | `INT` | — | 回显软件写入的序号 |
| 6.0 | `CurSeg` | `INT` | — | 当前执行段号 |
| 8.0 | `Pos` | `ARRAY[0..7] OF REAL` | mm / deg | **各轴实际位置** |
| 40.0 | `Vel` | `ARRAY[0..7] OF REAL` | mm/s、deg/s | 各轴实际速度 |
| 72.0 | `Heartbeat` | `DINT` | — | 心跳计数（每周期 +1） |
| 76.0 | `ModeMask` | `BYTE` | — | 模式位：0=手动 1=自动 2=回零 |
| 77.0 | `EnableMask` | `BYTE` | — | 各轴使能位（bit0=J1…bit5=J6） |
| 78.0 | `Reserved2` | `WORD` | — | 对齐预留 |
| 80.0 | `SyncErr` | `REAL` | mm | **双驱同步偏差**（非龙门结构时填 0） |
| 84.0 | `Timestamp` | `DINT` | ms | PLC 侧时间戳 |
| 88.0 | `Reserved3` | `DINT` | — | 预留 |

**结构总长 92 字节，按 4 字节对齐，DB 声明的长度为 96 字节。**

### 6.2 状态字 `Status`

| 位 | 名称 | 说明 |
|---|---|---|
| 0 | `ST_IDLE` | 待机 |
| 1 | `ST_READY` | 就绪（已装载轨迹且校验通过） |
| 2 | `ST_RUNNING` | 执行中 |
| 3 | `ST_PAUSED` | 暂停 |
| 4 | `ST_DONE` | 当前轨迹正常结束 |
| 5 | `ST_ALARM` | 报警（详见 `AlarmWord`） |
| 6 | `ST_HOMED` | 已完成回零 |
| 7 | `ST_COMM_OK` | PLC 侧通讯正常 |

### 6.3 应答字 `Ack`

| 位 | 名称 | 说明 |
|---|---|---|
| 0 | `ACK_LOAD_OK` | 装载校验通过 |
| 1 | `ACK_LOAD_NG` | 装载校验失败（越界／轴数不符／限位） |
| 2 | `ACK_START_OK` | 已接受启动 |
| 3 | `ACK_START_NG` | 启动被拒（未就绪／未回零／报警中） |
| 4 | `ACK_STOP_DONE` | 已停止 |
| 5 | `ACK_RESET_DONE` | 已复位 |
| 6 | `ACK_HOME_DONE` | 回零完成 |
| 7 | — | 预留 |

### 6.4 报警字 `AlarmWord`

| 位 | 名称 | 处理 |
|---|---|---|
| 0 | 软限位超程 | 软件提示并禁止下发后续段 |
| 1 | 伺服报警 | 软件显示并停止 |
| 2 | 跟随误差超限 | 软件告警 |
| 3 | **双驱同步误差超限** | ⚠️ 软件告警并停止（Q-9 为"是"时启用） |
| 4 | 目标点不可达 | 软件提示重新编程 |
| 5 | 数据越界 | 软件提示检查配置 |
| 6 | 安全回路动作 | 软件显示并停止 |
| 7 | 急停 | 软件显示并停止 |

> ⚠️ **双驱同步专题**：若桁架为龙门双驱结构（两伺服驱动同一横梁、机械非刚性连接），PLC 须上报 `SyncErr`。**若不做此监视，横梁姿态在数学上无法唯一确定，模型会出现"斜着走"或抖动。** 本项在 Q-9 确认前不启用。

---

## 第 7 章 坐标系与单位约定

### 7.1 场景坐标系

| 项 | 约定 |
|---|---|
| 手性 | **右手系** |
| 原点 | ⚠️【待确认：建议取设备基础／产线基准的原点，或甲方图纸标注的全局坐标系原点】 |
| Z 轴 | **竖直向上为正** |
| X 轴 | ⚠️【待确认：建议取大车行走方向（产线物流方向）为正】 |
| Y 轴 | 按右手法则由 X、Z 导出 |
| 朝向 | 从设备正面（操作面）看向设备 |

> ⚠️ **注意**：STEP 标准默认为 Z 轴向上；但工厂／建筑类设计软件常导出 Y 轴向上的模型。**模型导入后必须先核对朝向，不得假定。**

### 7.2 单位约定

| 物理量 | 单位 | 说明 |
|---|---|---|
| 长度 | **mm** | 全部位置数据 |
| 角度 | **度（deg）** | 旋转轴位置 |
| 线速度 | **mm/s** | |
| 角速度 | **deg/s** | |
| 加速度 | **mm/s²**、**deg/s²** | |
| 时间 | **ms** | 停留时间、超时 |
| 速度倍率 | **%** | 1.0–100.0 |

### 7.3 角度正方向约定

| 项 | 约定 |
|---|---|
| 正方向 | **从该轴正端向原点方向看去，逆时针为正** |
| 一致性 | ⚠️ 必须与 PLC 侧轴的正方向定义**逐轴核对**，不得推断 |

### 7.4 数据形态 ⚠️ 最高危项

| 项 | 约定 |
|---|---|
| **下发**（`Pos`） | **工程值**（mm / deg） |
| **回读**（`Pos`） | ⚠️**【待确认 Q-10】工程值 or 原始值（脉冲/计数）** |
| 若为原始值 | 软件侧须按 `工程值 = (原始值 − 零点原始值) × 比例系数 × 方向符号` 换算，**比例系数与零点原始值须由 PLC 侧提供并写入配置文件** |

> ⚠️ **`方向符号`（±1）与`零点偏移`是标定四要素中的两项，必须现场实测，不得推断。** 详见《开发规程》W-7.2。

### 7.5 零点定义

| 轴 | 机械零点定义 | 零点对应工程值 |
|---|---|---|
| J1–J6 | ⚠️【待确认：回零开关位置 or 行程某一端】 | ⚠️【待确认：0 或行程中点等】 |

---

## 第 8 章 下发方式与数据粒度

### 8.1 主方案 A（推荐）：软件出关键点位 + PLC 插补

| 项 | 内容 |
|---|---|
| 软件输出 | 关键点位的段数据（`Pos[]` + `Vel`/`Acc`/`Dec`/`BlendTol`） |
| PLC 职责 | 段内插补、连续路径过渡 |
| 优点 | 数据量小、不怕通讯抖动；**PLC 侧限位与安全逻辑始终生效**；现场可由电气工程师独立调整加减速与 blending 参数 |
| 适用前提 | P-1、P-2 成立 |

### 8.2 备选方案 B：软件插补密集点流

**仅在方案 A 前提不成立时启用**（见附录 B）。
⚠️ 注意：方案 B 同样受制于 PLC 的逐点定位能力，段数多一个量级，抖动风险更高。**方案 B 不是并列选项，是降级方案。**

### 8.3 段类型与插补方式的对应关系（重要技术约定）

| 段类型 | 机构条件 | 插补方式 | 说明 |
|---|---|---|---|
| 移动副组合段 | 仅含 J1/J2/J4（直线轴） | **关节空间线性插补即可** | 各轴正交时，关节空间直线插补 = 笛卡尔直线运动，无需细分 |
| 含旋转轴的位姿段 | 含 J3/J5/J6 | 关节空间插补 | 姿态变化无严格路径形状要求时适用 |
| **需笛卡尔直线的轮廓段** | 沿型腔轮廓打磨、大面积均匀喷涂等 | **由软件细分为密集关键点后下发** | ⚠️ 含旋转轴的笛卡尔直线无法用关节空间线性插补还原，必须细分 |
| 圆弧段 | — | PLC 圆弧插补（如支持） | ⚠️【待确认：PLC 是否支持圆弧插补】 |

> ⚠️ **轮廓段细分注意**：细分成过多小线段会导致 PLC 逐段减速到零。**须同时提供 `BlendMode=1` 与 `BlendTol`，并实测轮廓偏差与节拍。**

### 8.4 碰撞校验的路径偏差余量（MUST）

> ⚠️ **软件校验的是软件插补路径，实机走的是 PLC 插补路径，两者在转角与加减速段可能有几毫米偏差。**

**约定：碰撞检测时机械臂几何须留保守包络，建议 2–5 mm（或按 PLC 的轮廓偏差参数设定）。**

若不预留，会出现最坏情况：**软件说"校验通过"，实机撞了。**

### 8.5 下发粒度建议

| 作业类型 | 建议段数 |
|---|---|
| 点位型（抓取、放置、测温、定点润滑） | 每工艺点 1–3 段 |
| 短轮廓（小范围打磨） | 10–50 段 |
| 长轮廓（大面积喷涂） | 单批 ≤ 50 段，分批下发 |

---

## 第 9 章 握手时序

### 9.1 正常下发时序（9 步）

| 步 | 方向 | 软件动作 | PLC 动作 | 超时 | 超时处理 |
|---|---|---|---|---|---|
| **1** | SW→PLC | 写入 `Seg[]`、`SegCount`、`SpeedOverride` | — | — | — |
| **2** | SW→PLC | `SeqID` +1，置 `CMD_LOAD` | — | 500 ms | 重试 3 次，仍失败 → 报错并停止 |
| **3** | PLC 内部 | — | 校验：轴数、范围、软限位、`SpeedOverride` 合法性 | — | 失败置 `ACK_LOAD_NG` |
| **4** | PLC→SW | 读 `ACK_LOAD_OK` / `ACK_LOAD_NG` | 置对应应答位 + `ST_READY` | **2 s** | 超时 → 报错；`NG` → 读取 `AlarmWord` 并提示具体原因 |
| **5** | SW→PLC | 清 `CMD_LOAD`，置 `CMD_START` | — | 500 ms | 重试 |
| **6** | PLC→SW | 读 `ACK_START_OK` | 置 `ACK_START_OK` + `ST_RUNNING` | **2 s** | 超时或 `NG` → 报错并停止 |
| **7** | PLC→SW | 20 Hz 读取 `Pos[]`、`Vel[]`、`CurSeg` | 更新实际值，`Heartbeat` +1 | **200 ms**（3 个周期） | 心跳丢失 → 界面告警"数据过期"并停止姿态跟随 |
| **8** | PLC→SW | — | 执行完毕置 `ST_DONE`、清零 `ST_RUNNING` | — | — |
| **9** | SW | 清 `CMD_START`，写审计日志（谁、何时、哪条轨迹、结果） | 清 `ST_DONE` | — | — |

### 9.2 异常时序

| 场景 | 处理 |
|---|---|
| 软件主动停止 | 置 `CMD_STOP` → PLC 减速停止 → 置 `ACK_STOP_DONE` |
| PLC 侧报警 | PLC 置 `ST_ALARM` + `AlarmWord` → 软件显示、停止跟随、记录日志 |
| 通讯中断 | 软件：界面明确提示 + 保持最后已知位置并标注"数据过期" + 自动重连 → 恢复后重新同步 `SeqID` / `Status` |
| 急停 | 硬回路动作 → PLC 置 `AlarmWord.7` → 软件显示并停止 |

### 9.3 三条强制原则

> **① 软件只请求，不命令。** 下发的是请求值，PLC 有权拒绝或钳位。
>
> **② 命令字为脉冲语义。** 置位后收到应答即清零，不得长期置位。
>
> **③ 禁止外推预测。** 通讯中断时宁可显示滞后，不得预测实机位置。外推会导致虚假报警，更危险的是从危险位预测到安全位而掩盖风险。

---

## 第 10 章 异常处理与恢复

| 异常 | 软件行为 | PLC 行为 |
|---|---|---|
| OPC UA 连接丢失 | 提示 + 状态置灰 + 自动重连（指数退避，最长 30 s） | 保持当前状态；若执行中则按其安全逻辑处理（⚠️【待确认：断开后 PLC 是否继续执行 / 停在哪一步】） |
| 心跳丢失 | 告警"数据过期"，停止姿态跟随 | 继续发送 |
| `SeqID` 不匹配 | 拒绝本次结果，重新同步 | 回显实际接收序号 |
| 数据越界 | 下发前即拒绝并提示具体越界轴与数值 | 接收后再校验，置 `ACK_LOAD_NG` |
| 配置非法 | 启动时即拒绝加载并报警，不进入主界面 | — |
| PLC 报警 | 显示报警位对应中文说明 + 建议动作 | 置 `AlarmWord` |
| 通讯恢复 | **重新读取全部状态**（不可假设回到断线前） | — |

---

## 第 11 章 安全职责划分

| 安全功能 | 软件 | PLC 安全程序 | 硬回路 |
|---|---|---|---|
| 轨迹可达性与碰撞校核 | ✅ | | |
| 碰撞路径禁止下发 | ✅（**按钮禁用 + 下发函数内部二次校验**） | | |
| 软限位 | 提示 | ✅ 强制 | |
| 硬限位 | | 监视 | ✅ |
| 超速／超加速保护 | 请求值不超限 | ✅ 强制钳位 | |
| 急停 | 显示 | 监视 | ✅ |
| 安全门／光栅 | | ✅ 联锁 | ✅ |
| 双驱同步误差监视 | 显示与告警 | ✅ 监视与停止 | |
| **Cat 3 / PL d 安全等级** | ❌ **不承担** | ✅ | ✅ |

> **本契约明确：软件不承担任何安全功能，不参与安全等级认证。**
> 软件侧的碰撞检测与限位校核属于**工艺保护**，不能替代安全功能。

---

## 第 12 章 变更控制

| 项 | 规则 |
|---|---|
| 可单方变更 | 数据块内部预留字段（`Reserved*`）的用途 |
| **须双方书面确认** | 第 5 章、第 6 章的**任何字段增删或类型／单位变更**、第 9 章时序变更、第 3 章架构变更 |
| 变更流程 | 提出 → 双方评估影响范围 → 更新契约版本号 → 双方重新签字 |
| 紧急变更 | ⚠️ **现场不得临时改接口**。确需变更时，须当日形成书面记录并在 3 日内补签 |

---

## 第 13 章 调试与联合验证

| 项 | 约定 |
|---|---|
| 软件侧模拟器 | 软件方按本契约实现 **OPC UA Server 模拟器**：按本契约提供节点，收到点位后虚拟执行，20 Hz 更新 `Pos[]` |
| PLC 侧验证 | 电气方使用 **PLCSIM Advanced** 验证 PLC 程序 |
| 一致性要求 | **模拟器的节点名、类型、单位必须与本契约完全一致**，不得自行简化 |
| 联合调试 | 双方各自单侧验证通过后合并；**不在设备现场做首次对接** |
| 首次对接时间 | 目标：设备安装前完成（见《开发规程》§4.3） |

---

## 第 14 章 签署

| | 软件方（我方） | 电气／PLC 方（对方） |
|---|---|---|
| 技术负责人 | 　 | 　 |
| 签字 | 　 | 　 |
| 日期 | 　 | 　 |

> **签署即表示：本契约第 5、6、7、9 章内容已确认，双方各自的开发工作以此为准，第 2.2 节待确认事项已全部填齐。**

---

## 附录 A：轴清单核对表（请电气与机械共同确认）

| 索引 | 轴名 | 运动类型 | 行程（mm 或 deg） | 最大速度 | 正方向 | 零点定义 | 单位 |
|---|---|---|---|---|---|---|---|
| `Pos[0]` | J1 大车行走 | 移动副 | | | | | mm |
| `Pos[1]` | J2 立柱升降 | 移动副 | | | | | mm |
| `Pos[2]` | J3 立柱回转 | 旋转副 | | | | | deg |
| `Pos[3]` | J4 横臂伸缩 | 移动副 | | | | | mm |
| `Pos[4]` | J5 腕部摆动 | 旋转副 | | | | | deg |
| `Pos[5]` | J6 末端回转 | 旋转副 | | | | | deg |
| `Pos[6]` | 夹爪／预留 | | | | | | |
| `Pos[7]` | 预留 | | | | | | |

---

## 附录 B：前提不成立时的替换条款

| 若前提不成立 | 替换内容 |
|---|---|
| **P-1 不成立**（PLC 无插补能力，仅独立定位轴） | 第 8 章改用方案 B：软件插补为密集点流（建议 ≤10 ms 周期），`SegType` 全部置 0（PTP），`SegCount` 上限提升至 50。**须重新评估节拍与抖动** |
| **P-2 不成立**（无 blending） | 限制轮廓段细分密度；对连续轮廓作业**须书面告知甲方节拍影响**，并评估升级硬件 |
| **P-3 不成立**（无 OPC UA） | 通讯方式重议：备选 S7 通讯（TCP/ISO-on-TCP）或经第三方网关转 OPC UA。**本契约第 3、4、5、6、9 章须整体重写** |
| **P-4 不成立**（不可新增 DB） | 第 4.1 节改用既有 DB，地址按电气方指定的空闲区重排，双方共同核对 |

---

> **V1.1 修订（2026-09-14，@research 代改·待 @auditor 复验）**：①§4.1 `DB_SW_to_PLC` 长度 96→**584 字节**（按 §5.1 头 8＋10×56＋预留 16 实算；`DB_PLC_to_SW` 96 字节经核与 §6.1 布局自洽，不动）；②"甲方/乙方"称谓统一为"软件方/电气方"——涉及主合同买方（通裕）的"甲方"字样保留原义。共 4 处。

## 附录 C：文件状态检查

| 检查项 | 状态 |
|---|---|
| 第 2.2 节 12 项待确认事项已填齐 | ☐ |
| 附录 A 轴清单已由电气与机械共同确认 | ☐ |
| 第 7.4 节数据形态（工程值／原始值）已明确 | ☐ |
| 第 9 章时序超时值已双方认可 | ☐ |
| 双方技术负责人已签字 | ☐ |
| **→ 门禁 G1 通过，S-1 结束** | ☐ |

---

## 附录 D：修订记录

| 版本 | 日期 | 修订内容 | 修订人 |
|---|---|---|---|
| V1.0（草案） | 2026-09-14 | 首次编制。软件侧可定义内容已全部写实；第 2.2 节 12 项待电气确认 | — |
| V1.1（草案） | 2026-09-14 | 硬伤①④代改：§4.1 DB_SW_to_PLC 长度 96→584 字节（按 §5.1 实算）；"甲方/乙方"统一为"软件方/电气方"（详见前文 V1.1 修订注记） | @research 代改 |
| V1.2（草案） | 2026-09-14 | 审核修订：①§5.2 备注误引 `EnableMask`——该字段在 §6.1 **回读块**（PLC→SW 只读回报），软件下发段无从置位，删去该误引（56 字节布局不动）；②§4.1 待确认引用 Q-7→Q-3/Q-4；③§7.4 回读数据形态待确认 Q-4→Q-10；④§5.2 轴数依据改为"附录 A 轴清单回执"；⑤版本头/状态勾与修订记录对齐 | @research 代改·待 @auditor 复验 |
