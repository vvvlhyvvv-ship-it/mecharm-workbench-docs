# 软件开发规则与合规交付要求

**适用项目**：锻造压机上料设备 · 三维模型导入与可视化 / 离线编程软件
**技术路线**：基于 Open CASCADE（OCCT）自研
**项目前提**：必须完全自主产权，且软件及全部资料交付给客户
**版本**：V1.0
**日期**：2026-09-14

---

## 0. 本文件的使用方式

- 本文件是**强制性开发规则**，不是建议书。
- 每条规则带编号（`R-x.x.x`）与强制级别：
  - **MUST** —— 必须遵守，违反即不得发布
  - **MUST NOT** —— 禁止，违反即不得发布
  - **SHOULD** —— 应当遵守，偏离需书面说明理由
  - **MAY** —— 可选
- 三个节点必须逐条对照本文件：**代码审查** → **发布评审** → **交付验收**。
- 与本文冲突的既有做法，**以本文为准**。

---

## 1. 项目前提与两条硬约束

**C1｜完全自主产权**
软件知识产权归我方所有，可自由授权、可办理软件著作权登记。

**C2｜全量交付**
软件连同全部资料（含源码、构建脚本、文档）交付给甲方及其关联方，且不得设使用限制、不得主张额外费用。

> ⚠️ **这两条约束与"使用第三方开源组件"之间存在一个必须如实处理的边界。后面所有规则都从第 2 章推导。**

---

## 2. 知识产权边界（先把这个说清楚）

### 2.1 「完全自主产权」的准确含义

**R-2.1.1（MUST）** 对外表述一律使用下述口径，**禁止使用"全部自主知识产权""100% 自主知识产权"**：

> 本软件的自有代码部分，著作权归我方所有。软件包含以下第三方开源组件，其著作权归各自权利人，遵循其各自许可证：Open CASCADE Technology（LGPL 2.1 + Open CASCADE Exception）、Qt / PySide6（LGPL v3）、FCL（BSD-3-Clause）、open62541（MPL v2）等。

**R-2.1.2（SHOULD）** 投标／验收文件中附《第三方组件与许可证清单》（见附录 A 模板）。

> **理由**：任何使用第三方组件的软件都不可能"100% 自主"。声称了，反而在评审或审计时被质疑；如实声明并说明"这些许可证均允许闭源商用与随设备交付"，**比夸大表述更有说服力，也更安全**。

### 2.2 资产归属与转授边界

| 资产 | 著作权归属 | 我方能否转授给甲方 |
|---|---|---|
| 我方自有代码 | 我方 | ✅ 可，且可交付全部源码 |
| OCCT / Qt / FCL 等组件源代码 | 各自权利人 | ⚠️ **只能"传递原许可证"，不得以我方名义再授权** |
| 我方编写的需求文档、设计文档、界面设计 | 我方 | ✅ |
| 我方自绘图标、自研的机构/工具库数据 | 我方 | ✅ |
| 第三方字体、第三方图标集、第三方模型库 | 各自权利人 | 按各自许可证，见 R-5 |

**R-2.2.1（MUST）** 交付协议中明确区分"我方著作权部分"与"第三方组件部分"，不得笼统写"全部知识产权归甲方"。

### 2.3 与采购商业软件的对比（建议写入投标文件）

| 对比项 | 采购 RoboDK Professional（￥35,000） | 自研 |
|---|---|---|
| 永久免费转授甲方及其关联方 | ❌ EULA §2.2 / §2.6(b) / §10.1 明文禁止转授 | ✅ 自有代码可自由授权 |
| 软件著作权登记 | ❌ 非我方著作权，不可登记 | ✅ 自有代码可登记 |
| 满足"不得设使用限制" | ❌ 受 EULA 约束 | ✅ |
| 全量交付源码 | ❌ 不可能 | ✅ 可交付 |
| 10 年后维护主动权 | 在供应商手里 | 在我方手里 |

> **这是选择自研的核心合规理由，不只是成本理由。**

---

## 3. 技术栈与许可证规则

### 3.1 选定技术栈

| 层 | 组件 | 许可证 |
|---|---|---|
| CAD 内核 | Open CASCADE Technology 7.x | LGPL 2.1 + Open CASCADE Exception |
| 界面 | Qt 6 / PySide6 | LGPL v3（开源版） |
| 语言运行时 | Python 3.x | PSF License |
| 碰撞检测（窄相） | FCL | BSD-3-Clause |
| 碰撞检测（备选） | Bullet | zlib |
| OPC UA 客户端 | open62541 / asyncua | MPL v2 / LGPL v3 |
| 数值计算 | NumPy / SciPy | BSD-3 |

### 3.2 许可证分级

- 🟢 **绿（可直接引入）**：MIT、BSD（2/3 条款）、Apache 2.0、zlib、PSF、MPL v2
- 🟡 **黄（可引入，须履行义务）**：LGPL 2.1 / LGPL 3.0 → 遵守第 3.4 节
- 🔴 **红（禁止引入）**：GPL、AGPL、SSPL、CC-BY-NC 等

### 3.3 禁止引入清单（MUST NOT）

**R-3.3.1** 禁止引入任何 **GPL / AGPL / SSPL** 组件。一旦引入，整个软件须开源，与 C1「完全自主产权」直接冲突。

**R-3.3.2** 禁止使用 **Qt Charts / Qt Data Visualization / Qt Virtual Keyboard / Qt Safe Renderer** 等 Qt 商业专属模块。
> ⚠️ 这是最隐蔽的坑：工业软件几乎一定要画曲线图，而 **Qt Charts 在开源版是 GPL v3**。
> **替代**：`pyqtgraph`（MIT）、`QCustomPlot`（MIT / 商业双授权）、或 Qt 6 的 `QtGraphs`（须核实其许可证）。
> 若确需 Qt Charts → 必须采购 **Qt 商业授权**，纳入预算。

**R-3.3.3** 禁止使用 PyQt（GPL 或商业授权），必须用 **PySide6**（LGPL v3）。

**R-3.3.4** 禁止引入**未标注许可证**的代码仓库或代码片段。

**R-3.3.5** 禁止从来源不明的网站、论坛、网盘复制代码片段。所有外部代码必须有可追溯的原始出处与许可证。

**R-3.3.6** 禁止将**商业字体**（微软雅黑、方正系、汉仪系、思源以外的商用受限字体等）随软件分发。见 R-5.1。

**R-3.3.7** 禁止复制竞品商业软件的代码、界面资源、图标、示例程序、文档原文。见第 6 章。

### 3.4 LGPL 组件必须履行的义务（MUST）

> 依据：OCCT 官方文档对专有应用的要求，以及 LGPL v2.1 第 6 条。

**R-3.4.1（MUST）** **所有 LGPL 组件必须动态链接（DLL / .so），禁止静态链接。**
- 静态链接会触发最重的一档义务：须把应用以"可修改形式"单独提供给用户，并附上全部重链接材料（目标文件）。
- **动态链接即自动满足 LGPL 关于"用户可替换该库"的要求。**
- 交付前须核验：安装目录中存在 `TK*.dll`（OCCT）等独立动态库文件，且主程序未将 OCCT 静态编入。

**R-3.4.2（MUST）** 在 **About 对话框**与**用户文档**中显著声明使用了第三方组件。
- OCCT 官方明确标注此项为 **mandatory**。
- 声明模板见附录 B。

**R-3.4.3（MUST）** 随软件提供第三方许可证全文，文件名 `THIRD-PARTY-LICENSES.txt`，位于安装目录根下。

**R-3.4.4（MUST）** 提供用户获取第三方组件源码的途径。
- 本项目为全量交付，**直接将 OCCT 等组件的源码副本一并交付**，并在文档中说明版本号与官方地址。这比只给链接更稳妥。

**R-3.4.5（MUST）** 不得对用户施加比 LGPL 更严格的限制——**不得限制用户修改或替换 OCCT 并重新运行本软件**。
> ⚠️ 本条与"我方想给软件加授权保护机制（加密狗 / 激活码 / 机器绑定）"存在冲突，处理方式见 **R-8.4**。

**R-3.4.6（MUST）** 若对任何 LGPL 组件的**源码做过修改**，修改部分须按 LGPL 开源，并保留修改说明。

---

## 4. 开源组件引入与管理流程

**R-4.1（MUST）** 引入任何新依赖前，提交《依赖引入申请》，含：组件名、版本、许可证、用途、是否有可替代方案、是否影响闭环交付。

**R-4.2（MUST）** 建立并持续维护 **SBOM（软件物料清单）**，格式见附录 C。每次发布更新。

**R-4.3（MUST）** 依赖版本严格锁定并纳入版本管理（`requirements.txt` / 锁文件）。禁止使用浮动版本（`>=`、`latest`）。

**R-4.4（MUST）** 每次发布前执行许可证扫描，输出《许可证合规扫描报告》并归档。
- 工具可用：`pip-licenses`、`ScanCode Toolkit`、`ORT`、`FOSSA`（择一或组合）。
- 扫描结果出现 🔴 红色许可证 → 阻断发布。

**R-4.5（MUST）** 建立**依赖离线包**（wheel / 安装包缓存）。
> **理由**：交付现场与甲方内网通常无外网，若不能离线安装依赖，"全量交付源码"就是一句空话。这是最常见的交付翻车点之一。

**R-4.6（SHOULD）** 优先选择"无传递依赖"或"传递依赖简单"的组件，降低 SBOM 维护成本。

---

## 5. 第三方资产（字体 / 图标 / 数据）

**R-5.1（MUST · 字体）** 软件内嵌或随软件分发的字体，必须是明确允许商用与再分发的字体：
- ✅ 允许：**思源黑体 / Source Han Sans**（SIL OFL 1.1）、**Noto Sans**（OFL）、**文泉驿**（GPL+FE，⚠️ 需核）、**HarmonyOS Sans**（须核商用条款）
- ❌ 禁止：**微软雅黑 / 中易宋体 / 方正系 / 汉仪系** —— 这些**不含随软件再分发授权**，商用分发属侵权
> ⚠️ **字体是三维可视化与工控软件最容易被忽略的侵权点。** 很多团队在 WPF / Qt 里直接写 `Microsoft YaHei` 就发布了——如果只是"调用系统已装字体"（不随软件分发字体文件），风险较低；但**一旦把字体文件打进安装包，就是分发行为，必须取得授权**。本规则要求：**只引用系统字体名，不打包字体文件**；若必须保证跨机器一致显示，则改用 OFL 字体并随包提供许可证。

**R-5.2（MUST · 图标）** 图标必须自绘，或使用 MIT / Apache 2.0 / CC0 图标集（如 Lucide、Feather、Tabler）。随包附许可证声明。

**R-5.3（MUST · 模型与数据）** 不得使用第三方商业软件自带的机器人库、机构库、工具库数据。所有机构定义、工装模型由我方自建，或来自甲方提供的模型（甲方模型的使用须符合 R-9.4）。

**R-5.4（SHOULD）** 建立《第三方资产台账》，登记每个资产的来源、许可证、授权凭证（如购买发票、OFL 声明）。

---

## 6. 清洁室开发规则（防止对竞品构成侵权）

> 依据：著作权保护"表达"而不保护"思想、处理过程、操作方法"（《计算机软件保护条例》第 6 条）。因此**独立实现同样功能不构成侵权**，前提是未接触、未复制对方代码与界面。

**R-6.1（MUST NOT）** 禁止对任何第三方商业软件进行**反编译、反汇编、调试、内存抓取、协议破解**。

**R-6.2（MUST NOT）** 实现人员不得以**竞品软件界面为参照物**进行对照开发。
> ⚠️ RoboDK EULA §2.6(g) 禁止"use the Software to develop a product which is competitive with any RoboDK product offerings"。若开着其试用版逐功能对照编写代码，即触及此条。

**R-6.3（MUST）** 功能规格的唯一输入是**我方已产出的需求文档**（《三维模型导入与可视化软件需求说明书》《第一步实施方案》等）。实现人员只看文档。

**R-6.4（MUST NOT）** 禁止复制竞品的文档、教程、示例代码、界面文案原文。

**R-6.5（MUST）** 交互设计由我方独立完成并留档：设计说明 + 界面草图 + 最终截图，注明设计日期与设计人。

**R-6.6（SHOULD）** 保留完整的开发过程证据：代码提交历史、设计评审记录、需求变更记录。**这些是"独立开发"的证明**，一旦被质疑，可自证清白。

**R-6.7（SHOULD）** 若团队成员曾安装竞品试用版，**不得在同一台机器上**进行对照式开发；用于评估的机器与开发机器分离。

---

## 7. 开发工程规范

### 7.1 需求与追溯

**R-7.1.1（MUST）** 每条功能需求有唯一编号（`FR-xx`），每个需求可追溯到：设计 → 代码 → 测试用例 → 验收结论。
**R-7.1.2（MUST）** 需求变更走书面变更流程，更新需求追溯矩阵。

### 7.2 代码规范与审查

**R-7.2.1（MUST）** 统一编码规范（命名、注释、异常处理、类型标注）。
**R-7.2.2（MUST）** 所有代码合入主干前必须经**至少一人代码审查**并留痕。
**R-7.2.3（MUST）** 启用静态检查（`ruff` / `mypy` / `clang-tidy` 视语言而定），零高危告警方可发布。
**R-7.2.4（SHOULD）** 关键算法（运动学、碰撞、坐标变换）必须有独立于实现者的复核人。

### 7.3 版本与配置管理

**R-7.3.1（MUST）** 使用 Git 管理，采用明确的分支策略，每个发布版本打 tag。
**R-7.3.2（MUST）** **工程文件（工程/程序数据）必须带版本号与兼容策略**，新版本软件能读旧版本工程文件。
**R-7.3.3（MUST）** 软件版本号与工程文件版本号**分别管理**，界面内可见。

### 7.4 构建与依赖

**R-7.4.1（MUST）** 提供**一键构建脚本**（`build.bat` / `build.sh`），在干净机器上可复现。
**R-7.4.2（MUST）** **离线可构建**：依赖全部本地化，构建过程不访问外网。
**R-7.4.3（MUST）** 记录构建环境（编译器/解释器版本、第三方库版本、构建日期、构建人）。
**R-7.4.4（SHOULD）** 保留构建产物与源码的对应关系，支持按版本重新出包。

### 7.5 配置与标定分离 ★

**R-7.5.1（MUST NOT）** **禁止将任何现场相关的数值硬编码进代码**，包括但不限于：轴数、行程、限位、零点偏移、比例系数、运动方向、PLC 地址、DB 块偏移、IP 地址、工位数量、模具尺寸、节拍参数。

**R-7.5.2（MUST）** 上述全部参数必须来自**外部配置文件**，且配置文件：
- 有明确的字段说明与单位
- 有版本号
- 支持导入/导出
- 有合法性校验（超范围即拒绝加载并报警）

**R-7.5.3（MUST）** 界面提供**参数查看与校验**功能，现场可直接读到当前生效值。
> **理由**：本项目最大的风险是"静默失败"——零点偏移错 20mm、方向反了，模型照样在动，画面毫无异常。硬编码会让每一次现场适配都要改代码、重新出包，且无法追溯。

### 7.6 日志与诊断 ★

**R-7.6.1（MUST）** 结构化日志，分级（DEBUG / INFO / WARN / ERROR / FATAL），带时间戳与模块标识。
**R-7.6.2（MUST）** 日志**自动滚动与限容**，防止占满工控机磁盘。
**R-7.6.3（MUST）** 记录关键事件：模型导入结果、配置加载结果、通讯建立/断开、轨迹下发、碰撞拦截、报警、参数变更。
**R-7.6.4（MUST）** 提供"导出诊断包"功能（日志 + 配置 + 软件版本 + 环境信息），现场可一键导出发给开发。
**R-7.6.5（MUST）** **日志中不得包含甲方的模型几何数据全文**，只记文件名、面数、耗时等元信息。见 R-9.4。

### 7.7 异常与降级

**R-7.7.1（MUST）** 所有外部输入（文件、网络、PLC 数据）必须校验并处理异常，**禁止未捕获异常导致进程退出**。
**R-7.7.2（MUST）** 通讯断线必须：明确提示 → 保持最后已知状态并标注"数据过期" → 自动重连 → 恢复后重新同步。
**R-7.7.3（MUST NOT）** 禁止在通讯异常时对模型姿态做**外推预测**。宁可滞后，不可超前。
> **理由**：从真实危险位预测到安全位会掩盖风险；从安全位预测到干涉位会产生虚假报警。

**R-7.7.4（MUST）** 所有失败场景给出**可操作**的提示（说明原因 + 建议动作），而非只报错误码。
**R-7.7.5（SHOULD）** 主动识别并诊断常见数据问题，例如：STEP 文件被企业加密软件加密（`%P` 文件头 / 高熵零填充）→ 应提示"文件未提供明文版本"，而非笼统报"格式不支持"。

### 7.8 编码与路径 ★

**R-7.8.1（MUST）** 全局统一 UTF-8，文件读写明确指定编码。
**R-7.8.2（MUST）** **必须支持中文路径、中文文件名、含空格的路径**（工地现场普遍如此）。
**R-7.8.3（MUST）** 路径长度兼容 Windows 长路径场景（≥260 字符）。
**R-7.8.4（MUST）** 不在系统盘/用户目录写入业务数据；所有数据落在软件安装目录或指定的数据盘目录。

### 7.9 性能预算

**R-7.9.1（MUST）** 每条性能指标须有**可测方法**与**判定阈值**，纳入验收。
**R-7.9.2（MUST）** 关键性能不得依赖开发机配置：**必须在目标工控机配置下实测**，并记录硬件型号。
**R-7.9.3（MUST）** 性能承诺值须留 ≥30% 余量后再写进标书。
**R-7.9.4（MUST）** 性能报告中区分"全部帧均值"与"最坏帧峰值"，不得只报均值。

### 7.10 测试策略

**R-7.10.1（MUST）** 分层测试：单元测试（算法/坐标变换）→ 集成测试（导入→拾取→路径→下发）→ 系统测试（含模拟 PLC）→ 现场测试。
**R-7.10.2（MUST）** 核心算法（运动学正解、坐标变换、碰撞判定）必须有**单元测试与已知用例**。
**R-7.10.3（MUST）** 建立**离线干跑测试**：无需真实设备，用模拟数据源跑通"关节值 → 运动学 → 模型姿态 → 碰撞判断"整条链。
**R-7.10.4（MUST）** 提供 **OPC UA Server 模拟器**，使通讯层在办公室即可开发与验证。
**R-7.10.5（MUST）** 每个交付版本保留回归测试结果。

### 7.11 安全

**R-7.11.1（MUST）** 通讯层不得接受未授权写入；下发通道须有权限校验。
**R-7.11.2（MUST）** 关键操作（参数修改、轨迹下发、标定变更）写入**审计日志**（谁、何时、改了什么）。
**R-7.11.3（MUST）** 软件**不承担安全功能**（Cat 3 / PL d、急停、限位由安全 PLC 与硬回路实现）。此边界须在文档与界面中明确。
**R-7.11.4（MUST）** 碰撞路径禁止下发的判定，必须在**按钮禁用 + 下发函数内部二次校验**双重实现，不得只做界面置灰。

---

## 8. 交付规则

### 8.1 交付物清单（MUST）

**R-8.1.1** 交付内容至少包含：

| 类别 | 交付物 |
|---|---|
| 可执行软件 | 安装包 / 免安装版、依赖动态库（含 OCCT 等 DLL） |
| **源代码** | 全部自有代码；第三方组件源码副本（OCCT 等） |
| **构建** | 一键构建脚本、锁定版本的依赖清单、**离线依赖包**、构建环境说明 |
| 第三方合规 | `THIRD-PARTY-LICENSES.txt`、**SBOM**、许可证扫描报告、《第三方组件与许可证清单》 |
| 文档 | 需求说明书、设计说明书、接口文档、用户手册、维护手册、部署手册 |
| 测试 | 测试报告、验收记录、性能实测报告（含硬件型号） |
| 配置 | 现场参数配置文件模板 + 字段说明 |
| 知识产权 | 软件著作权登记证书（副本）、知识产权归属说明 |
| 培训 | 培训教材、操作视频、培训签到与考核记录 |

**R-8.1.2（MUST）** 交付清单在**合同签订前**与甲方书面确认，避免验收期扯皮。

### 8.2 交付技术前提

**R-8.2.1（MUST）** **验收测试须在"干净机器"上执行**：无开发环境、无外网、仅使用交付包，从零完成"安装 → 构建 → 运行"全流程。
> **理由**："全量交付源码"最常见的翻车方式是：客户拿到源码却编不出来（缺依赖、缺构建脚本、需要外网、路径写死）。**这一条是全量交付的成败关键。**

**R-8.2.2（MUST）** 交付包内提供《从源码构建指南》，含逐步操作与常见问题。

**R-8.2.3（MUST）** 交付前在甲方目标工控机（同型号同配置）上完成一次完整安装与运行验证。

### 8.3 授权机制与"不得设使用限制"的冲突处理 ⚠️

**R-8.3.1（MUST）** **交付给本项目的软件，不得设置任何使用限制**：不得用加密狗、激活码、机器绑定、使用期限、功能阉割等方式限制甲方及其关联方使用。

**R-8.3.2（MUST）** 交付给甲方的版本与供我方自用/其他客户使用的版本，**在代码层面允许存在授权模块，但交付版本必须整体关闭**，且不得因授权校验失败而影响功能。

**R-8.3.3（MUST NOT）** 不得在交付版中预留"远程停用""到期锁定"等可被解释为使用限制的机制。

> ⚠️ **这一条必须在报价与合同阶段就跟商务确认**：一旦承诺"不得设使用限制"，就意味着这套代码交出后，甲方可自行复制、修改、用于其关联方的其他设备。**这是我方商业模式的重大让步，但也是招标的硬条款。** 若后续还要向其他客户销售同类软件，须在合同中区分"本项目交付物"与"我方通用产品"。

**R-8.3.4（MUST）** 交付版本中**不得包含我方为其他客户开发的功能、数据或标记**。

### 8.4 保密与数据剥离

**R-8.4.1（MUST）** 交付前**剥离全部甲方模型与工艺数据**（或按合同约定处理），确保不把我方其他客户的数据带出。
**R-8.4.2（MUST）** 甲方提供的三维模型、工艺参数、图纸属甲方保密信息，仅用于本项目，不得用于其他项目或对外展示。
**R-8.4.3（MUST）** 演示材料、宣传材料中使用甲方模型须取得书面许可。
**R-8.4.4（SHOULD）** 建立数据访问台账，记录甲方数据的存储位置与访问人员。

---

## 9. 人员与知识产权管理

**R-9.1（MUST）** 所有参与开发的员工签署《职务成果知识产权归属协议》，明确职务开发成果著作权归公司。

**R-9.2（MUST）** 外包、兼职、实习、合作开发人员，**必须签署知识产权转让/归属条款**后方可接触代码。
> ⚠️ **这是"完全自主产权"能否成立的前提。** 若有关键代码由外部人员编写且未约定归属，软著登记与后续授权都会出问题。

**R-9.3（MUST）** 人员离职时完成代码与文档交接，并审计其在离职前 N 个月的提交记录，确认无异常外传。

**R-9.4（MUST · AI 辅助编码）** 使用 AI 工具生成代码时：
- 禁止将甲方保密数据（模型、图纸、工艺参数）提交给任何外部 AI 服务
- AI 生成的关键代码须经人工复核，确认无来源不明的第三方代码片段
- 记录 AI 辅助范围，纳入代码审查关注点
> **理由**：AI 生成代码的著作权状态尚不明确，且可能近似于训练数据中的既有代码。**"完全自主产权"要求这段来源可解释。** 本项目允许使用 AI 工具提效，但不允许把 AI 输出当作"免审查代码"直接入库。

---

## 10. 红线清单（违反即不得发布）

| # | 红线 |
|---|---|
| 1 | 引入任何 GPL / AGPL / SSPL 组件 |
| 2 | 使用 Qt Charts 等 GPL 模块 |
| 3 | 静态链接 OCCT 等 LGPL 组件 |
| 4 | 随软件打包无商用授权的字体文件 |
| 5 | 在 About / 文档中漏掉第三方组件声明 |
| 6 | 反编译、破解任何第三方商业软件 |
| 7 | 以竞品界面为参照物对照开发 |
| 8 | 复制竞品代码、界面资源、文档原文 |
| 9 | 硬编码现场参数（轴数、行程、零点、PLC 地址等） |
| 10 | 交付版设置使用限制（加密狗 / 激活码 / 期限） |
| 11 | 交付包上网依赖（不能离线构建） |
| 12 | 交付前未剥离他方客户数据 |
| 13 | 声称"100% 自主知识产权" |
| 14 | 关键代码由未签署知识产权协议的人员编写 |

---

## 11. 发布前 / 交付前检查清单

### 11.1 每次发布

- [ ] 许可证扫描通过，无红色许可证
- [ ] SBOM 已更新
- [ ] 新增/变更依赖已登记
- [ ] 静态检查零高危告警
- [ ] 代码审查记录完整
- [ ] 单元测试 + 回归测试通过
- [ ] 版本号已更新，Git 已打 tag
- [ ] `THIRD-PARTY-LICENSES.txt` 已随包更新

### 11.2 交付前（Gate）

- [ ] 交付物清单逐项核对完毕（8.1）
- [ ] **干净机器 + 无外网**环境下完成"安装 → 构建 → 运行"验证
- [ ] 安装目录确认存在独立的 LGPL 动态库文件（非静态链接）
- [ ] About 对话框与文档含第三方组件声明
- [ ] 交付包内无甲方以外的模型/数据
- [ ] 交付版无任何使用限制机制
- [ ] 《第三方组件与许可证清单》已出具
- [ ] 软件著作权登记已完成或已受理
- [ ] 全部开发参与人员的知识产权协议已归档
- [ ] 用户手册 / 维护手册 / 部署手册 / 构建指南齐套
- [ ] 培训与验收计划已确认

---

## 附录 A：《第三方组件与许可证清单》模板

| 组件名 | 版本 | 许可证 | 使用方式 | 链接方式 | 义务履行情况 |
|---|---|---|---|---|---|
| Open CASCADE Technology | 7.x.x | LGPL 2.1 + OCCT Exception | CAD 内核 | 动态链接（DLL） | About 声明 ✅ ／ 许可证全文 ✅ ／ 源码已交付 ✅ |
| PySide6 | 6.x.x | LGPL v3 | 界面 | 动态链接 | 同上 |
| FCL | 0.7.x | BSD-3-Clause | 碰撞检测 | 静态 | 保留版权声明 |
| open62541 | 1.x | MPL v2 | OPC UA 客户端 | 动态链接 | 无修改，无需公开 |
| 思源黑体 | 2.x | SIL OFL 1.1 | 界面字体 | 随包 | 附 OFL 许可证 |

---

## 附录 B：第三方组件声明模板（About 对话框 / 文档）

```
本软件使用了以下第三方开源组件：

Open CASCADE Technology（http://www.opencascade.com）
Copyright (c) 1999-2024 by OPEN CASCADE S.A.S.
本软件使用了 Open CASCADE Technology，其依据 GNU Lesser General Public
License 2.1（含 Open CASCADE Exception）发布。您有权依照该许可证获取、
修改并替换该组件。许可证全文见安装目录 THIRD-PARTY-LICENSES.txt。

Qt for Python (PySide6)（https://www.qt.io）
本软件使用了 PySide6，其依据 GNU Lesser General Public License v3 发布。

FCL — Flexible Collision Library
本软件使用了 FCL，其依据 BSD 3-Clause License 发布，版权归其作者所有。

完整许可证文本与组件源码获取方式，见安装目录 THIRD-PARTY-LICENSES.txt
与 SOURCE-CODE-NOTICE.txt。
```

---

## 附录 C：SBOM（软件物料清单）模板

```csv
组件名,版本,许可证,许可证类型,来源URL,引入日期,链接方式,是否修改,义务履行
Open CASCADE Technology,7.8.0,LGPL-2.1-with-OCCT-exception,黄,https://dev.opencascade.org,2026-09-20,动态链接,否,声明+许可证+源码
PySide6,6.7.0,LGPL-3.0,黄,https://pypi.org,2026-09-20,动态链接,否,声明+许可证
FCL,0.7.0,BSD-3-Clause,绿,https://github.com/flexible-collision-library/fcl,2026-09-20,静态,否,版权声明
```

---

## 附录 D：本文件的修订记录

| 版本 | 日期 | 修订内容 | 修订人 |
|---|---|---|---|
| V1.0 | 2026-09-14 | 首次发布 | — |

---

**本文件为强制规则。任何偏离须书面申请并经技术负责人批准，记录在案。**
