# 进度与验收：机械臂工作台 · 完全重构

- 建档：工程智拓 @pm ｜ 2026-09-14（v2，随 D-1 完全重构口径改版；v1 旧原型路线作废；v3 2026-09-14 审核修订：解析逆解口径/契约章号/face_map/配置字段/常驻复查项；**v4 2026-09-15 并行编排定案：加波次/会话标记＋仓路径变更，@user 批复"全按建议"**；**v5 2026-09-15 追加代码粒度硬约束：单文件 ≤300 行，详见 04 文档 §4.5**；**v6 2026-09-15 20:25 路径重拍（维持 D 盘、理由更正：E 盘实测存在）＋ Q 编号双体系引用规范（04 §5.7／G9）＋ 环境实测（无 conda、无可用 3.11）**；**v7 2026-09-15 20:45 @user 审核反馈：①"缺 coreutils"归因作废（会话相关 PATH 差异，非本机缺文件；"误判无 E 盘"机制未确认）＋ 禁用裸 `sort`／`find`（静默落 Windows 版）；② 新增 Q 编号"回填错行"防错（04 §5.7-⑤／G10）＋ 回填作业规程 ＋ 沟通单时效口径更正（已定稿待递，配 V1.1 草案）**；**v8 2026-09-15 21:10 @user 第三轮审核：① 沟通单 V1.1"一字未改"声明更正（实测 12 项表内 2 处引号样式变动：电Q-9 问题列、电Q-6 为什么问列）＋ V1.1 第四节第 2 项改指页脚 ＋ 修订记录重复字删；② Q 项引用写法统一（`电Q-x` ≡ `沟通单 Q-x`，择一，V1.1 递出后全仓统一）——本表 T07 行、外部依赖节已同步**；**v9 2026-09-15 21:35 @user 第四轮复核：① 执行层补齐——`Tasks\` 下 5 处裸 Q 编号已改带体系名（T04:23／T07:11→`契约 Q-11`；T07:7／T07:20→`电Q-8`；T09:13→`电Q-2`；T01 单 0 命中），复验 0 残留；② T01 开工前机器事实多通道复测（干净起点／无 conda／Python 3.12.7／git 2.50.0／uv 3.11 仍不可用／D 盘 121.0GB、E 盘 715.8GB／conda-forge pythonocc-core 7.9.3）；③ 新增网络与代理条（系统代理 7897 在线，另有会话级环境变量代理 24633 亦在线、NO_PROXY 未设；三通道×三目标全 200、直连亦通；下载失败先测端口勿归因渠道；离线须断网验证，属 T10）；④ 登记 CR-2026-01 时序（须在 T01 合并前出）**；**v10 2026-09-15 21:15 新增配套件《05_开工前复盘与沉淀_V1.0.md》——阶段状态快照、6 次事实性纠错留案（含 1 处自我更正）、10 条可复用纪律（指向技能 v1.4.0）、9 项项目专属约束索引、沉淀落点索引、新会话交接清单**；**v11 2026-09-15 21:55 @user 指示：吸收《三维模型导入能力综合技术方案》（张晓东）及《审阅意见 V1.0》——T05 卡扩口径（原生转换链/单次通过释放/双缓存/三阶段进度/离线预处理管线/P0 实测）、T08 卡落实保守包络条款＋碰撞两段制、03 §3/§8/§9 增补、04 §6.4 T05 派单卡与 §7.2 新增 G11–G15、登记 CR-2026-03（草案）；其中 G11（契约 8 轴上界 vs 实机 22 轴）待裁决，建议 T03 开工前定**）；**v12 2026-09-15 22:20 @user 批准 G11 裁决（①＋② 并行）并落地：03 §4 配置模式（role/modes/coupling 两型/envelope/deflection＋"实机轴系口径"块，含契约扩容＋AxisMask 随契约 V1.3、"未用轴位填 0"废止）、T03 卡整卡改写（22 轴样例）、T04/T07/T09 卡挂口径、04 §6.3 T03 派单卡与 §7.2 G11 标已定＋新增 G16、CR-2026-03 升 V1.1**；**v13 2026-09-15 22:35 @user 指示"按建议继续全部完成"：G16 落地——02 升 V1.1（顶栏工作模式选择器、§3 模式表增工作模式行、步骤②前置锁定、步骤③换臂作废）、T02/T06/T07 卡同步接线、04 §7.2 G16 标已定、CR-2026-03 升 V1.2**；**v14 2026-09-15 22:45 @user 明确职责边界：主要动作由 PLC 编程，涉及轨迹编程的工况由软件输出轨迹参数交 PLC——写入 03 §4 实机轴系口径块与 T07 卡产物口径，04 §7.2-G11 与 CR-2026-03 §三-1 同步；G11 遗留待确认项收窄为"工具臂专用轴是否参与轨迹编程"**）；**v15 2026-09-15 23:07 @user 定案（时间戳取自文件 mtime；原写"23:10"系估算，2026-09-15 23:30 更正）**：①仓路径并入项目目录 `D:\ZCode项目\多功能机械臂软件`——实测该目录**已是既有 git 仓**（main、3 次提交、origin=mecharm-workbench-docs、跟踪 22 个文档）→ **代码并入既有仓、T01 不再 git init**，新增同仓两条纪律（功能分支禁改文档与 CR；`.workbuddy/` 入 .gitignore）；②对外承诺口径确认＝**P0 实测前不承诺导入性能数字**。同步：CR-2026-01 升 V1.1（本节仓路径口径条已改写、"新建空仓/干净起点"作废）、CR-2026-03 升 V1.3、00_README／Tasks\T01／04 §1.3·§5.1·§5.2·§6.2／05／01-D-7 全部改写**）；**v16 2026-09-15 23:30 @user 第五轮审计（七条全部成立，逐条修正）：①T01 卡与完成标准改为"禁 `git add -A`、只按路径 add；完成标准＝本单交付路径内无未提交改动"＋新增 §5.5 附5 合并前检查命令（**必须带 `-c core.quotepath=false`**，否则中文路径被转义→假通过）；②T09 过渡期口径（`pack_profile=v1_2_8axis` 按契约 V1.2 实现，AxisMask 只留配置内）＋T07/03 §4 同步；③**V1.3 扩容影响测算入 CR-2026-03 §三-5**（段 56→124 B、下发块 584→1264 B、50 段方案 6224 B、**回读块 92/96→约 228 B**、建议新增掩码命名 `SegAxisMask` 防与 `EnableMask` 二次误引）；④组件增补流程与落盘归属（04 §4 矩阵：`environment.yml` 归属改指挥方、`view/vendor/**` 由引入单报备落盘、附6 越界检查）＋Draco/meshopt 降为"包名待核、暂不引入"＋G14 口径收紧为"禁前端 WASM 做 **CAD 解析**"；⑤04 §1.3-① 破版行重建并改现行路径＋"决议列以后出变更单为准"；⑥外部依据归档进仓 `参考资料/`（方案、审阅意见、踏勘底稿＋README）；⑦杂项（121.0→120.8、常驻复查正则补三字段、05 §二-1 归因泄漏删除、时间戳改取系统钟）**）；**v17 2026-09-15 @user 第六轮审计（六条全部成立，逐条修正）：①T01 派单卡不再写死基线 commit 号（原写 `ebac7a6`，现 main 已 `44769d2`，比对即"开工即停"）——判据改为只看"文档路径是否脏"；②**双缓存落盘路径补齐**：03 §4 新增 `paths.cache_dir`（默认 `%LOCALAPPDATA%\mecharm\cache`，仓外；加载器校验仓内路径拒启动）＋`.gitignore` 加 `cache/`／`data/`／`*.brep`＋04 §4 矩阵新增缓存行＋§5.1-6 推送边界＋T05/T03 卡同步；**并实测远端可见性：`API 200 private= False` → 远端为公开仓**，立 04 §7.1-10 风险条；③CR-2026-03 §三-5 测算表"依据列"改按 124 B 的真实算式重写（原 @8.0 口径复算得 128 B），回读块依据补三步（223→224→228），**新增 `EnableMask` 加宽能否吞并 `Reserved2`（L253 WORD@78.0）的 V1.3 悬置**；④04 §5.5-附6 重做（原正则对 `three-mesh-bvh`／vendor 解码器／`import DracoPy` 三条实测全 MISS）→ 改"`view/vendor/` 目录白名单＋AST import 比对 `requirements.lock.txt`"两条硬判据，附5 模式补 `参考资料/`；⑤T09 卡两个"2."已重编号（口径块移出步骤为独立小节）；⑥04 §7.2-G11 引用 §三-2 → §三-5）**；**v18 2026-09-16 00:28（系统钟）@user 第六轮批复末句授权"如果还有不合适的你直接修改，改到能开工为止"——第七轮据此由指挥侧代理落地。先核收：**P1–P6 六条全部已落地且互洽**；本轮另查出**四类新缺陷并直接修**：**① ⛔ 推送冻结令（新设，即刻生效，04 §5.1-6）**——此前**全仓没有任何一条禁止 `git push`**，而实测远端为**公开仓**、本地 `main` 领先 **3 个未推提交**（含 `参考资料/` **四件**，逐件实测每份抬头均含**甲方全称「通裕重工股份有限公司」＋招标项目编号 `GXTC-A1-26170051`**，《踏勘确认清单与运动轴清单-附录A填报底稿》另含实机 22 轴结构）→ **一次习惯性 push 即外泄**。同步落点：04 §6.2 T01 卡＋`Tasks\T01`（禁止事项与自测项各一条"只 commit 不 push"）＋`参考资料/README` 规则 5；并写明**本包从未要求任何单推送**、汇报出现"已推送"按违规打回；**撤销须 @user 决定可见性处置后由指挥会话执行**。**② 04 §7.1-10 重写**——原记"已推 4 次"系误记，实测 `git ls-remote --heads origin` → 远端 `main` = `2569104`＝**3 次**；补**已公开内容清单**（22 件，含 S-1 契约 V1.2 草案——其 **L529** 含买方称谓"通裕"、沟通单 V1.0、三规程、任务包旧版、**投标期排期对照表 v1**：澄清 9/17 12:00／踏勘 9/16／领取文件 9/15 17:00／**开标 9/30 9:00**／**内部完工内线 9/25**／"16(3)②③＝废标条款"判项／"投标线＝旧原型顶材料"内部口径）与**再推一次才公开的**（`参考资料/` 四件＋99 至 v17 的审计链）；证据双通道（未认证 API `private=False` ＋ `raw.githubusercontent` 200／404）；补两条边界：**改 Private 不消除既成事实**、**重写历史须 @user 明确指示且任何会话不得自行执行**。**③ 04 §5.5 附6-② AST 脚本两处缺陷修入文件**（第六轮已复现、当时标注未修）：加 **`LOCAL` 仓内包白名单**（原会把 `from core.config import load_machine` 判成越界第三方包 → **T02 起每一单验收必撞假阳性**）＋扫描作用域由 `core/comm/app` 扩为含 **`tools/`、`tests/`**（原 `tools/pre.py: import meshoptimizer` **完全漏检**）；改前／改后输出与残余局限（JS 侧不在②覆盖内）记入注 1。**④ 04 §6.2 T01 卡九处**——完成标准由"**git 工作树干净**"改为"**本单交付路径内无未提交改动**"（原写法与卡内 ②③④ 自相矛盾，会诱导实现会话把指挥方的文档改动**提交掉或 stash 掉**，本轮最高危卡内缺陷）；新增**时点值通则**（卡内一切 commit 号／提交数／文件数／磁盘容量皆背景值、**不作核对项**）；`参考资料/**` 补入禁改清单与同仓纪律；**T01 在 `main` 上作业登记为 §5.1-1"每单一分支＋worktree"的唯一例外并给终止条件**（T01 收口即失效）。**⑤ 容量口径两处残留补齐**——04 §7.1-7 与 **CR-2026-01 §六** 的 121.0→**120.8GB**（2026-09-16 00:01 `shutil.disk_usage` 复测，标时点值）＋加 `paths.cache_dir` 落 **C 盘**的监控口径；CR-2026-01 以其 **V1.3** 行补正，并在 V1.2 行**追加**更正说明（不改写原文）。**⑥ 表格结构四处修复**（复测：全仓 `.md` 表格**列数不一致的行 = 0**）——04 §7 修订记录表**第五轮与第六轮两行被上一轮编辑粘成同一物理行、且第五轮行的"版本／日期"两列丢失**（已拆两行、按时间升序重排并注记来历）；04 §5.5 ②③ 两行与 03 §3 `collision.py` 三态签名代码段内**未转义竖线**破表（逻辑列 11／14／6 → 5／5／4）；CR-2026-03 §七 **表头 3 列 vs V1.4／V1.5 四单元格**（补「编制」列并回填 V1.0–V1.3，其 V1.6 行留痕）。**⑦ 自查两条（记此自警）**——一次编辑曾**误删 CR-2026-03 V1.5 行末"声明前不得单方引数"**，`git diff` 自检时还原（现该行与 HEAD 逐字一致）；新写的两处补记时间戳（00:35／00:32）**系估算**，已按系统钟 00:28 更正。**⏳ 待 @user 决策四项（本包及任何会话不得代行）**：远端改 Private 还是维持公开；是否重写历史并联系 GitHub 清除缓存／fork；`参考资料/` 迁出仓外（README 规则 5 的 (a) 案）还是随仓走私仓并在公开前脱敏；契约 V1.3 对 `EnableMask` 加宽与 `Reserved2` 的显式声明（声明前 §三-5 各数字**不得发给电气**）。**⚠️ 另记：9/16＝踏勘日兼《沟通单》递单日，该件仍为草案**）
- 更新规则：每单状态变化即改本表；验收记录只追加不删除
- 范围口径（D-2）：本包只管软件功能实现；投标取证材料另行安排，不在本表

## 状态表

| 单号 | 任务 | 状态 | 验收轮次 | 备注 |
|---|---|---|---|---|
| T01 | 环境与仓库启动 | 待做 | — | `[W0-A]` OCC 渠道已核实：conda-forge pythonocc-core 7.9.3；PyPI 装不到；**开工判据＝只看"文档路径是否脏"（`git status --porcelain -- 前端任务包 参考资料` 已跟踪条目），**不比对 commit 号**（2026-09-15 第六轮审计）**；`.gitignore` 已含 `.workbuddy/`／`cache/`／`data/`／`*.brep` |
| T02 | 工作台 UI 外壳 | 待做 | — | `[W1-B]` 与 T03 并行（03 §7 原文允许） |
| T03 | 参数体系 machine.yaml | 待做 | — | `[W1-A]` 界面零硬编码前提；含已批准新增 `tools/lint_no_magic.py`；**新增 `paths.cache_dir`（默认仓外 `%LOCALAPPDATA%\mecharm\cache`，仓内路径拒载），2026-09-15 第六轮审计** |
| T04 | 运动学正解与坐标变换 | 待做 | — | `[W2-A]` 手算复核防自证；**本单不做 ik**（ik 落 T07） |
| T05 | 模型导入与视口显示 | 待做 | — | `[W2-B]` 16(1)(2) 废标条款单；按放宽项 R2 提前于 T04 开工；**2026-09-15 吸收《综合技术方案》扩口径（CR-2026-03／G12–G15）：原生转换链＋单次通过释放＋双缓存＋三阶段进度＋离线预处理管线＋P0 实测（性能数字 P0 前一律暂定）**；**缓存/资产库落盘＝`paths.cache_dir`（仓外），`.gitignore` 已加 `cache/`／`data/`／`*.brep`（第六轮审计）** |
| T06 | 面拾取与点位示教 | 待做 | — | `[W3-B]` 与 T04 余量并行（放宽项 R4） |
| T07 | 路径生成与动画 | 待做 | — | `[W4-A]` **电Q-8（下发轨迹段格式 blending）**；**解析逆解 ik 落本单**（构型以**契约 Q-11** 轴清单为准） |
| T08 | 碰撞三态与禁发双阻断 | 待做 | — | `[W5-A]` 16(3)②③ 废标条款单，验收必测穿透拦截；**保守包络 2–5mm 已于 2026-09-15 落实进 T08 卡正文**（CR-2026-03／G4·G13：权威判定保持 OCC 精判，前端预检仅可选交互层） |
| T09 | OPC UA 模拟器与客户端 | 待做 | — | `[W2-C]` 禁连真 PLC（D-6）；按放宽项 R3 与内核链并行（规程 §4.2） |
| T10 | 全链路收口与打包 | 待做 | — | `[W6-D]` 收口单；补契约 §9.1 步 9 审计日志 |

状态口径：待做 → 在做 → 待验 → 已过 / 打回

### 并行编排口径（2026-09-15 @user 定案，详见 04_多会话并行分工指导_V1.0.md）

- 备注列 `[W波次-会话]`：W0–W6 为开工波次；**A**＝参数·运动学线（T01→T03→T04→T07→T08）｜**B**＝几何·界面线（T02→T05→T06）｜**C**＝通讯线（T09）｜**D**＝收口（T10）。同一波次内可同时开单，**实现会话上限 3**。
- 隔离机制：**每单一个 git worktree ＋ 独占分支 `task/TXX`**；main 只由指挥会话合并；ZCode 不得改本表与任务包文档。
- **仓路径口径（2026-09-15 23:04 @user 再定 — 最新）**：定 **`D:\ZCode项目\多功能机械臂软件`**（**并入项目目录**，不再另建"机械臂工作台"目录）。实测该目录**已是 git 仓**：`main`、3 次提交、**`origin = .../mecharm-workbench-docs.git`**、跟踪 22 个文档 → **代码并入既有仓，T01 不再 `git init`**；新增同仓纪律（功能分支禁改 `前端任务包/**` 与 `CR-*.md`；`.workbuddy/` 不入库）。下列 20:25／20:45 条文为历史留痕，其中"`D:\ZCode项目\机械臂工作台`"路径**已被本条取代**（见 CR-2026-01 V1.1）。
- **仓路径口径（2026-09-15 20:25 @user 重拍，历史留痕）**：01/D-7 原写 `E:\ZCode项目\机械臂工作台` → 曾定 `D:\ZCode项目\机械臂工作台`，理由＝与文档目录同级、同在工作盘。⚠️ 一度使用的理由"本机无 E 盘"**已作废**：实测 **`E:\` 存在**（931.5GB／可用 **715.8GB**），`D:\` 可用 **120.8GB**。⚠️ **20:45 二次更正**：曾把该误判归因于"Bash 缺 coreutils"，**该归因同样作废**——双方各自实测：@user 终端 `/usr/bin` 下 `ls/grep/awk/wc/sort/find` 全正常、`git` 在 `/mingw64/bin`；我方某会话 PATH 不含 `/usr/bin` 故报 `command not found`，但 `/usr/bin/{ls,grep,awk}`、`/mingw64/bin/git` **磁盘上均在** → **本质是会话相关 PATH 差异**。**"误判无 E 盘"的机制至今未确认，本表不写归因**，只保留纪律"探测失败 ≠ 事实不存在、须换独立通道复核"（见 04 文档 §5.5 前置注／§7.1-8）。须补书面变更单（建议编号 `CR-2026-01`）并在单中**一并记录此更正**。**Miniforge 定 `D:\Miniforge3`**（环境实测：**无 conda/Miniforge3**；系统 Python 3.12.7；uv 托管的 3.11 不可用 → 当前无任何可用 3.11）。
  - ⚠️ **Bash 命令可用性随会话变化（全线自测前先自检）**：跑 `grep/awk/wc` 之前先 `command -v grep awk wc`；无解析再补 `export PATH="/c/Program Files/Git/usr/bin:$PATH"`（**会话内防御，非本机固定前提**），或改用 PowerShell 版。**禁用裸 `sort`／`find` 取证**——本会话实测它们**静默解析到 `C:\Windows\system32\sort.exe`／`find.exe`**，**语义不同却不报错**，属"命令成功但落在意料之外的同名程序上"，结论不可用。
  - ⚠️ **D 盘可用仅 121GB**：Miniforge＋pkgs 缓存＋`mecharm` 环境＋打包产物预计 5–10GB，须监控；低于 20GB 时停下汇报（E 盘 715.8GB 为备选落点）。
- **Q 项引用写法（2026-09-15 21:10 统一，见 04 文档 §5.7-④）**：前缀式 `电Q-x` 与体系名式 `沟通单 Q-x` **等价、择一**（同一处不得并列两种）；**随《沟通单 V1.1》递出生效，全仓统一 `电Q-x`**（递出前引该单项目仍写 `沟通单 Q-x`）；`契约 Q-x` 不加前缀。故本表 T07 行等处的 `沟通单 Q-8` 已统一为 `电Q-8`。**适用范围＝执行层**：`Tasks\T01–T10` 任务单同等适用，**实测 5 处裸编号已于 2026-09-15 全部改为带体系名（复验 0 残留）**，T01 单 0 命中；自检脚本见 04 文档 §5.7-④-5。
- **不可并行的一对**：`T04 ∥ T07`（同写 `core/kinematics.py`）、`T05 ∥ T06`（同写 `core/geometry.py`）；其余并行均以 04 文档 §4 文件归属矩阵为准。
- 放宽项：R2（T05 提前）、R3（T09 提前）、R4（T06 提前）均已批准，各附风险与回退条款，见 04 文档 §3.2。
- **网络与代理（2026-09-15 实测）**：系统代理**已启用** `127.0.0.1:7897`（端口在线）；本机**另有会话级环境变量** `HTTP_PROXY`／`HTTPS_PROXY=http://127.0.0.1:24633`（**另一个端口，亦在线，`NO_PROXY` 未设置**）→ **环境随会话变化**，与上文 Bash 条同构。实测**直连／7897／24633 × pypi.org／conda.anaconda.org／TUNA 全部 HTTP 200**（直连亦通）。⚠️ **下载失败先测这两个端口在不在线，不要先归因渠道**（conda-forge `pythonocc-core` 7.9.3 的成立性已单独实测）。⚠️ **"开发机能下载"≠"离线能跑"**：D-8 离线要求（`view/vendor` 本地 three.js、禁运行期 CDN）**必须断网／禁代理验证**，属 **T10 验收项**。
- **CR-2026-01 时序（@user 2026-09-15 指示）**：仓路径变更单**须在 T01 合并前出**，否则仓的 origin 路径会先于书面依据存在。已出草案：`CR-2026-01_变更单_代码仓路径与并行编排口径_V1.0.md`。
- **代码粒度硬约束（2026-09-15 @user 指令）**：**单个源文件 ≤300 物理行、单函数 ≤50 行**；超限按职责拆分（不是压行），拆分新增的源文件须在派单卡内先行报备。细则、白名单、反作弊条款、自查命令见 **04 文档 §4.5**。本节第 3 条"常驻复查项"**原文仍为三项**；粒度检查作为附加项（04 文档 §5.5-附4），在收单时一并复核。
- **热点文件包化＝条件式预授权（2026-09-15 @user 批复 G8）**：`core/kinematics.py` 与 `core/geometry.py` **若实测 >300 行**，对应单可按 04 文档 **§4.5-⑦ 预案直接包化**（包＋`__init__` 只做 re-export＋交付 `tests/test_public_api.py`），**不必再请示**；包化实际合并后由指挥方出变更单（`CR-2026-02`）留痕，并把该对放开为"可分头写"。**⚠️ 包化前 `T04∥T07`、`T05∥T06` 仍禁并行（上文口径不变）；且包化只消灭合并冲突、不消灭功能依赖，合并顺序仍是 T04→T07、T05→T06。**

## 验收流程（每单固定）
1. ZCode 汇报：改了什么文件 / 怎么自测（贴实测输出）/ 遗留问题
2. 我对照本单"完成标准"**逐项读代码+跑测试复核**（不听信自报）
3. **常驻复查项（每单必查）**：①grep 全仓无轴参数魔法数字 ②core 层无 GUI/通讯包 import ③界面无术语上屏（B-Rep/位姿矩阵/NodeId 等）
4. 过了改状态、commit 确认，才出下一单；不过打回并列明条目

## 验收记录

（暂无）

## 外部依赖与挂账（不阻塞本包，但影响后续）
- ⚠️ **远端为公开仓（2026-09-15 第六轮审计实测）**：`origin = https://github.com/vvvlhyvvv-ship-it/mecharm-workbench-docs.git`；**实测方法与原始输出**＝Python `urllib` 未认证访问 `https://api.github.com/repos/vvvlhyvvv-ship-it/mecharm-workbench-docs` → **`API 200 private= False`**（未认证可读＝公开；私有仓匿名返回 404）→ **已推 4 次提交，含招标第 16 条口径、S-1 契约、沟通单、任务包全文**。**处置（待 @user 拍板）**：**①首选把该仓改为 Private**（不改写历史、不影响本地；⚠️ 私仓≠涉密保证）；**②若维持公开**，则硬执行三条边界：`evidence/` 只放不含甲方数据的取证物、缓存/资产库落 `paths.cache_dir`（仓外 `%LOCALAPPDATA%\mecharm\cache`）、**严禁再推含甲方数据的任何文件**。详见 **04 §7.1-10** 与 §4 矩阵、§5.1-6
- **电Q-1~电Q-12 电气回执**（＝《沟通单》12 项；⚠️ 沟通单实为"**已定稿待递**"，9/16 踏勘当面递；原写"已发"口径不准确，2026-09-15 20:45 更正）→ 回执冻结后改 machine.yaml opcua 节 + 契约签字，才进真机联调
  - ⚠️ **Q 项存在两套编号体系**（契约 §2.2 表 vs 沟通单 §一/§二，编号相同、含义不同；契约 L150/L342 的交叉引用已混用两套）→ **引用须可判定归属**（写法规则**唯一定义处**见 04 文档 **§5.7-④**，本台账统一用 `电Q-x`），对照表见 **§5.7**，契约缺陷登记为 04 文档 §7.2-**G9**
  - ⚠️ **回填错行风险**（04 文档 §5.7-**⑤**／§7.2-**G10**）：沟通单 L4 承诺"答复回填契约 §2.2 回执栏"，而电气按沟通单编号作答、原回执表无"问题原文"列 → 若按"对 §2.2 逐行填"作业，**`电Q-8`（blending）的答复会落进契约 §2.2 的 Q-8 行（此处为**契约体系**）＝"PLC 程序由谁编写"**。**改编号修不了这一步**，必须独立设防
  - **回填作业规程（回执回收时执行，不随单递）**：① 逐封要求电气答复**带问题原文**；② 回填时**按"问题原文比对"而非按"编号对齐"**；③ 每行备注列记"来源＝沟通单 `电Q-x`（原题摘要）"；④ **双人核对**。⏳ 配套《沟通单 **V1.1 草案**》已出（编号加前缀 `电Q-` ＋ 回执表加"问题原文"列），**须在 9/16 递单前定稿，递出后只能用上述规程兜底**
- 澄清结果若 JT 翻案 → 变更单重排 T05
- 甲方真实 STEP 样本到位后 → T05 的导入用例补真实大模型回归（并入 T05 步骤 8 的 P0 实测口径）
- **CR-2026-03（三维模型导入能力方案吸收，草案，2026-09-15）**：T05/T08 卡扩口径、03 增补、04 §7.2 新增 G11–G16；**G11 已于 2026-09-15 裁决（@user 批准 ①＋② 并行）**：契约扩容＋AxisMask（随契约 V1.3 窗口）、编程范围＝轨迹级轴（上限 9、YA~YE 只读）、三条结构要求已进 03 §4 与 T03 卡；**遗留待确认**：工具臂专用轴是否全由 PLC 专用程序自控（列入 9/16 踏勘确认清单）；**G16（换臂工作模式界面入口）已于 2026-09-15 落地**（02 升 V1.1＋T02/T06/T07 接线）
