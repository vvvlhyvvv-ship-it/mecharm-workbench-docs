# 进度与验收：机械臂工作台 · 完全重构

- 建档：工程智拓 @pm ｜ 2026-09-14（v2，随 D-1 完全重构口径改版；v1 旧原型路线作废；v3 2026-09-14 审核修订：解析逆解口径/契约章号/face_map/配置字段/常驻复查项；**v4 2026-09-15 并行编排定案：加波次/会话标记＋仓路径变更，@user 批复"全按建议"**；**v5 2026-09-15 追加代码粒度硬约束：单文件 ≤300 行，详见 04 文档 §4.5**；**v6 2026-09-15 20:25 路径重拍（维持 D 盘、理由更正：E 盘实测存在）＋ Q 编号双体系引用规范（04 §5.7／G9）＋ 环境实测（无 conda、无可用 3.11）**；**v7 2026-09-15 20:45 @user 审核反馈：①"缺 coreutils"归因作废（会话相关 PATH 差异，非本机缺文件；"误判无 E 盘"机制未确认）＋ 禁用裸 `sort`／`find`（静默落 Windows 版）；② 新增 Q 编号"回填错行"防错（04 §5.7-⑤／G10）＋ 回填作业规程 ＋ 沟通单时效口径更正（已定稿待递，配 V1.1 草案）**；**v8 2026-09-15 21:10 @user 第三轮审核：① 沟通单 V1.1"一字未改"声明更正（实测 12 项表内 2 处引号样式变动：电Q-9 问题列、电Q-6 为什么问列）＋ V1.1 第四节第 2 项改指页脚 ＋ 修订记录重复字删；② Q 项引用写法统一（`电Q-x` ≡ `沟通单 Q-x`，择一，V1.1 递出后全仓统一）——本表 T07 行、外部依赖节已同步**；**v9 2026-09-15 21:35 @user 第四轮复核：① 执行层补齐——`Tasks\` 下 5 处裸 Q 编号已改带体系名（T04:23／T07:11→`契约 Q-11`；T07:7／T07:20→`电Q-8`；T09:13→`电Q-2`；T01 单 0 命中），复验 0 残留；② T01 开工前机器事实多通道复测（干净起点／无 conda／Python 3.12.7／git 2.50.0／uv 3.11 仍不可用／D 盘 121.0GB、E 盘 715.8GB／conda-forge pythonocc-core 7.9.3）；③ 新增网络与代理条（系统代理 7897 在线，另有会话级环境变量代理 24633 亦在线、NO_PROXY 未设；三通道×三目标全 200、直连亦通；下载失败先测端口勿归因渠道；离线须断网验证，属 T10）；④ 登记 CR-2026-01 时序（须在 T01 合并前出）**；**v10 2026-09-15 21:15 新增配套件《05_开工前复盘与沉淀_V1.0.md》——阶段状态快照、6 次事实性纠错留案（含 1 处自我更正）、10 条可复用纪律（指向技能 v1.4.0）、9 项项目专属约束索引、沉淀落点索引、新会话交接清单**；**v11 2026-09-15 21:55 @user 指示：吸收《三维模型导入能力综合技术方案》（张晓东）及《审阅意见 V1.0》——T05 卡扩口径（原生转换链/单次通过释放/双缓存/三阶段进度/离线预处理管线/P0 实测）、T08 卡落实保守包络条款＋碰撞两段制、03 §3/§8/§9 增补、04 §6.4 T05 派单卡与 §7.2 新增 G11–G15、登记 CR-2026-03（草案）；其中 G11（契约 8 轴上界 vs 实机 22 轴）待裁决，建议 T03 开工前定**）；**v12 2026-09-15 22:20 @user 批准 G11 裁决（①＋② 并行）并落地：03 §4 配置模式（role/modes/coupling 两型/envelope/deflection＋"实机轴系口径"块，含契约扩容＋AxisMask 随契约 V1.3、"未用轴位填 0"废止）、T03 卡整卡改写（22 轴样例）、T04/T07/T09 卡挂口径、04 §6.3 T03 派单卡与 §7.2 G11 标已定＋新增 G16、CR-2026-03 升 V1.1**；**v13 2026-09-15 22:35 @user 指示"按建议继续全部完成"：G16 落地——02 升 V1.1（顶栏工作模式选择器、§3 模式表增工作模式行、步骤②前置锁定、步骤③换臂作废）、T02/T06/T07 卡同步接线、04 §7.2 G16 标已定、CR-2026-03 升 V1.2**；**v14 2026-09-15 22:45 @user 明确职责边界：主要动作由 PLC 编程，涉及轨迹编程的工况由软件输出轨迹参数交 PLC——写入 03 §4 实机轴系口径块与 T07 卡产物口径，04 §7.2-G11 与 CR-2026-03 §三-1 同步；G11 遗留待确认项收窄为"工具臂专用轴是否参与轨迹编程"**）；**v15 2026-09-15 23:07 @user 定案（时间戳取自文件 mtime；原写"23:10"系估算，2026-09-15 23:30 更正）**：①仓路径并入项目目录 `D:\ZCode项目\多功能机械臂软件`——实测该目录**已是既有 git 仓**（main、3 次提交、origin=mecharm-workbench-docs、跟踪 22 个文档）→ **代码并入既有仓、T01 不再 git init**，新增同仓两条纪律（功能分支禁改文档与 CR；`.workbuddy/` 入 .gitignore）；②对外承诺口径确认＝**P0 实测前不承诺导入性能数字**。同步：CR-2026-01 升 V1.1（本节仓路径口径条已改写、"新建空仓/干净起点"作废）、CR-2026-03 升 V1.3、00_README／Tasks\T01／04 §1.3·§5.1·§5.2·§6.2／05／01-D-7 全部改写**）；**v16 2026-09-15 23:30 @user 第五轮审计（七条全部成立，逐条修正）：①T01 卡与完成标准改为"禁 `git add -A`、只按路径 add；完成标准＝本单交付路径内无未提交改动"＋新增 §5.5 附5 合并前检查命令（**必须带 `-c core.quotepath=false`**，否则中文路径被转义→假通过）；②T09 过渡期口径（`pack_profile=v1_2_8axis` 按契约 V1.2 实现，AxisMask 只留配置内）＋T07/03 §4 同步；③**V1.3 扩容影响测算入 CR-2026-03 §三-5**（段 56→124 B、下发块 584→1264 B、50 段方案 6224 B、**回读块 92/96→约 228 B**、建议新增掩码命名 `SegAxisMask` 防与 `EnableMask` 二次误引）；④组件增补流程与落盘归属（04 §4 矩阵：`environment.yml` 归属改指挥方、`view/vendor/**` 由引入单报备落盘、附6 越界检查）＋Draco/meshopt 降为"包名待核、暂不引入"＋G14 口径收紧为"禁前端 WASM 做 **CAD 解析**"；⑤04 §1.3-① 破版行重建并改现行路径＋"决议列以后出变更单为准"；⑥外部依据归档进仓 `参考资料/`（方案、审阅意见、踏勘底稿＋README）；⑦杂项（121.0→120.8、常驻复查正则补三字段、05 §二-1 归因泄漏删除、时间戳改取系统钟）**）；**v17 2026-09-15 @user 第六轮审计（六条全部成立，逐条修正）：①T01 派单卡不再写死基线 commit 号（原写 `ebac7a6`，现 main 已 `44769d2`，比对即"开工即停"）——判据改为只看"文档路径是否脏"；②**双缓存落盘路径补齐**：03 §4 新增 `paths.cache_dir`（默认 `%LOCALAPPDATA%\mecharm\cache`，仓外；加载器校验仓内路径拒启动）＋`.gitignore` 加 `cache/`／`data/`／`*.brep`＋04 §4 矩阵新增缓存行＋§5.1-6 推送边界＋T05/T03 卡同步；**并实测远端可见性：`API 200 private= False` → 远端为公开仓**，立 04 §7.1-10 风险条；③CR-2026-03 §三-5 测算表"依据列"改按 124 B 的真实算式重写（原 @8.0 口径复算得 128 B），回读块依据补三步（223→224→228），**新增 `EnableMask` 加宽能否吞并 `Reserved2`（L253 WORD@78.0）的 V1.3 悬置**；④04 §5.5-附6 重做（原正则对 `three-mesh-bvh`／vendor 解码器／`import DracoPy` 三条实测全 MISS）→ 改"`view/vendor/` 目录白名单＋AST import 比对 `requirements.lock.txt`"两条硬判据，附5 模式补 `参考资料/`；⑤T09 卡两个"2."已重编号（口径块移出步骤为独立小节）；⑥04 §7.2-G11 引用 §三-2 → §三-5）**；**v18 2026-09-16 00:28（系统钟）@user 第六轮批复末句授权"如果还有不合适的你直接修改，改到能开工为止"——第七轮据此由指挥侧代理落地。先核收：**P1–P6 六条全部已落地且互洽**；本轮另查出**四类新缺陷并直接修**：**① ⛔ 推送冻结令（新设，即刻生效，04 §5.1-6）**——此前**全仓没有任何一条禁止 `git push`**，而实测远端为**公开仓**、本地 `main` 领先 **3 个未推提交**（含 `参考资料/` **四件**，逐件实测每份抬头均含**甲方全称「通裕重工股份有限公司」＋招标项目编号 `GXTC-A1-26170051`**，《踏勘确认清单与运动轴清单-附录A填报底稿》另含实机 22 轴结构）→ **一次习惯性 push 即外泄**。同步落点：04 §6.2 T01 卡＋`Tasks\T01`（禁止事项与自测项各一条"只 commit 不 push"）＋`参考资料/README` 规则 5；并写明**本包从未要求任何单推送**、汇报出现"已推送"按违规打回；**撤销须 @user 决定可见性处置后由指挥会话执行**。**② 04 §7.1-10 重写**——原记"已推 4 次"系误记，实测 `git ls-remote --heads origin` → 远端 `main` = `2569104`＝**3 次**；补**已公开内容清单**（22 件，含 S-1 契约 V1.2 草案——其 **L529** 含买方称谓"通裕"、沟通单 V1.0、三规程、任务包旧版、**投标期排期对照表 v1**：澄清 9/17 12:00／踏勘 9/16／领取文件 9/15 17:00／**开标 9/30 9:00**／**内部完工内线 9/25**／"16(3)②③＝废标条款"判项／"投标线＝旧原型顶材料"内部口径）与**再推一次才公开的**（`参考资料/` 四件＋99 至 v17 的审计链）；证据双通道（未认证 API `private=False` ＋ `raw.githubusercontent` 200／404）；补两条边界：**改 Private 不消除既成事实**、**重写历史须 @user 明确指示且任何会话不得自行执行**。**③ 04 §5.5 附6-② AST 脚本两处缺陷修入文件**（第六轮已复现、当时标注未修）：加 **`LOCAL` 仓内包白名单**（原会把 `from core.config import load_machine` 判成越界第三方包 → **T02 起每一单验收必撞假阳性**）＋扫描作用域由 `core/comm/app` 扩为含 **`tools/`、`tests/`**（原 `tools/pre.py: import meshoptimizer` **完全漏检**）；改前／改后输出与残余局限（JS 侧不在②覆盖内）记入注 1。**④ 04 §6.2 T01 卡九处**——完成标准由"**git 工作树干净**"改为"**本单交付路径内无未提交改动**"（原写法与卡内 ②③④ 自相矛盾，会诱导实现会话把指挥方的文档改动**提交掉或 stash 掉**，本轮最高危卡内缺陷）；新增**时点值通则**（卡内一切 commit 号／提交数／文件数／磁盘容量皆背景值、**不作核对项**）；`参考资料/**` 补入禁改清单与同仓纪律；**T01 在 `main` 上作业登记为 §5.1-1"每单一分支＋worktree"的唯一例外并给终止条件**（T01 收口即失效）。**⑤ 容量口径两处残留补齐**——04 §7.1-7 与 **CR-2026-01 §六** 的 121.0→**120.8GB**（2026-09-16 00:01 `shutil.disk_usage` 复测，标时点值）＋加 `paths.cache_dir` 落 **C 盘**的监控口径；CR-2026-01 以其 **V1.3** 行补正，并在 V1.2 行**追加**更正说明（不改写原文）。**⑥ 表格结构四处修复**（复测：全仓 `.md` 表格**列数不一致的行 = 0**）——04 §7 修订记录表**第五轮与第六轮两行被上一轮编辑粘成同一物理行、且第五轮行的"版本／日期"两列丢失**（已拆两行、按时间升序重排并注记来历）；04 §5.5 ②③ 两行与 03 §3 `collision.py` 三态签名代码段内**未转义竖线**破表（逻辑列 11／14／6 → 5／5／4）；CR-2026-03 §七 **表头 3 列 vs V1.4／V1.5 四单元格**（补「编制」列并回填 V1.0–V1.3，其 V1.6 行留痕）。**⑦ 自查两条（记此自警）**——一次编辑曾**误删 CR-2026-03 V1.5 行末"声明前不得单方引数"**，`git diff` 自检时还原（现该行与 HEAD 逐字一致）；新写的两处补记时间戳（00:35／00:32）**系估算**，已按系统钟 00:28 更正。**⏳ 待 @user 决策四项（本包及任何会话不得代行）**：远端改 Private 还是维持公开；是否重写历史并联系 GitHub 清除缓存／fork；`参考资料/` 迁出仓外（README 规则 5 的 (a) 案）还是随仓走私仓并在公开前脱敏；契约 V1.3 对 `EnableMask` 加宽与 `Reserved2` 的显式声明（声明前 §三-5 各数字**不得发给电气**）。**⚠️ 另记：9/16＝踏勘日兼《沟通单》递单日，该件仍为草案**；**v19 2026-09-16 02:11（系统钟）T01 交付后同步＋两项口径追认（@user 指令，WorkBuddy 直改；技术细节落在 04 与 `Tasks\T01`／`T02`／`T10`，本条只记要点与落点，避免两处复述漂移）**：**① STEP 读取器导入串全仓更正**——`from OCP.STEP import …` → **`from OCC.Core.STEPControl import STEPControl_Reader`**（实测 `import OCP` → `ModuleNotFoundError`，`OCP` 是另一套绑定；正确串在 `mecharm` 环境**实测通过**），落点＝`Tasks\T01` 完成标准＋步骤 5、04 §2.2 T01 行、04 §6.2 T01 卡；⚠️ **04 附6-② fixture 里的 `OCP` 属历史实测留痕，照录不改、就地加注**（那是"造出来的第三方包名样例"，非本项目模块名）。**② 追认锁文件命令**＝`pip list --format=freeze`（非卡内字面 `pip freeze`）：实测交付件 **31 行、含 `pip==26.2.1`**，而 `pip freeze` 默认漏 `pip`／`setuptools`／`wheel`，复现不出装包工具链 → **卡内字面已按实改，交付件不重做**。**③ 追认 T01＝2 个 commit**（`chore(T01): 环境基线` ＋ `chore(T01): 遗留问题处置——ICU 入口引导抽出共用＋vendor 换行保真`）：遗留处置发生在**收口汇报之后**，与环境基线是两件独立事实 → **不改写历史去 squash**（squash 会重写已复核的基线提交并抹掉留痕）；04 §5.1 硬规矩 1 记 **T01 例外**，原"只看 `HEAD`"的核对改为 **`git log --stat -2`**。**④ 04 §4 矩阵补两行**（原矩阵漏登记 T01 第二 commit 的两个交付件）：**`.gitattributes`**（仓根，T01 首次生成 → 此后**归指挥方**，其他单只读；实测理由＝本机系统级 `core.autocrlf=true` 会把 checkout 出的 `three.module.js` 转 CRLF、**1,272,972 → 1,326,016 B**、SHA256 **`76dea815…` → `c6eacae1…`**，于是"按哈希核 vendor 原件"**每个 worktree 都假失败**；`git worktree add` 自动继承，各单无需处置）＋ **`app/bootstrap.py`**（唯一写者 T01，其他单只读、`import app.*` 自动生效，**禁各单复制自写 shim**）。**⑤ T02／T10 卡与 04 §6.3 T02 派单卡各加入口引导条款**：入口无需自写 ICU shim；**从 `app` 包外直接 import PySide6**（独立脚本、**PyInstaller 入口**、`tools/` 临时脚本）**必须显式** `from app.bootstrap import preload_windows_icu` 且置于 PySide6 之前（成因＝conda `icu` 包那份 `icuuc.dll` 被 `Qt6Core.dll` 先命中 → **WinError 127**；`icu` 是 `libxml2`（OCCT XML 链）硬依赖**不能移除**）；T10 另在步骤 6 写明"不得以开发机 smoke 能起来充当 dist 双击启动的证据"。**⑥ T01 完成标准路径清单补 `.gitattributes`／`.gitignore`**（原清单未列 → 新增交付件不被判据覆盖）。**⑦ 记一条验收防误判**：`smoke.py` 的 OCC 行打印 **`7.9.0`**（来自 `OCC/__init__.py` 硬编码 `VERSION`，`PYTHONOCC_VERSION_PATCH=0`），而实装 conda 包为 **`pythonocc-core 7.9.3`＋`occt 7.9.3`**（`environment.yml` 实测）→ **两者不同源，不得据 smoke 输出打回"装错版本"**。**⑧ 顺手修 T01 备注列一处 `**` 不配对**（原 5 个 → 4 个；**字面照录，只调加粗边界**，与第七轮同类缺陷同源）。**⑨ T01 状态 待做 → 待验**（⚠️ **这是交付事实登记，不是验收结论**："已过／打回"仍归 @user，`## 验收记录` 一节仍为"（暂无）"）。**复测（2026-09-16 02:11，本机实测）**：`python tools/smoke.py` 在 `mecharm` 环境**退出码 0**，打印 `python 3.11.16`／`ICU 预载 C:\WINDOWS\System32\icuuc.dll`／`OCC 7.9.0`／`PySide6 6.11.2`／`asyncua 2.0.1`／`numpy 2.4.6`／`STEP 读取器 OK`；`view/vendor/three.module.js` = **1,272,972 B、LF-only、SHA256 前缀 `76dea815`**（与 `.gitattributes` 注释一致）；全仓 `.md` 表格**列数不一致行 = 0**；`git rev-list --count origin/main..main` = **6**（**2026-09-16 02:18 复测**；本地 `main` 共 **9** 次提交、`origin/main` = **`2569104`（3 次）**，未推 6 个提交覆盖 **39 个文件**——含 `参考资料/` 四件、`CR-2026-01`／`CR-2026-03`、《沟通单 V1.1 草案》、**T01 代码基线**（`app/bootstrap.py`／`tools/smoke.py`／`environment.yml`／`requirements.lock.txt`／`view/vendor/three.module.js` 1,272,972 B）与 04／99 审计链。⚠️ 本轮 `git ls-remote` 因**代理 `127.0.0.1:7897` 连接失败**不可用，远端身份取自**本地跟踪引用 `origin/main`**；远端树实测 **22 个文件**、`git grep 通裕重工 origin/main` 与 `git grep GXTC origin/main` **均 0 命中**、代码件 **0 个**。⛔ **推送冻结令未解，本轮未发生任何远端写操作**。**⑦ 自查一条**：本项初稿写"3"系**沿用第七轮 00:01 旧读数、未复测**，与第七轮 ⑦(b) 同类——**时点值必须现测**；已在 04 修订行 ⑦、本处、T01 备注 ⑦ 三处更正，并在 04 §5.1-6／§7.1-10 各**追加复测条（时点见该条自身）**，**原 00:01 记录照录不改**）。**⏳ 待 @user 决策四项（同 v18，未变）＋ `CR-2026-01` 须在 T01 合并前签字**；**v20 2026-09-16 02:29（系统钟）W1 开单准备：文档同步提交 ＋ 两个 worktree 建好（@user 指令"worktree 由你建"，并就"建前是否先提交文档"批复选项①"先提交再建"）**：**① 先量后动**——实测 `git worktree add` 的检出基线是 **HEAD `77b8dd3`**，而本轮 5 个文档改动当时尚未提交；HEAD 版 `Tasks\T02` 卡 **0 处**含 `bootstrap`（28 行，工作树版 33 行）、`Tasks\T01` 卡判据**仍是错的 `from OCP.STEP import …`** → **不先提交，两个实现会话读到的都是旧卡**（依 §5.1 硬规矩 5「文档改动只在 `main` 上由指挥方提交」）。**② 实际执行顺序**（为使本条数字全是实测值而非预期值，与"时点值必须现测"同源）：**先建 worktree 并实测 → 再写本条 → 再提交文档 → 最后把两个 `task/TXX` 分支 `git merge --ff-only main` 快进到含新卡的 `main`**（两分支建出后 0 提交，故为**严格快进、不重写历史**；该动作属指挥会话职责，不违 §5.1 硬规矩 2 对**实现会话**的禁令）。**③ 两个 worktree 实测（02:29）**：落点 `D:/ZCode项目/多功能机械臂软件-wt/t02`（分支 `task/T02`）与 `…-wt/t03`（分支 `task/T03`），均在**仓外同级目录**（§5.1-4）；**`.gitattributes` 自动继承（实测两 worktree 内该文件均存在）**，`view/vendor/three.module.js` 在两 worktree 内均为 **1,272,972 B／CRLF 计数 0／SHA256 前缀 `76dea815`**，与 `main` 工作树**逐字节一致** → §5.1 命令块那条"worktree 自动继承 `.gitattributes`、各单无需任何换行处置"的注释**由实测坐实**。**④ 未推提交数 6 → 7**（本次文档同步提交自身即第 7 个）；⛔ **推送冻结令未解，本轮未发生任何远端写操作**，`origin/main` 仍 **`2569104`**。**⑤ T02／T03 状态仍为"待做"**——**建好 worktree ≠ 已派单**，"在做"由 @user 起实现会话时改；两行备注已记 worktree 落点与分支；**v21 2026-09-16 02:31（系统钟）把 v20 暴露的顺序陷阱升格为硬规矩**：该陷阱**不是一次性的**——W2–W6 每波都要建 worktree，故写入 **04 §5.1 硬规矩 7**「**先在 `main` 提交任务包文档改动，再 `git worktree add`**」，含建前检查命令（`git status --porcelain -- 前端任务包 参考资料` 须**为空**）、已先建时的补救（各 worktree 内 `git merge --ff-only main`，分支 0 提交时为**严格快进**）、W1 实测（`Updating 77b8dd3..8310b8a / Fast-forward`，快进后两处 `dirty = 0`）与"worktree 自动继承 `.gitattributes`"的实测坐实；⚠️ **编号追加在 6 之后**，因为全仓多处引用"§5.1-6 推送冻结令"，重编号会打断引用。**未推提交数 7 → 8**（本条所在提交即第 8 个）；⛔ **冻结令未解**，`origin/main` 仍 **`2569104`**、远端树仍 **22 个文件**、`通裕重工`／`GXTC` 两串 **0 命中**、代码件 **0 个**。**三个工作树实测已完全一致（本条所在提交快进后复测）**：`main`／`task/T02`／`task/T03` 同在一个提交，`view/vendor/three.module.js` 三处同为 **1,272,972 B／CRLF 计数 0／SHA256 前缀 `76dea815`**，`Tasks\T02` 卡三处均 **33 行、含 `bootstrap` 3 处**，04 三处均 **958 行**（＝加入硬规矩 7 之后的现测值；⚠️ 本句初稿写 947 系**加规矩前的读数**，同一"时点值必须现测"教训，已更正））；**v22 2026-09-16 03:03（系统钟）T02 收单通过并合并（会话 B 完工交接；指挥会话按 04 §5.3 逐项复核、不听自报）**：**① 复核面**＝附4／附5／附6 全跑＋offscreen 行为级复跑（G16 三态与二次确认回退、桥 echo、字号量测）＋两档截图逐张目核（无横向滚动、无甲方数据）＋合并后 pytest／smoke／常驻三项；会话 B 六组自测结论**除"回看可演示"外全部复现**。**② 一处汇报与实测不符，按实测更正入 T02 行备注**：回看态（`done`）在壳内**无 UI 触发点**（`mark_completed` 无人调用），而状态机本身实测正确（`mark_completed(1)`＋`set_current(2)` 后 state[1]=`done`）→ 判**遗留项 F1** 而非打回，理由＝02 §1"点已完成步骤可回看"的触发者是**业务完成**、T02 阶段无业务，§5.3 的打回针对判据不成立；**F1 已落 `Tasks\T05` 步骤 6 与完成标准各一条**（导入成功调 `mark_completed(1)`、收单必检回看态），台账不先于事实；另记 F2（选回"未选定"被静默忽略）／F3（切换日志"已清空…"在 T02 是未发生的事实）／F4（`viewpane` 用 `__file__` 定位 `index.html`，PyInstaller onedir 下失效 → T10 收单必检）。**③ 合并**＝`git merge --ff-only task/T02`（`634fbdb..a67ab15`，严格快进、无 merge commit、无冲突）；合并后回归全绿（pytest **0 用例**——测试件 T03 才交；smoke rc=0；常驻三项 0 命中；vendor 仍 1,272,972 B／CR 0／`76dea815`）。**④ 未推提交数 9 → 10（本条所在提交即第 10 个）、本地 main 共 13 次提交**；⛔ **冻结令未解**，`origin/main` 仍 **`2569104`**、本轮**零远端写**。**⑤ t03 worktree 实测有会话 C 进行中改动**（`tests/conftest.py` 已改＋`config/machine.yaml`／`core/config/`／`tests/fixtures/`／`tests/test_config.py` 未跟踪）→ **本轮不动它**：硬规矩 7 的 `merge --ff-only` 补救只适用于**0 提交**分支，T03 工作树已脏，对账留到其收单轮。**⑥ worktree `t02` 暂不删**（§5.1-③ 要求收单后删；会话 B 可能仍驻留该目录，待 @user 确认其退出后由指挥会话删）。**⑦ T02 状态 待做→已过**（过渡态 在做／待验 由 T02 行备注补记，理由＝实现会话按 §5.1-5 禁改台账）；**T01 状态仍 待验、`## 验收记录` 仍"（暂无）"，未动**；**v23 2026-09-16 07:00（系统钟）T03 收单通过、合并 `0ed1779`、文档回填与 t04 worktree（指挥会话）**：**① 验收结论归指挥会话**（@user 2026-09-16 明示"T03 验收结论（已过／打回）归你；合并与 t04 worktree 也归你"），结论 **已过**，逐项实测记录见本表 T03 行备注九段（①—⑨）。⚠️ **台账口径不一致，待 @user 示下**：T01 行仍写"已过／打回 仍归 @user"，而 04 §5.3 与 T02／T03 实操（含本轮明示）均为**指挥会话判**；T01 至今 **待验**、`## 验收记录` 仍"（暂无）"，本轮**未动**——T01 是否按同一口径由我判，请 @user 定。**② 文档回填（会话 C 点名两项＋连带共 11 处）**：04 §2.2 T03 产出列改 `core/config/`（包）＋实测 **488 行**缘由＋"2026-09-16 收单时回填本列"；04 §4 矩阵行拆为四件＋长注（**事后报备**、依据是 **§6.3 派单卡不是 §4.5-⑦**、re-export 断言落在 `tests/test_config.py::test_package_root_reexports_every_public_symbol`、未建 `test_public_api.py` 的撞名理由）；04 §5.5-① 期望列原文"`core/config.py` 的字段名"→"**`core/config/`**（包；字段名常量在 `schema.py`、校验消息在 `validate.py`）"，并记 lint 扫描面 **8**（worktree）／**19**（合并后）两次 rc=0；04 §4.5-⑦ 硬要求 3 → `tests/test_public_api_<包名>.py`（撞名口径＋T03 先例）；04 §6.4 **T04 派单卡新增 X3 倍速链口径块＋"T03 交付实况两条"块**、T04／T05 卡产出件改 `test_public_api_kinematics.py`／`test_public_api_geometry.py`；03 §2 目录树 `config.py`→`config/`（补 2 空格保框线对齐）＋新增包化说明条（实测 30／156／104／296）；03 §4 字段块补 `pending` 行；03 §4 实机轴系口径补 X3 子条；`Tasks/T03` 加交付实况注（并声明卡内其余 `core/config.py` 字样一并覆盖，含步骤 2）；`Tasks/T04` 步骤 4 加 X3 `master` 自指判读、步骤 6 加 T03 交付实况（`links.length_mm` 全为占位 `0.0` → 用例① 手算须假定无连杆偏置；22 轴全 `pending`、行程占位 `[0,1000]` mm／`[-180,180]` deg；入口 `from core.config import load_machine`）。**③ X3 口径（@user 点名"T04 派单前必须写进派单卡，否则 T04 会自己猜"）**：`master == 自身` ⇒ **驱动级倍速**（该轴工程位置＝自身驱动位移 × ratio），⛔ **禁**读成"X3＝另一根轴 × 2"的**跨轴约束**；`master != 自身` ⇒ 从动轴＝主动轴 × ratio；**回执后只改 `machine.yaml`、不改代码** → 换算必须**配置驱动**、禁写死 `* 2`；最终依据＝踏勘确认清单第 4 项回执。已落 03 §4／`Tasks/T04`／04 §6.4 T04 卡三处，项目记忆同步。**④ 冻结令复测（本轮零远端写）**：未推提交数 **13 → 14**（本条所在提交即第 14 个）、本地 main 提交数 **16 → 17**、`origin/main` 仍 **`2569104`**、远端树 **22 件／代码件 0／`通裕重工`·`GXTC` 命中 0**、无 `origin/main` reflog；会话 C 自报"未执行任何远端写操作"经我复测**成立**（其 worktree 无 upstream、无 push 痕迹）。**⑤ worktree**：按 04 §5.1 硬规矩 7 **先提交文档、再 `git worktree add`**（建前 `git status --porcelain -- 前端任务包 参考资料` 必须为空）→ 新建 `D:/ZCode项目/多功能机械臂软件-wt/t04`、分支 `task/T04`（**建好≠已派单**，"在做"由 @user 起实现会话时改）；`t02`／`t03` **仍暂不删**（§5.1-③ 要求收单后删；会话 B／C 可能仍驻留该目录，待 @user 确认退出后由指挥会话一并删）；**t05 本轮不建**——会话 C 交接只点名 t04，T05（W2-B）属 04 §6.4 第三批且带 F1 必检项，待 @user 起会话时再建。**⑥ 一处自我更正（署名归属，非交付缺陷）**：v22 ⑤ 与本条 ②④⑤、T03 行 ④⑨ 所称"**会话 C**"系**我的推断、无据**——实测两份交接件：**T02 交接件自带抬头「【会话 B / T02 完工交接】」**，而 **T03 交接件全文 417 字、无任何会话标识**；按 04 §2.2／§6.3 分工 T03 归 **会话 A**（W1-A），而 §6.4 里"会话 C"是第三批另一单 → **本表凡"会话 C"处一律读作「T03 实现会话（按分工＝会话 A）」**，原文照录不改、以本条为准（同"时点值必须现测"一脉：**署名也是事实，不得推断**）。连带两件事：本条提交后按 **§5.1 硬规矩 7** 补救口径在 `t04` 内 `git merge --ff-only main`（分支 0 提交＝严格快进、不重写历史），使 T04 会话读到含本更正的台账与新卡；**未推提交数 14 → 15、本地 main 提交数 17 → 18**（本条所在提交即第 15／18 个），`origin/main` 仍 **`2569104`**、本轮仍**零远端写**。**v24 2026-09-16 07:17（系统钟）第三批全部开单：T05 派单（会话 B）＋ T04／T09 状态补登（指挥会话）**：**① 派单决断（会话 B 问"T05 是与 T04 并行另起，还是等 T04 收单后再起"）＝并行**，依据**不是我的新裁量而是既有批复**：**04 §3.2 放宽项 R2（2026-09-15 已批准生效）**原文"**T05 提前到 T04 之前开工（只要求 T01+T02 完成）**"＋§6.4 第三批表头"**W1 全部合并后同时开，A｜B｜C**"＋`Tasks/T05` 卡自身已写"按指挥方批准的并行方案提前开工，不等 T04"。**两对绝对禁并行**（§3.2：`T04∥T07` 同写 `core/kinematics.py`、`T05∥T06` 同写 `core/geometry.py`）——本批是 **T04∥T05∥T09**，**不涉任一对**；三单文件集实测零交集（`core/kinematics.py`＋`tests/test_kinematics.py` ／ `core/geometry.py`＋`view/js/loader.js`＋可选 `app/` 接线 ／ `comm/`（**仓根顶层**，非 `core/comm/`）＋模拟器）；**本批 `app/` 单写者＝T05**（§4 矩阵行 174 的追加接线名单只含 T05／T06／T07／T08／T10，T04／T09 均不在内）→ 三单同开不违"同一时刻只允许一个会话改 `app/`"；R2 的**回退条款已照抄进派单**（"一旦发现 T05 需要 T04 的任何输出 → **立即停下**，等 T04 合并"／卡内"■ 卡住即停…不许自行越界"）。**② t05 worktree**：07:12 按 §5.1 硬规矩 7 建（建前 `git status --porcelain -- 前端任务包 参考资料` 实测为空），落点 `…-wt/t05`、分支 `task/T05`、基线 `d136403`＝建时 main tip；07:17 实测 **0 提交／dirty 0**、`core/geometry.py` 与 `view/js/loader.js` **均不存在**（本单交付件，正确起点）、树内 `Tasks/T05` 卡为最新版（陈旧串 `core/config.py` **0** 命中、含 `mark_completed(1)` 与 `paths.cache_dir`）、树内 04 **977 行**含 `test_public_api_geometry.py` 与 R2 原文、vendor **1,272,972 B／CRLF 0／`76dea815`** 与 main 逐字节一致。**③ 派单附实测八条**（07:15–07:17 现测于 t05 树，目的是让承接会话**不必猜**）：**(1) 配置入口签名是 `load_machine(path: str)`——必填路径、无默认值**（⚠️ 我此前记的"`load_machine()`"是**错的**，实测 `TypeError: load_machine() missing 1 required positional argument: 'path'`），规范调法 `load_machine(str(REPO_ROOT / "config" / "machine.yaml"))`，`REPO_ROOT` 由 `core.config` 导出、在 worktree 内实测解析为 **t05 自身根**（`…-wt\t05`，非 main 仓根）；**(2)** `cfg.paths` **只有 `cache_dir` 一个字段**；**(3)** `cache_dir` 实测**已展开为绝对路径** `C:\Users\lihaoyuan\AppData\Local\mecharm\cache`、**在仓外**（加载器已做 `expandvars` → **下游禁再展开、禁写死路径**）；**(4)** 该目录**尚不存在**（父目录 `…\mecharm` 亦不存在）→ 本单须自建，且**不可用（不存在／无写权限）时降级为不缓存＋告警、绝不回落仓内**（卡步骤 4 原文）；**(5)** `limits.tessellate_deflection_mm = 0.5`（连带实测 `collision_envelope_mm = 3.0`、`path_sample_step_mm = 50.0`、`speed_max_mm_s = 500.0`）→ 弦高容差**从这里读、禁散写字面量**；**(6)** OCC 链实测可用：`STEPControl_Reader`＋`XCAFDoc_DocumentTool`＋`STEPCAFControl_Reader` 三者导入 **OK**；**(7)** **`trimesh` 未装**（`ModuleNotFoundError`；`requirements.lock.txt` **31 行**内无 trimesh）→ 如需它做网格简化，只在汇报"遗留问题"里提**依赖增补申请**，⛔ **禁自行 `pip install`／改 `environment.yml`／改锁文件**、**未在环境文件中的第三方包一律不得 import**；**(8)** 磁盘余量 **C: 51.4 GB／D: 141.8 GB**（缓存与离线资产库落 **C 盘**，大模型资产须盯这条）。另附**占位值警告**：**22 轴全 `pending: true` → 告警 1 条**、`links` **13 条且 `length_mm` 全为 `0.0`**、`pack_profile = v1_2_8axis` → 占位值不得当实测真值、不得据此对外承诺。**④ T04／T09 状态补登**（此前两行均"待做"且**无 worktree 记录**，与实况不符）：**T04 待做→在做**——实测**会话 A**（`efecf84e`）已由 @user 起单（"…看一下能开始04了么？"→"按你的意见执行，一口气跑完"→其自述"开工"），t04 树 07:00 建、07:17 实测 **0 提交／dirty 0**（读材料阶段）；**T09 待做→在做**——@user 直接告**会话 C**（`1386afab`）"你是会话C"并给 T09 卡，就其"t09 worktree 未建、我不能自建"的阻塞批复"**选A，请执行worktree创建并批复文件名**"，故 **t09 由实现会话自建**（实测已在：`…-wt/t09`、`task/T09`、`d136403`、**0 提交／dirty 0**）。⚠️ **这是 §5.1 硬规矩 2「worktree 由指挥会话建」的第二次例外**（第一次＝T01 在 `main` 上作业，v18 ④），@user 明示授权、会话 C 主动请求台账留痕，**本条与 T09 行即留痕**；**例外只及"谁执行 `git worktree add`"这一动作**，硬规矩 7 建前检查仍适用（我 07:17 复测文档区确为空）、硬规矩 2 对实现会话"不 merge／不 rebase／不动 main"的禁令不松动、推送冻结令照旧。**⑤ v23 ⑥ 的署名更正由实测坐实（不再是又一次推断）**：本仓四个会话标题实测为**「A」`efecf84e`／「B」`ae570776`／「C」`1386afab`／「指挥会话」`dc0a8a6a`**；按**写盘动作**归因——`core/config/{validate,schema,loader}.py` 与 `tests/test_config.py` 的 Write／Edit **只出现在「A」**（16／4／5／9 次），`app/shell.py`／`view/index.html` 集中在**「B」**（21／24 次提及，T02 交付件），**「C」两类皆 0** → **T03＝会话 A、T02＝会话 B 成立**，v23 ⑥ 的更正方向正确；且**「C」确实存在、现做 T09** → v23 ⑥ 那句"§6.4 里'会话 C'是第三批另一单"也由推断变为**实测事实**。**纪律升级**：署名**可查**（会话标题＋各 transcript 的写盘动作即证据），**下次不得再靠分工表反推**。**⑥ 冻结令复测（07:17，本轮零远端写）**：未推提交数 **15**（本条所在提交后 **→16**）、本地 main 提交数 **18**（**→19**）、`origin/main` 仍 **`2569104`**、远端树 **22 件**、`origin/main` reflog **空**；**四树 vendor 逐字节一致**（main／t04／t05／t09 各 **1,272,972 B／CRLF 0／SHA256 `76dea815`**）；会话 B 转达的 ⛔ 冻结提醒已复述进 T05 派单与 T05 行。**⑦ 一处我自己的判断错误更正**：v23 ⑤ 与我对 @user 的汇报均写"**t05 本轮不建**"（理由"交接只点名 t04"）——该判断已被 @user 本轮指令推翻；更严重的是我当时**未察觉 T04／T09 已由 @user 另行起单**（我只建了 t04 worktree、**从未派过 T04 单**，却在汇报里以"待 @user 起会话"含糊带过）→ **教训：worktree 建好≠已派单，反之"我没派单"也≠"没人在做"**；判断某单是否开工，必须查**会话实况**（transcript／`git worktree list`／分支提交数），不能只看自己有没有发过派单。**⑧ 待 @user 决策项（未变，本包不代行）**：远端可见性处置与是否重写历史；`参考资料/` 去留；契约 V1.3 的 `EnableMask`／`Reserved2` 声明（G4 依赖）；**T01 验收归属**（T01 仍"待验"、`## 验收记录` 仍"（暂无）"）；**t02／t03 worktree 是否删**（§5.1-③；两会话已分别转做 T05／T09，目录可能仍驻留）。⚠️ **9/16＝踏勘日兼《沟通单》递单日，该件仍为草案**：X3 倍速链、22 轴行程／零点／正方向、`links` 长度全等回执 → **T04／T05／T09 现在都是在占位值上开工**，回执到后**只改 `machine.yaml`**（X3 那条连代码都不改）。**⑨ 一项台账外观缺陷登记（非 G 级、不影响判据）**：本行（第 3 行版本链）的 `**` 计数实测 **665＝奇数**，即**有一处加粗未配对**；逐段定位后确认**系历史遗留、非本轮引入**——HEAD 版即为 **475（奇数）**，奇数段为 **v15（9）／v16（11）／v19（117）**，而**本轮追加的 v24 段自身 190＝偶数（配对完整）**，且 v24 追加后**该行前 15,302 字符与 HEAD 逐字一致**（严格追加）。**本轮不修**，理由＝版本链是审计留痕、**原文照录不改**；待 @user 示意后按 **v18 ⑧ 的先例**处置（**只调加粗边界、字面一字不改**）。⚠️ 全仓 `.md` **表格**结构本轮复测仍**列数不一致 0**（160 个表格块），本项只涉这一行散文的渲染外观。**v25 2026-09-16 07:30（系统钟）T09 阻塞 G17 裁决落地＋T04／T05 开工条件复核＋三会话派单（指挥会话；@user 本轮指令"你继续一口气干完，目标达到04、05、09开工条件，遇到问题按你的建议执行，然后给对应会话消息，让他们完成04、05、09"——故本轮不再出选项表，按我的建议直接执行并留痕）**：**① G17 新立并裁决＝A＋D**——会话 C 报的阻塞经我复核**成立**，且属"实现会话无法自救"一类：`config/machine.yaml` 节点表（L189–204）覆盖不到契约 §9.1 步骤 1/2/4/6/7 所需的七个符号，而 `validate.py:226-245 _nodes()` **只按 `schema.py:36-37` 的 `READ_NODE_SPEC`／`WRITE_NODE_SPEC` 遍历** → **yaml 里自行加键不报错、被静默丢弃**。**我独立复核了 C 的证据链**（不是采信自报）：两个 SPEC 字面确为三键／三键；契约 §5.1 确有 `SeqID`@1.0／`SpeedOverride`@4.0，§6.1 确有 `Ack`@1.0／`AlarmWord`@2.0／`SeqID`@4.0／`CurSeg`@6.0／`Vel[0..7]`@40.0——**逐条对上**。裁决全文与理由入 **04 §7.2-G17**（新行；G 编号 1..17 连续；⚠️ 首次插入时误落在 G16 之前，已按升序换回，复测 **941=G16／942=G17**、04 全文 **CRLF 978／157,880 B** 与改前同＝只换行序未动字节量）。**否决 B**（降级为"只看 `Status` 位＋超时推断"）：`Tasks/T09` 步骤 5＋完成标准"注入拒绝异常→**客户端收到原因码**"将不可达，`AlarmWord`（契约 §6.4，含急停 bit7）亦无从显示；**否决 C**（代码里写死符号名）：直接违反完成标准第 5 条"**改 machine.yaml 节点表即可换点表（代码零改动）**"。**② 03 §4 节点表就地扩七键**（原 L90–91 两行 → 13 行）：`read_nodes` 增 `axis_vel: [NodeId...]`／`ack`／`alarm_word`／`seq_id`／`cur_seg`（**三→八键**），`write_nodes` 增 `seq_id`／`speed_override`（**三→五键**）；附三条注释（`axis_pos`／`axis_vel` 个数**同受 pack_profile 约束**、七键来历＝G17、**本轮不加 `mode_mask`／`enable_mask`／`sync_err`／`timestamp` 及理由**＝无消费方＋`sync_err` 按契约 §6.4 bit3「电Q-9 确认前不启用」＋`EnableMask`↔`Reserved2` 是 @user 的 V1.3 悬置项，加了即替 @user 决策）。**③ 跨归属授权写进 §4 矩阵两行**（不写则我自己的越界检查会在 T09 收单时误判为违规）：`config/machine.yaml` 行加"**唯一授权例外**：T09 可改 **`opcua:` 节**节点表，**只及该节**，`axes`／`links`／`modes`／`limits`／`paths` 仍一律只读"；`core/config/`＋`tests/fixtures/` 行加"授权例外：T09 可改 `schema.py` 的 SPEC＋`validate.py`／`loader.py` 相应校验＋**全部 8 个 fixture**"，并钉四条约束（新键**必填**／`axis_vel` 同受 `PACK_PROFILE_AXIS_COUNT` 约束／**`tests/test_config.py` 24 用例全绿且不得改断言**／`lint_no_magic.py` 判定规则不得动、`tools/config_check.py` 只读）。**为什么必填而不是可选**：实测 **8 个 fixture 全部含 `opcua:` 节**，可选＝漏检；统一补键后各 `bad_*` 的「共 N 项问题」计数**不变**（缺陷仍是各自注入的那一处）→ 断言无需改，风险最低。**④ 段数组写法并案裁决**：`seg_array` 本期按**单节点写打包 ByteString（10×56＝560 B）**，⛔ **禁逐变量写**（会引入 80 次写与一致性窗口）；该假设须进**代码注释＋收单汇报**，等 **电Q-2**（OPC UA 接入参数）与契约 **Q-7**（实际 DB 编号）回执后再定是否改结构化节点。**⑤ T04 开工条件复核三项（全绿）**：**(1)** `Transform4x4`／`Violation` 全仓检索**只出现在 `kinematics.py` 的签名里**（03 §3 L50／L52、`Tasks/T04` L10／L12／L13／L16），而 **`collision.py` 用的是 `CollisionResult`**（03 §3 L53、`Tasks/T08` L17）→ **T04 定型这两个类型与 T08 归属不撞**；**(2)** 测试文件名 `tests/test_kinematics.py`＋`tests/test_public_api_kinematics.py` 与 T05（`test_public_api_geometry.py`）、T09（`test_opcua.py`）**零重名**，§4 矩阵"按文件名分单，天然不撞"成立；**(3)** 包化预案已在其卡内（§4.5-⑦ 授权范围含 `core/kinematics/`，**实测 >300 行才拆**、`__init__.py` 只 re-export）→ 两个数据类型应落 `transform.py`／`limits.py`，**不得写进 `__init__.py`**（该约束已随派单消息发给会话 A）。**⑥ T05 开工条件**：v24 ③ 的实测八条已随派单发出，本轮**无新增缺口**；`trimesh` 实测未装的处置口径不变（只在汇报里提**依赖增补申请**，⛔ 禁自行 `pip install`／改环境文件）。**⑦ 一处我自己的路径笔误更正（就地改＋本条留痕）**：v24 ① 与 T09 行 ④ 把 T09 交付件写成 **`core/comm/`**，而权威路径是**仓根顶层 `comm/`**（03 §2 目录树把 `comm/` 与 `core/` **并列**；04 §4 矩阵该行写 `comm/simulator.py`／`comm/opcua_client.py`；`Tasks/T09` 目标节同；骨架实测 `comm/__init__.py` 在仓根）→ **两处已就地更正**。**为什么不适用"原文照录不改"**：这是**当前生效的路径口径、不是历史测量值**，留着会让会话 C 在错位置建文件（＝活的危害），故按 **v18 ⑧ 先例**"修在文件里＋台账登记"。⚠️ 台账 v18 ③ 那处 **`core/comm/app`** 是附6-② AST 扫描的**作用域列表**（＝`core/`、`comm/`、`app/` 三个目录），**不是路径**，**照录不改**。**教训（与"时点值必须现测"同源）：路径串也要现查，"我记得在哪"不算证据。** **⑧ 三会话派单消息**（@user 本轮明示"给对应会话消息"→ 不再走 v24 那种"自足文本交 @user 转达"的降级路径）：**A**（`efecf84e`，T04）＝开工确认＋⑤ 的三项复核结论；**B**（`ae570776`，T05）＝v24 已拟的完整派单卡（实测八条＋四条红线＋F1 必检）；**C**（`1386afab`，T09）＝G17 裁决＋A＋D 授权＋`seg_array` 裁决＋"**扩表落地前先干 D 的不阻塞部分**"（Server 节点树骨架／56 B 段打包／20 Hz 回读／5 s 离线判定＋自动重连＋stale 看门狗／非 Ack 用例）。⚠️ **发送顺序＝先提交文档、再三树 `merge --ff-only main`、最后发消息**（否则三个会话读到的是**不含 G17 的旧卡**，与 §5.1 硬规矩 7 同一个陷阱）；**实际发送结果在本会话对 @user 的汇报里给**，若某条被 MCP 拒发则降级为自足文本并另行登记。**⑨ 冻结令复测（07:30，本轮零远端写）**：未推提交数 **16**（本条所在提交后 **→17**）、本地 main 提交数 **19**（**→20**）、`origin/main` 仍 **`2569104`**、远端树 **22 件／代码件 0**、`通裕重工`·`GXTC` **0 命中**、`origin/main` reflog **0 行**；**四树 vendor 逐字节一致**（main／t04／t05／t09 各 **1,272,972 B／CR 0／SHA256 `76dea815`**）。**⑩ 三树开工前实测状态（07:30）**：`task/T04`／`task/T05`／`task/T09` 均 **0 提交／0 落后**（＝硬规矩 7 的 `merge --ff-only` 补救仍适用），t04 树有**未跟踪 `core/kinematics/`**（会话 A 已开始按包化预案落地）、t05／t09 树 **dirty 0**。**⑪ 待 @user 决策项（同 v24 ⑧，未变，本包不代行）**＋⚠️ **9/16＝踏勘日兼《沟通单》递单日、该件仍为草案**：X3 倍速链、22 轴行程／零点／正方向、`links` 长度、**以及本轮 G17 依赖的电Q-2／契约 Q-7** 全等回执 → **T04／T05／T09 都在占位值上开工**，回执到后**只改 `machine.yaml`**。**⑫ 更正 v24 ⑨ 的两个错测数字（直接对 git 对象复测，不再用上一轮的脚本读数）**：v24 ⑨ 写"本行 `**` 计数实测 **665＝奇数**"与"本轮追加的 v24 段自身 **190＝偶数（配对完整）**"——**两个数字都错，且结论因此反了**。实测（`git show <ref>:前端任务包/99_进度与验收.md` 取第 3 行后直接 `count("**")`）：**`0241348`（含 v24 的 HEAD）＝ 694，偶数**；**`d136403`（v24 之前）＝ 475，奇数**（这一项 v24 ⑨ 说对了）；**按版本段切分后的奇数段＝ v15（9）／v16（11）／v19（117）／v24（211）**——即 **v24 段自身就是奇数、我自己也引入了一处未配对**，而 v24 ⑨ 却把它记成"偶数＝配对完整"、把整行记成"奇数＝纯历史遗留"。**真实情形恰好相反**：整行在 HEAD 是**偶数**（三个历史奇数段＋v24 这个奇数段，奇＋奇＝偶，**纯属抵消、不是配对正确**），未配对的 `**` 至少 **4 处**（v15／v16／v19／v24 各一）。⚠️ 本轮 ⑦ 的就地更正（`core/comm/`→`comm/`（**仓根顶层**…））又给 v24 段加了**一对**（211→**213**，仍奇）；v25 段自身 **178＝偶数**、追加后 v2..v24 前缀 **696＝偶数**、整行 **874＝偶数**。**为什么本轮不继续追到具体位置**：**计数法只能判奇偶、定位不了位置**（一旦某处失配，其后所有"应在粗体内／外"的判定整体错位，"最长粗体段"启发式实测也指不出元凶——四段的最长粗体跨段分别为 77／123／165／65 字符，都在正常范围内）；影响面**纯属这一行散文的渲染外观**（全仓 `.md` **表格**结构本轮复测仍 **34 文件／160 表格块／列数不一致 0**，判据不受影响）；而本轮优先级是**三单开工**。**处置**：留待 @user 示意后按 **v18 ⑧ 先例**逐段修（**只调 `**` 边界、字面一字不改**），修前须**逐段渲染目核**而不是只数个数。**教训（并入"时点值必须现测"）：测量脚本的读数也是测量值——换了实现或换了作用域就要重跑，不得把上一轮脚本的输出当本轮事实；更要紧的是，"我这段是干净的"这类自证结论必须用**独立于自己那段**的口径验（本轮错在拿自己写的脚本读自己刚写的段落）。**　**v26（2026-09-16 07:39，指挥会话）——G17 裁决落地执行完毕：文档已提交／三树已 ff／三会话已派单**：**① 提交**：`5cccda6`「docs: G17 节点表扩表裁决（A＋D）＋T04 类型定型口径＋99 v25」，实测 **5 文件／+49／−6**（03 §4、04＝§7.2-G17＋§4 矩阵两行＋§6.4 T09 卡、本台账 v25、Tasks/T04 步骤 7、Tasks/T09 步骤 1）。提交前实测 `git status --porcelain` **只有这 5 个文档文件为脏**（无代码、无 untracked 混入），按路径逐个 `git add`，⛔ **未用 `-A`**。**② 三树 ff**：t04／t05／t09 各执行 `git merge --ff-only main`，`0241348`→`5cccda6`，复测三树均 **0 ahead／0 behind**；t04 的三个 untracked（`core/kinematics/`、`tests/test_kinematics.py`、`tests/test_public_api_kinematics.py`）**不阻塞 ff**（本轮只动文档），且**说明会话 A 已按 §4.5-⑦ 包化预案起手**——已在其派单消息里要求收单时贴 §4.5-⑥ 实测行数与「原 X 行→拆成 A/B/C，各 N 行」、并确认 `__init__.py` 只有 re-export＋`__all__`。**③ 发送顺序按 v25 ⑧ 执行**：先提交→再三树 ff→最后发消息，**三个会话读到的都是含 G17 的新卡**；`send_message_to_chat_session` 三次全部返回 `messageSent`（A `efecf84e`＝running、B `ae570776`＝opening、C `1386afab`＝opening）。⚠️ **此前顾虑的「MCP 拒发会话间消息」不成立**：@user 本轮明示「然后给对应会话消息」，属**指挥方直接指令**，不是会话间自行转委派。**④ 冻结计数刷新（覆盖 v25 ⑨ 的时点值——那两个数已过期）**：**未推送 16→17**、**本地提交 19→20**；`origin/main` 仍 `2569104`（未动）；`origin/main` 的 reflog **0 条**；`git reflog show --all` 内 push 痕迹 **0**；远端树复测仍 **22 文件／代码类（`.py`/`.js`/`.html`/`.css`）0／「通裕重工·GXTC」命中 0**。**⑤ vendor 六树复测**：`view/vendor/three.module.js` 在 main／t02／t03／t04／t05／t09 **六树全部 1,272,972 B／CR 0／SHA256 前八位 `76dea815`**，逐字节一致。⚠️ **顺带更正我自己的一处路径口误**：先前多处把 vendor 写作 `view/vendor/three/three.module.js`（多一层 `three/`），**实际路径无该子目录**（以 `find` 实测与 `.gitattributes` 第 7 行 `view/vendor/** -text` 为准）——这是**本轮第二次栽在同一类错误**上（第一次＝`core/comm/`，见 v25 ⑦），教训同前：**路径串也要现查，「我记得在哪」不算证据**。**⑥ v26 自身的鸡生蛋问题**：登记本条又产生一个新提交，使三树再度落后 1（**纯文档**）。处置＝**提交后立即再 ff 一次**，维持「三树 0 ahead／0 behind」这条不变式；**此后不再为纯登记性提交反复 ff**（避免在会话作业中途反复动其工作树），下一轮台账更新**并入各单收单轮次**一起同步。**⑦ 三单开工条件均已具备**：T04（前置 T01＋T03 已合并；X3 自指口径＋类型定型口径已进卡；无越界项）、T05（前置 T01＋T02 已合并，依 §3.2-R2 提前开工；四条红线与 F1 `stepbar.mark_completed(1)` 均已在卡内点名）、T09（前置 T03 已合并，依 R3 与内核链并行；G17 阻塞已裁且授权已落档四处）。**⑧ 仍未消解的外部悬置（三单都在占位值上作业，非本会话可解）**：9/16＝踏勘日兼《沟通单》递单日而**该文件仍是草案**；22 轴全 `pending: true`、`links.length_mm` 全 0.0、X3 ratio 待回执、G17 的 `seg_array` 写法待 `电Q-2`／契约 `Q-7`。**⑨ 归 @user 独裁、我只标不办的四项**（v25 ⑪ 原样有效）：远端可见性／是否改写历史、`参考资料/` 去留、契约 V1.3 的 `EnableMask`↔`Reserved2`、T01 验收权（T01 仍「待验」且卡内 `## 验收记录` 为「（暂无）」）；另 t02／t03 两个 worktree 待删、`**` 未配对外观项（≥4 处：v15／v16／v19／v24）按 v25 ⑫ 留待示意后逐段修。　**⑩ 计数口径补正（登记提交无法自指，属结构性偏差，故显式写明）**：④ 的「未推送 **17**／本地 **20**」是**截至 `5cccda6`** 的读数；把**登记 v26 的提交本身**（`af5a3cc`）计入后实测为**未推送 18／本地 21**（`origin/main` 仍 `2569104`、工作区 clean，三树再 ff 后复测仍 **0 ahead／0 behind**，均在 `af5a3cc`）；**本条补正的提交**再计入则为 **19／22**（其 sha 见 `git log -1`）。⇒ **台账的冻结计数天然比实际少 1，且每补一次就再少 1——这就是递归本身**，故**数字追到此为止**。**口径定为：台账的冻结计数一律标「截至 `<sha>`」；要当前值就现跑 `git rev-list --count origin/main..main` 与 `git rev-list --count main`，⛔ 不得把台账数字当现值引用。**　**v27（2026-09-16 10:52，系统钟；指挥会话）——T05 收单复核：主体通过、⏸ 暂不合并（两项整改）＋裁 A／裁 B 落地（依 04 §5.3「不听自报」，全部行为级重跑）**：**① 复核面**：交付 `6e02269`「feat(T05): 模型导入与显示」实测 **15 文件／+1471／−18**、工作树 clean、`__pycache__` **0**；**8 条完成标准逐条独立重跑**（含 `face_map` 抽 9 个三角形与真值 B-Rep 面法向夹角量测：**平面 0.000°／圆柱面 2.318°**；offscreen-Qt 复现 **F1 闭环**——`mark_completed(1)` 后点回①，面板 index=0；**229 ms 导入期间主线程 16 次 tick**＝不阻塞；三阶段进度＋协作式取消 `已取消导入`；中文路径＋中文文件名导入成功；STL 判 `is_brep=False` 且步骤②锁）；**附4** >300 行 **0 件**、**附5** **0 命中**、**附6** 修正后 **0 命中**、`paths.cache_dir` 实测**在仓外**、pytest **44 passed**、`loader.js` **只显不判／无 CDN**。逐条记录已写入**本表 T05 行备注（①—⑧）**与 **`Tasks/T05` 卡末「收单记录」节**，本条只记要点与裁决，避免两处复述漂移。**② 会话 B 自报与实测不符两处（按实测更正，其原文照录不改）**：**(a)** 其报「附6-② 假阳性 **18** 处」，实测 **20 处、全部是 `OCC`**；**(b)** 其遗留 **C／D 两条归因均被实测推翻**——C 称 XCAF 不可用（实测 `STEPCAFControl_Reader`＋`XCAFDoc_DocumentTool` 导入与读取**均 OK**）、D 称 `BRepTools` 不可用（实测 `breptools.Write`／`breptools.Read` **均 OK**，且 `Read` 的第三参 `BRep_Builder()` 是**必填**）→ 两条的**结论**（自动命名兜底、不做 OCAF SaveAs）尚可辩护，但**理由是错的、不得作为口径传播**。⚠️ **真实成因是命名习惯**：pythonocc 的可用符号是**模块级小写函数**（`breptools`／`brepgprop.VolumeProperties`／`topods`），**CamelCase 类名（`BRepTools`／`BRepGProp`／`TopoDS`）一律 ImportError**——本会话自己连撞 **4 次**，已记项目记忆。**③ 两项整改（阻塞合并；均属复核侧才看得到的缺陷）**：**整改-1｜缓存全命中时 `face_index` 被静默清空**——`core/geometry/tessellate.py:_load_mesh`（**L81**）在**显示网格＋`.brep` 双缓存全命中**的「秒开」路径上返回 `MeshPart(..., face_index={})`，而 `face_map`／`face_index` **不下发前端**（`encode_mesh_parts` **L192** 只发 `id/name/n_vertices/n_tris/vertices/tris`）→ **完成标准 6「二次导入走缓存」照样通过，但 T06 取真值法向的唯一依据被掏空**。⚠️ 这是**「缓存命中悄悄掏空下游契约」这一类缺陷**：判据全绿、危害在**下一单**才显形。整改口径＝缓存命中亦须能按 `face_map` 重建 `face_index`（或按需重载 `.brep`），并补一条**「命中路径也断言 `face_index` 非空」**的用例。**整改-2｜`flyTo` 不感知画幅比，在壳自己声明的最小中栏尺寸下切掉模型**——`view/js/loader.js` 的 `flyTo(box)` 取 `orbit.radius = max(box.getSize().length()*1.4, 1)`，**只用包围盒对角线、不看 `camera.aspect`**（相机 `PerspectiveCamera(45,1,…)`，`resize()` 才把 aspect 改成 `w/max(h,1)`）→ 在 `app/shell.py` 自己声明的 **`CENTER_MIN=320`** 下（实测 **320×500 竖幅**）模型**左右被裁**；会话 B 的自测是 **480×360 横幅**，**天然看不见**。整改口径＝按 `aspect` 取**较窄方向**的视锥半张角定半径（竖幅看垂直 FOV、横幅看水平），并在完成标准里补一条**「最小中栏尺寸下整模型可见」**。**④ 裁 A（脚本 alias，影响后续多单）＝已直接修根因，不止加 alias**：B 建议「给 `OCC` 加 alias」，实测**根因不在 alias 表，而在脚本只读了一份登记源**——`pythonocc-core=7.9.3`／`occt=7.9.3` **只登记在 `environment.yml`**，而 04 附6-② 脚本**只读 `requirements.lock.txt`**（31 行内无 OCC 条目）→ **凡 conda 段装的包全部假阳性**。已改 **04 附6-②（443–478 行）**：**两份登记源都读**（兼容 `- occt=7.9.3=…` conda 写法与 `- x==1.0` pip 写法）＋ **alias 表改为条件放行**（`allowed |= {v for k,v in alias.items() if k in allowed}`，即**只在发行名已登记时才放行其 import 名**，⛔ 禁无条件白名单）。**为什么不只加 alias**：无条件 alias＝给**未登记的包**开后门——实测修正后**新抓出 `PIL`**（`pillow` 从未登记，旧脚本却因 alias 里写了 `pillow→pil` 而**永久放行**）。**反向取证**（证明修完不是橡皮章）：造 **4 条真违规** fixture 注入 → **4/4 全部命中**；`t05`＋`main` 两树修正后实测 **0 命中**。⚠️ 连带坐实 **v19 ⑦** 的版本口径：实装 conda 包为 **`pythonocc-core 7.9.3`＋`occt 7.9.3`**，smoke 打印的 `7.9.0` 来自 `OCC/__init__.py` 硬编码，**两者不同源**（不得据此打回「装错版本」）。**⑤ 裁 B（trimesh 依赖增补申请）＝本轮不核准，维持 ⏸**（四条理由已写入 **03 §8 该行状态格**）：**(1) 条件未成就**——该行准入条件原文是「T05 若**实测**需简化」，而 **T05 卡步骤 8 的 P0 导入性能实测未做**（B 亦自认）→ 触发条件不成立；**(2) LOD×3 不需要 trimesh**——`limits.tessellate_deflection_mm` 走三档弦高、用**现装** `BRepMesh_IncrementalMesh` 即可出三级 LOD，**零新依赖**；**(3) 单批 trimesh 批不出能力**——其 decimation 需额外 backend，实测 `trimesh`／`scipy`／`open3d`／`fast_simplification`／`networkx`／`shapely` **全部未装** → 真要做需 **≥2 个包各自过一遍准入**，与「一次增补」的申请粒度不符；**(4) 碰撞代理的消费者是 T08**、不是 T05。**处置＝剥离**：**T05 卡步骤 3 的「简化／LOD」与步骤 7 的「离线资产库」从本单剥离**，与 T08 的碰撞代理需求**并成一次依赖裁决、由 P0 实测数字触发**（P0 前任何性能／体积数字仍为**暂定**，⛔ 不得对外承诺）。**⑥ §4 矩阵回填三处**（交付实况倒逼，**事后报备**）：**(a)** `core/geometry.py` 行 → **`core/geometry/` 包**行：实测 >300 行后按 **§4.5-⑦ 预授权**拆为 `import_model.py` **297**／`tessellate.py` **205**／`__init__.py` **32**（⚠️ 与 `core/config/` 不同——那一包**不在 ⑦ 预授权内**、依据是派单卡，见 v23 ②）；**`T05∥T06` 绝对禁并行不变**；合并时冲突点＝`__init__.py` 的 re-export，**归指挥会话**。**(b)** 新增 **`view/css/app.css`** 行（T05 首建；后续追加由指挥会话合并；规范源＝02 §4；Qt 侧对应 `app/theme.py`）。**(c)** 新增 **`tests/cad_samples.py`** 行（T05 首建、**未预登记 → 追认**；T06／T08 复用；夹具纪律重申＝**禁写死仓内绝对路径、用 `tmp_path` 生成**）。**⑦ 合并次序＝⛔ 本轮不合并 `task/T05`**：整改-1 是 **T06 的输入契约缺陷**，先合就等于把 T06 建在坏地基上；待 B 的整改落地后**两个 commit 一起合**（`6e02269` ＋整改 commit），按 **T01／T03 先例不 squash、不改写历史**（04 §5.1 硬规矩 1）；合并后再按 **§3.2-R4** 起 T06（`T05∥T06` 禁并行 → **必须等 T05 合并**）。**⑧ 冻结计数（按 v26 ⑩ 口径标「截至」，⛔ 不得当现值引用）**：**截至 `7369df9`** 未推送 **19**／本地 main **22**；`origin/main` 仍 **`2569104`**、其 reflog **0 条**、本轮**零远端写**；要当前值现跑 `git rev-list --count origin/main..main` 与 `git rev-list --count main`。**⑨ 顺带实测的旁枝事实（只登记、未复核，不构成验收结论）**：`task/T04` 已有 **3 个 commit**（tip `782b6ae`，末条为「用例④ 补斜框——原三框全是符号置换阵，往返误差恒 0 证明不了判据」）、相对 main **0 behind** → **T04 交付已到、待开收单轮**；`task/T09` 仍 **0 提交**；`task/T03` 已并入 main（**0 ahead／9 behind**）；现存六个 worktree＝main／t02／t03／t04／t05／t09（t02／t03 **仍待 @user 确认后删**）。**⑩ 三项待办移交下一轮**：T04 收单复核（含 §4.5-⑥ 实测行数与「原 X 行→拆成 A/B/C，各 N 行」、`__init__.py` 只 re-export＋`__all__` 的核对）；T05 整改回单后合并＋起 T06；**v26 ⑥ 那条「不再为纯登记性提交反复 ff」在本轮失效**——本轮 t04／t05 已各有提交，**本轮 `695100f` 提交后实测三树一律未 ff**——判据是**工作树是否干净**，不是「有没有提交」：`t09` 虽 **0 ahead**，但实测 **21 项进行中改动**（`config/machine.yaml`＋`core/config/{loader,schema}.py`＋**8 个 fixture** 全脏＋`comm/` 五个未跟踪件＋`evidence/T09/`＝会话 C 正在落 G17 扩表）→ **属「实现会话作业中途」，⛔ 不动其工作树**；`t04`（3 提交）／`t05`（1 提交）**均已与 main 分叉，`--ff-only` 本就不适用**，留到各自收单轮由指挥会话一并同步（`t05` 的整改要求**随消息直投**，不靠 ff 传达）。⚠️ 本句初稿写「只 ff 仍 0 提交的树（t09）」，与同句 ⛔ 原则自相矛盾，已按实测更正（同「时点值必须现测」一脉：**判据要用当下实测状态，不能用上一轮的树况记忆**）。**v28 2026-09-16 11:43（系统钟）@user 本轮六项指令落地（「仓库不急，做完我再改」／「3. 授权」／「4. 并行开」／「5 你评测一下能否删」／「沟通单还得等几天需要电气的开会确定，先做不挡手的」／「6 先不移出」）——指挥会话执行**：**① T01 收单复核＝已过（第 1 轮）**，九条完成标准**逐项实测**（详条落状态表 T01 行备注 **⑧**，本节不复述）；**验收结论归指挥会话**的口径由 @user 本轮明示授权确立（与 T02／T03／T05 同口径，04 §5.3），本表 T01 行 ① 处原写「已过／打回 仍归 @user」的**口径不一致就此收掉**（原文照录不改、以 ⑧ 为准）。**② `## 验收记录` 一节由「（暂无）」启用为「每轮验收索引表」**——补登 T01／T02／T03／T05 四轮（轮次／时点／结论／详条落点四列），**只索引不复述**（理由同 Q 编号「唯一定义处」：详条已在状态表备注里，两处复述必漂移）；时点取法写明＝优先取该轮**落档提交的作者时间**（可复现），无落档提交时取状态表已记的系统钟时间。**③ 两处陈旧项更正**：**(a)** 外部依赖节首条「已推 **4** 次提交」→ **3 次**（远端 `main` = `2569104`、22 件）——v18 ② 当时已实测更正并改入 **04 §7.1-10**，而 **99 这一处漏改**、至今才补齐；⚠️ **同一数字在两处不一致本身即缺陷**，教训同「时点值必须现测」但形态不同：那次是**没复测**，这次是**改了一处漏了一处** → 更正类动作须**全仓 grep 该数字**，不能只改眼前那一处。**(b)** 《沟通单》「9/16 踏勘当面递」与「须在 9/16 递单前定稿」两个时点**作废**（@user：递单再等几天、须电气开会确定），连带影响已记（`电Q-*` 回执晚到 ⇒ T04／T05／T09 在占位值上落的用例要重算；回执到后**只改 `machine.yaml`**、X3 那条连代码都不改）；**并附一条建议**：递单延后其实给了 **V1.1 真正定稿**的窗口，定稿后 G9（两套 Q 编号）／G10（回填错行）在源头即闭合，不必依赖事后回填规程兜底。**④ T04 收单轮与 T05 整改轮并行开**（@user「4. 并行开」）：实测 `task/T04` **3 个 commit**（`de7ecf3` 主体／`bd72921` 坐标框类改名 `Frame`→`CoordFrame` 以避开 03 §3 已定名的「回读帧」／`782b6ae` 用例④补斜框）、**6 文件／+852 行**、t04 树 dirty **0**、**0 个文件 >300 行**（最大 `tests/test_kinematics.py` **299**）；按 §4.5-⑦ 预授权走包化（`core/kinematics/{__init__,fk,transform,limits}.py` = **34／182／191／77** 行）＋`tests/test_public_api_kinematics.py`（**69** 行，G3 撞名口径）。⚠️ **一条值得记的事实**：`782b6ae` 的提交标题是「用例④ 补斜框——原三框全是符号置换阵，往返误差恒 0 证明不了判据」，即**会话 A 自行发现了本项目已登记的「假证据形态①：阈值判据被测试数据架空」并主动修正**——这是**第一次由实现会话自己抓出该类缺陷**（此前两轮都由指挥会话复核时抓出），说明该条纪律已开始下沉到实现侧。另两对绝对禁并行不受影响（`T04∥T07` 未开 T07；本轮只复核不并行改同一文件）。**⑤ worktree 处置：本轮只评测不删**——@user 指令是「你评测一下能否删」，删除属 destructive 动作，**评测结论上报后等点头再执行**（实测 `task/T02` **0 ahead／13 behind**、`task/T03` **0 ahead／12 behind**，均已完全并入 ⇒ 删掉零丢失风险；`git worktree list` 现仍**六树**）。**⑥ 暂缓三项（@user 明示，本包不代行）**：远端可见性处置（「做完我再改」）、`参考资料/` 迁出（「6 先不移出」）、《沟通单》递单（「等几天」）——**均已就地记入外部依赖节**；⛔ **推送冻结令继续生效**，`参考资料/` 四件（含甲方全称＋招标编号 `GXTC-A1-26170051`）**继续留在本地未推提交里**，冻结令是当下唯一屏障。**⑦ 冻结计数（标「截至 sha」口径，见 v26-⑩）**：**截至 `9989c3a`＝未推送 22／本地 main 25**，`origin/main` 仍 **`2569104`**、其 reflog **0 条**、`git reflog show --all` 内 push 痕迹 **0**、`git status -sb` 首行 `## main...origin/main [ahead 22]`、vendor **1,272,972 B／CR 字节 0／SHA256 前缀 `76dea815`** → **本轮零远端写**；⚠️ 本条所在提交后**预测 →23／→26**（预测写在提交前、复测写在提交后，两读并列即为验证）。**⑧ 六树现状（本轮实测，落笔时系统钟 11:43）**：`main` `9989c3a`／`t02` `a67ab15`（0 ahead）／`t03` `5d17691`（0 ahead）／`t04` `782b6ae`（**3 ahead／3 behind**，dirty 0＝已交付）／`t05` `6e02269`（**1 ahead／3 behind**，dirty 0＝整改单已直投、等 B 落笔）／`t09` `7369df9`（**0 ahead／3 behind**，**dirty 21**＝会话 C 正在落 G17 扩表）；**三树一律未 ff**（判据＝工作树是否干净，见 v27-⑩；`t04`／`t05` 已与 main 分叉，`--ff-only` 本就不适用）。**v29 2026-09-16 17:10（系统钟）T04 收单复核＝已过并合并（指挥会话；依 @user 本轮「3. 授权」＝验收结论归指挥会话、「4. 并行开」＝与 T05 整改轮并行）**：**① 结论与合并**＝`git merge --no-ff task/T04` → **merge commit `ae5a89a`**（作者时间 **12:02:11**）；**T04 状态 在做→已过、验收轮次 1**（过渡态 待做→在做 已由 v23／v24 补记），`## 验收记录` 索引表**补 T04 行**（时点按本节「时点取法」注＝取落档提交作者时间 **12:02**，非系统钟）。**非快进**（两侧已分叉，`--ff-only` 本就不适用）；实测 T04 侧文件集＝`core/kinematics/{__init__,fk,limits,transform}.py`＋`tests/test_kinematics.py`＋`tests/test_public_api_kinematics.py` **恰 6 件**，main 侧自分叉点起＝`前端任务包/` **四件**，`comm -12` **交集为空** → 零冲突。**三笔交付按 §5.1 硬规矩 1 的 T01／T03 先例认定合规、不 squash**（`de7ecf3` 07:44 主体／`bd72921` 07:46 改名 `Frame`→`CoordFrame`／`782b6ae` 07:51 用例④补斜框；后两笔是交付后自查出的缺陷修正，各自独立事实）。**② 复核面（04 §5.3「不听自报」，全部现测于合并后 main 树，⛔ 未沿用任何交付时读数）**：pytest **88 passed**（`test_config.py` 24＋`test_kinematics.py` 59＋`test_public_api_kinematics.py` 5）；**附4** >300 行文件 **0**；**附5** T04 侧 ∩ 文档区 **0 命中**（带 `-c core.quotepath=false`）；**附6-②** AST 越界 import **0 命中／`rc=0`**（脚本**从 04 注 1 原文提取后实跑、⛔ 不是另写一份**）；**常驻①** `tools/lint_no_magic.py` **`rc=0`／扫描 23 个文件**；**常驻②** core 层 GUI／通讯 import **0 命中**；**常驻③ 本单无界面交付，不适用**（照实记，⛔ 不写"通过"）；vendor 仍 **1,272,972 B／CRLF 0／SHA256 前缀 `76dea815`**。**③ 两处独立取证（不采信自报，本轮复核的实质部分）**：**(a) 六条 FK 手算我自己重推一遍**（卡内「手算复核防自证」的正解）——链＝`base→gantry(X1,x)→column(Z1,z)→head(RA,z,len 200)→tool(无驱动轴,x,len 100)→tube(X3,x,len 0)`，六用例（全零位／X1 上端 500／Z1 上端 300／RA 正转 90／X3 驱动 10／三轴齐动）**逐条独立算出六根连杆原点、与 `FK_CASES` 期望值全部一致**；其中 `X3 驱动 10 → tube x=120` 坐实**自指 ratio＝驱动级倍速**（工程值 10×2＝20，⛔ 不是跨轴约束）、`X3=600 → Violation 记 1200.0` 坐实**行程检查用工程位置**。**(b) 附7 假证据形态① 复测坐实**——4 框×3 点共 **12 组**往返：`IDENTITY_FRAME`／`Y_UP_TO_Z_UP`／`TILTED` 三框 **9 组全部恒 `0.000000e+00`**（元素仅 0／±1，无舍入），**只有 `OBLIQUE`**（绕 z 转 30°＋斜原点 `(2.5,−7.5,11.0)`）在点 `(1234.5,−678.9,4321.0)` 上给出 **`1.136868e-13` mm** ⇒ **判据 `1e-6` 被数据架空约 7 个数量级**（worst／threshold＝`1.137e-07`），补斜框后才真受浮点检验。⚠️ **这是本项目第一次由实现会话自查出该类缺陷**（`782b6ae` 提交标题即自陈「原三框全是符号置换阵，往返误差恒 0 证明不了判据」；此前 T04 用例④、T05 视口两例都由复核侧抓出）→ **该条纪律已开始下沉到实现侧**，值得记。**④ 两道注入探针（在仓外临时副本 `D:/ZCode项目/_probe_t04_inject`，⛔ 未碰仓内任何文件；该副本基线 88 passed）**：**(1)** 把 `rotation()` 的 `sin` 取负（右手定则→左手）→ **5 failed／83 passed／`rc=1`**，失败集含 `test_rotation_positive_follows_right_hand_rule`、`test_fk_revolute_orientation_matches_hand_calc`、`test_fk_link_origins_match_hand_calc[RA正转90]`／`[三轴齐动]`、`test_multiply_composes_parent_then_child`；**(2)** 把 `to_column_major()` 改成原样返回（去掉行主序→列主序转置）→ **1 failed／87 passed／`rc=1`**，失败项**恰为** `test_to_column_major_moves_translation_to_tail`。**两次都是 `rc=1`（断言失败）而非 `rc=2`（收集错误），且失败集包含目标用例** ⇒ 避开附7 形态②（探针根本没跑起来却被读成「测试通过」）。**⑤ 一处自报数不予采信、就地标注**：卡内与 04 §2.1 记的「`core/kinematics.py` 实测 **428 行**」**是会话 A 的自报值、事后不可复现**——包化前的单文件**从未提交**，该数只存在于其 `core/kinematics/__init__.py` docstring 与 `de7ecf3` 提交消息里。**不影响判据**（§4.5-⑦ 的触发条件是「实测超限」，而**可复核的那一半＝拆出的四件逐件实测 34／182／191／77 均 ≤300**），但已在 99 T04 行备注 ③ 与 04 修订记录本轮行标注，**防后人把自报数当实测数引**（同「时点值必须现测」一脉：**不可复现的数一律标源**）。**⑥ 签名裁决＝显式传参、程序合规 ⇒ 立 G18**：`fk`／`build_model` 需要「哪根轴驱动哪根连杆、沿哪个方向动」，而**全仓无配置源**（`machine.yaml` 的 `links:` 只有 `{id, length_mm, parent}`，绑定只活在行尾 `#` 注释；《踏勘确认清单》**第 7 项**写明它「决定运动学链的正确定义」＝**待回执**）→ 三条路（等回执／自行加字段／显式传参）中**会话 A 选了显式传参并停下汇报为遗留⑤**；实测签名 `build_model(cfg, joints)`／`fk(joint_values, model)`／`check_limits(joint_values, cfg)`，`Joint` 自带 `motion` 字段、**合成链样例只活在 `tests/test_kinematics.py` 内**（其 docstring 明写「合成链与 `SAMPLE_JOINTS` 都是测试数据、不是机台事实」）⇒ **未越界改 `machine.yaml`、未私增字段，属正确处置**；缺口本身立 **04 §7.2-G18**（**需 @user 裁字段名与承接单**；⏳ **只阻塞 T07 的 `ik`，不阻塞 T04 已交付的 `fk`／`check_limits`**）。**⑦ 连带立 G19（属问甲方动作）**：实测 `sorted(set(SAMPLE_CFG.axes) - bound)` = **`FB1 FB2 FC KE RA RC ZA1 ZA2 ZB ZC ZD`（11 根臂专用轴）**在 `links:` 无对应连杆（`links:` 实测 **13 项**、末五项即 `mount_a~e` 安装架，**安装架之后的臂体链整段缺失**）⇒ `fk` 不出其位姿 ⇒ **T08 碰撞判定物理上覆盖不到五套工装臂**；现由守卫用例 `tests/test_kinematics.py::test_sample_arm_axes_have_no_link` 锁定现状（⚠️ 该用例是「**现状锁**」不是「**正确性证明**」，本轮实跑 **1 passed**）；处置＝**⛔ 任何会话不得自行补链**（`machine.yaml` 唯一写者是 T03，补链＝私改配置＋凭空造几何），T08 卡须写「结论只覆盖已建模的 13 根连杆、未建模臂须在界面与报告里显式标注、⛔ 禁把『未检出碰撞』报成『通过』」。**⑧ 文档回填（全部按 `git diff -U0` **现测**计数，⛔ 不按预估；行号一律为**改后新侧**实测值，⛔ 不心算）**：**04 十五处／14 个 hunk／+33 −10 行**（§2.1 T04 行 **L75**／§2.1 T07 行 **L78**／§2.2 依赖图 T07 注 **L116**／§3.2-R2 **L141**／§3.2-R4 **L143**／§3.2 末注四行 **L147–150**／§3.3-W3 **与 W4** 两行 **L159–160**（**两行同处一个 hunk**，故 15 处只出 14 hunk）／§4 矩阵 kinematics 行 **L186**／§5.4 合并顺序条 **L413**／**§5.5 附7 行 L446**／**§5.5 注3「四种假证据形态」表 L506–514**／§7.2-G8 进度核记四条 **L985**／§7.2 新增 G18＋G19 两行 **L995–996**（两行计一处）／**§7 修订记录本轮行 L1033–1039**；逐处详列见 **04 修订记录本轮行 ①**）＋ **03 八处／8 个 hunk／+27 −5 行**（§2 目录树 **L28**／§2 包形态说明条 **L43**／§3 签名表 kinematics 行（3 行改 5 行）**L51–55**／§3 `opcua_client` 行 `Frame` 消歧 **L60**／§3「签名回填的依据」块 **L64–72**／§4 `links` 行 G18 缺口标注 **L97–104**／§5 行主序口径条 **L155**／§9 修订记录 **V1.5** 行 **L205**）＋ **`Tasks\T07` 一处／1 hunk／+7 行**（新增「开工前置」块四条：只准新增 `ik.py`／同步更新两个公开面守卫 `test_init_only_reexports` 与 `test_package_layout_follows_the_preauthorized_plan` 且**只准新增断言**／行主序 vs 列主序与 `to_column_major()` 出口／G18 闸门）＋ **`Tasks\T08` 三处／3 hunk／+14 行**（「⚠️ 覆盖面口径」块四条 **L11–21**＋完成标准一项 **L41**＋禁止事项两条 **L48–50**）＋ **99 台账七处／6 个 hunk**（第 3 行 **v29** 自身／T04 行状态·轮次·备注 ①—⑩ **L14**／T06 行陈旧 R4 更正**与** T07 行四条前置 **L16–17**（**两行同处一个 hunk**）／「不可并行的一对」条 **L33**／其下执行状态核记 **L39**／`## 验收记录` 索引表补 T04 行 **L57**）。⚠️ **G8 原估「改 03 与三张任务单共 5 处文字」实测为 15 处**（03 四＋04 十一），**原估未计 04 自身**，且波次表与依赖图里的「热点文件」理由也须跟着改 ⇒ **后续同类回填按实测处数记**（已写进 G8 行核记 ④）；⚠️ **该 15 处与本条 ⑧ 的总数不同源**——G8 口径**只计「包化连带」那一部分**，本条 ⑧ 另含 G18／G19 立案、附7／注3 新增等**非包化连带**项，故 ⑧ 的数必然大于 15，两者不得互引。⚠️ **本条自身初稿也栽在同一类错误上**：原写「04 十二处／03 四处／99 台账四处」，实测**04 漏计三处**（§5.5 附7＋注3＋§7 修订记录本轮行）、**03 漏计四处**（§3 `Frame` 消歧＋§3 依据块＋§4 `links` 缺口标注＋§5 行主序条）、**99 漏计三处**（「不可并行的一对」条＋执行状态核记＋第 3 行 v29 自身），已按 `git diff -U0` 全部重算，详见 **⑮**。**⑨ 一处既有缺口登记（我自己的流程漏项，非交付缺陷）**：**04 修订记录自 02:11（T01 轮）之后即断档**——**T02（03:03）／T03（06:57）／G17 裁决（07:30）／T05 第一轮（11:02）／T01 收单（11:43）五轮改动均未在该表留行**，内容只活在本台账 **v20–v28**。原因＝那几轮我把回填重心放在 99 与任务卡，**未按该表既有惯例逐轮记行**。处置＝**⛔ 不追溯补造五行**（补造会把该表「写入时的系统钟读数」变成事后估算，直接违反第七轮 ⑦(b) 立的纪律），改为**在 04 修订记录本轮行 ⑤ 内一次性点名断档区间**、自本轮起恢复逐轮记行，并**补一条判据**：凡改 04 正文（§2–§7 任何一处），**同轮必须在该表留行**，收单复核时把「该表是否有本轮行」列为常驻检查项。**⑩ 四处行号引用自我更正**：G18／G19 与 04 修订记录本轮行原写 `links:`「**L138–150，13 项**」——实测 **`links:` 键在 L138、13 个条目在 L139–151**，**两端各偏一行**；**四处已改**（04 §7.2-G18「现状」列 **L995**＋**同一行**的「依据」列 **L995**＋§7.2-G19「依据」列 **L996**＋§7 修订记录本轮行 ② **L1035**；⚠️ 初稿写「三处」，系把 **G18 同行的两列当一处计**，已按 `grep` 命中位置重算），G18「现状」列内留「原写…2026-09-16 复测更正」痕（照录不改原则）。**成因**＝上一段写该行时按「键行＋13 项」心算了区间而未复核末行行号，与「时点值必须现测」同源：**行号也是事实，不得心算**。**⑪ worktree 处置（@user「5 你评测一下能否删」＝**只评测、⛔ 本轮未执行任何删除**）**：实测**六树现状**——`main` `ae5a89a`；`t02` `a67ab15`（**0 ahead／18 behind**、dirty **0**、2.1 MB）；`t03` `5d17691`（**0 ahead／17 behind**、dirty **0**、2.0 MB）；`t04` `782b6ae`（**0 ahead／5 behind**、dirty **0**、2.6 MB）；`t05` `21d4c03`（**2 ahead／8 behind**、dirty **0**、2.7 MB）；`t09` `4616574`（**1 ahead／8 behind**、dirty **0**、2.7 MB）。**评测结论**：**(a) `t02`／`t03`／`t04` 三树可删、零丢失风险**——三树均 **0 ahead**（提交已全部并入 main）、**dirty 0**（无未提交改动），`git status --porcelain --ignored=matching` 显示**唯一非跟踪内容是被 `.gitignore` 忽略的 `__pycache__`／`.pytest_cache`**（t02 一项／t03 五项／t04 六项），无任何未登记产物；合计收益约 **6.7 MB**（2.1＋2.0＋2.6），**收益很小**，删除的真正价值是**减少「同一文件多份可写副本」的误编辑面**；可逆性＝**随时 `git worktree add` 重建**（分支引用不随 worktree 删除而消失）。**(b) `t05`／`t09` 两树必须保留**——各含**未合并提交**（`21d4c03`／`4616574`），删除即丢交付。**(c) 一处前置条件**：删 `t02`／`t03` 须先确认**会话 B／C 已退出该目录**（Windows 下目录被占用会让 `git worktree remove` 失败或留下半删状态）；v22 ⑥ 与 v23 ⑤ 记的「暂不删」理由即此，**本轮实测两会话已分别转做 T05／T09，但占用状态无法从 git 侧判定** ⇒ **仍须 @user 点头**。⛔ **删除属 destructive 动作，评测结论上报后等点头再执行，本轮一个都没删**。**⑫ 两项新事实（本轮实测发现，各自可开收单轮，⏳ 等 @user 起）**：**(a) T05 整改笔已落地**——`task/T05` 现 **2 ahead**，新增 `21d4c03`（**11:33**，标题「**fix(T05): 补 .brep 双缓存与 face_index 命中路径＋视口按 aspect 取景**」，**恰对应第一轮打回的整改-1／整改-2 两条**）⇒ **可开 T05 第二轮收单**，必检两项＝**附7 形态③**（缓存命中路径下 `face_index` 非空、与首导入同构）与**形态④**（竖幅 **320×500**＝本软件自声明的最小中心视口下不裁切，⛔ 不得只测横幅）；合并时**两笔一并 `--no-ff`、不 squash**（照 T01／T03／T04 先例）。**(b) T09 已提交**——`task/T09` 现 **1 ahead**，`4616574`（**11:18**，「**feat(T09): OPC UA 桥与模拟器**」），且 **dirty 由 v28 ⑧ 记的 21 收口为 0** ⇒ **可开 T09 收单**，必检含 **G17 扩表落地**（`read_nodes` 八键／`write_nodes` 五键、`tests/fixtures/` 全部 8 个样例同步补键、`tests/test_config.py` **24 用例全绿且未改断言**、各 `bad_*` 的「共 N 项问题」计数不变）与 **`seg_array` 按单节点打包 ByteString（10×56＝560 B）** 的假设是否写进代码注释＋汇报。**⑬ 冻结计数（标「截至 sha」口径，见 v26-⑩）**：**截至 `ae5a89a`＝未推送 27／本地 main 30**，`origin/main` 仍 **`2569104`**、`git status -sb` 首行 `## main...origin/main [ahead 27]` ⇒ **本轮零远端写**；⚠️ **本条所在提交后预测 →28／→31**（预测写在提交前、复测写在提交后，两读并列即为验证）。⛔ **推送冻结令未解**（@user 本轮「仓库不急，做完我再改」＝可见性处置暂缓，在此之前冻结令是 `参考资料/` 四件含甲方全称＋招标编号 `GXTC-A1-26170051` 的**唯一屏障**）。⚠️ 复测（提交后，系统钟 17:52）：本条所在提交为 3fcf977，实测未推送 28／本地 main 31，与上句预测逐位一致；origin/main 仍为 2569104、其 reflog 0 条、git status --porcelain 提交后为空 ⇒ 本轮零远端写。按 v26-⑩ 口径，承载本复测的那一次提交不再追记（递归止于此）。另：本句刻意不含任何加粗标记与反引号段，故不改变 ⑭ 所报的 568／284／157 三个数——这是「自检条目自身也是被检文本」这一陷阱的可操作解法。**⑭ 本段自检（全部实测，⛔ 非声称；数字为 ⑧⑩⑭⑮ 全部更正落盘后的**重测值**，⛔ 不是初稿读数）**：**加粗配对**——v29 段原始加粗标记计数 **568＝偶数**、**284 个加粗段全部配对**（非贪婪配对后**残余 0**、最长段 **94** 字符、无空段、无跨行段、**无吞掉下一个版本标记的失控粗体**、v29 段内与全行**均无三连星号**）⇒ 按 **v25 ⑫** 立的「追加段自身须偶数」判据**通过**（⚠️ 初稿把该判据出处写成「v28 ⑨」——那条系本轮误插入历史段落、**已整条删除**，见 **⑮(a)**；⚠️ 初稿记的 **323／322／161／96** 四个数是 ⑧⑩⑭⑮ 四处更正**落盘之前**的读数、已过期，本条为重测）。**行内代码污染实测为 0**：v29 段内 **157 个反引号段，其中含加粗标记的 0 个** ⇒ 本段**原始计数与剔除行内代码后的计数相同**（均为 **568**）；⚠️ 故 ⑮(b) 那条「先剔除反引号段再判奇偶」的口径在**本段并未改变结论**，但它**必须改成只用于「查有没有把加粗标记写进行内代码」这一问**——**配对判定要在原文上做**，因为剔除会把相邻两个加粗标记**拼接**起来、**凭空造出三连星号**（实测：剔除后出现 **1** 处三连星号，而原文 **0** 处），照剔除后的文本判配对会**误报一处失配**。**自洽校验**：全行（含 v2—v28 历史段）计数 **1990＝偶数**、`ae5a89a` 版 **1422＝偶数**，两者之差 **568** 恰为本段增量。**严格追加已由等值比对坐实**：v29 之前的前缀 **36,177 字符**与 `ae5a89a` 第 3 行**逐字相等**（`==` 实测 `True`，⛔ 不是只比长度；与 71a539f 比同为此结果，两者都是 v29 追加前的最后落档版）。⚠️ 基线必须点名到具体提交、不得写 HEAD：本轮文档提交 3fcf977 之后 HEAD 已含 v29 段，拿 HEAD 比会得到 False，那是基线漂移不是追加失败——本次就是这么发现的，已把三处操作性基线 HEAD 换成具名 sha，另在 ⑮(a) 补两处（操作性教训一处改为点名 ae5a89a、照抄被删子条原文措辞一处就地标注为非操作性基线），故 v29 段内不再有任何以 HEAD 为基线的判据。**数字口径**：段内一切数字均为**本轮现测**，唯一例外是 ⑤ 明标的「428 行」自报值（已就地标源、不当实测数用）。⚠️ 本条数字系第三次重测，且暴露一个结构性陷阱：先前记的 504／252／156／1926 那一组是 ⑭ 重写与 ⑮(b) 追加落盘之前的读数——写自检条目本身会改变被检文本，故落盘后必须重测，实测为 568／284／157／1990；本次只改数字、未增删任何加粗标记与反引号段，故改后读数不变（已复跑确认）。处置口径＝凡改 ⑭⑮ 一律改完再测、测完只回填数字，直到读数不动为止；⛔ 不得把改前读数当改后事实，也不得为了「一次测准」而把自检条目写在被检文本之外（那会让自检与正文脱钩、失去同段偶数判据的意义）。**⑮ 本轮自查出三处「我自己写进台账的缺陷」并已全部更正（属复核侧自身的假证据，按 附7 同口径处置：⛔ 不得以「我已经写了自检」充当通过）**：**(a) 误插入历史段落**——写 v29 时 `old_string` 只锚定 v28 段末一句，而 `new_string` 在该句之后**多写了一个 v28 的「⑨ 一处台账外观缺陷的处置」子条**，等于**改动了已复核过的历史段落字面**（违反严格追加）；更严重的是该子条的三个数（整行 **665 奇**／`HEAD` **475 奇**／v24 段 **190 偶**；此处 HEAD 系照抄被删子条的原文措辞、不是操作性基线）**正是 v25 ⑫ 已复测并判定为错的原值**（实测 **694 偶／475 奇／v24 段 211 奇**），照抄即**把已被推翻的错数重新播回台账**。处置＝**整条删除**，删后实测前缀 **36,177 字符与 `ae5a89a` 第 3 行 `==` 为 `True`**（严格追加复原）。**教训**：`old_string` 短而 `new_string` 长时，**多出来的部分会被当成「原文」混进历史段**——追加类编辑改后必须跑一次「前缀与 `ae5a89a` 等值」比对，⛔ 不能只靠肉眼；基线一律点名到具体提交、⛔ 不得写 HEAD，因为本轮文档提交之后 HEAD 已含新段、拿 HEAD 比必得 False（那是基线漂移不是追加失败，详见 ⑭）。**(b) 声称了一个没做过的测量**——⑭ 初稿写「加粗计数实测**偶数**」而**当时未执行任何计数命令**，属 附7 的同类（把「应该是」写成「实测是」）；已改为**先测后写**，并连带查出**计数法本身的一个缺陷**：原始 `count` 会被行内代码里的字面星号污染，**必须先剔除反引号段再判奇偶**（本条即按此口径重测，见 ⑭）。⚠️ **该口径在 ⑭ 重写后已自我修正**：初稿之所以出现「原始 **323** 奇／剔除 **322** 偶」，是因为**初稿的 ⑭ 自身在反引号里引用了一个字面加粗标记**去解释计数法；重写后 v29 段 **157 个反引号段中含加粗标记的为 0 个**、原始与剔除后**同为 568**，且实测**剔除会把相邻加粗标记拼接、凭空造出三连星号**（剔除后 **1** 处、原文 **0** 处）⇒ 口径改为「**剔除只用于查有没有污染，配对判定一律在原文上做**」。**(c) 处数与行号用的是草稿期预估值**——⑧ 初稿「04 十二处／03 四处／99 四处」与 ⑩ 初稿「三处」均在后续又改了数处之后**未重算**；且 **04 修订记录本轮行 ① 的行号自 §3.3-W4 起整体错位一项**（原写 W4＝**L186**／§4 矩阵＝**L413**／§5.4＝**L446**，实测 **W4＝L160／矩阵＝L186／§5.4＝L413／附7＝L446**），成因＝**漏列 附7 与 注3 两处后，后续行号顺次顶替**——即 ⑩ 那条「行号不得心算」的教训**在同一轮里第二次发生**，且形态更隐蔽（每一处都「看着像对的」）。已全部按 `git diff -U0` 重算并回填 99／04／03 三个文件。⚠️ 本轮末次更正（系统钟 2026-09-16 18:02）：复跑基线扫描时发现 ⑮(a) 内尚余两处反引号 HEAD，已按上款口径处置；本次四处改动均未增删任何加粗标记与反引号段，改后复测 v29 段仍为 568 个加粗标记／284 个加粗段／157 个反引号段、全行仍为 1990＝偶数，且前缀与 ae5a89a 第 3 行逐字相等仍为真。**v30 2026-09-16 20:40（系统钟）T05 第二轮收单复核＝已过并合并 ＋ `T05 ∥ T06` 解禁（两对全部解禁）＋ T06 派单口径落地（指挥会话；依 @user 批复「全按建议」＝① 开 T05 第二轮收单 ② 开 T09 收单 ③ 删 t02／t03／t04 worktree（先确认会话已退出））**：**① 结论与合并**＝`git merge --no-ff task/T05` → **merge commit `8440765`**（作者时间 **20:26:51**）；**两笔交付 commit 原样保留**（`6e02269` feat ＋ `21d4c03` fix，⛔ **未 squash**，依 §5.1-1 的 T01／T03／T04 先例——卡片「一个 commit」是**主交付标题口径**、不是硬判据）；实测并入 **17 文件／+1698 −18**、**零冲突**、合并后 `git status` 空。**T05 状态 打回（局部整改）→已过、验收轮次 1→2**，`## 验收记录` 索引表**补 T05 第 2 轮行**（时点按本节「时点取法」注＝取**落档提交作者时间 20:26**，非系统钟）。**② 三项整改全部行为级独立复现（§5.3「不听自报」，⛔ 未采信任何自报数字）**——详条**一律落在状态表 T05 行备注 ⑨—⑯**（本节只记结论与落点，避免两处复述漂移，同 Q 编号「唯一定义处」思路）：**整改-1**（`.brep` 双缓存＋命中路径 `face_index` 重建）**通过**，含**几何同构复测**（15 面，最差质心偏差 **4.718e-16 mm**、最差 `abs(dot)−1`＝**0.0**，⚠️ 交付用例只断言**键集相等**＝弱判据）、**负控**（删 3 个 `.brep` 即回落重解析）与**附7 形态② 注入 rc=1**（失败项正是核心判据、0 处 `error`）；**整改-2**（按 aspect 双向取景）**通过**，含 **320×500／431×625／625×431 三档四边不触边、触边像素 0** 与**旧式负控复现 186／218／0 触边像素**（横屏 0＝附7 形态④ 取向盲区）；**整改-3**（三处 docstring 事实）**逐条复测为真**，含 `TDocStd_Document(TCollection_ExtendedString(...))` 崩溃的**双通道读数**（bash **rc=127**／subprocess **rc=3221226505**，同一崩溃两个读数都真——首轮只测一个通道、误判 docstring 不可复现，属**探测方法缺陷、不是文档缺陷**）。**③ 合并后回归在 main 上重跑**（⛔ 不是分支上重跑，A／B 各自绿 ≠ 合并后绿）：pytest **109 passed**、`lint_no_magic` **rc=0（31 文件）**、附4／附5／附6-①② **全零命中**、仓内 **0 个 `.brep`／`__pycache__`**、无 `cache/`／`data/` 目录、`evidence/T05/` 入库边界合规。**④ 本轮文档回填实测二十一处**（**04 八／03 五／99 五／05 一／Tasks 二**），**按实测处数记、不按预估**（v29-⑮(c) 的教训；G8 原估 5 处 → T04 轮实测 15 处 → 本轮 21 处）；其中**两处属「原句加删除线照录留痕＋失效标注」**（04 §3.2 末注、04 §4 矩阵 kinematics 行末句），**一处属预案外增件追认**（`brep_cache.py`——§4.5-⑦ 对 `core/geometry/` 只列三件，而整改-1 必须有落点、塞进 `tessellate.py` 会顶破 300 行；三条理由与**防扩大解释的边界**见 04 §7.2-G8 核记 **⑤**：⛔ 只覆盖**收单裁定驱动**的新增件，不构成「实现会话可自行往预案包里加文件」的先例）。**⑤ 新增 04 §6.6「第四批（T05 合并后开，会话 B）」＋ T06 派单卡**（⛔ **未改 §6.5 编号**，因「§6.5 汇报模板」被他处交叉引用、改号会断链）；`Tasks\T06` 卡就地改**步骤 2**（产出件改包形态：只新增 `core/geometry/face_point.py`、禁改四件）与**步骤 6**（pytest 须含无理数用例＋全命中路径用例），并增「**开工前置与口径**」五节，把**六条收单必检**钉进卡内（附7 四形态各一条 ＋ 真法向≠三角形法向 ＋ F3 真清空点位列表）。**⑥ 一处复测方法教训（对 T06／T08 同样适用，已写进 04 §4 矩阵 geometry 行与 `Tasks\T06` 卡）**：`core/geometry/__init__.py` re-export 了与子模块**同名的函数** `import_model`／`tessellate`，故 `import core.geometry.tessellate as _ts` 绑到的是**函数**而非模块、monkeypatch **静默无效**——首轮探测因此得出「`.brep` 文件 0 个」的**假阴性**，须用 `importlib.import_module(...)`；⚠️ 这属**附7 四形态之外的第五种假证据：探针自身失效导致的假阴性**（不是假阳性），本轮一并登记、暂不改 04 注3 的四形态清单（理由＝该清单是「**假阳性**（看着绿其实没测）」的四形态，假阴性属**探测方法**问题，落点应在 §5.3 的复测规程而非注3）。**⑦ 冻结计数（标「截至 sha」口径，见 v26-⑩）**：**截至 `8440765`＝未推送 32／本地 main 35**，`origin/main` 仍 **`2569104`**、远端 reflog **0 条**、`git status -sb` 首行 `## main...origin/main [ahead 32]` ⇒ ⛔ **推送冻结令未解、零远端写入**；**本轮文档提交（本行所在笔）落地后＝未推送 33／本地 main 36**（**算术推定**，提交后须复测并回填，见 v29-⑬ 的「提交后复测」口径）。⚠️ 复测（提交后，系统钟 20:56）：本轮文档提交为 e86e4b3，实测未推送 33／本地 main 36，与上句算术推定逐位一致；origin/main 仍为 2569104、其 reflog 0 条、git status --porcelain 提交后为空、git status -sb 首行为 main...origin/main [ahead 33] ⇒ 本轮零远端写。按 v26-⑩／v29-⑬ 口径，承载本复测的那一次提交不再追记（递归止于此）。另：本句刻意不含任何加粗标记与反引号段，故不改变 v30 段与全行的自检计数。**⑧ worktree 现状（本轮实测，`git worktree list`）**：`t02` `a67ab15`／`t03` `5d17691`／`t04` `782b6ae`／`t05` `21d4c03`／`t09` `4616574` 五树俱在；评测结论（@user 批复 ③ 的前置）＝**t02／t03／t04 三树 ahead=0、dirty=0、只余可再生缓存，各 2–3 MB、合计约 12 MB ⇒ 可删**；**t05（2 ahead）与 t09（1 ahead）⛔ 不得删**；删除的**唯一前置**＝确认会话 B／C 已退出那些目录（**尚未执行删除**）。**⑨ 遗留（不在本轮）**：**T09 收单**（`4616574`，1 ahead——须核 G17 扩表落地：`read_nodes` 八键／`write_nodes` 五键、8 个 `tests/fixtures/` 样例全改、`tests/test_config.py` **24 条用例全绿且未改断言**、各 `bad_*` 的「共 N 项问题」计数不变、`lint_no_magic.py` 判定规则未动、`tools/config_check.py` 只读，以及 `seg_array` 按**单节点打包 ByteString**（10×56＝560 B）的假设是否进了代码注释与汇报）。**v31 2026-09-16 21:17（系统钟；时点取 merge commit `f8de0b8` 的作者时间，可复现）T09 收单复核＝已过并合并（`--no-ff`，merge commit `f8de0b8`）＋ G17 扩表落地核记 ＋ 新立案 G20 两项 ＋ 预案外增件追认 ＋ t02／t03／t04 三树删除（依 @user "全按建议" 批复的第 ②③ 项授权）**：**① 交付形态与合并可行性**：`task/T09` 单笔 `4616574 feat(T09): OPC UA 桥与模拟器`（**27 文件／+2640−18**），父＝merge-base `7369df9`；reflog 只有 `Created from HEAD`＋四次 `merge main: Fast-forward`＋一次 `commit:` ⇒ **无 rebase／amend／reset**、无 upstream、`origin/main` 全程 `2569104`。合并前预判＝main 侧 31 文件 ∩ T09 侧 27 文件 **空集** ⇒ 预告零冲突，实测相符。**② 复核口径＝04 §5.3「不听自报」**，全部数字由指挥方实测；详条落状态表 T09 行备注 **⑥—⑯** ＋ `Tasks\T09` 卡末新增「收单记录」节，本段只记结论与落点。**③ 裁定＝已过**：完成标准六条逐项实测通过（段长 **56 B**／整批 **560＝10×56**、60 s 收 **1199 帧** OLS 斜率 **20.0103 Hz**＋服务端自计 **20.0000 Hz** 落后 0 次、kill 后 **4.58 s** 判离线且降级期不外推、14 组改名点表**零改码**到 ST_DONE、取证物扫甲方字样 **CLEAN**）；**G17 四条硬约束逐条独立探针复测为真**（7 个新键逐个删均 `ConfigError`；`axis_vel` 7／9 个均拒、两键同时 7 分别报两行；`tests/test_config.py` 零改动且七个 `bad_*` 计数维持、8 个 fixture 均 +10／−0；`validate.py`／`lint_no_magic.py`／`config_check.py` 全部零改动）；合并后 main 回归 pytest **130 passed**（＝109＋21 逐位相符）、`lint_no_magic` **rc=0／42 文件**、附4／附5／附6 全空、`comm_selftest.py` 在 main 复跑 **rc=0／6 PASS**。**④ 附7 四形态排查完毕**：形态①以**负控**排除（`publish_interval_ms` 50→60、窗口缩 20 s、阈值不动 → 斜率 **16.6642 Hz／B＝FAIL／rc=1**，估计量无偏 −0.0025 Hz）、形态②以**三处注入**排除（`git archive task/T09` 沙箱，**三次均 rc=1、无一次 rc=2**）、形态③无对应路径（`comm` 侧无缓存件）、形态④不适用（无 UI 交付）。**⑤ 三项自报偏离裁定＝接受（语义未松）**：`MIN_HZ = 20.0` **阈值本身未下调**（只减 0.01 Hz 量具容差）；**验收判据仍 `< 5.0 s`**（只把实现常量 `OFFLINE_AFTER_S` 收紧到 4.5——条件是 `age > 阈值` 且每 0.1 s 轮询，取 5.0 必不可达）；`comm/opcua_client.py` **实测 536 行**超 §4.5-① 上限，故按派单卡预授权拆包六件。**⑥ 追认两类增件并划反扩边界**：`comm/opcua_client/errors.py`＋`handshake.py`（卡要求**报备非闭集**、包已预授权）；**`comm/virtual_motion.py`／`plc_logic.py`／`plc_nodes.py` 属预案外增件**（卡只预授权拆 `opcua_client.py`、未授权拆 `simulator.py`，404→322→277），与 T05 的 `brep_cache.py` 同类 ⇒ 追认，⚠️ **但只覆盖本轮三件、不构成"通讯侧可自由拆件"的先例**。**⑦ 新立案 G20（两项，04 §7.2）**：**G20-①** `tools/config_check.py:57` 的节点表摘要**已过期**（仍硬编码 G17 前的键名，漏 5 读键＋2 写键；**成因＝G17 硬约束 4 令 T09 不得动该文件 ⇒ 扩表的后果无人被授权处置**，是"只读约束"造成的**归属真空**，⛔ 不是 T09 失职）；**G20-②** `publish_interval_ms` **无 CI 守卫**（绝对 ≥20 Hz 只活在手动工具，静默改 60 时 pytest 仍全绿）→ 建议归 T10 `e2e_smoke.py`。**⑧ 三条遗留登记**：`HZ_RESOLUTION = 0.01` 的推导注释与实际散布不符（实为散布**约 2 倍非 5 倍**；**不构成架空**——负控差 3.3 Hz，但实测最小值只高出通过线 **0.0097** ⇒ **红闪风险非假绿风险**；注释在已合并业务码内，按 §5.1-5 **指挥方不得自行改**）；**契约 §4.3「字节序由 Server 自动转换」与 `seg_array` 打包 ByteString 后对 Server 不透明相抵**（本单选 S7 原生大端、已在 `write.py` docstring 与汇报声明、**未自行改契约**＝合规，待 `电Q-2`／契约 `Q-7` 回执复定）；报告把 `PACK_PROFILE_AXIS_COUNT` 记成本轮新增（**实为 T03 既有的 diff 上下文行**，§5.3 又一例）。**⑨ 矩阵缺口补登**：`tools/README.md` **原在 04 §4 矩阵中无登记归属人**，而该文件自身规矩要求新增工具须登记 ⇒ T09 加两行的**行为正确、缺口在矩阵**；本轮补行，口径＝**追加式归属登记表，各单只加自己那一行**。**⑩ worktree 删除（@user 批复 ③ 的执行）**：确认 `.git/worktrees/{t02,t03,t04}/` 内**无 `.lock`**、无进程占用 `*机械臂软件-wt*` 路径后，以**非 force** 的 `git worktree remove` 删掉 **t02／t03／t04 三树**（分支 `task/T02`／`T03`／`T04` **全部保留**）；**t05／t06／t09 未动**——t06 dirty=1（会话 B 在做），t05／t09 虽已 ahead=0／dirty=0 **但不在批复范围** ⇒ **只提议、不执行**。**⑪ 冻结计数**（`截至 f8de0b8` 的现值＝未推送 **36**／本地 main **39**；本轮只 1 笔文档提交 ⇒ 提交后算术推定为未推送 **37**／本地 main **40**，`origin/main` 仍应为 `2569104`、其 reflog 仍应 0 条；**算术推定**，提交后须复测并回填，见 v29-⑬ 的「提交后复测」口径）。提交后复测（系统钟 2026-09-16 21:52，落档 7d6abf4）：未推送 37、本地 main 40、origin/main 仍 2569104、其 reflog 0 条、git status -sb 首行为 ahead 37 —— 与上述算术推定逐位相符；本句为惰性句（无粗体标记、无反引号、无竖线），承载它的这一笔提交自身不再复测，递归止于此。　v32（2026-09-16 22:20，系统钟；指挥会话；@user 本轮三令：05 与 t09 worktree 不删、G18 派活、G20 派活，外加 HZ_RESOLUTION 0.01 改 0.02 按指挥方意见执行）——G18 裁决授权 T07 跨归属落地、G20 两项归 T10、HZ_RESOLUTION 改 0.02 并由指挥方本轮执行：本节为惰性段，全程不用粗体标记、不用反引号、不用竖线，故不改变 v31 段与全行的自检计数。其一，G18 经 AskUserQuestion 由 user 裁「授权 T07 跨归属落地」，含义为 T07 代已收单的 T03 给 config/machine.yaml 的 links 节每项补 axis 与 motion 两键、并改 core/config 的 schema 与 validate 与 loader 三件以及 tests/fixtures 全部 8 个样例，顺序是先落 links 扩字段再开 ik.py；该裁决与 G17 同型但显式不援引 G17（G17 只及 opcua 节），落点为 04 第 7.2 节 G18 行决议格、04 第 4 节矩阵 config/machine.yaml 行与 core/config 行各加「第二授权例外」、03 第 4 节 links 行与注释块、03 第 9 节修订记录 V1.8、Tasks 下 T07 卡开工前置第 4 条闸门解除、本表 T07 行备注 4 照录留痕。其二，G20 经同一问由 user 裁「两项都归 T10」，G20 之一为 tools/config_check.py 节点表摘要改为按 SPEC 生成（现状漏 5 读键加 2 写键，成因是 G17 硬约束 4 令 T09 不得动该只读件、扩表后果无人被授权处置，属归属真空不是 T09 失职），该裁决即 T10 改这件 T03 归属文件的跨归属授权凭据；G20 之二为 tools/e2e_smoke.py 增 publish_interval_ms 不大于 50 的守卫加负控（静默改 60 须可红），落点为 Tasks 下 T10 卡新增 G20 承接块加完成标准一项、04 第 7.2 节 G20 行决议格、本表 T10 行。其三，HZ_RESOLUTION 由 0.01 改 0.02 属 user 明示授权下指挥方对已合并业务码的单点改动，是 04 第 5.1 节硬规矩 5「指挥方不得自行改业务码」的显式例外，理由为原推导注释称散布的 5 倍而实测约 2 倍、最小值只高出通过线 0.0097 属红闪风险不是假绿风险，改动落 tools/comm_selftest.py 第 53 至 54 行常量与注释，MIN_HZ 仍 20.0 不动，改后负控（publish_interval 50 改 60 得 16.6642 Hz）仍 FAIL、rc 仍 1，文件仍恰 300 行压 4.5 之一上限、AST 与 lint 均 rc 0。其四，冻结计数按 v26 之十「截至 sha」口径：截至本轮提交前现值为未推送 38、本地 main 41、origin/main 仍 2569104、其 reflog 0 条、git status -sb 首行 ahead 38；本轮只 1 笔文档提交，提交后算术推定为未推送 39、本地 main 42，提交后须复测并回填（见 v29 之十三口径）。其五，worktree 处置遵 user 本轮明示，t05 与 t09 两树不删（v31 之十六已记其 ahead 为 0、dirty 为 0，但 user 指令优先于「可删」评测结论）；t06 树仍由会话 B 占用未动。其六，本轮无远端写、推送冻结令未解（user 此前「仓库不急、做完我再改」仍有效），参考资料四件含甲方全称与招标编号 GXTC-A1-26170051 继续留在本地未推提交里、冻结令是唯一屏障。提交后复测（系统钟 2026-09-16 22:24，落档 14fd575）：未推送 39、本地 main 42、origin/main 仍 2569104、其 reflog 0 条、git status -sb 首行为 ahead 39 —— 与上述算术推定逐位相符；本句为惰性句（无粗体标记、无反引号、无竖线），承载它的这一笔提交自身不再复测，递归止于此。v33 2026-09-16 23:02（系统钟）T06 面拾取与点位示教收单通过并合并（会话 B 完工交接、指挥会话按 04 §5.3 不听自报逐项实测）：其一，交付形态＝task/T06 单笔 bfd04e8（10 件、加 1090 减 3，merge-base c8abade，工作树净、无 upstream、未 push），禁改文件 environment.yml／requirements.lock.txt／view/vendor 三处零改动；其二，合并＝git merge --no-ff task/T06 得 merge commit 9ea633e（main 侧自 merge-base 起 35 件含 T09 合并与文档、T06 侧 10 件，comm -12 交集为空、ort 策略零冲突），交付单笔原样保留未 squash；其三，合并集成笔 3e67ef3＝指挥方专属动作（B 未动 init 与 index.html）：core/geometry 包根 re-export 四符号 face_point_from_tri／face_center／FacePoint／Waypoint（all 由 8 增至 12）、tests/test_public_api_geometry.py 的 PUBLIC 清单锁步同步（守卫断言为 sorted all 等于 sorted PUBLIC；B 自报「无文件数断言、加 face_point.py 不破坏它」就文件数而言属实、但漏报了这条符号锁步断言，指挥方据 T06 派单卡开工前置口径 ② 处置）、view/index.html 注册 pick.js 于 loader.js 之后（注册序 bridge→loader→pick）；其四，六条收单必检行为级复跑全过（缓存命中最小点积 1.000000000000／15 面、圆柱侧面真法向 2.318 度对平面 0.000000 度、三例手算残差 3.553e-15／3.553e-15／1.776e-15、注入探针把 side_max 改后 rc=1 且侧面变 0.000 度、最小中心视口 320×500 三标记 DOM 全在框内且列表 hsb.max=0、F3 清空经 pick.enable markers 空集使 rowCount 归 0／next_id 归 1／标记 DOM 归 0）；其五，合并后回归全绿（pytest 136 passed＝T09 后 130 加 T06 6 条、lint_no_magic rc=0 扫 45 文件、附4 空、附6-② AST 越界 0 命中、常驻② core 层无 GUI 通讯 import 空、包根 re-export smoke 通）；其六，B 五项裁定全部追认（render-spy 改用 lookAt 与 add 原型包裹以截相机与场景、pick.enable schema 取 on 与 markers、face_id 与 u 与 v 闭环、中键旋转无平移的局限登记）；其七，冻结计数（标截至 sha 口径）截至 3e67ef3＝未推送 43、本地 main 46、origin/main 仍 2569104、其 reflog 0 条、git status -sb 首行为 ahead 43，本 v33 文档提交后推定 44／47（承载本句的这一笔自身不再复测、递归止于此）；其八，worktree t05／t06／t09 本轮均不删（user 指令 05 与 t09 不删、t06 同处置待 user 确认会话 B 退出后由指挥会话删）；其九，本轮无远端写、推送冻结令未解（user 此前仓库不急做完我再改仍有效），参考资料四件含甲方全称与招标编号 GXTC-A1-26170051 继续留在本地未推提交里、冻结令是唯一屏障。本句为惰性句（无粗体标记、无反引号、无竖线），承载它的这一笔提交自身不再复测，递归止于此。v33-② 提交后复测（系统钟 2026-09-16 23:13，落档 ad5d63c）：未推送 44、本地 main 47、origin/main 仍 2569104、其 reflog 0 条、git status -sb 首行为 ahead 44 —— 与 v33 其七的算术推定（44／47）逐位相符；worktree 实测四树俱在（主树 ad5d63c、t05 21d4c03、t06 bfd04e8、t09 4616574，t05／t06／t09 均未删）；本轮无远端写、推送冻结令未解。本句为惰性句（无粗体标记、无反引号、无竖线），承载它的这一笔提交自身不再复测，递归止于此。**v34 2026-09-17 03:36（系统钟）T07 收单通过、合并 `2baca80`＋集成笔 `8a0708e`、文档回填（指挥会话2；04 §5.3「不听自报」全部行为级独立复测；A2 取代 A 为 A 线现役实现会话）**：**① 交付形态**＝`task/T07` 共 12 笔本地提交、HEAD `6ec1a6d`、merge-base＝main `4b8be39`、工作树 dirty 0、分支无 upstream、`origin/main` 全程 `2569104`、未推送；主交付标题笔 `97d4d9f feat(T07): 路径生成与动画`（逐字符合卡片）；12 笔原样保留未 squash（多笔惯例）。**② 禁改件 zero-diff**＝T04 已验收的 `fk.py`／`transform.py`／`limits.py` 一字未改，`core/kinematics/` 仅 `__init__.py`（+3 re-export ik）＋新增 `ik.py`；`view/index.html` A2 未碰（注册行留给指挥方，合规）。**③ G18 范围正确**＝`config/machine.yaml` 只动 `links:` 节（13 项补 `axis`／`motion`，占位口径、踏勘第7项回执未到）、`opcua` 命中 0；`core/config/{schema,validate,loader}.py` links-scoped；8 fixtures 各 `bad_*`「共 N 项问题」计数不变。**④ 行为级复测（未采信任何自报）**＝pytest **202 passed**、`lint_no_magic` rc=0（50 文件）、`config_check` rc=0（22 轴 pending WARN＝占位、与回执未到一致）；粒度全 ≤300；`ik` 无硬编码轴 id、无数值迭代（`ik.py:118` 的 `while`＝父链有界遍历非解算环、`_POSE_TOL=1e-9`＝比对容差非收敛）；**指挥方独立 ik 往返**（自造合成链、⛔ 非 A2 用例）手算 `{SX:250,SZ:120,WR:-60}` 精确命中、`fk∘ik` 偏差 `0.0`、不可达 `x=900`→`IkError`、现场链零位往返偏差 `0.0`；G18 注入 5 探针全部带 detail 拒绝（缺 axis／缺 motion／axis 不在 axes 表／motion 非法／axis-motion 不同生）。**⑤ 报备项裁定**＝`core/kinematics/ik.py`／`app/steps/step3_path.py`／`core/path.py`／`view/js/path.js` 均预案内（卡＋04 矩阵预授权）；`app/pathctl.py`（246，预案外增件）＋`tests/test_links_binding.py`（225）**追认**——理由＝`shell.py` 交付时 297 行逼近 300 上限、控制器内联必破限，G18 links 绑定需专测；**反扩边界＝只覆盖本轮两件，不构成「app/ 可自由拆件」先例**（与 §7.2-G8 核记⑤ `brep_cache.py`、G17 三件同一处置法）；A2 flagged 遗留⑦（`__init__.py` re-export ik 是否越界）→ **裁定合规**：T07 卡「开工前置」②＋04 §4 矩阵 kinematics 行**明文授权** T07 自行改 `__init__.py`（仅在 import 行与 `__all__` 追加）＋同步两守卫，与 geometry 行「re-export 由指挥方合并时统一加」分属两包、口径不同，A2 处置正确（`SUBMODULES` 4→5 加 `ik.py`、PUBLIC 未动、断言只增不删）。**⑥ 合并**＝`git merge --no-ff task/T07`→merge commit `2baca80`（ort、零冲突，45 文件 +3851−51）；**集成笔 `8a0708e`**（指挥方专属、与交付笔不 squash）＝`view/index.html` 注册 `js/path.js`（顺序 bridge→loader→pick→path，因 path.js 再包裹 `__mecharm_dispatch`、截走 `path.show`／`pose.update`，与 T06 集成笔 `3e67ef3` 注册 pick.js 同一手法）。**⑦ 合并后回归（main）**＝pytest **201 passed＋1 flake**（`tests/test_opcua.py::test_readback_frequency_holds_nominal_cadence`＝T09 墙钟节律用例，全量并发下时序漂移；隔离重跑 ×2 均 1 passed，且 T07 全程未碰 `comm/` ⇒ 非本单回归；⚠️ 记此为已知 flake、关联 §7.2-G20-② `publish_interval_ms` 无 CI 守卫）；`lint_no_magic` rc=0（50 文件）；粒度全 ≤300。**⑧ 冻结计数（标「截至 sha」口径）**：**截至 `8a0708e`＝未推送 59／本地 main 62**（48＋14：12 交付＋1 merge＋1 集成，逐位实算相符）、`origin/main` 仍 `2569104`（3 提交）、远端 reflog 0 条 ⇒ **本轮零远端写**；⛔ 推送冻结令未解。**⑨ worktree**：t07 收单后按 §5.1-③ 应删，但依 @user「05／t09 worktree 不删」同口径**本轮暂不删**（A2 可能仍驻留该目录；T08 将另建 t08）。**⑩ 待回填连带**（本笔同提）：03 §5 新登记 `perf.fps`（视口→壳，A2 新定通道、T09/T10 可复用）＋§3 `ik`／`gen_path` 签名、04 §4 矩阵 T07 行追认 pathctl.py/step3_path.py/test_links_binding.py、`Tasks\T07` 卡末「收单记录」节。本句为惰性句（承载它的这一笔提交自身不再复测，递归止于此）。
- 更新规则：每单状态变化即改本表；验收记录只追加不删除
- 范围口径（D-2）：本包只管软件功能实现；投标取证材料另行安排，不在本表

## 状态表

| 单号 | 任务 | 状态 | 验收轮次 | 备注 |
|---|---|---|---|---|
| T01 | 环境与仓库启动 | **已过** | 1 | `[W0-A]` OCC 渠道已核实：conda-forge pythonocc-core 7.9.3；PyPI 装不到；**开工判据＝只看"文档路径是否脏"**（`git status --porcelain -- 前端任务包 参考资料` 已跟踪条目），**不比对 commit 号**（2026-09-15 第六轮审计）；`.gitignore` 已含 `.workbuddy/`／`cache/`／`data/`／`*.brep`。<br>**2026-09-16 交付实况（要点；细节见本表 v19 与 `Tasks\T01`）**：⚠️ 状态改"**待验**"系**交付事实登记、非验收结论**——"已过／打回"仍归 @user，`## 验收记录` 一节仍为"（暂无）"。① **实为 2 个 commit**（环境基线 ＋ 遗留问题处置：ICU 入口引导抽出共用＋vendor 换行保真），**已追认为 04 §5.1 硬规矩 1「一次 commit」的 T01 例外，不改写历史去 squash**；核查看 `git log --stat -2`。② 锁文件实测由 **`pip list --format=freeze`** 生成（**31 行、含 `pip==26.2.1`**；`pip freeze` 会漏装包工具链），卡内字面已按实改。③ **STEP 读取器判据更正为 `from OCC.Core.STEPControl import STEPControl_Reader`**（原写 `from OCP.STEP import …` 系误：实测 `import OCP` → `ModuleNotFoundError`；正确串在 `mecharm` 环境实测通过）。④ ⚠️ **验收防误判**：`smoke.py` 的 OCC 行打印 **`7.9.0`**＝`OCC/__init__.py` 硬编码 `VERSION` 常量，**实装 conda 包为 `pythonocc-core 7.9.3`＋`occt 7.9.3`**（`environment.yml` 实测），两者不同源，**不得据此打回"装错版本"**。⑤ 新增交付件 **`.gitattributes`**（`view/vendor/** -text`，vendor 换行保真）与 **`app/bootstrap.py`**（Windows ICU 入口引导）**已入 04 §4 矩阵**（前者此后归指挥方、后者唯一写者 T01），并补进 T01 完成标准的路径清单。⑥ `view/vendor/three.module.js` 实测 **1,272,972 B／LF-only／SHA256 前缀 `76dea815`**；`python tools/smoke.py` 在 `mecharm` 环境**退出码 0**（打印 `ICU 预载 C:\WINDOWS\System32\icuuc.dll`）。⑦ ⛔ **未发生任何远端写操作**（`git rev-list --count origin/main..main` = **6**，**2026-09-16 02:18 复测**；原写"3"系**沿用第七轮 00:01 旧读数未复测**，已更正，明细见本表 v19 ⑦；推送冻结令未解）。⑧ **2026-09-16 11:43 收单复核（第 1 轮）＝已过**——**授权来源**：@user 本轮明示「3. 授权」＝T01 验收结论归指挥会话判，与 T02／T03／T05 同口径（04 §5.3）；本行 ① 处原写「已过／打回 仍归 @user」的**口径不一致就此收掉**（原文照录不改，以本段为准）。**九条完成标准逐项实测**（未采信任何自报）：判据 1 `tools/smoke.py` 在 `mecharm` 环境 **rc=0**，原始打印 `python 3.11.16`／`ICU 预载 C:\WINDOWS\System32\icuuc.dll`／`OCC 7.9.0`／`PySide6 6.11.2`／`asyncua 2.0.1`／`numpy 2.4.6`／`yaml 6.0.3`／`pytest 9.1.1`／`结论：全部通过`；判据 2 同一输出内含 `from OCC.Core.STEPControl import STEPControl_Reader OK -> <class 'OCC.Core.STEPControl.STEPControl_Reader'>`；**OCC 打印 `7.9.0` 未作打回依据**（本行 ④ 已立此防误判条款，实装以 `environment.yml` 的 `pythonocc-core=7.9.3=novtk_h6eb2bb1_103`＋`occt=7.9.3=novtk_h1b358ef_103` 为准）；判据 3 两份环境文件均已入库（`environment.yml` **98 行**／`requirements.lock.txt` **31 行**，均在 `80bfc7d`）；判据 4 `git config --list --local` 实测含 `user.name=lhy`／`user.email=vvvlhyvvv@gmail.com`；判据 5 按卡内**原样路径清单**跑 `git status --porcelain -- app core comm view config tests tools evidence environment.yml requirements.lock.txt .gitattributes .gitignore` → **输出为空**；判据 6 两个提交合计 **15 个路径**，`git -c core.quotepath=false diff --name-only 80bfc7d~1 77b8dd3 \| grep -E '^(前端任务包/\|CR-\|参考资料/)'` → **0 命中**；判据 7 属过程项（开工前置判定），以判据 6 的**零文档触碰**为间接证据，无法事后直接复现，**照实记为过程项**；判据 8 `git ls-files \| grep -E '(^\|/)(cache\|data)/\|\.brep$'` → **0 命中**；判据 9 `origin/main` reflog **0 条**、`git reflog show --all` 内 push 痕迹 **0**、未推 **22**（截至 `9989c3a`）→ **零远端写**。**另附 vendor 与 three.js 版本实测**：`view/vendor/three.module.js` = **1,272,972 B／CR 字节 0／SHA256 前缀 `76dea815`**（`tr -cd '\r' \| wc -c` 量法，不用 grep 计数）、`REVISION = '160'` → 满足卡内步骤 3「r160+ 的本地文件、禁 CDN 运行期引用」。**附带跑的两道全树检查**：附4（`git ls-files -co` 排除 vendor／fixtures 后 >300 行文件）**0 命中**、附6-② AST 越界 import（登记源＝`environment.yml`＋`requirements.lock.txt` 两份，即 v27 裁 A 修后版）**0 命中**；**附5 对本单不适用**——T01 是 §5.1-1「每单一分支＋worktree」记在案的唯一例外，在 `main` 上作业、无 `task/T01` 分支可比 `main...task/TXX`。**⚠️ 两条读法说明（都不是失分项，写下来防后人误判）**：**(a)** 判据 8 的前半句「`evidence/` 内除占位 README 外无任何取证物」**按 T01 交付时点判**——现该目录已有 `evidence/T02/shell_1366x768.png` 与 `shell_1920x1080.png`，属 **T02 交付件**（已在 T02 收单轮核过「无甲方数据」），**不得回头算 T01 的账**（同「时点值必须现测」一脉：判据也要按被 judging 的那一单的时点读）；**(b)** `git config --list --local` 实测另有 `http.proxy`／`https.proxy=http://127.0.0.1:7897` 两行，**非 T01 步骤 0 所设**（该步只设 `user.name`／`user.email`），属既有 repo-local 配置；该端口**已死**（04 §7.1 实测两个代理端口均不可达），客观效果是任何误触的远端操作直接失败，**是否清除待 @user 决定**（属 git config 变更，本轮未动）。**遗留挂账 0 项**，`## 验收记录` 一节已补本单首条记录 |
| T02 | 工作台 UI 外壳 | 已过 | — | `[W1-B]` 与 T03 并行（03 §7 原文允许）；**2026-09-16 02:29 worktree 已建**：`D:/ZCode项目/多功能机械臂软件-wt/t02`、分支 `task/T02`（**建好≠已派单**，"在做"由 @user 起实现会话时改；见本表 v20）；**2026-09-16 03:03 收单通过并合并（指挥会话，04 §5.3；状态 待做→在做→待验→已过 一次登记——实现会话按 §5.1-5 禁改台账，过渡态由本条补记）**：`task/T02` = `a67ab15`（13 件全新增／+930、一单一次 commit、无 upstream、未 push）以**严格快进** `634fbdb..a67ab15` 并入 main。**复核（不听自报、逐项实测）**：附5 空、附6-① 空、附6-② AST 越界 **0**、附4 粒度（最大 `shell.py` **187**≤300、AST 函数最长 **27**≤50、>120 字符行 **0**）、`environment.yml`／`requirements.lock.txt`／`view/vendor/` **0 改动**；offscreen 行为级复跑：初始 enabled `[T,F,F,F,F]`、点禁用步不发信号、首次选定解锁②-⑤**不弹**确认、切换弹确认 Yes→广播 `invalidate`＋日志"结果作废"、No→回退原模式；量测正文 **14**／主按钮 **16 粗体**高 **48**／步骤钮 **16** 高 **50**／顶栏 **56**（与 02 §4 逐条对齐）；两档截图无横向滚动、状态栏可见 `桥 echo：收到 pong {'echo': {'seq': 1}}`、截图**无甲方数据**（§5.1-6 推送边界核过）；合并后回归 pytest（**0 用例**，测试件 T03 才交）＋smoke rc=0＋常驻三项 **0 命中**。**遗留四项（均不修码、登记在案）**：**F1** `mark_completed`（回看态）在 T02 壳内**无 UI 触发点**——状态机实测正确（`mark_completed(1)`＋`set_current(2)` 后 state[1]=`done`），但运行中的外壳看不到完成态；02 §1 原文"点已完成步骤可回看"的触发者是**业务完成**、T02 阶段无业务 → **首个业务单（T05）完成一步时必须调 `mark_completed`，列为 T05 收单必检项**；会话 B 汇报自称"回看可演示"无证据，本条以实测口径更正；**F2** 选回"未选定"被 `_on_change` 静默忽略（组合框显示未选定但步骤仍解锁），G16 判据未覆盖此路径；**F3** 切换日志"已清空当前点位与路径"在 T02 阶段是**未发生的事实**（清空由 T06/T07 接线）；**F4** `viewpane._INDEX_HTML` 用 `Path(__file__).parents[1]`，PyInstaller onedir 下失效 → **T10 收单必检项**。**另记两条**：指挥会话自用的验证脚本首跑撞 WinError 127（从 `app` 包外 import PySide6 漏预载行）→ ICU 入口引导条款实测有效；worktree `t02` **暂不删**（§5.1-③ 要求收单后删；会话 B 可能仍驻留该目录，待 @user 确认其退出后由指挥会话删） |
| T03 | 参数体系 machine.yaml | **已过** | — | `[W1-A]` 界面零硬编码前提；含已批准新增 `tools/lint_no_magic.py`；**新增 `paths.cache_dir`（默认仓外 `%LOCALAPPDATA%\mecharm\cache`，仓内路径拒载），2026-09-15 第六轮审计**；**2026-09-16 02:29 worktree 已建**：`D:/ZCode项目/多功能机械臂软件-wt/t03`、分支 `task/T03`（**建好≠已派单**，"在做"由 @user 起实现会话时改；见本表 v20）。<br>**2026-09-16 06:57 收单通过并合并（指挥会话，04 §5.3；状态 待做→在做→待验→已过 一次登记——实现会话按 §5.1-5 禁改台账，过渡态由本条补记；验收结论归指挥会话，@user 2026-09-16 明示）**：**① 交付**＝`task/T03` = `5d17691`（**两提交**：`da4085a` 主体＋`5d17691` 遗留处置），**16 件全新增／+1631**、工作树干净、无 upstream、未 push。**第二提交按 §5.1 硬规矩 1 的 T01 例认定为合规**（内容＝`pack_profile↔axis_pos` 一致性校验＋X3 口径定案留痕，均系 @user 授权"按建议执行"后落地，commit 消息自述理由；**不 rebase、不 squash**，保留留痕）。**② 合并**＝`git merge --no-ff task/T03` → **merge commit `0ed1779`**：**非快进**（main 已含 T02 两提交），两侧改动文件**零交集**（main 侧 `app/ view/ evidence/ 前端任务包/`、T03 侧 `config/ core/config/ tools/ tests/`）→ **零冲突**。**③ 逐项复核（不听自报，全部实测）**：pytest **24 passed**（`python -m pytest` 与裸 `pytest`、worktree 内与合并后、两种 cwd 四种组合均绿）；`config_check` **rc=0／1／2** 三条路径均跑；**7 类**非法样例（卡要求 6 类，交付为超集）逐条拒载、消息含字段名与「共 N 项问题」计数（`bad_missing_field` 2／`bad_type` 3／`bad_travel` 1／`bad_direction` 1／`bad_modes` 2／`bad_coupling_ratio` 2／`bad_cache_dir_relative` 1）；`cache_dir` **相对路径／仓内绝对路径／仓根本身三例均拒载**＋仓外绝对路径对照通过；**22 轴**（轨迹级 17／工序级 5）**全标 `pending: true` → 告警 1 条列出全部 22 轴名、不拒绝启动**；`modes` 五模式轨迹级计数 **9/9/9/7/7 均 ≤9**；X3 `{type: ratio, master: X3, ratio: 2.0}`、sync 三轴 X1/Z1/Z2（group＋`sync_tol_mm=0.5`）；`pack_profile=v1_2_8axis` 对应 `axis_pos` **8 个**节点（少一个即拒载，V1.3 记 `None` 跳过校验）；字段与 **03 §4 逐项对齐**（七节＋limits 8 键＋opcua 7 键＋读写节点子表）→ **无自增删字段**、yaml **无中文键名**。**④ 附加项**：附4 全仓 **25** 文件最大 `core/config/validate.py` **296≤300**、AST 函数最长 `app/shell.py:__init__` **27≤50**、**>120 字符行 0**、裸 `open` **0**；附5 **空**、附6-① **空**、附6-② AST 越界第三方包 **0**（非仓内非 stdlib 仅 `pytest`／`yaml`，均在 `requirements.lock.txt` 31 条内）、`environment.yml`／`requirements.lock.txt`／`view/vendor/` **0 改动**、`view/` **0 新增**；常驻①（`lint_no_magic` **rc=0**，扫描面 **8→19** 文件，即合并后已覆盖 T02 的 `app/`／`view/`——**会话 C 点名的"合并后复跑三项"已完成**）／②**0**／③**0**、附1 **0**、附2 **0**；`smoke` **rc=0**；vendor 仍 **1,272,972 B／CRLF 0／SHA256 `76dea815`**；新增 16 件敏感串（`通裕重工`／`通裕`／`GXTC`／`A1-26170051`）**0 命中**、**无 `evidence/` 新增**。**⑤ 一处自查更正（我的脚本，非交付缺陷）**：附4 的"一行多语句"启发式曾命中 `tests/test_config.py:159`，逐字核为**误报**——分号在 NodeId 字符串字面量 `'ns=3;s="DB_PLC_to_SW"…"']` 内，该行是单条赋值；反作弊条款未违反。**⑥ v22 ⑤ 增量更正**：当时记"t03 有会话 C 进行中改动（`tests/conftest.py` 已改＋四项未跟踪）"，交付后实测 `git diff main...task/T03 -- tests/conftest.py` **为空**、该文件在两树**均 0 字节**（T01 建空、§5.4 列为指挥会话合并时统一补的冲突点，**本轮无需补**）→ 原记录照录不改，此条为增量事实。**⑦ 登记四项**：**G1** `config/machine.yaml` 注释第 5 行仍写"`core/config.py`"（包化后的旧文件名自指）——该文件是 T03 唯一写者的热点文件、**指挥方不得私改**，留待 Q 回执回填时顺手改，**不单独开工**；**G2** `core/config.py` 实测 **488 行** > §4.5-① 上限 → 按 **04 §6.3 T03 派单卡预授权**拆为 `core/config/{__init__,schema,loader,validate}.py`（**30／156／104／296** 行，`__init__` 只 re-export＋`__all__`，调用形态不变），属**事后报备**，已回填 04 §2.2 产出列／§4 矩阵／§5.5-① 期望列与 03 §2 目录树（**依据是派单卡、不是 §4.5-⑦**，⑦ 的授权范围仍只限 kinematics／geometry，其他文件不得援引）；**G3** `tests/test_public_api.py` 文件名撞车（T04 与 T05 卡各自认领同名文件，而 §4 矩阵按文件名分单）→ T03 侧已规避（re-export 断言放进 `test_config.py::test_package_root_reexports_every_public_symbol`），**改名口径已落 §4.5-⑦ 硬要求 3 与两张派单卡**：`test_public_api_kinematics.py`／`test_public_api_geometry.py`；**G4** `PACK_PROFILE_AXIS_COUNT` 的 `v1_3_24axis` 记 `None`（跳过节点数校验）——**契约 V1.3 冻结后只回填数字即生效、不改代码**（CR-2026-03 §三-5 的 `EnableMask`／`Reserved2` 悬置未解，见本表外部依赖节）。**⑧ 连带修正 03 §4 字段块**：`pending` 字段原只见于该节下文 prose（"回执前样例逐轴标 `pending: true`"）而**字段块内漏列**，与 T03 卡禁止事项"禁自增删字段"字面冲突 → 已补进块内（可省、默认 false、加载器只告警）。**⑨ worktree `t03` 暂不删**（§5.1-③ 要求收单后删；会话 C 可能仍驻留该目录，与 `t02` 同处置，待 @user 确认后由指挥会话一并删） |
| T04 | 运动学正解与坐标变换 | **已过** | 1 | `[W2-A]` 手算复核防自证；**本单不做 ik**（ik 落 T07）；**2026-09-16 07:00 worktree 已建**：`D:/ZCode项目/多功能机械臂软件-wt/t04`、分支 `task/T04`（建时基线 `0afff53`，后按 §5.1 硬规矩 7 补救口径 `merge --ff-only` 快进到 `d136403`）；**2026-09-16 07:17 状态 待做→在做**（过渡态由本条补记——实现会话按 §5.1-5 禁改台账）：实测**会话 A**（`efecf84e`，会话标题「A」）已由 @user 起单开工（其自述"开工。先把必读材料读全"），而 t04 树 **0 提交／dirty 0**＝仍在读材料阶段、**尚无代码落地**；**X3 `master` 自指口径已在其卡内**（04 §6.4 专块＋`Tasks/T04` 步骤 4＋03 §4，见本表 v23 ③）；**本批 `app/` 单写者不是本单**（§4 矩阵追加接线名单不含 T04，见 T05／T09 行与 v24 ①）<br>**2026-09-16 16:55（系统钟）收单复核＝已过（第 1 轮；指挥会话，04 §5.3「不听自报」全部实测；@user 本轮「4. 并行开」授权与 T05 整改轮并行）**：**① 交付**＝`task/T04` **3 个 commit**（`de7ecf3` 07:44 主体／`bd72921` 07:46 坐标框类改名 `Frame`→`CoordFrame`／`782b6ae` 07:51 用例④补斜框），**6 文件／+852**、t04 树 dirty **0**、无 upstream、未 push；**三笔按 §5.1 硬规矩 1 的 T01／T03 先例认定合规，不 squash、不改写历史**（后两笔是交付后自查出的缺陷修正，各自独立事实）。**② 合并**＝`git merge --no-ff task/T04` → **merge commit `ae5a89a`**（作者时间 12:02:11）：**非快进**（两侧已分叉，`--ff-only` 不适用）；实测 T04 侧文件集＝`core/kinematics/{__init__,fk,limits,transform}.py`＋`tests/test_kinematics.py`＋`tests/test_public_api_kinematics.py` **恰 6 件**，main 侧自分叉点起＝`前端任务包/` 四件，**`comm -12` 交集为空** → 零冲突。**③ 包化（§4.5-⑦ 预授权，实测触发）**：`core/kinematics.py` 实测 **428 行** > §4.5-① 上限 → 拆为 `__init__` **34**／`fk` **182**／`transform` **191**／`limits` **77**；`__init__.py` AST 核过**顶层只有 docstring／import／`__all__`**；守卫件 `tests/test_public_api_kinematics.py` **69 行**（G3 撞名口径）；两个数据类型落位符合派单口径（`Transform4x4`／`CoordFrame` 在 `transform.py`、`Violation` 在 `limits.py`，**均未写进 `__init__.py`**）。⚠️ **「428 行」是会话 A 的自报值、事后不可复现**——包化前的单文件**从未提交**，该数只存在于其 `__init__.py` docstring 与 `de7ecf3` 提交消息里；**不影响判据**（§4.5-⑦ 的触发条件是「实测超限」，而**拆出的四件逐件实测均 ≤300**，这才是可复核的那一半），但记在此处防后人把自报数当实测数引。**④ 六条 FK 手算我自己重推一遍（卡内「手算复核防自证」的正解）**：链＝`base→gantry(X1,x)→column(Z1,z)→head(RA,z,len 200)→tool(无驱动轴,x,len 100)→tube(X3,x,len 0)`，六个用例（全零位／X1 上端 500／Z1 上端 300／RA 正转 90／X3 驱动 10／三轴齐动）**逐条独立算出六根连杆原点，与 `FK_CASES` 期望值全部一致**；其中 `X3 驱动 10 → tube x=120` 坐实**自指 ratio＝驱动级倍速**（工程值 10×2＝20，⛔ 不是跨轴约束）、`X3=600 → Violation 记 1200.0` 坐实**行程检查用工程位置**。**⑤ 附7 假证据形态① 在本单被实现会话自己抓出**：`782b6ae` 提交标题即「原三框全是符号置换阵，往返误差恒 0 证明不了判据」——**我复测坐实**：4 框×3 点共 12 组往返，`IDENTITY_FRAME`／`Y_UP_TO_Z_UP`／`TILTED` 三框 **9 组全部恒 `0.000000e+00`**（元素仅 0／±1，无舍入），只有 `OBLIQUE`（绕 z 转 30°＋斜原点 `(2.5,−7.5,11.0)`）在点 `(1234.5,−678.9,4321.0)` 上给出 **`1.136868e-13` mm** ⇒ **判据 `1e-6` 被数据架空了约 7 个数量级**（worst／threshold＝`1.137e-07`），补斜框后才真受浮点检验。**这是本项目第一次由实现会话自行发现该类缺陷**（此前 T04 用例④、T05 视口两例都由复核侧抓出）→ 纪律已开始下沉到实现侧。**⑥ 两道注入探针（在仓外临时副本 `D:/ZCode项目/_probe_t04_inject`，⛔ 未碰仓内文件；该副本基线 88 passed）**：**(1)** 把 `rotation()` 的 `sin` 取负（右手定则→左手）→ **5 failed／83 passed／`rc=1`**，失败集含 `test_rotation_positive_follows_right_hand_rule`、`test_fk_revolute_orientation_matches_hand_calc`、`test_fk_link_origins_match_hand_calc[RA正转90]`／`[三轴齐动]`、`test_multiply_composes_parent_then_child`；**(2)** 把 `to_column_major()` 改成原样返回（行主序→列主序的转置被去掉）→ **1 failed／87 passed／`rc=1`**，失败项恰为 `test_to_column_major_moves_translation_to_tail`。**两次都是 `rc=1`（断言失败）而非 `rc=2`（收集错误），且失败集包含目标用例** ⇒ 避开附7 形态②（探针根本没跑起来却被读成「测试通过」）。**⑦ 签名裁决＝显式传参，程序合规**：`fk`／`build_model` 需要「哪根轴驱动哪根连杆、沿哪个方向动」，而**全仓无配置源**（`machine.yaml` 的 `links:` 只有 `link_id`／`length_mm`／`parent`，该信息只存在于注释；《踏勘确认清单》第 7 项写明它「决定运动学链的正确定义」＝待回执）→ 三条路（等回执／自行加字段／显式传参）中**会话 A 选了显式传参并停下汇报为遗留⑤**，实测签名为 `build_model(cfg, joints)`／`fk(joint_values, model)`／`check_limits(joint_values, cfg)`，`Joint` 自带 `motion` 字段、**合成链样例只活在 `tests/test_kinematics.py` 内**（其 docstring 明写「合成链与 `SAMPLE_JOINTS` 都是测试数据、不是机台事实」）⇒ **未越界改 `machine.yaml`、未私增字段，属正确处置**；缺口本身已登记 **04 §7.2-G18**（**阻塞 T07 的 `ik`**，⛔ 不阻塞本单已交付的 `fk`／`check_limits`）。**⑧ 连带登记 G19**：`machine.yaml` 的 **11 根臂专用轴**（`FB1 FB2 FC KE RA RC ZA1 ZA2 ZB ZC ZD`）**无对应 `links` 条目** ⇒ `fk` 不出位姿 ⇒ **T08 碰撞覆盖不到五条工具臂**；现由守卫用例 `tests/test_kinematics.py::test_sample_arm_axes_have_no_link` 锁定现状（⚠️ 该用例是**「现状锁」不是「正确性证明」**）；处置＝**属问甲方动作、⛔ 任何会话不得自行补链**，T08 卡须写「结论只覆盖已建模的 13 根连杆，未建模臂须在界面与报告里显式标注，⛔ 禁把『未检出碰撞』报成『通过』」。**⑨ 合并后回归（全部在 main 树现测，非沿用交付时读数）**：pytest **88 passed**（`test_config.py` 24＋`test_kinematics.py` 59＋`test_public_api_kinematics.py` 5）；**附4** >300 行文件 **0**；**附5** T04 侧 ∩ 文档区 **0 命中**；**附6-②** AST 越界 import **0 命中／`rc=0`**（脚本从 04 注 1 原文提取后实跑，**不是另写一份**）；**常驻①** `lint_no_magic.py` **`rc=0`／扫描 23 个文件**；**常驻②** core 层 GUI／通讯 import **0 命中**；**常驻③ 本单无界面交付，不适用**。**⑩ 冻结计数（标「截至 sha」口径，见 v26-⑩）**：**截至 `ae5a89a`＝未推送 27／本地 main 30**，`origin/main` 仍 **`2569104`**、`git status -sb` 首行 `## main...origin/main [ahead 27]` ⇒ **本轮零远端写**；⛔ 推送冻结令未解。 |
| T05 | 模型导入与视口显示 | **已过** | 2 | `[W2-B]` 16(1)(2) 废标条款单；按放宽项 R2 提前于 T04 开工；**2026-09-15 吸收《综合技术方案》扩口径（CR-2026-03／G12–G15）：原生转换链＋单次通过释放＋双缓存＋三阶段进度＋离线预处理管线＋P0 实测（性能数字 P0 前一律暂定）**；**缓存/资产库落盘＝`paths.cache_dir`（仓外），`.gitignore` 已加 `cache/`／`data/`／`*.brep`（第六轮审计）**。<br>**2026-09-16 开单（指挥会话）**：**① 07:12 worktree 已建**：`D:/ZCode项目/多功能机械臂软件-wt/t05`、分支 `task/T05`、基线 `d136403`＝建时 main tip（§5.1 硬规矩 7 建前检查 `git status --porcelain -- 前端任务包 参考资料` 实测为空）；树内 `Tasks/T05` 卡为最新版（陈旧串 `core/config.py` **0** 命中、含 `mark_completed(1)` 与 `paths.cache_dir`），本单交付件 `core/geometry.py`／`view/js/loader.js` **实测均不存在**（正确起点）。**② 07:17 状态 待做→在做**（过渡态由本条补记——实现会话按 §5.1-5 禁改台账）：@user 指令"合并 T03 并安排 T05 派单"（会话 B 转达），指挥会话据 **04 §3.2 放宽项 R2** 判 **T04 ∥ T05 并行**并向**会话 B** 派单（附实测补充口径八条，见本表 v24 ③）；实测 t05 树 **0 提交／dirty 0**＝派单已发、**代码尚未落地**。**③ 并行合法性**：本批＝T04∥T05∥T09，**不涉 §3.2 两对绝对禁并行**（`T04∥T07` 同写 `core/kinematics.py`、`T05∥T06` 同写 `core/geometry.py`）；三单文件集实测零交集；**本批 `app/` 单写者＝T05**（§4 矩阵行 174 的追加接线名单为 T05／T06／T07／T08／T10，T04／T09 均不在内）→ 不违"同一时刻只允许一个会话改 `app/`"；R2 回退条款已照抄进派单（**一旦发现需要 T04 的任何输出或需改 `core/kinematics.py` → 立即停下汇报**）。**④ 收单必检项预登**：**F1**（导入成功须调 `stepbar.mark_completed(1)`、回看态可演示，T02 遗留）；缓存**只落 `paths.cache_dir`（仓外）、不可用则降级为不缓存＋告警、绝不回落仓内**；`evidence/` **禁甲方真实模型截图／含甲方名称或工件尺寸画面**；弦高容差**从 `limits.tessellate_deflection_mm` 读**（实测 `0.5`）禁散写字面量；**`trimesh` 实测未装**（锁文件 31 行内无）→ 如需只提**依赖增补申请**，⛔ 禁自行 `pip install`／改环境文件、**未在环境文件中的第三方包一律不得 import**；`view/index.html` 的 `<script>` 注册行**由指挥会话合并时统一补**（§4 矩阵行 176）；若 `core/geometry.py` 实测 >300 行 → 按 **§4.5-⑦ 预授权**拆 `core/geometry/{__init__,import_model,tessellate}.py` 并交 **`tests/test_public_api_geometry.py`**（G3 撞名口径）。<br>**2026-09-16 收单复核（指挥会话，04 §5.3「不听自报」；结论＝打回局部整改、⏸ 暂不合并；详条已落 `Tasks\T05` 卡末「收单记录」节）**：**① 交付**＝`task/T05` = `6e02269`（**15 件、+1471/−18**；一单一次 commit ✓、标题逐字符合 ✓、工作树 `git status` **空**、**0** 个 `__pycache__` 入库、无 upstream、未 push）。**② 完成标准 8/8 全部行为级重跑通过**（未采信任何自报数字）：**1** 装配树**深 3**／3 零件／`is_brep=True`；**2 face_map 九点抽查**（每部件首/中/末）——用 `ShapeAnalysis_Surface.ValueOfUV` 反投到 B-Rep 面、再取 `GeomLProp_SLProps` 真法向比对：**平面面 0.000°、圆柱侧面 2.318°**（弦高偏差，合理），`face_index` 键与 `face_map` 唯一值**完全一致**（零件 1/2/3＝6/3/6 面）；**3** STL `is_brep=False`，且**整壳内即使已提交工作模式，步骤②-⑤ 仍全锁 `[False]*4`**，换实体 STEP＋模式才 `[True]*4`（双条件逻辑正确）；**4** `工件目录/主轴装配体.step` 中文名＋中文目录导入成功；**5** **离屏 Qt**（`QT_QPA_PLATFORM=offscreen`）建起 `MainWindow`，`_on_imported` 真调 `mark_completed(1)`（`_completed[0]=True`），走到②后钮① `state="done"`、点回① → `current_step=1`＋右栏 `_stack.currentIndex()=0` ⇒ **T02 遗留 F1 至此闭环**；**6** miss `load_ms=41.3`（首次冷启 230.9）→ **hit 1.7–1.9 ms**，命中仍判 `is_brep`、树深仍 3（未跳过判定）；**7** `ImportWorker` 确为 `QThread`，导入 **229 ms** 期间主线程 10 ms 定时器走了 **16 拍**（UI 活），三阶段实测 `['解析','三角化','优化']`，`cancel()` → `failed='已取消导入'`（协作式、阶段边界生效）；**8** ✓。**③ 附加项**：附4 全仓 **0** 文件 >300 行；附5 **空**；附6-① 未新增 vendor、② **修正后 0 命中**（见 v27 ①）；`cache_dir` 实测落 `C:\Users\lihaoyuan\AppData\Local\mecharm\cache`（**仓外**）、仓内 **0** 个 `.brep`、无 `cache/`／`data/`；pytest **44 passed**；`view/js/loader.js` 纯显示、无 CDN、`face_map`／`face_index` **未下发前端**（红线①守住）；`view/index.html` 只追加一行 `<script type="module" src="js/loader.js">`。**④ 视口链路实测（B 如实自报"无法提供截图"，指挥方补做）**：`python -m http.server 8811` 服 `view/` ＋另起**带 CORS 头**的临时服务喂**由交付 API 真生成的** payload（`assembly_step`→`import_model`→`tessellate`→`encode_mesh_parts`：3 部件／124 三角／154 顶点／4737 B）→ `mesh.load` 后 canvas 431×625、**非背景像素 34.33%**、bbox[18,130,430,460]；`hl.set` 三语义 **deny 73.2% 红／ok 100% 蓝／warn 99.6% 琥珀**、**未知语义回落 ok**、`ids:[]` **精确回到基线**（fg 47326→47326）；`focus:true` → 占比 17.57%→30.83% 且居中。**⑤ 打回的两项整改（阻塞合并）**：**整改-1｜`.brep` 双缓存缺失（目标③＋步骤4-①），且理由经实测证伪**——全仓 **0** 处 `.brep` 写入，只有 `manifest.json`＋`mesh.npz`；docstring 称"`BRepTools` 形状序列化不可用"，实测 `breptools.Write`／`breptools.Read`（须显式传 `BRep_Builder`）往返 20×12×8 箱体：写出 **2540 B**、文件头 `DBRep_DrawableShape / CASCADE Topology V3`、读回**体积 1920.0／误差 0.0／6 个面**（同句里 OCAF `SaveAs` **确实**不可用——本 build 的 `TDocStd_Document` 无任何 `Save*` 方法——但该半句不构成放弃 `.brep` 的理由）。**后果不止性能**：`_load_mesh` 返回的 `MeshPart.face_index={}`，而 `_attach_shapes_if_needed` 只在**显示缓存未命中**时才重解析源文件 ⇒ 实测**二次导入全命中时 `face_index=[0,0,0]`**，即卡片要求的"秒开"路径**恰好清空了 T06 取真值法向的唯一依据**，而完成标准 6 仍然通过。**整改-2｜`flyTo`／`fitAll` 不看 aspect，窄视口裁切模型**——`orbit.radius = 包围盒对角线×1.4`，只对着 45° **垂直** FOV 配距离：竖视口 431×625（aspect 0.69）→ 右边缘列 **217** 个前景像素的**平顶竖直切口**、`bbox x1=430=w−1`；同模型横视口 625×431 → 边缘 **0**、`bbox[176,90,496,317]` 完整含边距；还原后复现。**在本软件自己声明的最小中心视口下同样裁切**：`app/shell.py` `CENTER_MIN=320`＋`setMinimumSize(...,600)` → 320×500（aspect 0.64）实测 `bbox x1=319=w−1`、右边缘列 **186**。⚠️ **B 自报的 canvas 480×360 是横向，恰好看不出此缺陷**——非漏报，属视口取向盲区（修法：按 `max(垂直, 水平/aspect)` 双向取半径）。**⑥ 三处 docstring 环境事实须更正**（随整改笔一起改）：`core/geometry/__init__.py:20` 删"`BRepTools` 形状序列化…不可用"（已证伪）；"XCAF 不可用"**不成立**——XCAF 可用，实际只是 `TDocStd_Document(TCollection_ExtendedString(...))` **这一个构造重载** exit 127（改普通 `str` 即可），且遍历 API 名不同（是 `GetComponents`／`GetSubShapes`／`IsAssembly`，**没有** `GetChildren`／`GetUserName`）；**零件名读不出**这部分**成立**（`TDataStd_Name` 只有 `DownCast/Dump/GetID/Set/SetID`，无 `Get`／`Find`），故自动命名兜底可接受——**但理由要写成真的**；三处"pythonocc-core **7.9.0**"→ **7.9.3**（`environment.yml` 登记 `pythonocc-core=7.9.3`／`occt=7.9.3`，`OCC.__init__.VERSION` 硬编码报 7.9.0 是已知陷阱）。**⑦ 剥离与遗留（不阻塞合并）**：步骤 3 的"简化→LOD 分级"＋步骤 7 的离线资产库管线（LOD×3／碰撞代理／`tools/` 脚本）**从 T05 剥离**，与 **T08 碰撞代理**合并成**一次**依赖裁决，触发点＝步骤 8 的 P0 实测出真实大模型面数；**trimesh 增补申请本轮不核准**（四条理由见 **03 §8** 准入清单 trimesh 行：条件未成就／LOD×3 零新依赖即可／单批 trimesh 批不出简化能力——本机实测 `trimesh`／`scipy`／`open3d`／`fast_simplification`／`networkx`／`shapely` **六个候选全 absent**／碰撞代理消费者是 T08）；步骤 8 P0 实测保持未做、**导入性能数字继续标"暂定"**；程序性两项＝`view/css/app.css` 矩阵**补行**、`tests/cad_samples.py` **追认**（均已落 04 §4）。**⑧ 合并时序**：⛔ **本轮不合并**——`face_index` 为空是 **T06 的输入契约缺陷**，先进 main 会让 T06 在坏地基上开工；等会话 B 的整改笔落地后**两笔一并合并**（照 T01／T03 遗留处置笔先例，**不 squash、不改写历史**），**T06 派单随后**（§3.2 `T05∥T06` 绝对禁并行不受影响，本就是串行）。⛔ **推送冻结令未解，本单只 commit 不 push**。<br>**2026-09-16 20:26 第二轮收单（指挥会话；@user 批复「全按建议」＝开本轮）：结论＝已过，已合并入 main**：**⑨ 合并**＝`git merge --no-ff task/T05` → **merge commit `8440765`**（作者时间 **20:26:51**）；**两笔交付 commit 原样保留**（`6e02269` feat ＋ `21d4c03` fix，⛔ **未 squash**，依 §5.1-1 的 T01／T03／T04 先例）；实测并入 **17 文件／+1698 −18**、**零冲突**、合并后 `git status` 空。**T05 状态 打回（局部整改）→已过、验收轮次 1→2**。**⑩ 整改-1 复核（`.brep` 双缓存＋命中路径 `face_index`）＝通过，且按强于交付用例的口径复测**：新增 `core/geometry/brep_cache.py`（**124 行**）；实测写出 **3 个 `.brep`（3655／11229／3486 B）**，默认根目录在**仓外** `C:\Users\lihaoyuan\AppData\Local\mecharm\cache`（复测时隔离到临时缓存目录，仓内 0 个）；把 `import_model._parse` 打成抛异常后二次导入**仍成功且 `face_index` 非空** ⇒ 命中路径**未回读源文件**。⚠️ **交付用例只断言键集相等（`set(a.face_index)==set(b.face_index)`）＝弱判据**（键对上≠面对上），故另测 **15 个面**的几何同构：最差质心偏差 **4.718e-16 mm**、最差相对面积偏差 **9.674e-16**、最差 `abs(dot(n₁,n₂))−1`＝**0.0**。**负控**：删掉那 3 个 `.brep` → 命中路径回落重解析并触发被替换的 `_parse` ⇒ `.brep` 是**承重件**、判据非恒绿。**附7 形态② 注入**：`_ensure_face_index` 换 no-op → pytest **rc=1**（⛔ 不是 rc=2 收集错误），失败项正是「全命中路径 `face_index` 不得为空（整改-1 核心判据）」、输出 **0 处 `error`**。桥载荷键实测 `['id','n_tris','n_vertices','name','tris','vertices']` ⇒ **`face_map`／`face_index` 未下发前端**（G14 ① 合规）。⚠️ **复测方法留痕（T06／T08 同受）**：包根 re-export 了与子模块**同名的函数**，`import core.geometry.tessellate as _ts` 绑到的是**函数**而非模块、monkeypatch **静默无效**（首轮探测因此得出「`.brep` 文件 0 个」的**假阴性**）；须 `importlib.import_module(...)`。**⑪ 整改-2 复核（`flyTo`／`fitAll` 按 aspect 双向取景）＝通过**：量测口径同第一轮（前景＝与底色 `#16202a` 任一通道差 >12；同一 JS 任务内 `render()`→`drawImage`→`getImageData`）。交付件实测 **320×500**（`app/shell.py` 自己声明的 `CENTER_MIN=320`，aspect 0.64）→ x0=50／x1=294／y0=148／y1=325、**431×625** → 68／398／176／415、**625×431** → 175／498／88／318，**三档四边全不触边、触边像素 0**；**负控**（换回旧式 `radius=对角线×1.4`）→ 320×500 `x1=319=W−1`、右缘触边 **186** 像素，431×625 `x1=430`、触边 **218**，横屏 625×431 触边 **0** ⇒ 缺陷与「**横屏看不出**」的取向盲区（附7 形态④）**双双复现**。会话 B 自报值（320×500 → 46／295／141／377 等）与实测**同向不等值**（样件各自现场生成），按 §5.3 以指挥方实测为准、自报值只留痕。**⑫ 整改-3（三处 docstring 事实更正）＝全部落地且逐条复测为真**：`breptools.Write`／`breptools.Read(shape,path,BRep_Builder())` 往返**可用**、**面序与三角化均保留**（centroids 按序相等、读回仍 all-triangulated）⇒ `rebuild_face_index` 按 `face_map.min()` 起始、只对有三角化的面递增，与原三角化口径逐字一致；XCAF **可用**，实际只是 `TDocStd_Document(TCollection_ExtendedString(...))` **这一个构造重载**崩溃——**双通道复测** bash **rc=127**／Python subprocess **rc=3221226505（0xC0000409）**、改 plain `str` 重载 **rc=0**（首轮只测一个通道、误判 docstring 不可复现，属**探测方法缺陷不是文档缺陷**）；遍历 API 实名 `GetComponents`／`GetSubShapes`／`IsAssembly`、`TDataStd_Name` 无 `Get`／`Find`、`TDocStd_Document` **确无 `SaveAs`**；三处 `7.9.0` → **7.9.3**（依环境文件登记值；`OCC.__init__.VERSION` 硬编码报 7.9.0 是已知陷阱）。**⑬ 合并后回归（在 main 上重跑，⛔ 不是分支上重跑）**：pytest **109 passed**、`lint_no_magic` **rc=0（31 文件）**、附4 **0 命中**（205 函数无一 >50 行；超 300 行只有 vendored `three.module.js`，属白名单）、附5 空、附6-① 未新增 vendor／② **0 命中**、常驻②／附2 空、常驻③ 只有变量名与 CSS 色值、附1 只有一处二进制 `path.open("rb")`、仓内 **0 个 `.brep`／`__pycache__`**、无 `cache/`／`data/` 目录、`evidence/T05/` 新增件入库边界合规（两处「甲方」字样均为**合规声明**而非甲方数据）。**⑭ 程序性追认与行数核正**：`brep_cache.py` 属**预案外增件**（§4.5-⑦ 对 `core/geometry/` 只列三件），由第一轮收单裁定「整改-1」直接驱动 ⇒ **本次追认**，三条理由与**防扩大解释的边界**见 04 §7.2-G8 核记 **⑤**；第一轮登记的行数 297／205／32 已**核正为实测 `import_model` 298／`tessellate` 228／`__init__` 39＋`brep_cache` 124**，四件均 <300。**⑮ 连带口径（两对全部解禁）**：`T05 ∥ T06` 的禁并行令**解除**、改为「**可分头写**」——§4.5-⑦ 触发条件「包化**实际发生并合并后**」两半句均已成就；⛔ **解禁只消灭合并冲突、不消灭功能依赖**（合并顺序仍 T05→T06；T06 **只新增 `core/geometry/face_point.py`、禁改已验收四件**，`__init__.py` 的 re-export 由指挥方合并时统一加）。落点**实测二十一处**（04 八／03 五／99 五／05 一／Tasks 二），明细见 04 修订记录本轮行 ①—⑤。**⑯ T06 派单**：W3 解锁条件已成就，随后建 `t06` worktree 并按 04 **§6.6（本轮新增）** 派单卡投会话 B；六条收单必检已钉进 `Tasks\T06` 卡（附7 四形态各一条＋真法向≠三角形法向＋F3 真清空点位列表）。⛔ **推送冻结令未解，本轮只 commit 不 push**（`origin/main` 仍 `2569104`、远端 reflog 0 条） |
| T06 | 面拾取与点位示教 | **已过** | 1 | `[W3-B]` ~~与 T04 余量并行（放宽项 R4）~~ ⚠️ **2026-09-16 更正（原句照录留痕）**：T04 已随 `ae5a89a` 并入 main，**R4 的「T04 余量」已不存在**（详见 04 §3.2-R4 与 §3.3-W3）⇒ **本波只等 T05 合并**。⚠️ **2026-09-16 二次更正（T05 第二轮收单后）**：原写「且 `T05∥T06` 的**绝对禁并行令仍生效**（`core/geometry/` 虽已按 §4.5-⑦ 包化，但 T05 尚未合并 ⇒ 解禁条件未成就，**T04 的解禁不构成先例**）」**已失效、照录留痕**——T05 已于 **`8440765`** 合并，⑦ 触发条件两半句均成就 ⇒ **解禁为「可分头写」**（⛔ **合并顺序仍 T05→T06**；本单**只新增 `core/geometry/face_point.py` 一件，禁改 T05 已验收的 `__init__`／`import_model`／`tessellate`／`brep_cache` 四件**，包根 re-export 由指挥方合并时统一加）。**开工前置＝T05 整改两笔合并入 main（已成就）**。**收单必检（六条；详条见 `Tasks\T06` 卡文末「开工前置与口径」③，本行只列条目）**：**(1)** 缓存**全命中**路径下真法向与冷路径**逐面一致**（点积≈1）——原写「缓存命中路径下 `face_index` 非空」的**升格版**，因为 T05 整改-1 的下游消费者就是本单，只验 `face_index` 非空不够、要验**取出来的法向真是 B-Rep 真值**（附7 形态③）；**(2)** 圆柱侧面上**真法向 ≠ 三角形法向**（夹角明显非 0，T05 收单时同一情形实测 2.318°）；**(3)** 手算换算 3 组用例**含无理数／斜置数据**（误差列不得恒 0，形态①）；**(4)** 取真法向段注入 no-op 后 pytest **rc=1** 且失败项就是该判据（形态②）；**(5)** **320×500 竖幅**下标号牌与列表不越界（形态④，⛔ 不接受只有横屏）；**(6)** **F3「结果作废」真清空点位列表**（含视口标号牌，⛔ 不只是置灰）。派单卡＝04 **§6.6**（本轮新增；⛔ 未改 §6.5 编号，因其被他处交叉引用）。<br>**2026-09-16 22:55（系统钟）收单复核＝已过并合并（第 1 轮；指挥会话，04 §5.3「不听自报」全部行为级实测；时点取 merge commit `9ea633e` 作者时间，可复现；详条见 `Tasks\T06` 卡末「收单记录」节）**：**① 交付形态**＝`task/T06` 单笔 `bfd04e8 feat(T06): 点位示教`（**10 文件／+1090−3**），父＝merge-base `c8abade`；分支无 upstream、`origin/main` 全程 `2569104`、t06 树 dirty **0**；新增源文件**仅 `core/geometry/face_point.py`（169 行）一件**，T05 已验收四件（`__init__`／`import_model`／`tessellate`／`brep_cache`）＋`view/index.html`＋`app/bridge.py` **实测零触碰**（禁改清单守住）。**② 合并**＝`git merge --no-ff task/T06` → **merge commit `9ea633e`**：main 侧（自分叉点起 35 文件，含 T09 合并与文档）∩ T06 侧 10 文件 **`comm -12` 交集为空** ⇒ 预告零冲突，`--no-ff` 实测相符（ort 策略、无冲突）。**③ 集成笔 `3e67ef3`（指挥方专属合并动作，与交付笔不 squash）**：T06 卡「开工前置与口径」② 明定 re-export 与脚本注册由指挥方合并时统一加、实现会话禁改 `__init__.py`／`index.html` 与守卫断言 ⇒ 本笔加 `core/geometry/__init__.py` 的 `face_point_from_tri`／`face_center`／`FacePoint`／`Waypoint` 四符号 re-export（`__all__` 8→12）＋`tests/test_public_api_geometry.py` 的 `PUBLIC` 锁步加同四符号（`test_all_matches_public_symbols` 不破）＋`view/index.html` 注册 `js/pick.js`（顺序 bridge→loader→pick，因 pick.js 再包裹 `__mecharm_dispatch`）。**④ 六条收单必检逐条行为级重跑（未采信任何自报）**：**(1)** 缓存全命中（显示网格＋`.brep` 双命中、`_ts._parse` 打成抛错证明走 `.brep` 重建而非回退解析）真法向 vs 冷路径**逐面 15 个面最小点积 `1.000000000000`**、取点位置 `atol=1e-6` 一致；**(2)** 圆柱侧面真法向 vs 三角形法向 **max `2.318°`**（弦高偏差）、平面端面同口径对照 **max `0.000000°`** ⇒ 真法向确取自 B-Rep 面真值、非网格法向；**(3)** 手算换算 3 组（斜置件＋无理重心坐标）残差 **`3.553e-15`／`3.553e-15`／`1.776e-15`**（**非零**、含无理坐标、u/v 不可互换）⇒ 误差列未恒 0（形态①排除）；**(4)** 取真法向段注入 no-op → pytest **`rc=1`**（⛔ 非 rc=2 收集错误）且失败项正是「侧面真法向须明显偏离三角形法向」判据（`side_max→0.000°`）⇒ 形态②排除；**(5)** **320×500 竖幅**——列表侧：我的离屏探针在 **280／320／360** 三档均 `horizontalScrollBar.maximum=0`／末列右缘==viewport 宽／`overflow=False`（B 自测脚本只测 280／360、漏了判据宽 320，我已补测）；标号牌侧：浏览器实测 3 牌全落 `[0,320]×[0,500]` 内（P1 `88.9/215.6`、P2 `224.9/210.9`、P3 `133.3/214.1`、fontSize 18px）⇒ 形态④排除；**(6)** **F3 真清空**——`step2.clear()` 后表格 `rowCount=0`、`_next_id` 复位 1、视口标号牌 DOM 经 **F3 实际路径 `pick.enable {markers:[]}`** 清 **0**（cursor 仍 crosshair、步骤②保持，符合 B 判断项），`invalidate`（换臂）路径亦清 0＋cursor default ⇒ ⚠️ B 自测 §A 只验了 `invalidate` 路径，而 shell.py 的 F3 实际走 `pick.enable {markers:[]}`，**我补验了该真实路径**（不听自报）。**⑤ 附加项＋常驻**：附4 **空**（最大件 `step2_pick.py` 239／`pick.js` 268／`face_point.py` 169／`test_face_point.py` 226 均 ≤300）；附5 **空**（T06 侧零文档触碰）；附6-① 仅 `view/js/pick.js`（B 自有新源、非 vendor、已报备）；附6-② AST 越界 **0 命中**；常驻① `lint_no_magic` **rc=0**（t06 树扫 34 文件、合并后 main 扫 45 文件）；常驻② core 层 GUI／通讯 import **0**；常驻③ 命中全为代码标识符／注释／CSS 选择子（`_brep_ok`／`is_brep`／`[result="brep"]`），界面文案仍只用「实体模型／面片模型」⇒ 无术语上屏。**⑥ pytest**：t06 分支 **115 passed**（与 B 自报逐位相符）、合并后 main **136 passed**（＝T09 后 130＋T06 的 6，guard 测试锁步更新后全绿）。**⑦ B 五项自报裁定**：(1) **render-spy 修复**（初版包裹 `WebGLRenderer.prototype.render` 对已建实例不触发——实例 render 是构造函数自有闭包；改挂 `Object3D.prototype.lookAt`＋`add` 观察 loader 私有的 camera／scene，不动 loader.js）→ **追认**，浏览器实测标号牌投影与手势分离均成立；(2) **`pick.enable` 载荷 schema `{on:bool, markers:[{id,name,pos}]}`**（03 §5 此前只登记用途未定 schema、B 为首实现者）→ **追认**，未新增桥 type／未改 `bridge.py`，schema 已回填 **03 §5**；(3) **`face_id`＝三角形序号、`u=bc.y`／`v=bc.z`** 与 core 仿射口径闭环 → **确认**（B §B 已把 pick 载荷喂 core `face_point_from_tri` 闭环、我复读 `toPickPayload` 与 `face_point.py` 口径一致）；(4) **中键＝旋转（无平移）＋hover 部件级** → **追认为登记局限**（loader.js 未实现 pan、T06 禁改无法补；逐 B-Rep 面高亮需前端持 `face_map`＝违 G14，故 hover 只到部件级），⛔ 非 T06 缺陷；(5) **合并时指挥方加 re-export＋script** → **已执行**（集成笔 `3e67ef3`）；另 B 三条判断项（导入新模型清旧点位／F3 清点位＋标号牌但保持步骤②、换臂才全 invalidate／标号牌样式内联因 app.css 属 T05）**均经核为正确处置**。**⑧ 冻结计数（标「截至 sha」口径，见 v26-⑩）**：**截至 `3e67ef3`＝未推送 43／本地 main 46**，`origin/main` 仍 **`2569104`**、远端 reflog **0 条** ⇒ **本轮零远端写**；⛔ 推送冻结令未解。**⑨ worktree**：t06 收单后按 §5.1-③ 应删，但 @user 本轮明示「**05／t09 worktree 不删**」、t06 未在豁免名单亦**本轮暂不删**（待 @user 确认会话 B 退出后由指挥会话一并处置，与 t05／t09 同口径） |
| T07 | 路径生成与动画 | **已过** | 1 | `[W4-A]` **电Q-8（下发轨迹段格式 blending）**；**解析逆解 ik 落本单**（构型以**契约 Q-11** 轴清单为准）。<br>**2026-09-16 16:55 追加开工前置（T04 收单连带，详见本表 T04 行备注 ③⑦⑧ 与 04 §7.2-G18）**：**① ⛔ 只准新增 `core/kinematics/ik.py`**——T04 已收单的 `core/kinematics/__init__.py`／`fk.py`／`transform.py`／`limits.py` **一律不得改**（如需改，先停下汇报，由指挥方裁）；**② 包化后新增模块必须同步更新两个公开面守卫**：`tests/test_public_api_kinematics.py::test_init_only_reexports` 与 `test_package_layout_follows_the_preauthorized_plan`（**只新增断言、不删旧断言**，否则守卫退化成橡皮章）；**③ 矩阵口径**＝`Transform4x4` **行主序** 16 元（索引 `row*4+col`、平移在 3／7／11），上屏 three.js `Matrix4.fromArray()` 为**列主序**（平移在 12／13／14）⇒ **出口必须调 `to_column_major()`**，ik 内部禁混用两种约定；**④ G18 闸门**——~~`ik` 所需的 `links.axis`／`links.motion` 字段名与落地单**尚未裁决**（04 §7.2-G18）⇒ **G18 未裁前 `core/kinematics/ik.py` 不得开工**~~ ⚠️ **2026-09-16 22:10 已裁（原句照录留痕）**：@user 裁「**授权 T07 跨归属落地**」⇒ **闸门解除**，T07 代 T03（已收单）给 `links:` 每项补 `axis`（驱动轴 id 或 `null`）／`motion`（`x｜y｜z`）两键＋改 `core/config/{schema,validate,loader}.py`＋8 fixtures，**先扩字段再开 `ik.py`**（凭据＝04 §7.2-G18 行＋§4 矩阵两行「第二授权例外」，⛔ 不援引 G17）；硬约束＝只改 `links:` 节（⛔ 不碰 `opcua:`）／8 fixtures 补键后各 `bad_*` 计数不变／`test_config.py` 24 用例全绿不改断言／`lint_no_magic.py` 不动、`config_check.py` 只读；回执（踏勘第 7 项）未到位前取值仍是占位。落点＝`Tasks\T07` 开工前置第 4 条＋03 §4 `links` 行与注释块（V1.8）。本单其余交付件（`core/path.py`／`view/js/path.js` 等）不受阻，可先行。<br>**2026-09-17 03:36（系统钟）收单复核＝已过并合并（第 1 轮；指挥会话2，04 §5.3「不听自报」全部行为级独立复测；A2 取代 A 为 A 线现役；详条见 v34 与 `Tasks\T07` 卡末「收单记录」节）**：**① 交付形态**＝`task/T07` 12 笔本地提交、HEAD `6ec1a6d`、merge-base＝main `4b8be39`、工作树 dirty 0、无 upstream、`origin/main` 全程 `2569104`、未推送；主交付标题笔 `97d4d9f feat(T07): 路径生成与动画`（逐字符合卡片）；12 笔原样保留未 squash。**② 禁改件 zero-diff**＝T04 已验收 `fk.py`／`transform.py`／`limits.py` 一字未改，`core/kinematics/` 仅 `__init__.py`（+3 re-export ik）＋新增 `ik.py`；`view/index.html` A2 未碰（注册行留给指挥方，合规）。**③ G18 范围正确**＝`machine.yaml` 只动 `links:` 节（13 项补 `axis`／`motion`、占位口径）、`opcua` 命中 0；`core/config/{schema,validate,loader}.py` links-scoped；8 fixtures 各 `bad_*`「共 N 项问题」计数不变。**④ 行为级复测**＝pytest **202 passed**、`lint_no_magic` rc=0（50 文件）、`config_check` rc=0（22 轴 pending WARN＝占位）；粒度全 ≤300；`ik` 无硬编码轴 id、无数值迭代（`ik.py:118` `while`＝父链有界遍历、`_POSE_TOL=1e-9`＝比对容差）；**指挥方独立 ik 往返**（自造合成链）手算 `{SX:250,SZ:120,WR:-60}` 精确命中、`fk∘ik` 偏差 `0.0`、不可达→`IkError`、现场链零位往返 `0.0`；G18 注入 5 探针全带 detail 拒绝。**⑤ 报备项裁定**＝`ik.py`／`step3_path.py`／`core/path.py`／`path.js` 预案内；`app/pathctl.py`（246，预案外增件）＋`tests/test_links_binding.py`（225）**追认**（理由＝`shell.py` 297 行逼近上限、G18 links 需专测；**反扩边界＝只覆盖本轮两件、不构成「app/ 可自由拆件」先例**）；A2 flagged 遗留⑦（re-export ik 是否越界）→ **裁定合规**（卡「开工前置」②＋04 §4 矩阵 kinematics 行明文授权 T07 自改 `__init__.py`＋同步两守卫，与 geometry 行「指挥方统一加」分属两包；`SUBMODULES` 4→5、PUBLIC 未动、断言只增不删）。**⑥ 合并**＝`git merge --no-ff task/T07`→merge commit `2baca80`（ort、零冲突，45 文件 +3851−51）；**集成笔 `8a0708e`**（指挥方专属、不 squash）＝`view/index.html` 注册 `js/path.js`（顺序 bridge→loader→pick→path，path.js 再包裹 `__mecharm_dispatch`、截走 `path.show`／`pose.update`，同 T06 集成笔 `3e67ef3` 手法）。**⑦ 合并后回归（main）**＝pytest **201 passed＋1 flake**（`test_opcua.py::test_readback_frequency_holds_nominal_cadence`＝T09 墙钟节律用例、全量并发下时序漂移；隔离重跑 ×2 均 passed，T07 全程未碰 `comm/` ⇒ 非本单回归；⚠️ 记为已知 flake、关联 §7.2-G20-②）；`lint_no_magic` rc=0（50 文件）；粒度全 ≤300。**⑧ 冻结计数（截至 `8a0708e`）**＝未推送 **59**／本地 main **62**（48＋14：12 交付＋1 merge＋1 集成，逐位实算相符）、`origin/main` 仍 `2569104`、远端 reflog 0 条 ⇒ **本轮零远端写**；⛔ 推送冻结令未解。**⑨ worktree**：t07 依 @user「05／t09 不删」同口径**本轮暂不删**（T08 将另建 t08）。**⑩ 连带回填**（本笔同提）：03 §5 新登记 `perf.fps` 通道＋§3 `ik`／`gen_path` 签名、04 §4 矩阵 T07 行追认三件、`Tasks\T07` 卡末「收单记录」节 |
| T08 | 碰撞三态与禁发双阻断 | 待做 | — | `[W5-A]` 16(3)②③ 废标条款单，验收必测穿透拦截；**保守包络 2–5mm 已于 2026-09-15 落实进 T08 卡正文**（CR-2026-03／G4·G13：权威判定保持 OCC 精判，前端预检仅可选交互层） |
| T09 | OPC UA 模拟器与客户端 | **已过** | 1 | `[W2-C]` 禁连真 PLC（D-6）；按放宽项 R3 与内核链并行（规程 §4.2）。<br>**2026-09-16 07:17 开单登记（指挥会话）**：**① 状态 待做→在做**——@user 直接告**会话 C**（`1386afab`，会话标题「C」）"你是会话C"并给 `Tasks/T09` 卡，其自核开工前置后报"t09 worktree 未建、我不能自建"，@user 批复"**选A，请执行worktree创建并批复文件名**"。**② worktree 实测已在**：`D:/ZCode项目/多功能机械臂软件-wt/t09`、分支 `task/T09`、`d136403`、**0 提交／dirty 0**（vendor 与 main 逐字节一致）。⚠️ **③ 例外留痕（会话 C 主动请求）**：本 worktree **由实现会话自建**，系 **04 §5.1 硬规矩 2「worktree 由指挥会话建」的第二次例外**（第一次＝T01 在 `main` 上作业，见本表 v18 ④）；@user 明示授权，**例外只及"谁执行 `git worktree add`"这一动作**——**硬规矩 7 的建前检查仍适用**（C 自述已复跑 `git status --porcelain -- 前端任务包 参考资料`，我 07:17 复测文档区确为空）、**硬规矩 2 对实现会话"不 merge／不 rebase／不动 main"的禁令不因本例外松动**、**推送冻结令照旧**。若建后 main 又前进，补救口径仍是 worktree 内 `git merge --ff-only main`（0 提交分支＝严格快进），**该动作归指挥会话**。**④ 本批 `app/` 单写者不是本单**（§4 矩阵行 174 追加接线名单为 T05／T06／T07／T08／T10，T09 不在内）；本单与 T04／T05 文件集零交集（`comm/`＋模拟器 vs `core/kinematics.py` vs `core/geometry.py`＋`view/js/loader.js`）→ 三单同开合法（§3.2 R2／R3，两对绝对禁并行均不涉及）。⚠️ **本项原写 `core/comm/` 系我的笔误**——权威路径是**仓根顶层 `comm/`**（03 §2 目录树把 `comm/` 与 `core/` 并列、04 §4 矩阵该行写 `comm/simulator.py`／`comm/opcua_client.py`、`Tasks/T09` 目标节同、现有骨架实测 `comm/__init__.py` 在仓根），**已就地更正**（v24 ① 同处同错亦改；台账里 v18 ③ 那处 `core/comm/app` 是 AST 扫描**作用域列表**＝`core/`、`comm/`、`app/`，不是路径，照录不改）。<br>**2026-09-16 07:30 G17 阻塞裁决（指挥会话，依 @user "遇到问题按你的建议执行"）**：**⑤ 会话 C 报的阻塞成立、且是实现会话无法自救的那一类**——`config/machine.yaml` 节点表（L189–204 ≡ 03 §4 L90–91）只有 `read_nodes: {axis_pos[8], status, heartbeat}`／`write_nodes: {cmd, seg_count, seg_array}`，而契约 **§9.1 步骤 1/2/4/6/7** 要按符号名读写 `SpeedOverride`／`SeqID`（下发）／`Ack`／`AlarmWord`／`SeqID`（回显）／`CurSeg`／`Vel[0..7]`；且 `validate.py:226-245 _nodes()` **只按 `schema.py:36-37` 的 SPEC 遍历** → **yaml 里自行加键不报错、被静默丢弃**（C 在临时副本实测：键仍是那三个）。**裁决＝A＋D**（详条与理由见 **04 §7.2-G17**，本行只记落点）：03 §4 L90–91 已由我扩为 `read_nodes` 八键／`write_nodes` 五键；**授权 T09 在 `task/T09` 内代 T03 改 `core/config/{schema,validate,loader}.py` 与 `config/machine.yaml`＋`tests/fixtures/` 全部 8 个样例**（跨归属编辑，**G17 行即授权凭据**，台账记"T09 代 T03 扩节点表，经 @user 授权"），新键**必填**、`axis_vel` 节点数**同受 `PACK_PROFILE_AXIS_COUNT` 约束**、`tests/test_config.py` **24 用例须全绿且不改断言**；**否决 B**（降级推断会让卡内"客户端收到原因码"不可达）、**否决 C**（写死符号名违反"改配置不改码"）；`mode_mask`／`enable_mask`／`sync_err`／`timestamp` **本轮不加**（无消费方；`sync_err` 按契约 §6.4 bit3 电Q-9 前不启用、`EnableMask`↔`Reserved2` 是 @user 的 V1.3 悬置项）；`seg_array` **本期按单节点写打包 ByteString（10×56＝560 B）**、⛔ 禁逐变量写，假设须进代码注释＋汇报，等电Q-2／契约 Q-7 回执再定<br>**2026-09-16 21:17 收单复核＝已过并合并（指挥会话；时点取 merge commit `f8de0b8` 的作者时间，可复现）**：**⑥ 交付形态**：`task/T09` 单笔 `4616574 feat(T09): OPC UA 桥与模拟器`（**27 文件／+2640−18**），父＝merge-base `7369df9`；reflog 只有 `Created from HEAD`＋四次 `merge main: Fast-forward`（`0241348`／`5cccda6`／`af5a3cc`／`7369df9`）＋一次 `commit:` ⇒ **无 rebase／amend／reset**、分支无 upstream、`origin/main` 全程 `2569104`。**合并可行性预判**＝main 侧 31 文件 ∩ T09 侧 27 文件 **空集** ⇒ 预告零冲突，`--no-ff` 实测相符，merge commit **`f8de0b8`**。**⑦ G17 扩表已落地，四条硬约束逐条独立探针复测为真**（详条见 `Tasks\T09` 卡末「收单记录」②）：`read_nodes` 八键（`axis_pos[8]`／`axis_vel[8]`／`status`／`heartbeat`／`ack`／`alarm_word`／`seq_id`／`cur_seg`）、`write_nodes` 五键（`cmd`／`seq_id`／`seg_count`／`speed_override`／`seg_array`）、两个轴数组各 **8** 节点、顶层键集未变（`machine, axes, modes, links, limits, opcua, paths`）、`publish_interval_ms` 仍 **50**；`machine.yaml` diff **两个 hunk 全落在 `opcua:` 节内**（其余各节零改动 ⇒ 越界检查通过，G17 的"只改 `opcua:` 节"授权未被突破）。硬约束 1（新键必填）＝逐个删 7 个新键 → **全部 `ConfigError`** 且带具名细节行；硬约束 2（`axis_vel` 同受 `PACK_PROFILE_AXIS_COUNT` 约束）＝给 7 个／9 个均拒，`axis_pos` 给 7 个拒并引契约 §5.2 `Pos[]`，**两键同时给 7 → 分别报两行**（`loader.py` 的 `_PROFILE_ARRAY_NODES` 双键循环）；硬约束 3（`tests/test_config.py` 24 用例全绿且不改断言）＝该文件 **T09 侧零改动**、七个 `bad_*` 的「共 N 项问题」计数全部维持、8 个 fixture 均 **+10／−0**（只加 `opcua:` 键、零删行）；硬约束 4（`lint_no_magic.py` 规则不得动／`config_check.py` 只读）＝两文件 **零改动**，`validate.py`／`core/config/__init__.py` 亦零改动。⚠️ **T09 "未改 `validate.py`" 的理由经核为真**：`_nodes()` 按 SPEC 遍历并对缺键通用报错，故**只扩 SPEC 即令新键必填**，无需另加校验分支——不是漏改。**⑧ 完成标准六条实测**（详条见卡末「收单记录」①）：段长 `struct.calcsize(">4B8f4f1i")`＝**56 B**、整批 **560 B＝10×56**、未驱动轴位与第 3–10 段（448 B）**全 0**、逆运算解回 `(120.0, 30.0, -15.0, 0.0, 45.0, 10.0, 0.0, 0.0)`；60 s 窗口收 **1199 帧**、OLS 斜率 **20.0103 Hz**、服务端自计（只用服务端钟、不经推送）**1200 周期／60.000 s → 20.0000 Hz**、扫描落后 **0 次**；kill 模拟器后 **4.58 s** 判离线（阈值 5.0 s）、回调序列 `['stale','offline']`、降级期 `last_frame` **原样保留不外推**（W-5.7，`test_buffer_refuses_to_extrapolate_and_reset_keeps_watchdog_hungry` 承重）；F 节以 **14 组改名点表**（`DB_Auf`／`DB_Ab`＋德文键名）复跑，**代码零改动**握手到 ST_DONE；D 节注入 `--fault reject` → 客户端收到原因码 **`0x0020`＝数据越界**；取证物 `evidence/T09/selftest.txt`（**5684 字符**）扫「通裕／通裕重工／GXTC／GXTC-A1-26170051／招标／甲方／重工／tongyu」→ **CLEAN**（R-9.4 合规）。**⑨ 合并后 main 回归**：pytest **130 passed**（＝109＋21，与预判逐位一致）、`lint_no_magic` **rc=0／42 文件**（T05 后是 31）、`config_check` rc=0、**附4 空**（以卡面口径 `wc -l` 复核；最大件 `tools/comm_selftest.py` **300 行正好压线**、`test_opcua.py` 299、`validate.py` 297、`simulator.py` 277；AST 扫 36 个 py 文件、**260 个函数无一 >50 行**）、**附5 空**、附6-① 未新增 vendor／② **rc=0 空**（正控证明非恒空：36 文件／196 条 import，第三方只见 `PySide6`／`asyncua`／`pytest`／`yaml`，均在 03 §8 准入清单内）、仓内 **0 个 `__pycache__`／`.pyc`／`.brep`／`cache/`**；`tools/comm_selftest.py --no-save` 在 main 上复跑 **rc=0／6 节全 PASS**（确定性量与取证物逐位同值，计时量在抖动内：服务端自计 `1200／59.985 s → 20.0050 Hz`、E 节离线 **4.50 s**）。**⑩ 附7 四形态排查完毕**：**形态①（阈值被数据架空）以负控排除**——把 `CONFIG` 换成 `publish_interval_ms: 60` 的临时 yaml、窗口缩到 20 s（`MIN_HZ`／`HZ_RESOLUTION` 不动）→ 收 **335 帧／20.187 s**、斜率 **16.6642 Hz**（端点法 16.6742／计数法 16.5948／10 s 子窗口 16.6650）、服务端自计 200 周期／12.015 s → 16.6459 Hz、**B＝FAIL、rc=1**；估计量无偏（16.667 处 −0.0025 Hz、20.0 处 −0.0002 Hz）。**形态②（注入后 rc≠1）以三处注入排除**——在 `git archive task/T09` 沙箱（基线 21 passed／rc=0）内：(a) `virtual_motion.py` 的 `if self.total <= 0.0 or elapsed >= self.total:` 删去后半 → **rc=1／3 failed**（含目标用例 `test_trapezoid_clamps_both_ends_and_never_extrapolates`，另两条是集成用例 ⇒ **覆盖更宽，非缺陷**）；(b) `clamp_speeds()` 去掉 `min(..., limits.speed_max_mm_s)` 钳位 → **rc=1／恰好 `test_clamp_takes_limits_from_config_not_literals` 失败**；(c) 向 `plc_nodes.py` 追加 `_FALLBACK_NODE = 'ns=3;s="DB_PLC_to_SW"."Pos[0]"'` → **rc=1／恰好 `test_code_contains_no_plc_symbol_literals` 失败**；还原后 21 passed／rc=0 ⇒ **三次均 rc=1（断言失败），无一次 rc=2（收集错误）**。**形态③（缓存命中掏空下游契约）无对应路径**——`comm` 侧无缓存件；**形态④（视口取向盲区）不适用**——本单无 UI 交付。**⑪ 三项自报偏离裁定＝接受（语义未松）**：(1) 回读判据由「10 s 计数」改为「60 s 窗口＋OLS 斜率＋量具容差 `HZ_RESOLUTION`」——**`MIN_HZ = 20.0` 阈值本身未下调**，只减 0.01 Hz 的量具分辨率容差，且已由 ⑩ 的负控证明判据非恒绿；(2) `OFFLINE_AFTER_S` 由 5.0 收紧到 **4.5**——**验收判据仍是 `< 5.0 s`**（`test_offline_is_judged_within_5s` 断言原样、`comm_selftest.py` 的 `OFFLINE_LIMIT_S = 5.0` 原样），只收紧实现常量，理由＝判离线条件是 `age > 阈值` 且每 `WATCHDOG_TICK_S=0.1` 轮询一次，取 5.0 必然落在 5 s 之外 ⇒ 判据不可达；(3) 文件拆分——`comm/opcua_client.py` **实测 536 行**超 §4.5-① 的 300 行上限，按派单卡预授权拆为包 `comm/opcua_client/`（六件）。**⑫ 追认两类增件，并划反扩边界**：`comm/opcua_client/errors.py`＋`handshake.py` 超出派单卡举例的四件（连接／订阅／写入／重连），但卡片要求的是**报备而非闭集**、包本身已预授权，`__init__.py` docstring 已写明两件补报理由（前者为断环、后者因握手与连接合一件时实测 340 行仍超限）；**`comm/virtual_motion.py`／`comm/plc_logic.py`／`comm/plc_nodes.py` 属预案外增件**——派单卡的代码粒度条款只预授权拆 `comm/opcua_client.py`，**未授权拆 `comm/simulator.py`**（404→322→277 行），与 T05 的 `brep_cache.py` 同类 ⇒ **本次追认**，⚠️ **但追认只覆盖本轮已交付的三件、不构成"通讯侧可自由拆件"的先例**，后续新增源文件仍须派单前登记或收单时追认。**⑬ 新立案 G20（两项，详条见 04 §7.2）**：**G20-①** `tools/config_check.py:57` 打印的节点表摘要**已过期**——G17 扩表后仍硬编码旧键名，输出「回读节点：axis_pos 8 个、status、heartbeat；下发节点：cmd、seg_count、seg_array」，**漏 5 个读键＋2 个写键**；属**显示层失真**（校验本身通过、rc=0），成因＝**G17 硬约束 4 令 T09 不得动该文件 ⇒ 扩表的后果无人被授权处置**，是"只读约束"造成的**归属真空**，⛔ 不是 T09 失职。**G20-②** `publish_interval_ms` **无 CI 守卫**——绝对「≥20 Hz」判据只活在手动工具 `tools/comm_selftest.py`，pytest 侧只有 fixture 值（全 50）与 `bad_type.yaml` 的字符串 `"50"` 类型拒收 ⇒ **把它静默改成 60 时 pytest 仍全绿**；建议归 T10（`e2e_smoke.py`）或加一条配置值守卫。**⑭ 三条遗留登记**：(1) `HZ_RESOLUTION = 0.01` 的推导注释与实际散布不符——注释称"＝两轮 60 s 实测散布（±0.002 Hz）的 5 倍"，但已公布的 60 s 斜率（19.9997／20.0003／20.0011／20.0103＋指挥方两次 19.9998）跨度 **0.0106 ≈ ±0.005** ⇒ 实为**散布的约 2 倍、不是 5 倍**；**不构成架空**（负控差 3.3 Hz），但实测最小值只高出通过线 **0.0097** ⇒ 属**红闪（red-flake）风险、不是假绿风险**；因该注释在**已合并业务码**内，按 §5.1-5 **指挥方不得自行改** ⇒ 登记遗留，建议改为 0.02 并订正推导，或维持原值并接受红闪风险。(2) **契约 §4.3 与 ByteString 相抵**——契约称"字节序由 Server 自动转换"，而 `seg_array` 按单节点打包 ByteString 后**对 Server 是不透明字节串、无法自动转换**；本单选 **S7 原生大端**，已在 `comm/opcua_client/write.py` docstring 与收单汇报中声明，**未自行改契约（合规）** ⇒ 登记为契约级遗留，待 `电Q-2`／契约 `Q-7` 回执后复定是否改结构化节点。(3) **报告一处不实**——汇报把 `PACK_PROFILE_AXIS_COUNT = {...}` 列成本轮改动，diff 显示它是**上下文行（T03 既有）**，属 §5.3「不听自报」的又一例，不构成阻塞。**⑮ 矩阵缺口补登**：`tools/README.md` **原在 04 §4 矩阵中无登记归属人**，而该文件自身的规矩要求"新增工具须登记" ⇒ T09 往它加两行的**行为正确、缺口在矩阵**；本轮已补行，口径＝**追加式归属登记表，各单只加自己那一行**。**同族第二处（本轮一并补）**＝`evidence/README.md`：矩阵原只登记 `evidence/TXX/` **子目录**、漏了该目录**自身的 README** ⇒ 亦已补行；且其末行占位句「本目录当前仅有此占位 README，无任何取证物（T01 完成标准之一）」在 T02／T05／T09 交件后已**事实过期**、而该文件无登记写者 ⇒ 由指挥方按新口径**作废该句**、改为**现存取证物登记表**（三行，各带甲方数据核查结论），实测 **26→37 行、LF-only 保持**。⚠️ 该件在仓根 `evidence/` 下、**不在 `前端任务包/` 内** ⇒ 本轮文档提交路径由五条增至**六条**。**⑯ worktree 删除（@user 批复 ③ 的执行）**：确认 `.git/worktrees/{t02,t03,t04}/` 内**无 `.lock`**（只有 COMMIT_EDITMSG／HEAD／ORIG_HEAD／commondir／gitdir／index／logs／refs）、无进程占用 `*机械臂软件-wt*` 路径后，以**非 force** 的 `git worktree remove` 删掉 **t02／t03／t04 三树**（分支 `task/T02`／`T03`／`T04` **全部保留**）；**t05／t06／t09 未动**——t06 dirty=1（会话 B 在做），t05／t09 虽已 ahead=0／dirty=0 **但不在批复范围** ⇒ **只提议、不执行**。 |
| T10 | 全链路收口与打包 | 待做 | — | `[W6-D]` 收口单；补契约 §9.1 步 9 审计日志<br>**2026-09-16 22:10（系统钟）G20 两项承接（@user 裁「两项都归 T10」；落点＝`Tasks\T10` 卡新增「G20 承接」块＋完成标准一项＋04 §7.2-G20 行）**：**G20-①** `tools/config_check.py` 的节点表摘要改为**按 SPEC 生成**（现状 L57-58 硬编码 G17 前的键名、漏 5 读键＋2 写键；成因＝G17 硬约束 4 令 T09 不得动该只读文件 ⇒ 扩表后果无人被授权处置，是「只读约束」造成的**归属真空**，⛔ 不是 T09 失职）——**本条裁决即 T10 改 `tools/config_check.py`（T03 归属件）的跨归属授权凭据**；**G20-②** `tools/e2e_smoke.py`（T10 步骤 7 交付件）增 `publish_interval_ms <= 50` 守卫＋负控（静默改 60 须可红，因绝对 ≥20 Hz 此前只活在手动工具、pytest 仍全绿）。⚠️ T10 仍「待做」、本轮**未起会话**，承接以**派单卡内容**形式落地（卡内已写死），待 @user 起 T10 会话时生效 |

状态口径：待做 → 在做 → 待验 → 已过 / 打回

### 并行编排口径（2026-09-15 @user 定案，详见 04_多会话并行分工指导_V1.0.md）

- 备注列 `[W波次-会话]`：W0–W6 为开工波次；**A**＝参数·运动学线（T01→T03→T04→T07→T08）｜**B**＝几何·界面线（T02→T05→T06）｜**C**＝通讯线（T09）｜**D**＝收口（T10）。同一波次内可同时开单，**实现会话上限 3**。
- 隔离机制：**每单一个 git worktree ＋ 独占分支 `task/TXX`**；main 只由指挥会话合并；ZCode 不得改本表与任务包文档。
- **仓路径口径（2026-09-15 23:04 @user 再定 — 最新）**：定 **`D:\ZCode项目\多功能机械臂软件`**（**并入项目目录**，不再另建"机械臂工作台"目录）。实测该目录**已是 git 仓**：`main`、3 次提交、**`origin = .../mecharm-workbench-docs.git`**、跟踪 22 个文档 → **代码并入既有仓，T01 不再 `git init`**；新增同仓纪律（功能分支禁改 `前端任务包/**` 与 `CR-*.md`；`.workbuddy/` 不入库）。下列 20:25／20:45 条文为历史留痕，其中"`D:\ZCode项目\机械臂工作台`"路径**已被本条取代**（见 CR-2026-01 V1.1）。
- **仓路径口径（2026-09-15 20:25 @user 重拍，历史留痕）**：01/D-7 原写 `E:\ZCode项目\机械臂工作台` → 曾定 `D:\ZCode项目\机械臂工作台`，理由＝与文档目录同级、同在工作盘。⚠️ 一度使用的理由"本机无 E 盘"**已作废**：实测 **`E:\` 存在**（931.5GB／可用 **715.8GB**），`D:\` 可用 **120.8GB**。⚠️ **20:45 二次更正**：曾把该误判归因于"Bash 缺 coreutils"，**该归因同样作废**——双方各自实测：@user 终端 `/usr/bin` 下 `ls/grep/awk/wc/sort/find` 全正常、`git` 在 `/mingw64/bin`；我方某会话 PATH 不含 `/usr/bin` 故报 `command not found`，但 `/usr/bin/{ls,grep,awk}`、`/mingw64/bin/git` **磁盘上均在** → **本质是会话相关 PATH 差异**。**"误判无 E 盘"的机制至今未确认，本表不写归因**，只保留纪律"探测失败 ≠ 事实不存在、须换独立通道复核"（见 04 文档 §5.5 前置注／§7.1-8）。须补书面变更单（建议编号 `CR-2026-01`）并在单中**一并记录此更正**。**Miniforge 定 `D:\Miniforge3`**（环境实测：**无 conda/Miniforge3**；系统 Python 3.12.7；uv 托管的 3.11 不可用 → 当前无任何可用 3.11）。
  - ⚠️ **Bash 命令可用性随会话变化（全线自测前先自检）**：跑 `grep/awk/wc` 之前先 `command -v grep awk wc`；无解析再补 `export PATH="/c/Program Files/Git/usr/bin:$PATH"`（**会话内防御，非本机固定前提**），或改用 PowerShell 版。**禁用裸 `sort`／`find` 取证**——本会话实测它们**静默解析到 `C:\Windows\system32\sort.exe`／`find.exe`**，**语义不同却不报错**，属"命令成功但落在意料之外的同名程序上"，结论不可用。
  - ⚠️ **D 盘可用仅 121GB**：Miniforge＋pkgs 缓存＋`mecharm` 环境＋打包产物预计 5–10GB，须监控；低于 20GB 时停下汇报（E 盘 715.8GB 为备选落点）。
- **Q 项引用写法（2026-09-15 21:10 统一，见 04 文档 §5.7-④）**：前缀式 `电Q-x` 与体系名式 `沟通单 Q-x` **等价、择一**（同一处不得并列两种）；**随《沟通单 V1.1》递出生效，全仓统一 `电Q-x`**（递出前引该单项目仍写 `沟通单 Q-x`）；`契约 Q-x` 不加前缀。故本表 T07 行等处的 `沟通单 Q-8` 已统一为 `电Q-8`。**适用范围＝执行层**：`Tasks\T01–T10` 任务单同等适用，**实测 5 处裸编号已于 2026-09-15 全部改为带体系名（复验 0 残留）**，T01 单 0 命中；自检脚本见 04 文档 §5.7-④-5。
- **不可并行的一对（⚠️ 2026-09-16 **两轮收单后已由两对缩为零对**；原小标题「不可并行的一对」与原注「T04 收单后已由两对缩为一对」**照录留痕、只作历史索引**）**：~~`T04 ∥ T07`（同写 `core/kinematics.py`）~~ **已解禁为「可分头写」**——`core/kinematics/` 按 04 §4.5-⑦ 预授权预案包化并已随 merge commit **`ae5a89a`** 并入 main，T07 的 `ik` 落**新增件 `core/kinematics/ik.py`**、与 T04 已验收四件零文件交集（⛔ **解禁只消灭合并冲突、不消灭功能依赖，合并顺序仍是 T04→T07**；且 T07 **只新增 `ik.py`、禁改已验收四件**，`__init__.py` 的 re-export 追加与 `tests/test_public_api_kinematics.py` 两条守卫须同步改）。~~**`T05 ∥ T06` 的禁并行令继续生效**——`core/geometry/` 虽已包化，但 **T05 尚未合并**，⑦ 的触发条件原文是「包化**实际发生并合并后**」，只满足前半 ⇒ **T04 的解禁不构成先例**~~（⚠️ **2026-09-16 T05 第二轮收单后失效，照录留痕**）；**两对至此全部解禁**——`core/geometry/` 已随 merge commit **`8440765`** 并入 main，⑦ 触发条件**两半句均成就** ⇒ `T05 ∥ T06` 亦改为「**可分头写**」（⛔ **解禁只消灭合并冲突、不消灭功能依赖**：合并顺序仍 T05→T06；T06 **只新增 `core/geometry/face_point.py`、禁改已验收四件**，`__init__.py` 的 re-export 由指挥方合并时统一加；`brep_cache.py` 属**预案外增件**、已追认，边界见 04 §7.2-G8 核记 ⑤）；其余并行均以 04 文档 §4 文件归属矩阵为准（详条见 04 §3.2 末注与 §7.2-G8 执行进度核记）。
- 放宽项：R2（T05 提前）、R3（T09 提前）、R4（T06 提前）均已批准，各附风险与回退条款，见 04 文档 §3.2。
- **网络与代理（2026-09-15 实测）**：系统代理**已启用** `127.0.0.1:7897`（端口在线）；本机**另有会话级环境变量** `HTTP_PROXY`／`HTTPS_PROXY=http://127.0.0.1:24633`（**另一个端口，亦在线，`NO_PROXY` 未设置**）→ **环境随会话变化**，与上文 Bash 条同构。实测**直连／7897／24633 × pypi.org／conda.anaconda.org／TUNA 全部 HTTP 200**（直连亦通）。⚠️ **下载失败先测这两个端口在不在线，不要先归因渠道**（conda-forge `pythonocc-core` 7.9.3 的成立性已单独实测）。⚠️ **"开发机能下载"≠"离线能跑"**：D-8 离线要求（`view/vendor` 本地 three.js、禁运行期 CDN）**必须断网／禁代理验证**，属 **T10 验收项**。
- **CR-2026-01 时序（@user 2026-09-15 指示）**：仓路径变更单**须在 T01 合并前出**，否则仓的 origin 路径会先于书面依据存在。已出草案：`CR-2026-01_变更单_代码仓路径与并行编排口径_V1.0.md`。
- **代码粒度硬约束（2026-09-15 @user 指令）**：**单个源文件 ≤300 物理行、单函数 ≤50 行**；超限按职责拆分（不是压行），拆分新增的源文件须在派单卡内先行报备。细则、白名单、反作弊条款、自查命令见 **04 文档 §4.5**。本节第 3 条"常驻复查项"**原文仍为三项**；粒度检查作为附加项（04 文档 §5.5-附4），在收单时一并复核。
- **热点文件包化＝条件式预授权（2026-09-15 @user 批复 G8）**：`core/kinematics.py` 与 `core/geometry.py` **若实测 >300 行**，对应单可按 04 文档 **§4.5-⑦ 预案直接包化**（包＋`__init__` 只做 re-export＋交付 `tests/test_public_api.py`），**不必再请示**；包化实际合并后由指挥方出变更单（`CR-2026-02`）留痕，并把该对放开为"可分头写"。~~**⚠️ 包化前 `T04∥T07`、`T05∥T06` 仍禁并行（上文口径不变）**~~（⚠️ **2026-09-16 失效、照录留痕**：两对**均已实测超限、包化并合并**——kinematics `ae5a89a`／geometry `8440765` ⇒ **全部解禁为「可分头写」**）；**且包化只消灭合并冲突、不消灭功能依赖，合并顺序仍是 T04→T07、T05→T06**（**此半句继续有效**，且各对「**只新增一件、禁改已验收件**」的约束一并继续有效）。
  - **执行状态核记（2026-09-16，原文照录不改）**：两个热点文件**均已实测超限并包化**——`core/kinematics.py` 实测 **428 行** → `core/kinematics/{__init__,fk,transform,limits}.py` = **34／182／191／77**（T04，**已随 `ae5a89a` 合并**）；`core/geometry.py` 实测超限 → `core/geometry/{__init__,import_model,tessellate}.py` = **32／297／205**（T05，~~**尚未合并**，两轮整改中~~ ⚠️ **2026-09-16 第二轮收单后核正：已合并 `8440765`**；整改后**实测行数为 `__init__` 39／`import_model` 298／`tessellate` 228**，另增**预案外增件** `brep_cache` **124**（已追认，见 04 §7.2-G8 核记 ⑤），**四件均 <300**）。**① 解禁状态**（⚠️ 原写「**解禁状态不同**：`T04∥T07` 已解禁为「可分头写」（包化**且已合并**），`T05∥T06` **仍禁并行**（只满足「包化」、不满足「合并」）」，**2026-09-16 T05 第二轮收单后已失效、照录留痕**）：**两对均已解禁为「可分头写」**（各自**包化且已合并**：`ae5a89a`／`8440765`）——见上一条。**② 未另立 `CR-2026-02`**：回填改由指挥方**就地落 03 与 04**（登记于 03 修订记录 **V1.5** 行、04 修订记录同轮行与 §7.2-G8 执行进度核记；**T05 轮另登记于 03 修订记录 V1.6 行、04 修订记录同轮行、05 修订记录 V1.2 行**），理由＝变更单的价值在「**改前先批**」，而 §4.5-⑦ 本身已是 @user 预授权、事后另立一单只是重复留痕；⛔ **这不构成后续同类事项免出变更单的先例**。**③ 原估「改 5 处文字」实测 15 处**（03 四处＋04 十一处），明细见 04 §7.2-G8 核记 ④；**T05 第二轮实测 21 处**（04 八／03 五／99 五／05 一／Tasks 二），明细见 04 修订记录本轮行 ①—⑤——**两轮均远超原估，坐实「按实测处数记、不按预估」**。**④ 交付的守卫文件名按 G3 撞名口径**＝`tests/test_public_api_kinematics.py`／`tests/test_public_api_geometry.py`（**不是**原字面的 `test_public_api.py`）；`core/config/` 那一包**不在 ⑦ 预授权范围内**，其包化依据是 **04 §6.3 T03 派单卡**（见 T03 行备注 ⑦-G2），⛔ 勿混引。

## 验收流程（每单固定）
1. ZCode 汇报：改了什么文件 / 怎么自测（贴实测输出）/ 遗留问题
2. 我对照本单"完成标准"**逐项读代码+跑测试复核**（不听信自报）
3. **常驻复查项（每单必查）**：①grep 全仓无轴参数魔法数字 ②core 层无 GUI/通讯包 import ③界面无术语上屏（B-Rep/位姿矩阵/NodeId 等）
4. 过了改状态、commit 确认，才出下一单；不过打回并列明条目

## 验收记录

> **本节＝每轮验收的索引**（2026-09-16 T01 轮起启用）。**只记轮次／时点／结论／详条落点，不复述细节**——详条一律落在状态表对应行的备注里，避免两处复述漂移（同 Q 编号「唯一定义处」的思路）。此前 T02／T03／T05 三轮的详条已在状态表备注内，本表一并补登索引。
> ⚠️ **时点取法**：优先取该轮**落档提交的作者时间**（`git log --format=%ci`，可复现），无落档提交时取状态表已记的系统钟时间。

| 单号 | 轮次 | 时点（2026-09-16） | 结论 | 详条落点 |
|---|---|---|---|---|
| T01 | 1 | 11:43 | **已过** | 状态表 T01 行备注 **⑧**（九条完成标准逐项实测＋两条读法说明） |
| T02 | 1 | 03:03 | **已过**（`a67ab15` 严格快进并入 main；遗留 F1–F4） | 状态表 T02 行备注；**F1** 已落 `Tasks\T05` 步骤 6＋完成标准（本轮已闭环）、**F4** 已落 T10 收单必检 |
| T03 | 1 | 06:57 | **已过**（`--no-ff` 并入，merge commit `0ed1779`；登记 G1–G4） | 状态表 T03 行备注 **①—⑨** |
| T04 | 1 | 12:02（落档 `ae5a89a`） | **已过**（`--no-ff` 并入，merge commit `ae5a89a`；登记 **G18**／**G19**） | 状态表 T04 行备注 **①—⑩**；**G18** 已落 04 §7.2 与本表 T07 行、**G19** 已落 04 §7.2 与 `Tasks\T08` 卡 |
| T05 | 1 | 11:02（落档 `695100f`／`3d9ed85`／`9989c3a`） | **打回（局部整改）**，⏸ **未合并** | 状态表 T05 行备注 **①—⑧** ＋ `Tasks\T05` 卡末「收单记录」节与「整改轮新增判据」块 |
| T05 | 2 | 20:26（落档 `8440765`） | **已过**（`--no-ff` 并入，merge commit `8440765`；两笔交付 commit `6e02269`＋`21d4c03` **原样保留、未 squash**） | 状态表 T05 行备注 **⑨—⑯** ＋ `Tasks\T05` 卡末「**第二轮收单记录**」节；**连带**＝`T05∥T06` 解禁（04 §3.2 末注／§3.3 W3 行／§4 矩阵 geometry 行与 kinematics 行末句／§7.2-G8 核记 ②⑤／**新增 §6.6 T06 派单卡**）、03 §9 **V1.6**、05 **V1.2**、99 状态表 **T06 行**与「**不可并行的一对**」条与包化两条 |
| T09 | 1 | 21:17（落档 `f8de0b8`） | **已过**（`--no-ff` 并入，merge commit `f8de0b8`；交付单笔 `4616574`） | 状态表 T09 行备注 **⑥—⑯** ＋ `Tasks\T09` 卡末新增「**收单记录**」节（七小节）；**连带**＝G17 扩表落地核记（04 §7.2-G17）、**新立案 G20** 两项（04 §7.2）、04 §2.1 T09 行改包形态＋§4 矩阵 comm 行重写／`tools/comm_selftest.py`＋`comm_selftest_kit.py` 两行／**`tools/README.md` 归属补行（原矩阵缺口）**／**`evidence/README.md` 同族补行＋作废其过期占位句（该件在仓根、不在 `前端任务包/` 内 ⇒ 本轮提交六条路径）**、03 §2 comm 目录树与 §3 签名行核正＋§4 新增包形态说明条＋§9 **V1.7**、05 沉淀两行＋**V1.3** |
| T06 | 1 | 22:55（落档 `9ea633e`） | **已过**（`--no-ff` 并入，merge commit `9ea633e`；交付单笔 `bfd04e8` **原样保留、未 squash**；**合并集成笔 `3e67ef3`**＝指挥方专属动作：`core/geometry/__init__.py` 包根 re-export 四符号＋`view/index.html` 注册 `pick.js`＋`tests/test_public_api_geometry.py` PUBLIC 锁步） | 状态表 T06 行备注 **①—⑨** ＋ `Tasks\T06` 卡末新增「**收单记录**」节；**连带**＝03 §3 `face_point` 签名补登（加 `parts` 首参＋`Point`→`FacePoint`＋登记 `FacePoint`／`Waypoint` 数据类）／§5 `pick.enable` schema `{on,markers:[{id,name,pos}]}`／§9 **V1.9**、04 §4 矩阵 geometry 行（撞车点「re-export 由指挥方合并时处置」已落地）、99 **v33** |
| T07 | 1 | 03:36（落档 `2baca80`） | **已过**（`--no-ff` 并入，merge commit `2baca80`；交付 12 笔 `2640b24`…`6ec1a6d` **原样保留、未 squash**；**合并集成笔 `8a0708e`**＝指挥方专属动作：`view/index.html` 注册 `path.js`，顺序 bridge→loader→pick→path） | 状态表 T07 行备注 **①—⑩** ＋ v34 ＋ `Tasks\T07` 卡末新增「**收单记录**」节；**连带**＝G18 扩 `links.axis/motion` 落地（03 §4／04 §4 矩阵 kinematics·config 两行「第二授权例外」）、追认 `app/pathctl.py`＋`tests/test_links_binding.py` 两件预案外增件（反扩边界＝只覆盖本轮）、03 §5 新登记 `perf.fps` 通道＋§3 `ik`／`gen_path` 签名、04 §4 矩阵 T07 行、登记已知 flake `test_readback_frequency_holds_nominal_cadence`（关联 §7.2-G20-②） |

## 外部依赖与挂账（不阻塞本包，但影响后续）
- ⚠️ **远端为公开仓（2026-09-15 第六轮审计实测）**：`origin = https://github.com/vvvlhyvvv-ship-it/mecharm-workbench-docs.git`；**实测方法与原始输出**＝Python `urllib` 未认证访问 `https://api.github.com/repos/vvvlhyvvv-ship-it/mecharm-workbench-docs` → **`API 200 private= False`**（未认证可读＝公开；私有仓匿名返回 404）→ **已推 3 次提交（远端 `main` = `2569104`、22 个文件），含招标第 16 条口径、S-1 契约、沟通单、任务包全文**。⚠️ **本处原写「已推 4 次」系误记**，v18 ② 已实测更正为 3 次并改入 **04 §7.1-10**，而 99 这一处**当时漏改**、至此（2026-09-16 11:43，v28）才补齐——同一数字在两处不一致本身即缺陷，已收口。**处置（@user 本轮明示「仓库不急，做完我再改」＝可见性处置暂缓、由 @user 在开发收口后自行执行；落笔时系统钟 2026-09-16 11:43。在此之前 ⛔ 推送冻结令继续生效，任何会话不得 `git push`／改 `remote`）**：**①首选把该仓改为 Private**（不改写历史、不影响本地；⚠️ 私仓≠涉密保证）；**②若维持公开**，则硬执行三条边界：`evidence/` 只放不含甲方数据的取证物、缓存/资产库落 `paths.cache_dir`（仓外 `%LOCALAPPDATA%\mecharm\cache`）、**严禁再推含甲方数据的任何文件**。详见 **04 §7.1-10** 与 §4 矩阵、§5.1-6
- **电Q-1~电Q-12 电气回执**（＝《沟通单》12 项；⚠️ 沟通单实为"**已定稿待递**"，9/16 踏勘当面递；原写"已发"口径不准确，2026-09-15 20:45 更正；⚠️ **2026-09-16 @user 本轮更新：递单再等几天，须电气开会后才能确定 → 原「9/16 踏勘当面递」的时点作废**，本节一切以本句为准。**连带影响**：`电Q-*` 回执（含 X3 倍速链踏勘确认清单第 4 项、22 轴行程／零点／正方向、`links` 长度）到得越晚，T04／T05／T09 在**占位值**上落的测试用例要重算的越多——现 `machine.yaml` 22 轴全 `pending: true`、行程占位 `[0,1000]` mm／`[-180,180]` deg、`links.length_mm` 全 `0.0`；回执到后**只改 `machine.yaml`**，X3 那条**连代码都不改**（换算配置驱动，见 03 §4／`Tasks\T04` 步骤 4））→ 回执冻结后改 machine.yaml opcua 节 + 契约签字，才进真机联调
  - ⚠️ **Q 项存在两套编号体系**（契约 §2.2 表 vs 沟通单 §一/§二，编号相同、含义不同；契约 L150/L342 的交叉引用已混用两套）→ **引用须可判定归属**（写法规则**唯一定义处**见 04 文档 **§5.7-④**，本台账统一用 `电Q-x`），对照表见 **§5.7**，契约缺陷登记为 04 文档 §7.2-**G9**
  - ⚠️ **回填错行风险**（04 文档 §5.7-**⑤**／§7.2-**G10**）：沟通单 L4 承诺"答复回填契约 §2.2 回执栏"，而电气按沟通单编号作答、原回执表无"问题原文"列 → 若按"对 §2.2 逐行填"作业，**`电Q-8`（blending）的答复会落进契约 §2.2 的 Q-8 行（此处为**契约体系**）＝"PLC 程序由谁编写"**。**改编号修不了这一步**，必须独立设防
  - **回填作业规程（回执回收时执行，不随单递）**：① 逐封要求电气答复**带问题原文**；② 回填时**按"问题原文比对"而非按"编号对齐"**；③ 每行备注列记"来源＝沟通单 `电Q-x`（原题摘要）"；④ **双人核对**。⏳ 配套《沟通单 **V1.1 草案**》已出（编号加前缀 `电Q-` ＋ 回执表加"问题原文"列），**须在递单前定稿（递单时点见上条 @user 2026-09-16 更新＝等电气开会，原「9/16」作废），递出后只能用上述规程兜底**。⚠️ **递单延后其实给了定稿一个窗口**：V1.1 有机会在递出前**真正定稿**（编号前缀＋"问题原文"列一起生效），而不必依赖 G10 的回填规程做事后兜底——**建议 @user 在电气开会前把 V1.1 定稿**，这样 G9／G10 两个缺陷在源头就闭合了。
- 澄清结果若 JT 翻案 → 变更单重排 T05
- 甲方真实 STEP 样本到位后 → T05 的导入用例补真实大模型回归（并入 T05 步骤 8 的 P0 实测口径）
- **CR-2026-03（三维模型导入能力方案吸收，草案，2026-09-15）**：T05/T08 卡扩口径、03 增补、04 §7.2 新增 G11–G16；**G11 已于 2026-09-15 裁决（@user 批准 ①＋② 并行）**：契约扩容＋AxisMask（随契约 V1.3 窗口）、编程范围＝轨迹级轴（上限 9、YA~YE 只读）、三条结构要求已进 03 §4 与 T03 卡；**遗留待确认**：工具臂专用轴是否全由 PLC 专用程序自控（列入 9/16 踏勘确认清单）；**G16（换臂工作模式界面入口）已于 2026-09-15 落地**（02 升 V1.1＋T02/T06/T07 接线）
