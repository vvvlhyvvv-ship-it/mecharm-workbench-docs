【机械臂工作台 · 完全重构任务包 —— 给 ZCode 的入口文件】

■ 一句话目标
从零重构"锻造产线操作员三维示教工作台"（Python + OCCT 几何内核 + PySide6 外壳 + 内嵌 Web 三维视口）：
导入 STEP 模型 → 模型上点关键点位 → 生成路径 → 碰撞校核 → 通过后下发 PLC。
旧原型一行代码不搬，只当"哪些功能已验证可行"的参考。

■ 阅读顺序（动手前必须按序读完）
1. 01_需求与决策_操作员UI_V1.0.md   —— 需求清单、已拍板决策（D-1 完全重构、D-2 不管投标素材）
2. 03_架构设计_完全重构_V1.0.md     —— 技术栈、模块划分、接口签名、参数体系、OCC 安装渠道（照此建骨架，禁自换栈）
3. 02_界面设计方案_操作员工作台_V1.0.md —— 界面布局与文案（做 UI 的单照此实现，禁自创布局）
4. 上级目录《大型工控软件开发规程》§4 依赖链 + S-1 接口契约书（下发/回读语义以契约为准）
5. Tasks\TXX_*.md —— 本次只做派单给你的那一单，做完停下汇报

■ 代码仓位置（2026-09-15 23:04 @user 定案，详见 CR-2026-01 §二 CR-1）
工作目录：D:\ZCode项目\多功能机械臂软件（**该目录已是 git 仓，T01 不再 git init**；代码与项目文档同仓，
          远端 origin = https://github.com/vvvlhyvvv-ship-it/mecharm-workbench-docs.git）
          ⚠️ 同仓纪律：功能分支（task/TXX）**禁改** `前端任务包\**` 与 `CR-*.md`；`.workbuddy/` 不入库
文档目录：D:\ZCode项目\多功能机械臂软件\前端任务包（本任务包，指挥方维护，ZCode 只读）

■ 分工铁律
- 指挥方出需求/架构/任务单/验收；ZCode 按单落码，不擅自改设计；有异议停下汇报
- 一次会话只推进一单（"并行"仅指另开会话另派单，同一仓同一时刻只进一单）；完工汇报三件事：改了什么文件 / 怎么自测（贴实测输出）/ 遗留问题
- 每单一个 git commit（格式：feat(TXX): <一句话>）
- core 层每个函数必须带 pytest 单测；界面不许硬编码任何轴参数（全走 machine.yaml）

■ 任务单索引（依赖链顺序，一次只推一单）
T01 环境与仓库启动          T06 面拾取与点位示教
T02 工作台UI外壳            T07 路径生成与动画
T03 参数体系machine.yaml    T08 碰撞三态与禁发双阻断
T04 运动学正解与坐标变换    T09 OPC UA模拟器与实时联动
T05 模型导入与视口显示      T10 下发握手+工程文件+打包
实时状态看 99_进度与验收.md，以该文件为准。
