# 架构设计：几何内核 + Web 显示层 V1.0

- 编制：工程智拓 @pm ｜ 2026-09-14 ｜ 依据：01 需求 D-3、开发规程 §4/§5（S-2 POC、S-4 顺序）、S-1 契约
- 读者：ZCode 照此建骨架；@user 只需看 §1 结论表

## 1. 为什么是这套架构（结论先行）

四项硬要求：①桌面工控机交付 ②真几何不能只渲染面片（16(2)）③≥10Hz 轴联动 ④碰撞必须几何级判定。

| 候选 | 判 | 原因 |
|---|---|---|
| 纯浏览器栈（three.js + WASM） | ✗ | 工控机离线部署弱、非桌面交付形态、WASM 几何能力浅 |
| PyQt+PyOpenGL 自绘 | ✗ | 渲染底座从零造，成本高效果差 |
| Qt3D | ✗ | 生态萎缩，显示网格管线不成熟 |
| 收费 SDK（HOOPS/Hosm） | ✗ 本期 | 许可成本未询价，违反 D-8 |
| **OCC 内核 + PySide6 外壳 + 内嵌 Web 视图（本地桥传显示网格）** | ✅ | 真 B-Rep 校验+成熟渲染底座+团队 Python 栈 |

## 2. 进程与数据流

```
┌─────────────────────────────────────────────────┐
│ PySide6 桌面外壳（工作台 UI：步骤条/树/列表/日志）  │
│   ├─ QWebEngineView ── 三维视口（three.js 渲染）   │
│   └─ QWebChannel ⇄ 本地 WS ── 双向桥（bridge.py） │
├─────────────────────────────────────────────────┤
│ core/（纯 Python，无 GUI 依赖，可单测）            │
│   config.py    参数体系（轴/行程/零点/比例/方向）   │
│   kinematics.py 串联移动副+回转副正解/解析逆解、坐标变换│
│   geometry.py  OCC 导入/装配树/B-Rep校验/tessellation│
│   collision.py 碰撞三态（OCC 精判，FCL 备选）      │
│   path.py      路径生成+行程/可达性校核            │
│   project.py   工程文件 JSON 读写（带版本号）      │
├─────────────────────────────────────────────────┤
│ comm/（独立于 core）                              │
│   opcua_client.py  asyncua 客户端，Subscription    │
│   simulator.py     按 S-1 点表自建 OPC UA Server   │
└─────────────────────────────────────────────────┘
```

- **单向数据流**：core 算 → 显示网格推给视口；视口只回传交互事件（拾取/视角）。禁止前端碰几何判定。
- **几何真身在 Python 侧**：拾取请求带 mesh_id+face_id+三角形重心坐标，core 换算回 B-Rep 面上的真实 3D 点与法向。

## 3. 模块职责与接口签名（骨架，ZCode 照签名填）

| 模块 | 关键函数 | 签名（定死） |
|---|---|---|
| geometry.py | 导入 | `import_model(path:str)->Assembly`（含 `is_brep:bool, tree:[AssemblyNode], stats:{parts,tris,load_ms}`） |
| geometry.py | 显示网格 | `tessellate(assembly, deflection:float)->list[MeshPart{id,name,vertices:nx3 float32,tris:nx3 uint32,face_map:nx1 int32}]`（face_map=三角形→B-Rep 面 ID 映射，T06 取真值法向的唯一依据） |
| kinematics.py | 正解 | `fk(joint_values:dict[str,float])->dict[str,Transform4x4]` 每部件位姿 |
| kinematics.py | 逆解 | `ik(target_pose:Transform4x4)->dict[str,float]`（**解析闭式解，禁数值迭代**；桁架移动副解位置、腕部回转副解姿态，二者解耦；多解按行程/连续性选解；契约 §5.2 下发关节空间目标即靠它） |
| kinematics.py | 校核 | `check_limits(joint_values)->list[Violation]`（行程/限位） |
| collision.py | 三态 | `check(path:list[Segment], clearance_warn_mm:float)->CollisionResult{verdict:'pass'|'warn'|'interfere', cases:[{seg_id,part_a,part_b,min_dist_mm,point}]}` |
| path.py | 生成 | `gen_path(points:list[Waypoint])->list[Segment]`（段类型 LINE/JOINT，含长度） |
| project.py | 工程 | `save/load(path)`，JSON schema 版本字段 `"$schema":"proj/v1"` |
| bridge.py | 桥消息 | JSON 协议见 §5 |
| opcua_client.py | 连接 | `connect(endpoint,cfg)->Session`；回读回调 `on_frames(list[Frame])`，Frame=时间戳+各轴工程值 |

**铁律**：core 不得 import PySide6/asyncio 之外的 GUI/通讯包；comm 不得 import 三维显示；一切跨界走 bridge 消息。

**导入与碰撞的性能化处置原则（2026-09-15 吸收《三维模型导入能力综合技术方案》及其审阅意见，随 CR-2026-03 登记）**：
- 导入全走**原生转换链**（装配树→三角化→简化→LOD→轻量化网格→缓存）；**不引入前端 WASM 直解路径**。
- 转换**单次通过**：三角化完成立即释放 XDE/B-Rep，峰值内存 = max 而非两者之和。
- **双缓存**：`.brep`（几何真身，拾取/精判按需重载、用毕可释放）＋ 轻量化网格（显示）；键＝源文件哈希，二次导入秒开。
- **碰撞两段制**：交互预检（可选，前端加速结构作用于简化代理，求快）与权威判定（core 侧 OCC 几何级精判＋保守包络 2–5mm，求准）分离；**预检结论不构成判定、不作为下发依据**。
- **两条导入路径产物形态一致**：固定设备资产走离线预处理管线（交付前一次性，随包交付）；客户模型现场直导走运行时转换链。
- 大模型相关性能数字（解析耗时/内存/面数/二次导入）在 **P0 实测前一律标"暂定"**，不写入对外承诺。
- 运行时按工位/区域按需加载，**严禁整线模型全量常驻**。

## 4. 参数体系（配置文件，先于业务逻辑——规程 §4.1）

`config/machine.yaml` 字段（T03 实现加载与校验）：

```
machine: {name, schema_ver}
axes:    [{id, type: prismatic|revolute, role: trajectory|setup,
           travel:[min,max], zero_offset_mm|deg,
           direction: 1|-1, scale: mm_per_pulse|deg_per_pulse 或 null(已是工程值),
           coupling: null | {type: sync, group, sync_tol_mm}
                      | {type: ratio, master, ratio}}]   # sync=龙门双驱同步；ratio=倍速链（X3 三级筒 1:2）
modes:   [{id, name, axes: [轴 id 子集]}]                # 工作模式→有效轴子集（快接换臂，2026-09-15 G11 定）
links:   [{id, length_mm, parent}]                    # 连杆几何参数
limits:  {speed_max_mm_s, speed_rapid_mm_s, speed_work_mm_s, accel_max_mm_s2,
          clearance_warn_mm, collision_envelope_mm, tessellate_deflection_mm,
          path_sample_step_mm}   # rapid=空程/work=作业(T07)；envelope=保守包络(T08)；deflection=弦高(T05)；采样步长(T08)
opcua:   {endpoint_url, security_policy, ns_index,
          pack_profile: v1_2_8axis|v1_3_24axis,      # 段打包布局开关（过渡期默认 v1_2_8axis，2026-09-15 审计后补）
          read_nodes: {axis_pos: [NodeId...], status: NodeId, heartbeat: NodeId},
          write_nodes: {cmd: NodeId, seg_count: NodeId, seg_array: NodeId},
          publish_interval_ms: 50}
```

- 界面任何地方**不许出现魔法数字**；加载时校验必填字段，缺项拒绝启动并列明细。
- ⚠️ `opcua:` 节点表 = 按 S-1 契约 §4/§5 自建（模拟期）；电气方 Q 回执冻结后只改此文件不改代码（D-6）。

**实机轴系口径（2026-09-15 G11 已定，随 CR-2026-03）**：
- 实机 **22 个受控运动轴**（X1／Z1／YA~YE／Y1／X2／Z2／X3／ZA1·ZA2·RA／ZB·FB1·FB2／ZC·FC·RC／ZD·KE）；契约 §5.2 的 8 轴上界（`Pos = ARRAY[0..7]`）与实机不符，**契约升版（扩容＋AxisMask）随 V1.3 窗口处理，软件侧零硬编码、届时只改 machine.yaml 不改码**。
- 轴分两类（`role` 字段）：**轨迹级联动轴**（X1/Z1/Y1/X2/Z2/X3＋所挂臂专用轴，上限 9 轴，逐点参与路径与碰撞校核）与**工序级设定轴**（YA~YE 料架对位，按批次设定，软件只读）。
- **快接换臂 → 工作模式（`modes`）决定当前有效轴子集**：下发段只含当前模式轴；`AxisMask=0` 的轴 PLC 保持当前值（契约原"未用轴位填 0"规则**在 V1.3 后**废止——扩容后填 0 即误动）。
- **过渡期口径（2026-09-15 审计后明确，防"卡内自相矛盾"）**：契约 V1.2 **现状仍是 8 轴布局**（`Pos[0..7]`、"未用轴位填 0"），而扩容与 AxisMask 属 **V1.3 目标形态**。故 **V1.3 冻结前**：段打包按 `opcua.pack_profile=v1_2_8axis`（56 B/段、填 0、无 AxisMask），22 轴全表与 AxisMask **只存在于 machine.yaml 内部**（供校验与将来的 `v1_3_24axis`）；V1.3 冻结后**改 profile 取值即切换、不改码**。**禁把 22 轴硬塞进 V1.2 布局**。
- **X3 倍速链 1:2**（二级筒动 10mm → 三级筒相对一级筒动 20mm）用 `coupling{type: ratio}` 显式表达，禁外部按两倍行程反推。
- 行程/零点/正方向以电气·机械回执为准；回执前样例逐轴标 `pending: true` 占位，加载器对占位**告警不拒绝**。
- **职责边界（2026-09-15 22:43 @user 明确）**：**主要动作由 PLC 编程**；**涉及轨迹编程的工况，由软件输出轨迹参数**（当前工作模式轨迹级轴的段序列：目标位置／速度／过渡方式）交 PLC 执行。软件**不输出整机运动程序**；PLC 负责把轨迹参数接入其主程序（启动条件、与工序动作的衔接、异常中止均由 PLC 侧程序负责）。

## 5. 桥消息协议（JSON，bridge.py ⇄ view/js，全 ASCII）

| 方向 | type | 载荷 |
|---|---|---|
| 壳→视口 | `mesh.load` | MeshPart 数组（顶点/索引 base64 Float32/Uint32） |
| 壳→视口 | `pose.update` | {joint_values 或每部件 4x4 扁平矩阵, ts} |
| 壳→视口 | `path.show` | 段数组；`pick.enable` 开关拾取模式 |
| 壳→视口 | `hl.set` | 高亮部件列表+颜色语义(ok/warn/deny) |
| 视口→壳 | `pick.face` | {mesh_id,face_id,u,v}（壳侧换真实点/法向） |
| 视口→壳 | `cam.view` | 视角变化（仅日志用，可后置） |

- 轴动高频通道：`pose.update` 直发关节值，视口端用与 core 同一份 links 参数做正解插值（线性插值到 60fps，**禁止外推预测**——规程 W-5.7）。
- **大模型传输预案**：`mesh.load` 按 MeshPart 分批推送；20MB 级模型若 JSON+base64 经 QWebChannel 吞吐不足（T05 实测耗时为准），降级为本地临时文件+路径传递或二进制通道——先实测再定，不许拍脑袋优化。

## 6. 环境搭建与 OCC 渠道（已实测，ZCode 照此装）

| 项 | 定案 |
|---|---|
| Python | 3.11（本机可用） |
| OCC | **conda-forge 渠道 `pythonocc-core`（7.9.3，索引已核实存在）**。实测 PyPI 直装 `cadquery-occt`/`pythonocc-core` 均找不到版本；本机无任何 conda 发行版——**T01 先装 Miniforge（D:\Miniforge3）** |
| 桌面 | PySide6（PyPI 可用，实测 6.11.2） |
| 显示层 | three.js 本地文件随包分发（禁 CDN 在线引用，车间可能无外网） |
| OPC UA | asyncua（PyPI 实测 2.0.1） |
| 兜底 | 若 conda 渠道也装不上 OCC → **停下汇报**，走降级分支：显示网格 STL/OFF 用 trimesh + numpy 自研凸包近似碰撞，B-Rep 校验改调 OCC 命令行工具，另立变更单 |
| 打包 | PyInstaller onedir（T10），依赖全部离线包化 |

## 7. 依赖链（不可颠倒——派单顺序即此链，与 00_README 索引一致）

```
T01 环境+仓库 → T02 UI外壳（可先行，不依赖内核） → T03 参数体系
→ T04 运动学正解+坐标变换 → T05 OCC导入+B-Rep校验+视口显示
→ T06 面拾取与点位 → T07 路径生成+动画 → T08 碰撞三态+禁发双阻断
→ T09 OPC UA模拟器+客户端 → T10 下发握手+实时联动+工程文件+打包
```
UI 外壳（T02）与内核链（T03-T09）可并行推进，汇合点=T10 全链路闭环。⚠️ "并行"仅指**另开会话另派单**；同一仓同一时刻只进一单，避免工作树打架。

## 8. 第三方组件准入清单（许可证合规，对应 M4 口径）

| 组件 | 许可证 | 用途 | 准入 |
|---|---|---|---|
| cadquery-occcore / OCCT | LGPL-2.1（OCCT Public License） | 几何内核 | ✅（动态链接，不改源码） |
| PySide6 | LGPL-3.0 | 桌面外壳 | ✅（允许用户替换 so/dll，打包走 onedir） |
| three.js | MIT | 显示渲染 | ✅ 本地分发 |
| asyncua | LGPL-3.0 | OPC UA | ✅ |
| numpy / pyyaml | BSD/MIT | 基础 | ✅ |
| FCL（若作碰撞备选） | BSD | 碰撞加速 | ⏸ 按需引入，先 OCC 精判 |
| trimesh（**pip 包名 `trimesh`**） | MIT | 网格简化／碰撞代理生成 | ⏸ **条件引入**：T05 若实测需简化，先走依赖增补（04 §4）＋组件准入登记；`environment.yml`/`requirements.lock.txt` 未增补前**禁 import** |
| Draco／meshopt（**算法族，不是包名**；pip 候选 `DracoPy`／`meshoptimizer`，JS 侧候选 `draco3d`／`meshoptimizer`） | Apache-2.0／MIT | 轻量化网格压缩 | ⏸ **暂不引入**：**包名与分发形态待核**（若走 wasm 解码器需 vendored 到 `view/vendor/`，且须确认不触 G14——见下行），T05 视实测吞吐再议 |
| three-mesh-bvh（**npm 包 `three-mesh-bvh`**） | MIT | 前端碰撞**预检层**（可选，非判定） | ⏸ 仅当 T08 后置预检层启用时引入；**vendored 到 `view/vendor/`**（04 §4 归属：由首个引入它的单报备后落盘） |
| occt-import-js（前端 CAD 解析） | — | — | ❌ 不引入（G14 口径：**禁前端 WASM 做 CAD 解析（STEP／IGES／B-Rep）**；⚠️ **网格编解码器（如 Draco 解码）不属此列**，勿扩大解释） |
| 任何收费 SDK | — | — | ❌ 询价后由 @user 拍板 |

## 9. 修订记录

| 版本 | 日期 | 修订内容 |
|---|---|---|
| V1.0 | 2026-09-14 | 首次编制 |
| V1.1 | 2026-09-14 | 审核修订（@user 拍板解法 A）：①kinematics 增解析逆解 ik 签名（契约 §5.2 关节空间下发的配套能力，禁迭代）；②tessellate 增 face_map（三角形→B-Rep 面映射，补 T06 前置缺口）；③limits 补 speed_rapid/speed_work/path_sample_step_mm 三字段；④§5 增大模型传输预案；⑤§7 明确"并行=另开会话另派单" |
| V1.2 | 2026-09-15 | 吸收《三维模型导入能力综合技术方案》（张晓东）及其审阅意见（随 CR-2026-03）：①§3 增"导入与碰撞的性能化处置原则"（原生转换链/单次通过释放/双缓存/碰撞两段制/两条导入路径/性能数字暂定口径）；②§8 组件准入增 trimesh、Draco/meshopt、three-mesh-bvh（预检层条件引入）、登记 occt-import-js 不引入 |
| V1.3 | 2026-09-15 | G11 裁决落地（@user 批准 ①＋② 并行，随 CR-2026-03）：①§4 axes 增 `role`（trajectory/setup）与 `coupling` 两型（sync/ratio），新增 `modes`（工作模式→轴子集）；②limits 增 `collision_envelope_mm`、`tessellate_deflection_mm`；③§4 增"实机轴系口径"块（22 轴、两类角色、轨迹级上限 9、契约扩容＋AxisMask 随契约 V1.3、"未用轴位填 0"规则废止、X3 倍速链 1:2、pending 占位告警）；④更正 §4 标题任务号笔误（T02→T03） |
