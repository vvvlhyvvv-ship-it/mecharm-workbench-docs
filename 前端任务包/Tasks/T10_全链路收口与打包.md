# T10 下发握手、实时联动、工程文件与打包

## 目标
把全链路缝死：选面→点位→路径→校核→下发→握手→模型随回读实时动（≥10Hz 角标落地）；仿真/联动模式互斥；工程文件保存/加载；PyInstaller 可交付包。**本单收口，做完=软件功能完整。**

## 前置阅读
02 设计方案 §2 步骤⑤、§3 ｜ 03 架构 §2 §5 ｜ S-1 契约第 9 章握手时序 ｜ T08/T09 已过验收

> ⚠️ **入口引导（T01 已交付 `app/bootstrap.py`，本单不必自己写；但打包入口必须显式调）**：`import app`／`app.*` 已自动预载 Windows 系统 ICU，走 `app` 包的入口**无需任何 ICU shim**；**但从 `app` 包外直接 import PySide6 的入口必须显式** `from app.bootstrap import preload_windows_icu`（放在 PySide6 之前）——**本单的典型情形就是 PyInstaller 入口脚本／`.spec` 里自建的 entry**（不 `import app` 起手时）。
> **成因（实测）**：conda 的 `icu` 包在 `%PREFIX%\Library\bin` 也放了一份 `icuuc.dll`，conda 的 python 启动时自动把该目录加入 DLL 搜索路径 → `Qt6Core.dll`（**静态**导入表含 `icuuc.dll`）先命中它，符号集不符报 **WinError 127**（`ERROR_PROC_NOT_FOUND`），表现为 `from PySide6 import QtCore` 直接失败；`icu` 是 `libxml2`（OCCT 的 XML 支持链）硬依赖，**不能移除**。
> ⛔ **禁复制粘贴自写 shim**（唯一写者＝T01，见 04 §4 矩阵该行）；`dist` 包起不来且报 WinError 127 时，**先查打包入口有没有调 `preload_windows_icu`**，不要改 `app/bootstrap.py`。

> ⚠️ **G20 承接（2026-09-16 22:10 @user 裁「两项都归 T10」；依据 04 §7.2-G20）**：
> **G20-① `tools/config_check.py` 节点表摘要已过期**——`:54,57` 硬编码 G17 前的键名，实测漏 `axis_vel`／`ack`／`alarm_word`／`seq_id`／`cur_seg` 五个读键与 `seq_id`／`speed_override` 两个写键（成因＝G17 硬约束④ 令 T09 不得动该文件、T03 已收单 ⇒ 归属真空，⛔ 不是 T09 失职）。**本单把它改为按 SPEC 遍历生成**（与 `core/config/validate.py` 的 `_nodes()`／`schema.py` 的 `READ_NODE_SPEC`／`WRITE_NODE_SPEC` 同源），从根上消灭「硬编码键名会过期」这一族缺陷；改完须复跑 `python tools/config_check.py config/machine.yaml` rc=0 且摘要列出全部读／写键。⚠️ **`config_check.py` 原唯一写者是 T03、已收单**——本条即 T10 改它的授权凭据（@user 2026-09-16 授权），台账记「T10 代 T03 改 config_check 摘要为 SPEC 驱动」。⛔ **只改摘要打印逻辑，不动校验判定**。
> **G20-② `publish_interval_ms` 无 CI 守卫**——绝对「≥20 Hz」判据只活在手动工具 `tools/comm_selftest.py`（`MIN_HZ = 20.0`），pytest 侧只断言相对节律 ⇒ 把 `publish_interval_ms` 静默改成 60 时 pytest 仍全绿（指挥方负控实测：50→60 时手动工具斜率 16.6642 Hz／B＝FAIL／rc=1）。**本单在 `tools/e2e_smoke.py`（步骤 7）承接一条配置值守卫**：断言 `publish_interval_ms <= 50`（等价 `1000 / publish_interval_ms >= 20`），使「≥20 Hz」在 CI 内可红；须附负控证据（临时把值改成 60 → 该守卫 FAIL，还原后 PASS）。

## 步骤
1. 下发流：`send_path()` 通过后→确认弹窗（摘要+"请求值"小字）→写段+命令字→右栏握手五段进度可视化→执行
2. 实时联动：客户端 `on_frames`→关节值（raw_to_eng 换算）→fk→`pose.update` 推视口；渲染端线性插值到 60fps；角标显示实测数据频率（**<10Hz 变黄+人话提示，禁外推**）
3. 模式互斥：播放动画=[仿真]，UA 数据驱动=[联动]；两态下 UI 可编辑性按 02 §3（联动中禁编辑点位，需先"暂停跟随"）
4. 异常路径实测（规程 W-6.4）：执行中断线→状态灯灰+日志+模型停住不跳飞；PLC 拒绝→红条+原因码+[查看日志]；数据越界→丢弃该帧并计数
5. 工程文件 `core/project.py`：save/load JSON（$schema=proj/v1；含点位/路径/校核结论快照/配置版本号），加载旧版本工程→按版本兼容策略迁移
6. 打包：PyInstaller onedir（含 view/ 静态资源与 config/），`dist/` 产物在**本机干净目录**双击 exe 全流程走通一遍。⚠️ conda 版 OCC 动态库/数据文件收不齐是已知坑：用 `--collect-all` 或自定义 hook 收全，卡住**停下汇报**，不许砍功能绕路
   - ⚠️ **打包入口必须显式引导 ICU**（见本卡"前置阅读"下那条）：若 PyInstaller 的入口不是 `import app` 起手（自建 `run.py`／`.spec` 里的 entry），**第一行必须** `from app.bootstrap import preload_windows_icu` 并调用 → 否则 `dist` 里同样会因 conda `icu` 的 `icuuc.dll` 被 `Qt6Core.dll` 先命中而 **WinError 127 起不来**。这属"dist 包双击启动"验收项，**不得以开发机上 `python tools/smoke.py`／任何 `import app.*` 的脚本能起来充当证据**（那两条路都已有引导，`dist` 的入口是第三条路）
7. 全链路冒烟脚本 `tools/e2e_smoke.py`：自动跑"导入测试模型→3 点→路径→校核→模拟器下发→跟随 10s"，断言各步完成

## 完成标准
- [ ] e2e_smoke 全绿（贴输出）；执行中角标实测频率 ≥10Hz
- [ ] 断线/拒绝/越界三异常用例行为正确（日志截图）
- [ ] 工程保存→改点→加载回滚成功；配置版本字段在文件里可见
- [ ] dist 包在未装 Python 的目录实测启动（或说明验证方式）
- [ ] G20-① config_check 节点表摘要改为按 SPEC 生成、列全读／写键（贴 rc=0 输出）；G20-② e2e_smoke 含 `publish_interval_ms <= 50` 守卫且负控可红（贴正反两次输出）
- [ ] commit：`feat(T10): 全链路收口与打包`

## 禁止事项
- 禁连真 PLC（联调归后续现场阶段，S-7/S-8）
- 禁在本单塞投标素材/截图规范（D-2 已移出本包）

## 收单记录（指挥方，2026-09-17 17:39 合并；时点＝merge commit `b4ede1e` 的作者时间，可复现）

**验收结论：已过（第 1 轮）——完成标准与收单必检逐项行为级实测通过，已合并入 main（merge commit `b4ede1e`，`--no-ff`，零冲突）。W6＝末波，全部 10 单收口完成。**
交付＝`task/T10` 上 **8 笔** `2b14e48…c9244da`（**30 文件、+3314／−67**，`git diff a89193a b4ede1e` 实测），**原样保留、未 squash**（依 §5.1-1 的 T01／T03／T04／T05／T06／T07／T08 先例）；merge 双亲＝main `a89193a` ＋ T10 tip `c9244da`。`origin/main` 全程仍 `2569104`（推送冻结令未解）。**禁改文件零改动**：`config/machine.yaml` **0 改动**（diff 命中 0）、vendor／`view/index.html` 仅追加注册行、`core/kinematics/`／`core/geometry/`／`core/path.py`／`core/collision.py`／`core/config/`／`comm/` 全清白。

**① 收单必检（数字均为指挥方独立实测，⛔ 不取自报——04 §5.3）**
- **pytest 251 passed**（main 全量，`QT_QPA_PLATFORM=offscreen`，33.23s 现测）；`lint_no_magic` **rc=0、扫 60 文件**；`config_check config/machine.yaml` **rc=0**（22 轴 pending WARN 属预期）。
- **G20-① 闭环**：`tools/config_check.py` 节点表摘要改为**按 SPEC 遍历生成**（与 `core/config/validate.py::_nodes()`／`schema.py` 的 `READ_NODE_SPEC`／`WRITE_NODE_SPEC` 同源），实测列出 **8 读键＋5 写键**、`rc=0`；⛔ 只改摘要打印逻辑、未动校验判定。
- **G20-② 闭环**：`tools/e2e_smoke.py` 含 `publish_interval_ms <= 50` 守卫；**仓外沙箱负控**（临时把值改 60 → 守卫 **FAIL／rc=1**，还原后 PASS／rc=0）实测可红，证据 `evidence/T10/e2e_smoke_negative.txt`。
- **e2e 全链路 16/16 判据 rc=0**（导入→3 点→路径→校核→模拟器下发→跟随），证据 `evidence/T10/e2e_smoke.txt`；执行中角标实测频率达标。
- **步骤⑤ 工程存档**：`core/project.py`（299 行）实测 **`$schema` 为 JSON 首键**、严格拒载非法存档抛 `ProjectError`、**GUI-free**（无 PySide6 import）；`tests/test_project.py`（244 行）**14 测试**全绿；save→改点→load 回滚成功、配置版本字段在文件里可见，证据 `evidence/T10/project_save_load_log.txt`。
- **步骤⑥ dist 双击启动**：PyInstaller onedir 产物在**本机干净目录**实测启动（PID 24568、436MB），**无 WinError 127**（`run.py` 首行 `from app.bootstrap import preload_windows_icu`＋PySide6 前调用，ICU 红线守住）；视口渲染＋桥 echo 实测通过，证据 `evidence/T10/dist_launch.txt`＋`dist_viewport_bridge.png`（**F4 截图目核无甲方数据**）。
- **F4 闭环（T02 遗留）**：`app/viewpane.py::resource_root()` 冻结态取 `sys._MEIPASS`、缺件**不静默回落**（反附7-③），onedir 下视口资源定位正确。

**② 附加项（合并后在 main 树现测）**
- **附4 粒度**：>300 行源文件 **0**（T10 各件实测：`app/linkctl.py` **300**（恰顶）／`core/project.py` **299**／`app/livectl.py` **299**／`app/checkctl.py` **264**／`app/steps/step5_send.py` **259**／`app/sendctl.py` **251**／`tools/e2e_smoke.py` **245**／`tests/test_project.py` **244**／`tools/e2e_rig.py` **190**／`app/sendseg.py` **183**／`app/theme.py` **179**／`mecharm.spec` **141**／`tests/test_t10_guards.py` **128**／`app/viewpane.py` **116**／`tools/e2e_faults.py` **106**／`tools/config_check.py` **105**／`app/panel.py` **54**／`run.py` **31**，均 ≤300）；**单函数最长 46 行**（`app/steps/step5_send.py::_build`）≤50。
- **env 两文件＝预批准 5 包纯增补、零 pin 扰动**：`environment.yml`＋`requirements.lock.txt` 各纯增 `altgraph==0.17.5`／`pefile==2024.8.26`／`pyinstaller==6.22.3`／`pyinstaller-hooks-contrib==2026.7`／`pywin32-ctypes==0.2.3`，逐行核 diff 为纯增补、各带「T10 指挥方授权增补：打包工具链（仅构建期）」标记，⛔ 无现有 pin 改动（核准人＝指挥方，04 §4 依赖增补流程）。
- **core 纯净**：`core/project.py` 无 GUI／app／view／comm import。
- `evidence/T10/`（脚本＋日志＋PNG）**全用自造数据、CLEAN of 甲方数据**：F4 截图目核无甲方名称／工件尺寸。

**③ 增件归属裁定**
- `app/steps/step5_send.py`（259）属 **§4.5-⑦ 预授权预案内**（04 §4 矩阵 L234「`app/panel.py` 装不下 5 步 → `app/steps/step1_import.py`…」，与 T05／T06／T07／T08 的 `stepN_*.py` 同族）；`core/project.py`（299）／`run.py`（31）／`mecharm.spec`（141）／`tools/e2e_smoke.py`（245）**预案内**（派单卡步骤 5／6／7 明列）。
- **预案外增件八件 ⇒ 本次追认**：app 侧四件 `sendctl.py`（251，步骤⑤下发编排）／`sendseg.py`（183，下发分段与握手节律）／`linkctl.py`（300，步骤②实时联动控制器）／`livectl.py`（299，实时数据视图与断链降级）＋ tools 侧 `e2e_rig.py`（190）／`e2e_faults.py`（106）（e2e 拆分，同 T09 `comm_selftest` 手法以守 tools/ 的 ≤300）＋ tests 侧 `test_project.py`（244）／`test_t10_guards.py`（128）＝**4＋2＋2**。理由＝`app/shell.py` 经 T08 后**恰顶 300 行、零余量**（04 §4 矩阵 app/ 行末句已预警），收口接线（下发／实时联动）内联必破限（**同 T07 `pathctl.py`／T08 `checkctl.py` 先例**）；**反扩边界＝只覆盖本轮八件，不构成「app/ 可自由拆件」先例**。已登记 04 §4 矩阵 app/ 行与 tools/ 行。
- 修改件＝`app/panel.py`（64→54，去⑤占位工厂、接 `Step5Pane`、**保 `send_btn`/`send_note` 属性名不变**）／`app/viewpane.py`（+48，F4 修）／`app/theme.py`（+13，附加式 QSS 选择子）／`app/checkctl.py`（+27，加 `send_confirmed` 信号＋`request_note` 钩子＋`set_gate` 镜像给⑤页）四件经核为 **T10 追加接线／F4 修复，非掏空**（04 §4 矩阵 app/ 行授权名单含 T10；`send_flow` 编排落 `checkctl` 而非 `shell.py`，正因 `shell.py` 零余量）。

**④ A2 自报项裁定**
- **签名偏离** `core/project.py` 的 `save(project,path)`／`load(path)->Project`／`migrate(data)->dict`／`to_dict(project)->dict`／`from_dict(data)->Project`（冻结签名仅 `save/load(path)`）→ **裁定合规**（显式传参＋数据类登记，同 T04／T06／T08 先例），03 §3 已回填。
- **§5 桥通道零新增**：实测下发流（`sendctl`／`sendseg`）与实时联动（`livectl`／`linkctl`）**复用既有 `pose.update`／`path.show`／`hl.set`**，⛔ 未定新视口 type、未改 `app/bridge.py`（全仓 dispatch type 串实测仍 10 个），`perf.fps` 行原注「T09/T10 可复用同一通道」至此坐实 → **裁定无需新增 §5 行**，03 §9 V1.11 已记此结论。
- **裁决：冻结态 WebEngine 单进程**——`run.py` 在 `sys.frozen` 时 `os.environ.setdefault("QTWEBENGINE_CHROMIUM_FLAGS","--single-process")`（多进程子进程在 onedir 下 0xC0000139 死）；**仅冻结态生效、dev／e2e 不受影响**，ProcessGone=0，属打包环境 accommodation、**裁定接受**（⛔ 不改 dev 路径）。

**⑤ 冻结计数（标「截至 sha」口径，见 99 v26-⑩）**：**截至 `b4ede1e`＝未推送 79／本地 main 82**，`origin/main` 仍 **`2569104`**、其 reflog **0** 条 ⇒ **本轮零远端写**；⛔ 推送冻结令未解。

**⑥ worktree**：`t10` **暂不删**（与 `t05`／`t06`／`t07`／`t08`／`t09` 同处置——@user 指令「05／t09 worktree 不删」，A2 可能仍驻留该目录，待 @user 确认会话退出后由指挥会话一并删）。

**⑦ 连带回填**：03 §3（`project.py` 五签名＋三常量）、03 §5（本轮零新增通道、复用既有，记于 §9）、03 §9 **V1.11**、04 §4 矩阵（`core/project.py`／`tools/e2e_smoke.py`＋`e2e_rig.py`＋`e2e_faults.py`／`run.py`＋`mecharm.spec` 行＋app/ 行增件归属＋env 行 PyInstaller 5 包注）、04 修订记录 T10 收口行、99 **v36** 与状态表 T10 行 ①—⑬＋验收记录 T10 行。

**⑧ 程序性自查（D 会话让位）**：T10 期间 D 会话让位、零污染；`build_log2.txt` 已清理；甲方截图误摄即删、改窗口定向重摄（`dist_viewport_bridge.png` 目核无甲方数据）。
