# T07 路径生成与动画播放

## 目标
`core/path.py`：点位序列→段序列（LINE/快速移动）+ 行程可达校核 + 段清单/总长/预估节拍；视口折线渲染与机械臂沿路径动画。步骤③完整可用。

**产物口径（2026-09-15 22:43 @user 明确）**：本单产物＝**轨迹参数**（当前工作模式轨迹级轴的段序列），**不是整机运动程序**——主要动作由 PLC 编程，软件只输出轨迹编程部分交 PLC 执行（见 03 §4"实机轴系口径"块末条）。

## 前置阅读
02 设计方案 §2 步骤③ ｜ 03 架构 §3 签名、§4 实机轴系口径 ｜ S-1 契约第 8 章（下发形态：连续轮廓 vs 逐段，**电Q-8（下发轨迹段格式）**未回执前按**逐段到达**设计，代码留 `blending:bool` 开关）

**轴口径（2026-09-15 G11）**：路径只作用于**当前工作模式的轨迹级轴**（本体 6 轴＋所挂臂专用轴，上限 9）；轴数与范围校验一律按 machine.yaml（契约扩容随契约 V1.3，软件侧只改配置）。**段打包口径（过渡期，2026-09-15 审计后明确）**：下发段按 machine.yaml `opcua.pack_profile` 打包——**V1.3 冻结前用 `v1_2_8axis`**（契约 V1.2 布局：56 B/段、`Pos[0..7]`、未用轴位填 0、**无 AxisMask 字段**），**AxisMask 只是 V1.3 的目标形态**，现在不写进段里；换 profile 不改码。

**开工前置（2026-09-16 T04 收单连带；依据＝99 台账 T04 行备注 ③⑦⑧ 与 04 §7.2-G8／G18）**：

1. ⛔ **只准新增 `core/kinematics/ik.py` 一件**。`core/kinematics/` 已按 04 §4.5-⑦ 预案包化并随 `ae5a89a` 合并入 main，T04 已验收的 `__init__.py`／`fk.py`／`transform.py`／`limits.py` **一律不得改**（含签名、含 `__all__`、含 docstring）。如实现过程中确需改动其中任何一件 → **停下汇报**，由指挥方裁，⛔ 禁自行改后补报。
2. **新增模块必须同步更新两个公开面守卫**（`tests/test_public_api_kinematics.py`）：`test_init_only_reexports`（断言 `__init__.py` 顶层只有 docstring／import／`__all__`）与 `test_package_layout_follows_the_preauthorized_plan`（断言包内文件集与 §4.5-⑦ 预案一致）。**只准新增断言、不准删旧断言**——删断言会让守卫退化成橡皮章，按 04 §5.5 附7「假证据形态」处置。若 `ik` 需要 re-export 到包根，须同时改 `__init__.py` 与这两个守卫，且改动理由写进汇报。
3. **矩阵口径**：`Transform4x4` 是**行主序** 16 元 tuple（索引 `row*4+col`，平移在 **3／7／11**）；three.js `Matrix4.fromArray()` 是**列主序**（平移在 **12／13／14**）⇒ **凡出到前端的矩阵必须调 `core.kinematics.to_column_major()`**。`ik` 内部一律用行主序，⛔ 禁在同一函数里混用两种约定（T04 已为此设了注入探针验证，本单改坏会被抓到）。
4. ✅ **G18 已裁，闸门解除（04 §7.2-G18；2026-09-16 22:10 @user 裁「授权 T07 跨归属落地」）**：`ik` 需要「哪根轴驱动哪根连杆、沿哪个方向动」，而 `config/machine.yaml` 的 `links:` 表原本只有 `{id, length_mm, parent}`（驱动绑定只活在行尾注释里、不受校验）。**@user 已授权 T07 跨归属代 T03**（T03 已收单合并）给 `links:` 每项补两键 **`axis`**（值＝某 `axes[].id`，或 `null`＝该连杆无驱动轴，如合成链的 `tool`、现场链的 `flange`）与 **`motion`**（`x｜y｜z`；`axis` 为 `null` 时 `motion` 亦 `null`），并同步改 `core/config/schema.py`／`validate.py`／`loader.py` 的 `links` 解析校验与 `tests/fixtures/` **全部 8 个样例**。**凭据＝04 §7.2-G18 行＋§4 矩阵两行的「第二授权例外」**，台账记「T07 代 T03 扩 links 绑定字段，经 @user 授权」。**时序：先落 `links` 扩字段、再开 `ik.py`**——`ik` 从配置读拓扑，⛔ 禁硬编码、禁把 `tests/test_kinematics.py` 的 `SAMPLE_JOINTS` 当机台事实复制。**硬约束（逐条）**：(a) **只改 `links:` 节**，`axes`／`modes`／`limits`／`opcua`／`paths` 各节一律只读——⛔ 尤其不得碰 `opcua:` 节（那是 G17／T09 的授权范围，**G18 不援引 G17**）；(b) 8 fixtures 同步补两键，各 `bad_*` 的「共 N 项问题」**计数不变**；(c) `tests/test_config.py` **24 用例全绿且不改断言**；(d) `lint_no_magic.py` 判定规则**不得动**、`tools/config_check.py` 只读（其 `links` 摘要只打印节数与 id，无需改）。⚠️ **回执（《踏勘确认清单》第 7 项）未到位前，`axis`／`motion` 取值仍是占位**（与 `length_mm` 现值 0.0 同性质），不得据此承诺可达性；**回执到后只改 `machine.yaml`、不改 `fk.py`／`ik.py`**。本单其余交付件（`core/path.py`／`view/js/path.js`／右栏段清单／步骤 3–6）不受阻、可先行。

## 步骤
1. `gen_path(points)->list[Segment]`：段型=点位型（空程快速移动）与轮廓型（作业速度）两种，界面可切；每段带 起/终/长度/预估时长（速度取 machine.yaml limits）
2. **先实现解析逆解**（T04 禁令口径的另一半）：`ik(target_pose)->joint_values`，桁架移动副解位置（线性方程）+腕部回转副解姿态（球腕三角），闭式解、**禁数值迭代**；多解按行程/连续性选解；构型以契约附录 A／**契约 Q-11（各运动轴清单/行程）**回执为准，回执未到前按 machine.yaml 样例机构推导并留纸面推导记录。然后每段端点调 `ik` 求关节目标→过 `check_limits`（T04）；无解或超限段标红且阻断生成结果确认（**不得照搬六轴机器人 IK 思路**——规程 W-5.2）
3. 视口 `view/js/path.js`：折线+箭头渲染、当前段高亮、播放=按段插值驱动 fk 姿态（线性插值，禁外推）；[▶][⏸]
4. 右栏：段清单表+汇总行（共 N 段·总长·预估节拍）；[生成路径]后点亮步骤④
5. 路径点改动或**切换工作模式**→结果作废状态（步骤④⑤置灰+提示；换臂作废沿用 T02 广播的"结果作废"消息，2026-09-15 G16 口径）
6. pytest：3 点折线段数/长度/时长与手算一致；越界点注入→该段标记阻断；ik 已知位姿用例手算复核（与 T04 同一防自证标准，手算记录贴汇报）

## 完成标准
- [ ] 5 点位生成→表格与视口一致；播放流畅（角标记录实测 fps）
- [ ] 越界用例：红段+无法进入④
- [ ] blending 开关存在且默认 False（等 **电Q-8（下发轨迹段格式）**回执再切）
- [ ] commit：`feat(T07): 路径生成与动画`

## 禁止事项
- 禁做圆弧/样条插补生成（本期直线+点位；招标口径 PLC 负责插补）。⚠️ 契约 ST_Seg.SegType 留 2=CIRC 是**能力上限**，本期只发 0=PTP/1=LIN，禁为"对齐契约"多做
- 禁在动画里"预测"下一帧姿态（W-5.7 红线：只准线性插值）

## 收单记录（指挥方，2026-09-17 03:36 合并；时点＝merge commit `2baca80` 的作者时间，可复现）

**验收结论：已过（第 1 轮）——完成标准与收单必检逐项行为级实测通过，已合并入 main（merge commit `2baca80`，`--no-ff`，零冲突）。**
交付＝`task/T07` 上 **12 笔** `2640b24…6ec1a6d`（**45 文件、+3851／−51**），**原样保留、未 squash**（依 §5.1-1 的 T01／T03／T04／T05／T06 先例）；父＝merge-base `2baca80^1`。`origin/main` 全程仍 `2569104`（推送冻结令未解）。**禁改文件零改动**：`git diff --name-only 2baca80^1...8a0708e -- environment.yml requirements.lock.txt view/vendor/` → **空**；`view/vendor/three.module.js` 仍 **1,272,972 B**。

**指挥方合并集成笔 `8a0708e`（专属动作，A2 未动 `view/index.html`）**：`view/index.html` 注册 `path.js`——`path.js` 再次包裹 `__mecharm_dispatch`、截走 `path.show`／`pose.update`，故注册序 **bridge→loader→pick→path**（须在 `pick.js` 之后），A2 交付笔未碰 `index.html`（合规）。

**① 收单必检（数字均为指挥方独立实测，⛔ 不取自报——04 §5.3）**
- **G18 时序与范围**：先落 `links` 扩字段（`config/machine.yaml` **13** 项补 `axis`／`motion`＋`core/config/schema.py`／`validate.py`／`loader.py` 的 `links` 解析校验＋**8** 个 fixtures 同步补键）**再开 `ik.py`**；⛔ 未碰 `opcua:` 节（G17／T09 范围，G18 不援引）；`tests/test_config.py` **24 用例全绿、断言未改**、各 `bad_*`「共 N 项问题」计数不变；`lint_no_magic.py` 规则未动、`tools/config_check.py` 只读。links 的 `axis`／`motion` 值在**踏勘第 7 项回执前是占位符**（与 `length_mm` 现值同性质），⛔ 不得据此承诺可达性。
- **G18 五处缺陷注入全 `rc=1`**（仓外沙箱、附7 形态②：确认是断言失败非 rc=2 收集错误）：links 缺 `axis`／`motion` 非法值／`axis` 指向不存在 id／8 fixtures 漏补键／`ik` 退回硬编码轴 id——五处均被 `tests/test_links_binding.py`（225 行，追认件）或 `test_ik.py` 抓到、`rc=1`。
- **ik 闭式解析、0 数值迭代**：逐项核实 `core/kinematics/ik.py`（300 行）无 jacobian／newton／ccd／收敛阈值；`ik.py:118` 的 `while` 是**有界父链遍历**（结构性的），`_POSE_TOL=1e-9` 是**比较容差**非收敛判据。**0 硬编码轴 id、配置驱动**（`derive_chain(cfg, end_link)` 从 `machine.yaml` 读拓扑）。
- **独立 ik 往返实测**：目标 `{SX:250, SZ:120, WR:-60}` → `ik` → `fk∘ik` 复得位姿偏差 **`0.0`**（行主序全链一致）；不可达目标 → **抛 `IkError`**（非静默返回）。
- **矩阵口径**：`Transform4x4` 行主序（平移 3／7／11），出前端经 `to_column_major()`（`app/pathctl.py:186` `pose = to_column_major(tool_pose_in_model(joints, self._chain, self._frame))`）；`view/js/path.js:169` `_mat.fromArray(flat)`（列主序），两种约定未在同一函数混用。
- **动画红线（W-5.7）**：`path.js` 播放＝按段**线性插值**驱动 fk 姿态，**禁外推／预测下一帧**；空闲 **500 ms**（`IDLE_STOP_MS`）自动停；视口帧率经 `perf.fps` 回传壳（03 §5 本轮补登通道）。

**② 附加项（合并后在 main 树现测）**
- pytest **202 passed**（main 全量）；⚠️ **合并即时那一轮曾见 `test_opcua.py::test_readback_frequency_holds_nominal_cadence` 1 例 FAILED（201 passed＋1 failed）**——按「不听自报」隔离复跑 ×2 均 `1 passed`，且 `git diff --name-only 4b8be39...6ec1a6d -- comm/` **空**（T07 从未碰 comm/）⇒ 判为 T09 墙钟节律测试的**负载敏感 flake、非 T07 回归**，登记为已知 flake（关联 §7.2-G20-②：`publish_interval_ms` 无 CI 守卫）；本轮重测 **202 passed、flake 未复现**。`lint_no_magic` **rc=0、扫 50 文件**；`config_check config/machine.yaml` **rc=0**（22 轴 pending WARN 属预期）。
- **附4** >300 行源文件 **0**（T07 各件实测：`ik.py` **300**／`core/path.py` **233**／`app/pathctl.py` **246**／`app/steps/step3_path.py` **242**／`view/js/path.js` **247**／`tests/test_ik.py` **300**／`test_path.py` **294**／`test_links_binding.py` **225**，均 ≤300；单函数 ≤50 行）。
- `core/kinematics/ik.py` **只新增**、`__init__.py` **只在 `__all__`／import 行追加** 5 符号（`Chain`／`IkError`／`derive_chain`／`ik`／`tool_link`），`tests/test_public_api_kinematics.py` 的 `SUBMODULES` **4→5**（加 `ik.py`）两条守卫同步改，T04 已验收四件零改动（合规）。
- `evidence/T07/`（脚本＋日志＋7 张 PNG）**全用自造基本体、CLEAN of 甲方数据**（与 `evidence/` 同口径）。

**③ 增件归属裁定**
- `app/steps/step3_path.py`（242）属 **§4.5-⑦ 预授权预案内**（04 §4 矩阵 L234「`app/panel.py` 装不下 5 步 → `app/steps/step1_import.py`…」，与 T05 `step1_import.py`／T06 `step2_pick.py` 同族）。
- `app/pathctl.py`（246）属**预案外增件 ⇒ 本次追认**，理由＝`app/shell.py` 交付时已 297 行逼近 §4.5-① 的 300 上限、步骤③控制器内联必破限；**反扩边界＝只覆盖本轮 `pathctl.py` 一件**，不构成「app/ 可自由拆件」先例（与 §7.2-G8 核记⑤ `brep_cache.py`、G17 三件同一处置法）。已登记 04 §4 矩阵 app/ 行。
- `tests/test_links_binding.py`（225，G18 缺陷注入承重件）**追认**（新增测试文件，派单前未预登记）。
- `app/panel.py`（+9）／`app/shell.py`（+24）为 T07 追加接线（合规，04 §4 矩阵 app/ 行授权名单含 T07）。

**④ 冻结计数（标「截至 sha」口径，见 99 v26-⑩）**：**截至 `8a0708e`＝未推送 59／本地 main 62**，`origin/main` 仍 **`2569104`**、其 reflog **0** 条、`git status -sb` 首行 `## main...origin/main [ahead 59]` ⇒ **本轮零远端写**；⛔ 推送冻结令未解。

**⑤ worktree**：`t07` **暂不删**（与 `t05`／`t06`／`t09` 同处置——@user 指令「05／t09 worktree 不删」，A2 可能仍驻留该目录，待 @user 确认会话退出后由指挥会话一并删）。

**⑥ 连带回填**：03 §3（`ik`／`derive_chain`／`gen_path` 签名回填）、03 §5（`perf.fps` 通道＋`pose.update`／`path.show` schema 细化）、04 §4 矩阵（`view/js/path.js`／`core/path.py`／kinematics 行 ik.py 已交付＋app/ 行增件归属）、99 v34 与状态表 T07 行＋验收记录 T07 行。

**⑦ A2 复核单补裁（指挥方，2026-09-17；A2 事后补交的 T07 复核单 ⑩③ 两项，合并后裁定，⛔ 不重开 T07）**
- **⑩ `ik.py` 构型类异常消息仍带工程表述（「解析逆解不成立」「球腕三角解」「回代 9 元不符」「G19」）→ 裁定：本轮可接受、不改，登记前瞻挂账**。指挥方独立核实可达性（⛔ 不采信 A2「现场链走不到」的自报）：`seg.reason` 确实**逐字上屏**（`app/steps/step3_path.py:204` tooltip＋`:226` 表下红字行），故凡能进 `seg.reason` 的 `IkError` 消息都算上屏文案。逐 raise 点核：jargon 消息全在 `_solve_orientation` 的**回转副分支**（`ik.py:254／257／269／274`，需 ≥1 根回转副）与 **0 回转副分支的 G19 句**（`ik.py:262-263`，仅当目标位姿**带旋转**即 `_require_rotation(pose, identity())` 失配时才抛）；现场链全移动副（0 回转副）且 A2 复核单 ⑫ 明记 **`Waypoint.normal` 不参与本单计算**⇒`gen_path` 只构造**位置目标（单位旋转）**⇒ 0 回转副分支的 `_require_rotation(pose, identity())` 恒过、G19 句**不触发**；真正会进 `seg.reason` 的是**位置可达性**句（`ik.py:181「链上没有沿 {motion} 的移动副…」／214「合不出 {total} mm：行程合计只到…」`，均纯中文无术语，与 A2 越界用例 (5000,0,0) 触发的红字原因一致）。**裁定＝本轮非上屏违例、T07 维持已过、不强改**（改则牵动 `tests/test_ik.py` 的 `match=` 断言、零当期收益）。⚠️ **前瞻挂账（登记备查，触发即须整改）**：一旦**甲方回执补齐臂链**（`links:` 增回转副⇒腕可建）或 **`gen_path` 开始用 `Waypoint.normal` 构造带姿态目标**，上述 jargon／G19 句即变为**可达且上屏**⇒ 届时**必须先按 02 §2 步骤③「禁止上屏」＋§4 全中文口径改写这些异常消息**（去「球腕三角解／解析逆解／回代 9 元／G19」等术语与内部编号）再放行；该改写属 T08／T10 或回执变更单范围，⛔ 不得静默沿用现消息上屏。
- **③ 02 §2 步骤③ 布局清单漏登记段型切换控件 → 已回填（02 V1.2）**：A2 实交付**段型二选一**（点位型／轮廓型，速度只读 `machine.yaml` limits）与**连续过渡复选框**（默认未勾选＝`blending=False`、电Q-8 占位），原 02 §2 步骤③ 清单只列[生成路径]／段清单表／汇总行／[▶][⏸]、漏此两件 ⇒ 指挥方补登于 02 §2 步骤③＋修订记录 V1.2，并钉明上屏文案禁出现 `blending`／`电Q-8` 术语（A2 自查整改笔 `6bae56a` 已把操作员可见文案的术语／英文／内部编号清掉，Python API 名 `blending` 与字段未动、合规）。
- **其余复核单项（①②⑤⑥⑦⑧⑨⑪—⑮）均与指挥方本轮独立复测一致或已登记**：①②（perf.fps 通道＋pose.update／path.show schema＋gen_path/ik 签名）已回填 03；⑤（`__init__.py` re-export ik 的卡红线② vs 04 L421 冲突）已裁**合规**（v34 ⑤：kinematics 行授权 T07 自改、与 geometry 行「指挥方统一加」分属两包）；⑥（新增文件名）`app/pathctl.py`＋`tests/test_links_binding.py` 已**追认**（v34 ⑤／本卡 ③）；⑦⑧⑨（G18 占位／电Q-8／契约 §7.1 无配置源）属**问甲方挂账**、回执到后只改 `machine.yaml`；⑭（`test_readback_frequency_holds_nominal_cadence` 时序 flake）已登记为已知 flake（关联 §7.2-G20-②）；⑪（`core.path.Segment` 与 `comm/opcua_client/write.py::Segment` 同名异义）登记备查、下发适配器属 T08／T10。
