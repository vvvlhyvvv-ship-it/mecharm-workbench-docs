# T06 面拾取与点位示教

## 目标
视口点击模型面→回传 mesh_id+face_id→core 换算真实 3D 点+法向→右栏点位列表管理。步骤②完整可用。

## 前置阅读
02 设计方案 §2 步骤② ｜ 03 架构 §5（pick.face 协议）｜ **T05 已合并 `8440765`**（本单硬前置＝T05 两项整改，详见文末「开工前置与口径」）｜ 04 §4 归属矩阵 `core/geometry/` 行 ＋ §4.5 代码粒度 ＋ §5.5 附1–附7

## 步骤
1. `view/js/pick.js`：raycaster 拾取，命中回传 `{mesh_id,face_id,u,v}`（三角形重心坐标）；悬停面高亮、点击落标号牌（序号+名称，标签随相机距离保底字号）
2. **`core/geometry/face_point.py`**（⚠️ 2026-09-16 回填：原写「`core/geometry.py` **增补**」，而 T05 已按 04 §4.5-⑦ 预案包化并合并 `8440765` ⇒ 本单**只新增 `face_point.py` 一件，禁改 T05 已验收的 `__init__`／`import_model`／`tessellate`／`brep_cache` 四件**）：`face_point_from_tri(mesh_id,face_id,u,v)` 与 `face_center(mesh_id,face_id)`（**法向从 B-Rep 面真值取**——经 `MeshPart.face_index[face_id]` 拿活 `TopoDS_Face`，再 `BRep_Tool.Surface(face)` → `GeomLProp_SLProps`；⛔ **不是三角形法向**。本机 pythonocc-core 7.9.3 实测两点：`GeomLProp_SLProps` 收的是 `Geom_Surface` **句柄**、不是 adaptor；OCC 符号是**模块级小写函数** `breptools`／`topods`，CamelCase 的 `BRepTools`／`TopoDS` 不存在）
3. 点位数据模型：`Waypoint{id,name,pos_mm,normal,source_face}`；列表 UI（增删改命名、手改坐标、非法值拒绝、删除不重排序号）接真数据
4. 拾取模式与视角旋转的鼠标手势分离：左键=取点，右键/中键=旋转平移，滚轮=缩放；右栏顶部固定提示"在模型上点击机械臂要到达的面"
5. **工作模式接线（2026-09-15 G16）**：右栏顶部第一行常显当前工作模式（如"当前：打磨臂"）；未选定工作模式时步骤②保持锁定（提示"先在顶栏选择当前安装的多功能臂"）；**收到"结果作废"消息时清空点位列表**（换臂后必须重新示教）
6. pytest：给定三角形+重心坐标的手算换算用例 **3 组（须含无理数／斜置数据，⛔ 禁用误差恒 0 的轴对齐整数框——04 §5.5 附7 形态①）**；法向与 B-Rep 面法向一致用例；**缓存全命中路径下仍取得到真法向的用例**（本单收单必检项，见文末 ③）

## 完成标准
- [ ] 连点 3 面：3 个标号牌+3 行列表；改坐标标号牌跟随
- [ ] 手改坐标非法值（字母/超行程）红框拒绝+人话日志
- [ ] 同一面重复点击不产生重复点位（去重或提示）
- [ ] commit：`feat(T06): 点位示教`

## 禁止事项
- 禁做"边线/顶点拾取"（本期只做面）；禁在前端存点位真值（数据源唯一在 core）

---

## 开工前置与口径（指挥方 2026-09-16 补，T05 第二轮收单后）

**① 开工前置＝T05 两项整改已合并入 main（已成就）**：merge commit `8440765`（2026-09-16 20:26），含 `.brep` 双缓存＋命中路径 `face_index` 重建（整改-1）与按 aspect 双向取景（整改-2）。⛔ **`face_index` 是本单取真值法向的唯一依据**——T05 第一轮交付时它在缓存全命中路径下为 `{}`（即"秒开"路径恰好掏空本单），该隐患已消除，但**本单收单必检要把它再验一遍**（见 ③ 第一条），否则同一形态会在本单复现。

**② 并行口径**：`T05 ∥ T06` 的禁并行令**已解除**——04 §4.5-⑦ 触发条件原文「包化**实际发生并合并后**」两半句均已成就，按 §7.2-G8 定案改为「**可分头写**」。⚠️ **解禁只消灭合并冲突、不消灭功能依赖**：合并顺序仍是 T05 → T06；本单**只新增 `core/geometry/face_point.py` 一件**，禁改 T05 已验收的四件——**含 `__init__.py`**：要 re-export `face_point` **由指挥方在合并时统一加**，实现会话**不要改 `__init__.py`**（撞车点已在 04 §4 矩阵 geometry 行登记）。若 `tests/test_public_api_geometry.py` 存在「包内文件不多不少三件」类断言，**只许在汇报里提出**、由指挥方处置，⛔ 不得自行改断言。

**③ 收单必检（本单新增；指挥方按 04 §5.3「不听自报」逐条行为级重跑）**
- **缓存全命中下真法向仍可得**：二次导入（显示网格＋`.brep` 双命中）后调 `face_point_from_tri`／`face_center`，法向须与**首次导入（冷路径）逐面一致**（点积 ≈1）。⚠️ 这是 T05 整改-1 的**下游消费者判据**：只在冷路径测＝附7 形态③（缓存命中路径掏空下游契约）会在本单复现而判据全绿。
- **法向不是三角形法向**：至少取一个**圆柱侧面**上的面，真法向与该三角形法向的夹角须**明显非 0**（T05 收单时同一情形实测 2.318°，属弦高偏差、合理）；若两者恒等，说明取的是网格法向 ⇒ **打回**。
- **阈值不被数据架空（附7 形态①）**：手算换算 3 组须含无理数／斜置数据；误差列**恒 0 视为空跑**。
- **注入须真失败（附7 形态②）**：把"取真法向"那段换成 no-op（或退回三角形法向）后跑 pytest，须得 **rc=1** 且失败项**正是**该判据；⛔ rc=2（收集错误）不算通过。
- **竖幅自测（附7 形态④）**：标号牌／点位列表在 **320×500**（`app/shell.py` `CENTER_MIN=320` 声明的软件最小中部视口）下不越界、不互相遮挡；⛔ 只贴横屏截图＝取向盲区（T05 第一轮就是这么漏掉裁切缺陷的）。
- **F3「结果作废」闭环**：收到作废消息后点位列表须**真清空**（含视口里的标号牌），⛔ 不只是把按钮置灰。

**④ 代码粒度与依赖纪律（04 §4.5 全仓硬约束）**
- 单文件 **≤300 行**、单函数 **≤50 行**，贴 §4.5-⑥ 命令输出。`face_point.py` 若实测超限：先做"去重复→抽函数"，仍超再按 §4.5-⑦ 报备——⚠️ ⑦ 的预授权范围只有 `core/kinematics/` 与 `core/geometry/` 两包，本件**在范围内**，但**预案结构里没有 `face_point` 之外的第五件**，再新增文件名须**先报备**。
- 前端 `view/js/pick.js` 超限可拆 `view/js/pick/` 子模块（**先报备**）。
- **禁自行 `pip install`／改 `environment.yml`／`requirements.lock.txt`**：如需 `three-mesh-bvh` 之类第三方包，**只在自己 worktree 里写 import 与申请、不得安装、不得改环境文件**，由指挥方核准增补后再合并；**未在环境文件中的包，验收时按"越界引入"打回**（04 §4 依赖增补流程）。其他 vendor 文件属 §4.5 白名单，但"新增源文件须先报备"这条**不豁免**。
- ⛔ **禁 `git push`／禁动 `remote`**（04 §5.1-6 推送冻结令）：本单**只 commit 不 push**；汇报里出现"已推送"即属违规、按打回处理。
- ⛔ 禁改 `config/machine.yaml`（T03 归属，**任何单不得私改**）、禁改 `99_进度与验收.md` 与本任务包全部文档（指挥方专属）、禁改 `tools/lint_no_magic.py` 判定规则、禁改 `app/bridge.py`（桥协议是共享契约，协议在 03 §5）。
- 夹具复用 **`tests/cad_samples.py`**（T05 首建，04 §4 矩阵已追认，T06／T08 共用）：只用 `tmp_path` 生成、**禁写死仓内绝对路径**、只用自造基本体（**禁甲方模型／名称／尺寸**）；缺陷注入放在**不被引用的条目**上。
- ⚠️ **monkeypatch 陷阱（T05 复测踩过）**：`core/geometry/__init__.py` re-export 了与子模块**同名的函数**，`import core.geometry.tessellate as _ts` 绑到的是**函数**而非模块，patch **静默无效**；须用 `importlib.import_module("core.geometry.tessellate")`。
- ⚠️ **缓存与取证落盘边界**：任何缓存／`.brep`／离线资产**绝不落仓内**（根＝`paths.cache_dir`，默认仓外 `%LOCALAPPDATA%\mecharm\cache`）；`evidence/T06/` **只放不含甲方数据**的取证物，甲方真实模型截图／含甲方名称或工件尺寸的画面**一律不得入库**。

**⑤ 完成标准增补（与原「完成标准」节并列，收单时一并逐项实测）**
- [ ] 缓存**全命中**路径下取真法向与冷路径**逐面一致**（贴点积实测）
- [ ] 圆柱侧面上**真法向 ≠ 三角形法向**（贴夹角实测，须明显非 0）
- [ ] 手算换算 3 组用例含无理数／斜置数据（贴误差列，⛔ 不得恒 0）
- [ ] **320×500 竖幅**下标号牌与列表不越界（贴该尺寸实测，⛔ 不接受只有横屏）
- [ ] 全仓 pytest 绿 ＋ `lint_no_magic` rc=0 ＋ 附4／附5／附6 零命中（贴命令输出）

---

## 收单记录（指挥方，2026-09-16 22:55 合并；时点＝merge commit `9ea633e` 的作者时间，可复现）

**验收结论：已过（第 1 轮）——完成标准与六条收单必检逐项行为级实测通过，已合并入 main（merge commit `9ea633e`，`--no-ff`，零冲突）。**
交付＝`task/T06` 上单笔 `bfd04e8 feat(T06): 点位示教`（**10 文件、+1090／−3**）；父＝merge-base `c8abade`；工作树 `git status` **空**、**0** 个 `__pycache__`／`.pyc` 入库；分支无 upstream，`origin/main` 全程仍 `2569104`（推送冻结令未解）。**禁改文件零改动**：`git diff --name-only c8abade...3e67ef3 -- environment.yml requirements.lock.txt view/vendor/` → **空**；`view/vendor/three.module.js` 仍 **1,272,972 B**。合并可行性预判：main 侧自 merge-base 起 **35** 文件（含 T09 合并与文档）∩ T06 侧 **10** 文件＝`comm -12` **空集** ⇒ 预告零冲突，`--no-ff` 实测 ort 策略相符、**交付单笔原样保留未 squash**（依 §5.1-1 的 T01／T03／T04／T05 先例）。

**指挥方合并集成笔 `3e67ef3`（专属动作，B 未动 `__init__.py`／`index.html`／断言）**：① `core/geometry/__init__.py` 加 `face_point` 四符号 re-export（`face_point_from_tri`／`face_center`／`FacePoint`／`Waypoint`，`__all__` **8→12**）；② `tests/test_public_api_geometry.py` 的 **PUBLIC 清单锁步同步**——⚠️ B 自报「无文件数断言、加 `face_point.py` 不破坏它（115 passed 已证）」就**文件数**而言属实，但**漏报了守卫 `test_all_matches_public_symbols` 的 `sorted(__all__)==sorted(PUBLIC)` 符号锁步断言**：PUBLIC 是硬编码 8 符号清单，加 re-export 而不同步改 PUBLIC 即**假失败**；指挥方据本卡「开工前置与口径」② 处置（实现会话禁改、由指挥方处置）；③ `view/index.html` 注册 `pick.js`（注册序 **bridge→loader→pick**，pick.js 再次包裹 `__mecharm_dispatch`、截走 `pick.enable`／`invalidate`，故须在 loader.js 之后）。

**① 六条收单必检（数字均为指挥方独立实测，⛔ 不取自报——04 §5.3）**
- **缓存全命中下真法向仍可得**：二次导入（显示网格＋`.brep` 双命中）后逐面取真法向，与冷路径**最小点积 `1.000000000000`**（`15` 个 B-Rep 面全核）；承重测试 `test_cache_full_hit_normals_match_cold_path` 把 `tessellate._parse` 打成抛错 ⇒ **命中路径一旦回退重解析源文件即失败**，证明走的是 `.brep` 重建（T05 整改-1 的下游消费者判据，附7 形态③未复现）。
- **法向不是三角形法向**：圆柱侧面真法向 vs 三角形法向 **max `2.318°`**（弦高偏差、合理），平面端面同口径对照 **`0.000000°`**；承重测试 `test_cylinder_side_true_normal_differs_from_triangle_normal`（断言 `side_max>0.5`、`planar_max<1e-6`）。
- **阈值不被数据架空（附7 形态①）**：手算重心换算 3 组含斜置数据，残差 **`3.553e-15`／`3.553e-15`／`1.776e-15`**（非恒 0、在浮点噪声内）；承重 `test_barycentric_matches_hand_computed_3groups`。
- **注入须真失败（附7 形态②）**：把"取真法向"换成退回三角形法向（`side_max`→`0.000°`）后跑 pytest，得 **`rc=1`** 且失败项**正是** `test_cylinder_side_true_normal_differs_from_triangle_normal`（⛔ 非 rc=2 收集错误）。
- **竖幅自测（附7 形态④）**：在软件声明的最小中心视口 **320×500**（`app/shell.py` `CENTER_MIN=320`）下，点位列表三档宽 **280／320／360** 横向溢出 `hsb.max=0`（无横向滚动条）；三块标号牌 DOM 坐标 **P1 (88.9,215.6)／P2 (224.9,210.9)／P3 (133.3,214.1)** 全在 320×500 框内、不越界不遮挡。⚠️ B 自报因 **NATIVE_BROWSER_VIEWPORT_UNAVAILABLE** 未能提供在应用内浏览器截图、改用 DOM 坐标，指挥方以 browser-use 离屏 harness 独立复现（起 http 服务、把 `#scene-host` 尺寸设 320×500、注入 pick.js、建 box、派发 `mesh.load`＋`pick.enable`、读回标号牌 DOM 坐标），三点全在框内。
- **F3「结果作废」闭环**：⚠️ **B 的取证 §A 走的是 `invalidate` 消息路径，而壳侧 F3 实际路径不同**——`app/shell.py` 的 `_on_invalidate_results`→`step2.clear()`→`waypoints_changed`→`_refresh_pick` 发的是 **`pick.enable {markers:[]}`**（非 `invalidate`）；指挥方独立核验**真实 F3 路径**：清空后点位列表 `rowCount=0`、`next_id=1`、视口标号牌 DOM **归 0**（真清空、含视口标号牌，⛔ 不只置灰按钮）。

**② 附加项（合并后在 main 树现测，非沿用交付时读数）**
- pytest **136 passed**（＝T09 后 130 ＋ T06 新增 6；t06 分支单独 **115 passed**）；`lint_no_magic` **rc=0、扫 45 文件**；`config_check` rc=0。
- **附4** >300 行文件 **0**（T06 三件实测：`core/geometry/face_point.py` **169**／`view/js/pick.js` **268**／`tests/test_face_point.py` **226**，均 <300；单函数 ≤50 行）；**附5** T06 侧 ∩ 文档区 **0 命中**；**附6-①** 未新增 vendor、**附6-②** AST 越界第三方 import **0 命中**；**常驻②** core 层无 GUI／通讯 import **空**；包根 re-export smoke 通（`from core.geometry import face_point_from_tri, face_center, FacePoint, Waypoint` 可导入）。
- `core/geometry/face_point.py` **只新增、未碰 T05 已验收四件**（`__init__`／`import_model`／`tessellate`／`brep_cache`）；`face_point.py` import 兄弟件 `from core.geometry.import_model import GeometryError`（无环），`MeshPart` 仅在 `TYPE_CHECKING` 下引用。

**③ B 五项自报裁定——全部追认**
- **render-spy 修法**：`WebGLRenderer` 实例的 `render` 是构造期赋的**自有闭包**（原型包裹不触发）⇒ pick.js 改包裹 `THREE.Object3D.prototype.lookAt`（截相机）＋`prototype.add`（截场景＝`group.parent`）。**追认**——属 JS 侧可复用教训，已记 05 沉淀。
- **`pick.enable` schema `{on, markers:[{id,name,pos}]}`**：**追认**，已回填 03 §5（新增独立行；`markers` 只下发 id／name／pos 三键，⛔ 不含 `normal`／`source_face`，真值唯一在 core）。
- **`face_id`／`u`／`v` 闭环**：`face_id`＝显示网格三角形序号（⛔ 非 B-Rep 面 id），壳经 `face_point_from_tri` 换算。**追认**，已钉进 03 §3／§5。
- **`face_point.py` 模块 docstring 自陈「不擅改文档、请指挥方回填 §3」**：**追认**——两处签名变更（加 `parts` 首参、`Point`→`FacePoint`）与数据类登记均由指挥方于本轮回填 03 §3（V1.9）。
- **中键旋转、无平移的局限**：**登记在案**（非阻塞；T07 动画与 T08 碰撞可视化如需平移另行接线）。

**④ 冻结计数（标「截至 sha」口径，见 99 v26-⑩）**：**截至 `3e67ef3`＝未推送 43／本地 main 46**，`origin/main` 仍 **`2569104`**、其 reflog **0** 条、`git status -sb` 首行 `## main...origin/main [ahead 43]` ⇒ **本轮零远端写**；⛔ 推送冻结令未解。

**⑤ worktree**：`t06` **暂不删**（§5.1-③ 要求收单后删；会话 B 可能仍驻留该目录，与 `t05`／`t09` 同处置——@user 指令「05／t09 worktree 不删」，待 @user 确认会话 B 退出后由指挥会话一并删）。

