# T01 环境与仓库启动

## 目标
启动代码仓 + Python 环境（含 OCC），装成即停，产出可复现的环境清单。**不写任何业务代码。**

## 前置阅读
03 架构 §6 环境表 ｜ 01 需求 D-1 D-8 ｜ CR-2026-01 §二 CR-1（仓路径与同仓纪律；**以其修订记录里的最新版为准**，勿按固定版本号取文）

## 步骤
0. **固化 git 身份（本单必做，否则提交失败）**：实测本机 `user.name`／`user.email` **local 与 global 均为空**（既有提交靠临时配置完成）→ 执行 `git config --local user.name "lhy"`＋`git config --local user.email "vvvlhyvvv@gmail.com"`（沿用既有作者口径），并在汇报里贴 `git config --list --local` 输出
1. **仓已存在，不再 `git init`**：工作目录即 `D:\ZCode项目\多功能机械臂软件`（2026-09-15 23:04 @user 定案），已配 `origin`（mecharm-workbench-docs）；本单只做：确认 `.gitignore`（**已含 `.workbuddy/`、`cache/`、`data/`、`*.brep` 等，勿删勿缩**）→ 建目录骨架 `app/ core/ comm/ view/ config/ tests/ tools/ evidence/`（各放占位 `__init__.py`/README；`evidence/` 存各单验收截图，入库留痕——⚠️ **只放不含甲方数据的取证物**，甲方真实模型截图/含甲方名称或工件尺寸的画面**一律不入库**，见 04 §4 矩阵该行）→ 建仓根 **`.gitattributes`**（内容仅一行 `view/vendor/** -text`；⚠️ **实测理由**：本机 **系统级** `core.autocrlf=true`（`C:/Program Files/Git/etc/gitconfig`），不加此行则 checkout 出的 `view/vendor/three.module.js` 被转成 CRLF——**1,272,972 B → 1,326,016 B、SHA256 由 `76dea815…` 变成 `c6eacae1…`**，于是"按哈希核 vendor 原件"这道控制**每个 worktree 都会假失败**；three.js 对 CRLF 不敏感，故属**校验口径问题**而非功能问题）→ 首次提交 `chore(T01)`。**同仓纪律：禁改 `前端任务包/**`、`参考资料/**` 与 `CR-*.md`，`.workbuddy/` 不入库**
   - ⚠️ **开工前置判定（只看"文档路径是否脏"，不比对 commit 号）**：`git status --porcelain -- 前端任务包 参考资料` 的**已跟踪条目**（` M`／`M `／`A `／`D `／`R ` 开头）非空 → **停下汇报**；`??` 未跟踪项不算。**基线 commit 号以派单消息为准，任务单内不写死**（写死会随 main 前移而"开工即停"）
   - ⚠️ **`app/bootstrap.py`（2026-09-16 遗留处置新增，属基础设施、非业务代码）**：conda 环境的 `icu` 包在 `%PREFIX%\Library\bin` 也放了一份 `icuuc.dll`，而 conda 的 python 启动时会把该目录自动加入 DLL 搜索路径 → `Qt6Core.dll`（**静态**导入表里有 `icuuc.dll`）先命中它，因符号集与系统 ICU 不一致报 **WinError 127**（`ERROR_PROC_NOT_FOUND`），表现为 `from PySide6 import QtCore` 直接失败；`icu` 是 `libxml2`（OCCT 的 XML 支持链）硬依赖，**不能移除** → 处置＝在导入 PySide6 **之前**按模块名预载系统 `C:\Windows\System32\icuuc.dll`。**完整实测依据见该文件 docstring，本卡不复述**。`app/__init__.py` 已调用它 → **`import app.*` 路径自动生效**；**从 `app` 包外直接 import PySide6 的入口（独立脚本、PyInstaller 入口、`tools/` 临时验证脚本）必须显式 `from app.bootstrap import preload_windows_icu`**（已写进 T02／T10 卡）。**唯一写者＝T01，其他单只读，禁各单复制粘贴自写 shim**（04 §4 矩阵该行）。
2. 装 Miniforge 到 `D:\Miniforge3`（静默安装 `/S /D=D:\Miniforge3`）；`conda create -n mecharm python=3.11 -y`
3. 装依赖并锁定：
   - conda-forge：`pythonocc-core`
   - pip（进 mecharm 环境）：`PySide6 pyyaml numpy asyncua pytest`
   - three.js：下载 r160+ 的 `three.module.js` 本地文件放 `view/vendor/`（禁 CDN 运行期引用）
4. `conda env export > environment.yml` + `pip list --format=freeze > requirements.lock.txt` 入库
   - ⚠️ **口径追认（2026-09-16，以实测交付件为准）**：原卡字面写 `pip freeze`，实际交付用 **`pip list --format=freeze`**——两者输出**不等价**：`pip freeze` 默认**不列 `pip`／`setuptools`／`wheel`**，而锁文件的用途是复现环境，缺了装包工具链自身的版本就复现不出同一环境。实测交付件 `requirements.lock.txt` **31 行、含 `pip==26.2.1`**，即 `pip list --format=freeze` 的产物。**此偏离予以追认，交付件不重做**；本行字面命令已按实改，避免后续复跑生成口径不同的第二份锁文件。
5. 冒烟脚本 `tools/smoke.py`：import OCC、PySide6、asyncua、numpy 各打印版本
   - ⚠️ **本项目 OCC 绑定的模块名是 `OCC`**（原卡写的 `OCP` 是**另一个**绑定，本环境实测 `import OCP` → `ModuleNotFoundError: No module named 'OCP'`）。`OCC` 无 pip/dist 元数据（conda 包不带 `dist-info`），故版本只能取 `OCC.VERSION` 常量——见"完成标准"第 1 项的版本号说明。
6. commit：`chore(T01): 环境基线`
   - ⚠️ **口径追认（2026-09-16）**：本单实际交付 **2 个 commit**——`chore(T01): 环境基线` ＋ `chore(T01): 遗留问题处置——ICU 入口引导抽出共用＋vendor 换行保真`。原因：遗留处置发生在**收口汇报之后**，与环境基线是两件独立事实。**不为此改写历史去 squash**（squash 会重写已复核的基线提交，并抹掉"遗留处置"这条留痕）。04 §5.1 硬规矩 1 的"一次 commit"对 **T01 记为例外**；T02 起仍按"一单一次 commit"执行。

## 完成标准
- [ ] `smoke.py` 跑通，四个包版本行打印出来（贴实测输出）
  - ⚠️ **OCC 行会打印 `7.9.0`，这不是装错版本、不得据此打回**（2026-09-16 实测追注）：`7.9.0` 来自 `OCC/__init__.py` 里**硬编码的 `VERSION` 常量**（`PYTHONOCC_VERSION_MAJOR/MINOR/PATCH = 7/9/0`），**与实装 conda 包版本不同源**；实装版本以 `environment.yml` 为准——实测 `pythonocc-core=7.9.3=novtk_h6eb2bb1_103`、依赖 `occt 7.9.3 *novtk*`、`python >=3.11,<3.12.0a0`。即"包 7.9.3／上游常量 7.9.0"，属 pythonocc 的版本常量滞后。
- [ ] `from OCC.Core.STEPControl import STEPControl_Reader` 成功
  - ⚠️ **原卡写 `from OCP.STEP import STEPControl_Reader` 系误，2026-09-16 更正**：`OCP` 是另一套绑定（cadquery 侧），本环境实测 `import OCP` → `ModuleNotFoundError`；正确串已在 `mecharm` 环境**实测通过**（返回 `<class 'OCC.Core.STEPControl.STEPControl_Reader'>`，`tools/smoke.py` 已按此串自核）。
- [ ] environment.yml / requirements.lock.txt 已入库
- [ ] `git config --list --local` 已含 user.name／user.email（贴输出）
- [ ] **本单交付路径内无未提交改动**：`git status --porcelain -- app core comm view config tests tools evidence environment.yml requirements.lock.txt .gitattributes .gitignore` 输出为空（⚠️ **不要求"整个工作树干净"**——main 同时是指挥方改文档的树，文档改动不属本单，**不得代为提交**）
  - ⚠️ **2026-09-16 补 `.gitattributes`**：本单遗留处置新增了仓根 `.gitattributes`（`view/vendor/** -text`，保 vendor 原件逐字节保真），原路径清单**未列它 → 该交付件不被判据覆盖**；`.gitignore` 一并列入（步骤 1 要"确认"它，属本单交付面）。
- [ ] 提交时**未使用** `git add -A`／`git add .`／`git commit -a`；`git log --stat -2` 里**无 `前端任务包/**`、`参考资料/**` 与 `CR-*.md`**（⚠️ 本单已追认为 **2 个 commit**，故看两个提交而非只看 `HEAD`）
- [ ] 开工前置判定已执行：`git status --porcelain -- 前端任务包 参考资料` 的已跟踪条目为空（**判据是"文档路径是否脏"，不比对 commit 号**）
- [ ] `evidence/` 内除占位 README 外**无任何取证物**；确认全仓无 `cache/`／`data/`／`*.brep` 入库（远端为**公开仓**，见 04 §7.1-10）
- [ ] **未发生任何远端写操作**：本单只 `commit`，**无 `git push`／`git remote` 变更**（推送已冻结，04 §5.1-6 冻结令）；汇报里贴 `git status -sb` 与 `git log --oneline -3` 输出

## 禁止事项
- **⛔ 禁一切远端写操作**（`git push`／`push --all`／`push --force`／`git remote set-url|remove|add`）：本包**没有任何单需要推送**，"提交"＝本地 commit；远端实测为**公开仓**且推送已冻结（04 §5.1-6、§7.1-10）→ 若工具或脚本提示 push，**拒绝并停下汇报**
- OCC 装不上（conda-forge 也失败）→ **停下汇报原文报错**，不许换 pip 渠道硬试、不许装任何收费 SDK
- 禁装 §8 清单之外的任何第三方包
