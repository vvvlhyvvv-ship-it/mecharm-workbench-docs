# T08 碰撞三态与禁发双阻断

## 目标
`core/collision.py`：路径沿线的干涉检查（机械臂各部件 vs 工件/模具/周边设备），三态判定+高亮定位；**干涉结果硬锁步骤⑤下发**（按钮禁用+函数内二次校验）。步骤④完整可用。招标 16(3)②③ 的正面回应单。

**2026-09-15 口径扩充（随 CR-2026-03，依据《三维模型导入能力综合技术方案》及其《审阅意见 V1.0》，@user 指示按建议调整）**：
①**权威判定保持 core 侧 OCC 几何级精判不变**——《方案》§3.4"前端加速结构精筛只作用于简化碰撞代理"的口径，**否决为下发依据**（04 §7.2-G13）；
②补**保守包络条款**（契约 §8.4，原 G4 挂账落实）：碰撞检测时机械臂几何须留保守包络 2–5mm（或按 PLC 轮廓偏差参数设定）；
③前端预检层列为**可选增强**（步骤 7）：仅作交互即时提示，不构成判定、不作为下发依据。

**⚠️ 覆盖面口径（2026-09-16 T04 收单连带；04 §7.2-G19，过渡期硬约束）**：

`config/machine.yaml` 的 **11 根臂专用轴**（`FB1 FB2 FC KE RA RC ZA1 ZA2 ZB ZC ZD`）在 `links:` 表里**没有对应连杆条目**（现状 `links:` 只有 13 项，驱动绑定只活在行尾注释）⇒ T04 的 `fk` **不出这 11 根轴的位姿** ⇒ **本单的碰撞判定物理上覆盖不到五套工装臂**。处置四条，缺一不可：

1. **结论口径必须写明覆盖面**：三态卡片、干涉列表、汇报与日志里的结论一律表述为「**已建模的 13 根连杆**范围内 未检出碰撞／预警／干涉」，⛔ **禁**表述为整机「未检出碰撞」。
2. ⛔ **禁把「未检出碰撞」报成「通过」**：未建模臂属**判定空白**，不是**判定为安全**。`CollisionResult.verdict` 在存在未建模臂参与当前工作模式时，UI 须**显式标注**「本模式含 N 根未建模臂，其干涉未校核」（N 从 machine.yaml 现算，⛔ 禁写死 11）。
3. ⛔ **任何会话不得自行给 `links:` 补链**：该缺口属**问甲方动作**（踏勘确认清单待回执），`machine.yaml` 唯一写者是 T03；自行补链＝私改配置＋凭空造几何，两者都按越界打回。T04 已设守卫用例 `tests/test_kinematics.py::test_sample_arm_axes_have_no_link` **锁定现状**——⚠️ 该用例是「**现状锁**」不是「**正确性证明**」，回执到达后由指挥方统一改 `machine.yaml` 并同步该守卫，⛔ 本单不得为让自己通过而改它。
4. **步骤⑤ 下发阻断不因覆盖面缺口放宽**：`send_path()` 的双阻断照旧；覆盖不到 ≠ 可以发。若当前工作模式含未建模臂，下发前须在弹窗内**复述第 2 条的标注文案**，由操作员确认。

> 依据：99 台账 T04 行备注 ⑧（2026-09-16 收单实测 `sorted(set(axes) - bound)` = 上述 11 根）。回执到达前本条一直有效；到达后由指挥方出变更单收口，⛔ 本单不得自行判定「已解决」。

## 前置阅读
02 设计方案 §2 步骤④ ｜ 03 架构 §3 签名 ｜ 01 需求 R-6 ｜ 招标 16(3)②③ ｜ S-1 契约 §8.4（保守包络）｜《审阅意见 V1.0》§五-1

## 步骤
1. 判定：路径按步长离散采姿态（步长=machine.yaml `limits.path_sample_step_mm`，样例配 50；姿态由 T07 的 ik 逐采样点求关节值→fk 得出），每姿态对臂身包围盒做粗筛（AABB），命中者用 **OCC 距离算法**（BRepExtrema）算最小距离真值
2. **保守包络（契约 §8.4）**：机械臂几何按包络值外扩后再参与上述判定；包络参数走 `machine.yaml` limits 节 `collision_envelope_mm`（2–5mm，或按 PLC 轮廓偏差参数；**随 T03 样例提供**），禁字面量散写
3. 三态：d≤0 干涉 / d<clearance_warn_mm 预警 / 其余通过；结果 CollisionResult{verdict, cases:[{seg_id,part_a,part_b,min_dist_mm,point}]}
4. UI：[开始校核]→三态卡片；干涉列表点击行→视口定位+双方红色高亮+顶部红条（文案见 02）；🟡预警允许进⑤但弹窗内显示黄条警告
5. **双阻断**：⑤按钮 disabled 状态 + `send_path()` 函数入口重新读校核状态并独立复判（校核结果带路径指纹 hash，路径改动 hash 失配也拒发）——两条拦截各留日志
6. 性能：单条 20 段路径校核耗时打日志；OCC 精判过慢则启用 FCL 宽相（需先按 §8 走组件引入，禁直接 pip 装）
7. **（可选增强，本单后置或 T10 收口期实施，不属本单必交付）前端预检层**：视口侧用 three-mesh-bvh 对**简化碰撞代理**做粗筛，仅作拖动点位等交互场景的即时提示；**预检结论不构成判定、不作为下发依据、不上屏为最终结论**；组件引入按 03 §8 走准入（three-mesh-bvh，MIT），**其 JS 包须 vendored 到 `view/vendor/`（04 §4 归属：由本单在汇报中报备后落盘，禁 CDN）**；**禁自行 `npm install`／改环境文件**，如需依赖增补走 04 §4 流程
8. pytest：三态用例集自造（两个 BOX 相交/间隙 5mm/间隙 500mm → 🔴🟡🟢）；hash 失配拒发用例；**包络生效用例**（包络内间隙按干涉/预警命中）

## 完成标准
- [ ] 三态用例全对；红条+高亮+列表三通道齐（16(3)③）
- [ ] 包络参数生效：包络内间隙的用例按干涉/预警命中（贴验证输出）
- [ ] DevTools/直调 `send_path()` 在干涉态被拦截且日志留痕
- [ ] 改一个点位后旧校核结果失效（hash 失配拒发）
- [ ] 校核耗时数字在汇报里（不承诺指标，先留实测）
- [ ] **覆盖面标注生效（G19）**：三态卡片与汇报里的结论均带「已建模的 13 根连杆范围内」限定；当前工作模式含未建模臂时，UI 显式标注「本模式含 N 根未建模臂，其干涉未校核」且 N 系现算（贴验证输出，含 N=0 的对照工况）
- [ ] commit：`feat(T08): 碰撞三态与禁发`

## 禁止事项
- 禁把碰撞判定写进前端；禁用显示网格距离冒充 B-Rep 距离（精判必须 OCC 真几何）
- **禁以任何形式的前端预检/网格代理结果替代 core 精判作为下发依据**（04 §7.2-G13）；预检层（若做）不得上屏为最终结论
- 禁改 T04/T05 已验收接口签名
- ⛔ **禁给 `config/machine.yaml` 的 `links:` 自行补链**、禁改守卫用例 `tests/test_kinematics.py::test_sample_arm_axes_have_no_link`（G19 属问甲方缺口，补链＝私改配置＋凭空造几何，两者都按越界打回）
- ⛔ **禁把「未检出碰撞」报成「通过」**：未建模臂是**判定空白**不是**判定为安全**，结论一律带覆盖面限定（G19 第 1–2 条）

## 收单记录（指挥方，2026-09-17 11:11 合并；时点＝merge commit `8afc60a` 的作者时间，可复现）

**验收结论：已过（第 1 轮）——完成标准与收单必检逐项行为级实测通过，已合并入 main（merge commit `8afc60a`，`--no-ff`，零冲突）。**
交付＝`task/T08` 上 **6 笔** `cbaf773…c7892cf`（**27 文件、+2915／−23**），**原样保留、未 squash**（依 §5.1-1 的 T01／T03／T04／T05／T06／T07 先例）；merge-base＝main `ce312d9`。`origin/main` 全程仍 `2569104`（推送冻结令未解）。**禁改文件零改动**：vendor／env／lock／`machine.yaml`／`view/index.html`／`core/kinematics/`／`core/geometry/`／`core/path.py`／`core/config/`／`test_kinematics.py`／`test_config.py`／`comm/` 全清白；`view/vendor/three.module.js` 仍 **1,272,972 B**。

**指挥方合并集成笔 `6f0f0f2`（专属动作，A2 未动 `view/index.html`）**：`view/index.html` 注册 `collision.js`——`collision.js` 再次包裹 `__mecharm_dispatch`、截走 `collision.show`／`collision.focus`，故注册序 **bridge→loader→pick→path→collision**（须叠最外层），A2 交付笔未碰 `index.html`（合规）。

**① 收单必检（数字均为指挥方独立实测，⛔ 不取自报——04 §5.3）**
- **指挥方手算间隙探针**（仓外 `Temp/cmdr_t08_probe.py`，自造合成链、自算 `max(0, x0−403)`）：板 402／403→干涉 `0.0`、415→预警 `12.0`、423／600→未检出，全 PASS；B-Rep 圆柱 `√578−5=19.0416` ≠ AABB `√2·12`（坐实精判用真几何非包围盒）；包络 3.0↔0.5 改值改判定（坐实包络承重）；G19 五模式 N=3/3/3/1/1＋全机 11 现算＋N=0 对照、`describe()` 含「已建模的 13 根连杆范围内」不含「通过」、`coverage()` 含「未校核」＋轴名；指纹 16 位 hex／改点位换／改段型换／`check` 内==独立算——全 PASS。
- **注入探针全 `rc=1`**（仓外沙箱、附7 形态②：确认是断言失败非 rc=2 收集错误）：A2 三条（预警带塌缩／包络减半／措辞松口）经指挥方复跑全 `rc=1`、目标精确咬住、无目标外失败；**指挥方另自加一条**（「无可校核段即抛错」退化成空判定）→ `rc=1` 恰咬 `test_blocked_segments_are_skipped_and_an_all_blocked_path_refuses_to_verdict`（G19-2／附7-① 排除）。
- **禁发双阻断**（offscreen）：F 节干涉态①按钮灰②`send_path()` 直调仍拒＋warning 留痕；G2 绕过作废信号换段→按钮仍 True 但入口重算指纹失配独立拒发；H 节弹窗两出口（[取消] 不发＋留痕／[确认下发] 才发）；K 节包络 3.0→🟡 5.0mm／12.0→🔴 0.0mm、`machine.yaml` 未改（取证只在内存换副本）。
- **视口三态**（offscreen 离屏驱动）：干涉红条 13 处／预警黄条＋⑤可进／通过态撤条且 ⛔ 无「通过」字样／作废态标记清零、定位染 warn 黄非 deny 红、碰撞对象 pickable=0（⛔ 不进拾取目标集）。
- **G13 守住**：`view/js/collision.js`（244 行）只投影 core 结论、⛔ 无前端复算冒充下发依据；障碍侧高亮转内层 `hl.set`、不自己染色。
- **G19 覆盖面**：`coverage()` 的 N 从 `machine.yaml` 现算（⛔ 禁写死 11），守卫 `tests/test_kinematics.py::test_sample_arm_axes_have_no_link` 未改；通过态 ⛔ 不报「通过」。

**② 附加项（合并后在 main 树现测）**
- pytest **226 passed**（main 全量）；`lint_no_magic` **rc=0、扫 54 文件**；`config_check config/machine.yaml` **rc=0**（22 轴 pending WARN 属预期）。
- **附4** >300 行源文件 **0**（T08 各件实测：`core/collision.py` **300**／`app/checkctl.py` **249**／`app/steps/step4_check.py` **188**／`view/js/collision.js` **244**／`tests/test_collision.py` **299**，均 ≤300；单函数 ≤50 行）。⚠️ **三件恰顶 300＝零余量**（`core/collision.py`／`core/kinematics/ik.py`／`app/shell.py`）⇒ T10 收口接线无内联余量。
- **core 纯净**：`core/collision.py` 无 GUI／app／view／comm／numpy import（OCC 允许）；精判只用 B-Rep（AST 核无 `tessellate`／`meshpart`）。
- `evidence/T08/`（脚本＋日志＋PNG）**全用自造基本体、CLEAN of 甲方数据**：敏感串命中全为 A2 自写「⛔ 非甲方工件」合规声明、零件名皆合成。

**③ 增件归属裁定**
- `app/steps/step4_check.py`（188）属 **§4.5-⑦ 预授权预案内**（04 §4 矩阵 L234「`app/panel.py` 装不下 5 步 → `app/steps/step1_import.py`…」，与 T05／T06／T07 同族）；`view/js/collision.js`（244）／`tests/test_collision.py`（299）**预案内**（派单卡明列）。
- `app/checkctl.py`（249，步骤④校核控制器＋双阻断）＋`core/collision.py`（300，新判定层）属**预案外增件 ⇒ 本次追认**，理由＝`app/shell.py` 经 T08 接线后**恰顶 300 行上限、零余量**，校核编排＋双阻断内联必破限（**同 T07 `pathctl.py` 先例**）；**反扩边界＝只覆盖本轮两件**，不构成「app/ 可自由拆件」先例（与 §7.2-G8 核记⑤ `brep_cache.py`、G17 三件、T07 `pathctl.py` 同一处置法）。已登记 04 §4 矩阵 app/ 行与 collision.py 行。
- `app/panel.py`（+22／−11）／`app/shell.py`（+13／−10，**合并后恰 300 行**）为 T08 追加接线（合规，04 §4 矩阵 app/ 行授权名单含 T08）。

**④ A2 自报项裁定**
- **签名偏离** `check(path, scene, clearance_warn_mm)`（冻结签名 `check(path, clearance_warn_mm)` 无几何来源）→ **裁定合规**（新增 `scene:CollisionScene` 第二参作显式数据源，同 T04 `fk(joint_values,model)`／T06 `face_point_from_tri(parts,…)` 先例），03 §3 已回填。
- **`coverage()`「N 根未建模臂」措辞歧义**（一模式一套臂却 N 根未建模**轴**）→ A2 按卡原文出句＋附轴名让 N 可核，**裁定可接受不改**（歧义源自卡片措辞，记前瞻挂账：甲方回执厘清臂↔轴映射后复定）。
- **包络=0→零体积代理盒→OCC 原始错（非人话 `CollisionError`）**→ 生产不可达（`machine.yaml` 只读、现值 3.0），A2 因 `collision.py` 顶 300 行、`config_check.py` 禁改未修，**裁定接受为报备项、登记前瞻挂账**（归配置校验硬化单：加 `collision_envelope_mm>0` 守卫或 collision 侧 clamp）。
- **`/2.0`**＝中点与线性比纯数学量、`lint_no_magic` rc=0，无问题；**`ensureGroup()` 修**（已建过也返回 true，否则二次校核误走「场景未就绪」）＋**stale-hint 修**（reason 空不清提示行）均正确处置；**`Segment` 同名异义**（`core.path` vs `comm/opcua_client/write.py`）异模块异语义，记前瞻注、无需改码。

**⑤ 冻结计数（标「截至 sha」口径，见 99 v26-⑩）**：**截至 `6f0f0f2`＝未推送 69／本地 main 72**（62＋6 交付＋1 merge＋1 集成，逐位实算相符），`origin/main` 仍 **`2569104`**、其 reflog **0** 条 ⇒ **本轮零远端写**；⛔ 推送冻结令未解。

**⑥ worktree**：`t08` **暂不删**（与 `t05`／`t06`／`t07`／`t09` 同处置——@user 指令「05／t09 worktree 不删」，A2 可能仍驻留该目录，待 @user 确认会话退出后由指挥会话一并删；T10 将另建 t10）。

**⑦ 连带回填**：03 §3（`check` 签名＋`CollisionScene`／`CollisionResult`／`CollisionCase`／`Obstacle` 四数据类）、03 §5（新登记 `collision.show`／`collision.focus` 两通道）、03 §9 V1.10、04 §4 矩阵（`core/collision.py`／`view/js/collision.js` 两行＋app/ 行增件归属）、99 v35 与状态表 T08 行＋验收记录 T08 行。

**⑧ 前瞻挂账（登记备查，触发即须整改）**
- **覆盖面措辞**：甲方回执厘清「臂↔轴」映射后，复定 `coverage()` 的「N 根未建模臂」表述（现 N 实为未建模**轴**数）。
- **包络=0 缺口**：归配置校验硬化单——`tools/config_check.py` 加 `collision_envelope_mm>0` 守卫，或 `core/collision.py` 侧 clamp（本件已顶 300 行，加行须先拆件）。
- **G20 两项归 T10**（⛔ 非本单）：G20-① `config_check.py:57` 节点表摘要写死→改 SPEC 驱动；G20-② `e2e_smoke.py` 加 `publish_interval_ms<=50` 守卫＋负对照。
