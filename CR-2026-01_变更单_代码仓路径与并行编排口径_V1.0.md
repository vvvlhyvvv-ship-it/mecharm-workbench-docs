# CR-2026-01 变更单：代码仓路径与并行编排口径

| 字段 | 内容 |
|---|---|
| **变更单编号** | CR-2026-01 |
| **提出日期** | 2026-09-15 |
| **提出方** | WorkBuddy（指挥方） |
| **批准方** | @user（项目经理） |
| **状态** | **待批准（书面签字）** —— CR-1 仓路径已由 @user 于 2026-09-15 23:04 定案（见 §二 CR-1）；⏳ **本单须在 T01 合并前生效**（见 §四 时序） |
| **被变更文件** | `01_需求与决策_操作员UI_V1.0.md` D-7；`00_README_怎么用.txt`（路径与编排口径表述） |
| **技术附件** | `前端任务包\04_多会话并行分工指导_V1.0.md`（全文即本变更的技术说明） |
| **流转依据** | 《软件开发规则与合规交付要求》R-7.1.2；《大型工控软件开发规程》R-8.2（变更须走书面流程） |

---

## 一、变更理由（**原文口径 vs 现行事实**）

| 项 | 原文口径 | 现行事实（2026-09-15 多通道实测） |
|---|---|---|
| 代码仓路径 | `E:\ZCode项目\机械臂工作台` | **`E:\ZCode项目` 不存在**。E 盘本体**存在**（931.5GB / 可用 715.8GB），但**根目录为个人资料盘**（`BaiduNetdiskDownload`、`BaiduSyncdisk`、`FOUND.000`、`22G101-*.pdf`、`AI视频创作`、`JGB`、`PS`、`RoboDK 安装包` 等） |
| 现行仓根（23:04 定案） | — | **`D:\ZCode项目\多功能机械臂软件`** —— **已存在且已是 git 仓库**（`main`、3 次提交、`origin = .../mecharm-workbench-docs.git`、跟踪 22 个文档）→ **代码并入既有仓，T01 不再 `git init`**（详见 §二 CR-1） |
| 工作盘选择 | — | `D:\ZCode项目\` 为**既有工程工作盘**，本项目文档目录已在其下（`D:\ZCode项目\多功能机械臂软件\前端任务包`）；`D:\` 可用 **120.8GB**（2026-09-15 23:13 复测，与 §六 一致） |
| 并行编排 | 00_README：一次会话只推进一单，同一仓同一时刻只进一单 | 采用 **git worktree ＋ `task/TXX` 独占分支**物理隔离工作树，满足"同时只进一单"的实质要求（每单各自独立工作树） |

### ⚠️ 必须一并归档的一项更正（审计链完整性）

本变更单**一度拟以"本机无 E 盘"为由**提出。该结论**已作废**：

- 复核方式：双方各自独立实测。@user 终端实测 `E:\` 存在；指挥方改用 **Python 绝对路径通道**（`os.path.exists` / `shutil.disk_usage`）复测，同样确认 `E:\` 存在（931.5GB / 可用 715.8GB）。
- **误判机制至今未确认**：当时的探测命令输出为空（多通道同时无回显），推测与**会话级 PATH 差异**有关，但**未取得确证**。按报数纪律，本单**不写归因**，只登记"该结论已作废"这一事实。
- 由此确立并登记为纪律（见 04 文档 §7.1-8）：**探测失败 ≠ 事实不存在；命令成功但落在意料之外的同名程序上 ≠ 事实**；凡结论与预期不符，须换独立通道复核。

> 因此，**本变更的成立理由不是"没有 E 盘"，而是"E 盘为个人资料盘且无 `E:\ZCode项目`，为与项目文档目录及既有工程同处工作盘，选定 D 盘"。** 变更单若被引用，请引用本理由。

---

## 二、变更项

### CR-1 代码仓路径（**主变更**）
- **由** `E:\ZCode项目\机械臂工作台`（01/D-7 原文）
- **改为** **`D:\ZCode项目\多功能机械臂软件`**（@user 2026-09-15 23:04 定案）
- **变更链（留痕）**：`E:\ZCode项目\机械臂工作台` →（2026-09-15 20:25 定 `D:\ZCode项目\机械臂工作台`）→（2026-09-15 23:04 定 `D:\ZCode项目\多功能机械臂软件`：**并入项目目录，不再另建"机械臂工作台"目录**）
- ⚠️ **实测事实更正（2026-09-15 23:05，Python 绝对路径 ＋ git 双通道）**：`D:\ZCode项目\多功能机械臂软件` **已存在且已是 git 仓库** —— `main` 分支、3 次提交（最近 2026-09-15 00:01）、**已配远端 `origin = https://github.com/vvvlhyvvv-ship-it/mecharm-workbench-docs.git`**、跟踪 22 个**文档**文件（任务包 00–03/99 ＋ Tasks T01–T10 ＋ 三规程 ＋ S-1 契约 ＋ 沟通单 V1.0 ＋ 排期表与补齐清单）。
  → 故本节原拟的"新建空仓"口径**作废**：**代码并入该既有文档仓**（同一仓、同一 `origin`），**T01 不执行 `git init`**。原"`D:\ZCode项目\机械臂工作台` 不存在（干净起点）"一句系对另一路径的探测结论，对现行路径**不适用**，一并作废。
- **同仓后的两条新增纪律（随本单生效）**：①**功能分支（`task/TXX`）禁改任务包文档与 `CR-*.md`** —— 文档改动只在 `main` 上由指挥方提交（沿用"ZCode 不得改任务包文档"原意并升格为分支纪律），否则每次合并都会撞文档；②`.workbuddy/` 与会话产物已加入 `.gitignore`，**不得入库、不得推远端**。
- 影响：全部 10 张任务单的工作目录、`git` 仓根、worktree 落点、打包产物输出路径；**10 张单与项目文档共处同一工作树**。
- 遗留待确认（1 项，不阻塞 T01）：远端仓名为 `...-docs`（文档仓语义），代码并入后是否另建或改名远端，由 @user 后续决定；不影响本地开发与提交。

### CR-2 并行编排口径
- 采用 04 文档 **方案乙**（3 项放宽：R2 `T05` 提前、R3 `T09` 与内核链并行、R4 `T06` 提前），每项附风险与回退条款。
- 隔离机制：**每单一个 git worktree ＋ 独占分支 `task/TXX`**；`main` 只由指挥会话合并；**实现会话上限 3 个（＋1 指挥会话）**。
- 不可并行的一对：`T04 ∥ T07`（同写 `core/kinematics.py`）、`T05 ∥ T06`（同写 `core/geometry.py`）——解除条件见 CR-4 与 04 文档 §4.5-⑦。
- **`ik` 归属定案：落 T07**（T04 只做 fk / 坐标变换 / 限位）。

### CR-3 Miniforge 安装位置
- 定为 **`D:\Miniforge3`**（静默安装 `/S /D=D:\Miniforge3`）。
- 附带环境事实（2026-09-15 实测）：本机**无 conda / mamba / micromamba / Miniforge3**；系统 Python **3.12.7**（架构要求 3.11）；uv 托管的 3.11 shim **启动失败**（`uv trampoline failed to spawn Python child process / entity not found`，复测仍失败）→ **当前无任何可用 3.11**，只能由 conda 提供。
- 容量监控：`D:\` 可用 121.0GB，Miniforge ＋ pkgs 缓存 ＋ `mecharm` 环境（含 OCC）预计 5–10GB；**低于 20GB 停下汇报**，E 盘为备选落点。

### CR-4 代码粒度硬约束（新增全仓约束）
- **单个源文件 ≤300 物理行、单函数 ≤50 行**；超限按职责拆分（不是压行）；拆分为新文件须先报备。
- 例外白名单：`view/vendor/**`、`environment.yml`、`requirements.lock.txt`、`tests/fixtures/**`、`evidence/**`。
- **热点文件包化＝条件式预授权**（@user 2026-09-15 批复）：`core/kinematics.py` 与 `core/geometry.py` **仅当实测 >300 行**时，对应单可按 04 文档 §4.5-⑦ 预案直接包化，无需再请示；包化实际合并后由指挥方出 **CR-2026-02** 留痕，并把该对放开为"可分头写"。**包化前 `T04∥T07`、`T05∥T06` 仍禁并行。**
- 细则：04 文档 §4.5。

### CR-5 执行层 Q 编号补齐（**缺陷修复，随附申报，非口径变更**）
- `Tasks\` 下 5 处裸 Q 编号已补齐体系名（原意不变，仅补体系名与一句话含义）：
  `T04:23`、`T07:11` → `契约 Q-11（各运动轴清单/行程）`；`T07:7`、`T07:20` → `电Q-8（下发轨迹段格式）`；`T09:13` → `电Q-2（OPC UA 接入参数/安全策略）`。
- 依据：Q 项存在**两套编号体系**（契约 §2.2 表 vs《沟通单》，编号相同、含义不同），④ 规范要求引用可判定归属。**T01 单零 Q 引用，不受影响。**
- 关联挂账：**G9**（契约自身混用两套编号）、**G10**（回执回填错行，改编号修不了）。
- 说明：本项属**消除歧义的缺陷修复**，不改任何技术要求与验收标准，故随本单申报、不单开变更单。

### CR-6 网络与代理告示（**新增风险登记，不改变技术要求**）
- 本机**系统代理已启用** `127.0.0.1:7897`（实测在线）；另有**会话级环境变量代理** `HTTP_PROXY`／`HTTPS_PROXY=http://127.0.0.1:24633`（实测亦在线，**`NO_PROXY` 未设置**）。
- 实测三组通道（直连 / 7897 / 24633）× 三个目标（pypi.org / conda.anaconda.org / TUNA 镜像）**全部 HTTP 200**，直连亦通。
- **处置口径**：下载失败时**先测代理端口在不在线，不要先归因渠道**。
- ⚠️ **"开发机能下载"证明不了"离线能跑"**：D-8 的离线要求（`view/vendor` 本地 three.js、禁运行期 CDN）**必须在断网或禁代理条件下验证**，属 **T10 验收项**。

---

## 三、影响分析

| 影响面 | 说明 | 是否改变技术要求 |
|---|---|---|
| 全部 10 单工作目录 | CR-1 直接决定 `git` 仓根与 worktree 落点 | 否 |
| 排期与波次 | CR-2 决定 W0–W6 波次与并行度 | 否 |
| T01 步骤 | CR-1、CR-3、CR-6 影响 T01 执行内容（建仓位置、Miniforge 落点、网络排查顺序） | 否（仅补充环境事实） |
| 各单交付物行数 | CR-4 新增全仓约束 | **是**（新增硬约束，已获 @user 批准） |
| 验收判据 | CR-5 仅补体系名，不触碰完成标准与禁止事项 | 否 |

**结论**：CR-1～CR-3、CR-5、CR-6 为**口径澄清与环境事实登记**，不改变任何技术要求；**CR-4 为新增硬约束**，已经 @user 于 2026-09-15 明确批准。

---

## 四、执行时序（**关键**）

```
CR-2026-01 批准（本单签字）
        ↓
T01 在既有仓（D:\ZCode项目\多功能机械臂软件）补 .gitignore 与目录骨架
    ＋ 装 Miniforge（D:\Miniforge3）＋ 提交 chore(T01)（origin 已存在，无需新建）
        ↓
T01 汇报 → 指挥方收单复核 → 合并到 main
        ↓
T02 / T03 并行开工（各自 worktree，落 D:\ZCode项目\多功能机械臂软件-wt\<tXX>）
```

> ⚠️ **本单须在 T01 合并前生效**（@user 2026-09-15 指示）：T01 一提交，**仓的 `origin` 与代码落点即成为既成事实**。若变更单晚于建仓，将出现"代码路径先于其书面依据存在"的审计断链。
>
> ⚠️ **补充实测（2026-09-15 23:05）**：`origin`（`mecharm-workbench-docs`）**在本次实测中已存在并已有 3 次文档提交** —— 即"远端先于书面依据"的情形在**文档维度上已既成事实**；本单的作用是让**代码并轨该 origin** 这一决定有书面依据，并补齐文档仓既定事实的登记。

---

## 五、批准

| 角色 | 姓名 / 代号 | 意见 | 日期 |
|---|---|---|---|
| 提出 | WorkBuddy（指挥方） | 提请批准 | 2026-09-15 |
| 批准 | @user（项目经理） | **CR-1 仓路径已定：`D:\ZCode项目\多功能机械臂软件`**（2026-09-15 23:04 口述确认）；其余各项（CR-2～CR-6）此前已分别口头批准，待书面签字 | 2026-09-15（待签） |

---

## 六、附：本单记录的环境事实（供后续会话与审计引用，均实测）

| 项 | 实测值 | 通道 |
|---|---|---|
| `D:\ZCode项目\多功能机械臂软件`（**现行仓根**） | **存在，且已是 git 仓库**：`main` 分支、3 次提交、`origin = .../mecharm-workbench-docs.git`、跟踪 22 个文档文件 | Python `pathlib` ＋ `git log` / `git remote` / `git ls-files` |
| 该仓工作树状态（2026-09-15 23:05） | **11 个文档已修改未提交 ＋ 6 个新文件未跟踪**（含 `.workbuddy/`、CR-2026-01/03、沟通单 V1.1 草案、04、05）→ 须先提交，再交付 T01 | `git status --porcelain` |
| `D:\ZCode项目\机械臂工作台` | **不存在**（原拟路径，已弃用） | Python `os.path.exists` |
| `E:\ZCode项目` | **不存在** | 同上 |
| `E:\` / `D:\` / `C:\` 可用空间 | 715.8GB / **120.8GB** / 48.0GB | `shutil.disk_usage` |
| conda / mamba / micromamba | **全无** | `shutil.which` |
| 系统 Python | **3.12.7** | 绝对路径直调 |
| git | **2.50.0.windows.1**（`C:\Program Files\Git\cmd\git.exe`） | 绝对路径直调 |
| git 身份配置 | **未配置**（`user.name`／`user.email` 在 local 与 global **均为空**）；既有 3 次提交作者为 `lhy <vvvlhyvvv@gmail.com>`（临时配置写入）→ **T01 须先固化身份，否则提交失败** | `git config --list` ＋ `git log --format` |
| uv 托管的 3.11 | **不可用**（trampoline / entity not found） | 绝对路径直调，复测两次 |
| conda-forge `pythonocc-core` | 最新 **7.9.3**（PyPI 无此包） | 渠道索引 |
| 网络出口 | 三通道 × 三目标 **全 200**（直连亦通） | `urllib` + `ssl` |

| 版本 | 日期 | 修订内容 | 编制 |
|---|---|---|---|
| V1.0 | 2026-09-15 21:40 | 首次编制：CR-1 仓路径（理由改写，含"无 E 盘"作废声明与机制未确认说明）、CR-2 并行编排口径、CR-3 Miniforge 位置、CR-4 代码粒度硬约束、CR-5 执行层 Q 编号补齐（缺陷修复）、CR-6 网络与代理告示；含执行时序与实测环境事实附表 | WorkBuddy（@user 指示） |
| V1.1 | 2026-09-15 23:10 | **CR-1 定案修订**：①仓路径改为 **`D:\ZCode项目\多功能机械臂软件`**（@user 23:04 定案，并入项目目录）＋记录三次变更链；②**实测事实更正**——该目录已是 git 仓（`main`、3 次提交、`origin=mecharm-workbench-docs`、跟踪 22 个文档），原"新建空仓"与"机械臂工作台 不存在即干净起点"口径**作废**，代码改为并入既有文档仓、T01 不再 `git init`；③新增同仓两条纪律（功能分支禁改文档与 CR；`.workbuddy/` 入 `.gitignore`）；④执行时序与环境事实附表同步更正；⑤批准表登记 CR-1 已定 | WorkBuddy（@user 23:04 口述定案） |
| V1.2 | 2026-09-15 23:30 | @user 第五轮审计修正：①§一 增"**现行仓根（23:04 定案）**"行（登记该目录已存在且已是 git 仓）；②§一 工作盘容量 **121.0→120.8GB**（与 §六 实测值对齐）；③§六 环境事实表已含 **git 身份未配置**（T01 第 0 步固化）与工作树状态两类新实测项 | WorkBuddy（@user 审计） |
