# T13 顶栏重构、StepBar 退役与左右栏范式切换

## 目标
一次性把导航范式从「五步步骤条＋右栏上下文页」切到演示稿形态：顶栏七大区、左栏三页签（暂挂旧业务页）、右栏常驻双面板、StepBar 退役为薄壳。**本单后产品全流程必须照常可用**（旧步骤页暂挂进页签，成形归 T14/T15）。

## 前置阅读
00 README §五纪律 ｜ 01 蓝图 **V1.2** §2 骨架＋「尺寸与节奏」表、§3.3 顶栏、§3.4 左栏（容器＋工具行＋选中件装配）、§3.5 视口、§3.6 右栏双面板、**§7 差异表 Δ-1／Δ-3／Δ-6／Δ-7／Δ-8／Δ-9／Δ-10／Δ-11** ｜ 99 台账「文件归属矩阵」＋ **L-7（StepBar 薄壳）／L-8（零余量）** ｜ 演示稿画面 03/11 ｜ 现读 `app/shell.py`、`app/wiring.py`（T12 抽出的件）、`app/stepbar.py`、`app/mode.py`、`app/assemblytree.py`、`app/panel.py`、`app/steps/`、`config/machine.yaml`（`modes`／`axes`／`links`／`opcua.read_nodes`）、`core/collision.py:126`

> ⚠️ **前置：T12 必须已收单**。T12 独占 `app/shell.py` 并已把控制器构造/信号连接抽到 `app/wiring.py`（目标 shell.py ≤ 约 220 行）。开工第一件事＝`wc -l app/shell.py` 报数；**若 T12 未瘦身或仍 300 行，停下上报**，不得边重排边超标（L-8）。
> ⚠️ 差异编号一律 `Δ-x`。旧卡面写的 `D-1/D-7/D-8` 是 V1.0 误用——`D-x` 只指 `前端任务包/01_需求与决策_操作员UI_V1.0.md:40–47` 的已决事项，同号不同义（§7 编号口径）。

## 文件归属（唯一写者）
- 改：`app/shell.py`（装配重排；**接 T12 续写，两单串行**）、`app/mode.py`（`WorkModeSelector` 迁右栏改造；`ModeBadge`/`OnlineLight` 并入顶栏灯组）、`app/assemblytree.py`（挂入页签＋工具行）、`app/panel.py`（五步容器退役为暂挂宿主）、**`app/stepbar.py`（改造为薄壳，⛔ 不 git rm，见步骤 1）**、`core/geometry/**`（**仅新增删除/重建纯函数＋pytest**）、`tools/e2e_smoke.py`＋`tools/e2e_rig.py`（导航判据改页签驱动；本单可与 T12 已改的 e2e 串行合入）
- 增件（文件名已预声明）：`app/topbar.py`（顶栏七区）、`app/tabshell.py`（左栏三页签容器）、`app/prog_tab.py`／`app/sim_tab.py`（编程/仿真页签体**暂挂初版**——旧 step 页堆叠；两件后续归 T14/T15 整体重写，本单只建初版）、`app/workhead_panel.py`（工作头面板）、`app/joints_panel.py`（机构与关节面板）、`app/chips.py`（数据芯片＋双灯＋使能钮）、`tools/e2e_tabs.py`（页签导航判据，L-2 预授权拆件）
- ⛔ 禁改：`app/theme.py`（T11 已收单）、`app/splash.py`／`app/stage.py`／`config/ui.yaml`（T12 已收单；**只可读，需加键先停下上报**）、`app/statusbar.py`、`app/checkctl.py`／`pathctl.py`／`sendctl.py`／`app/steps/**`（**非本单归属**，见步骤 1）、`comm/**`、`view/js/**`、`core/collision.py`（已满 300/300）

> **`core/` 授权范围（裁决 6）**：本单**获授权改 `core/geometry/`，但只增纯函数**（删除选中件／删除整组／重建 parts），带 pytest；⛔ 禁改 T05/T06 已验收件的既有行为与签名，⛔ 禁动 `core/collision.py`、`core/path.py`、`core/config/`。旧卡「禁动 core/」一句据此放宽，放宽范围**仅限上述**。

## 步骤
1. **StepBar 退役为薄壳（⛔ 不是 `git rm`，L-7）**：`git rm app/stepbar.py` 会当场断掉本单**不拥有**的 8 个源文件（`app/checkctl.py:53,58,150`、`app/pathctl.py:47,51,73,135`、`app/sendctl.py:57,61,181,241,246`、`app/steps/step1_import.py:22,93`、`step2_pick.py:29,64`、`step3_path.py:29,68`、`step4_check.py:31,67`、`step5_send.py:30,64`）、`tools/README.md`，以及 3 个**已冻结**取证脚本（`evidence/T07/_drive_step3.py`、`evidence/T07/_drive_shell_path.py`、`evidence/T08/_rig.py`）。处置：
   - `app/stepbar.py` 保留 `STEP_LABELS` 与 `StepBar` 类；`self.stepbar` 在 shell 里**仍实例化但不加入任何布局**（不再显示），使 `mark_completed()`／`e2e_rig.py:91 step_clicked.emit(n)`／`e2e_smoke.py:188 stepbar._completed[4]` 全部照常
   - 导航职责移交页签；**步骤完成态改由页签徽标＋页内四步流水线指示（T14）表达**
   - 文件顶部加一行退役说明（指向 99 台账 L-7），⛔ 禁写「已删除」类误导注释
   - step1–5 的 `STEP_LABELS` 引用由 T14/T15/T16 各自清理，**最后清理者（或指挥方）删件**
2. `app/topbar.py` 按 §3.3 顺序建七区（**注意：无模式组，Δ-1**）：
   1. 品牌区（LOGO 占位＋`brand_name`＋`brand_sub`，全读 ui.yaml，⛔ 禁字面量）
   2. 菜单按钮组：三维导入（徽标＝件数）｜装配体模式（**禁用**＋「待后续版本」）｜测量（**禁用**＋同提示）｜报警与日志（徽标位**留接口**，T16 接数）。演示稿此组末的「21:9 预览」钮**不落地**（Δ-8）
   3. **使能按钮（Δ-11）**：**渲染两态外形但一律禁用**＋tooltip「待契约回执」。点表**故意未收** `enable_mask`（`config/machine.yaml:195–197`，契约 §6.1 偏移 77，EnableMask 能否吞并 Reserved2 是 V1.3 @user 悬置项 CR-2026-03 §三-5）⇒ 无数据源只能禁用；⛔ **不得拿 `status` 位（ST_READY 等）推导「使能」**——使能≠就绪，语义未裁。旧卡「回读使能态，离线灰」一句作废
   4. 数据芯片三枚（等宽数字）：渲染 fps＝桥 `perf.fps` 实测｜状态同步 Hz＝回读帧率实测｜碰撞检测 ms＝`CollisionResult.elapsed_ms` 实测；未测得一律「—」（Δ-6，⛔ 禁填 58/10/2.4）
   5. 双灯：①实时链路灯（在线绿/离线灰＋「实时 N Hz」或「离线」，由现 `OnlineLight` 迁移）②本次轨迹判定灯（通过绿/不通过红＋步数，由现校核结论迁移）
   6. 用户盒：头像字＋姓名＋工号＋日期时间（走时）＋「退出」（退出＝回登录页并**回 std 档**，调 T12 留的信号）
3. `app/tabshell.py` 三页签（页签头高 36px）：**装配树**（挂现有树＋工具行）｜**编程**＝实例化 `prog_tab.ProgTab`（暂挂初版＝step1–3 现页堆叠，标「T14 重排中」注释）｜**路径仿真**＝实例化 `sim_tab.SimTab`（暂挂初版＝step4–5 现页堆叠＋干涉清单空容器挂点）。页签徽标＝件数/工步数/干涉数（**真实值，无则 0**）
4. **装配树工具行（裁决 6：本期做）**＝「删除选中／删除整组／清空全部模型」三钮：
   - 「清空全部模型」＝发 `mesh.load {reset:true, parts:[]}`（**零新桥通道**，先例 `app/shell.py:252`）；清空类动作**二次确认**
   - 「删除选中／删除整组」＝在 `core/geometry/` **新增纯函数**重建 parts＋沿用现路径/校核失效机制；**未选中时「删除选中」保持 `disabled`**（演示稿同位本就禁用）
   - **「选中件装配」三选（固定／关节绑定／自由）＋「绑定到〈轴〉」＋应用/解除＝控件按演示稿位置渲染但一律禁用**＋「待后续版本」（Δ-9，裁决 6 推期 3）。理由＝桥 `pose.update` 按**连杆名**下发、装不下任意导入件；`config/machine.yaml:134` 明写 `length_mm` **全部为占位 0.0**（待契约 Q-11 图纸回执），13 条 links 只覆盖 11 轴 ⇒ 现在做等于在占位数据上承诺姿态（违 §1-1）
5. `app/workhead_panel.py`：五臂列表读 `machine.yaml` `modes`（编号①–⑤＋名称，实测 5 条：打磨 9 轴／氧化皮吸附 9 轴／脱模剂喷涂 9 轴／氧化皮破碎 7 轴／玻璃垫放置 7 轴），选中＝★当前主工作头（**沿用原 `WorkModeSelector` 的选定/作废语义与二次确认，G16 行为不变**）。参数行：**「行程范围」按该模式 `axes[].travel` 汇总得出（真值）；「额定负载」与「末端形式」因 `modes[]` 实测只有 `id`/`name`/`axes` 三键 ⇒ 恒显「—」**（§1-1，⛔ 禁填演示值）；「特写跟随 TCP」禁用＋如实提示（Δ-7）
6. `app/joints_panel.py`：**22 轴四组分组（不要按 `links` 简单分组）**——①「共用机构」＝X1/Z1/Y1/X2/Z2/X3（五模式都含）②「当前工作头〈模式名〉」＝当前模式专属轴（如打磨：ZA1/ZA2/RA）③「安装架」＝YA/YB/YC/YD/YE ④「**未建模机构**」＝其余无连杆轴，**直接复用现成纯函数 `core/collision.py:126 unmodeled_axes(cfg, scope)`**（G19 守卫已覆盖，⛔ 不另写一套判定）
   - 行＝轴 id＋当前值＋单位（联动时实时刷新）；**回读覆盖只有 8/22**：`opcua.pack_profile: v1_2_8axis` ⇒ `Pos[0..7]`/`Vel[0..7]` 按 `axes` 表序对应 **X1/Z1/Y1/X2/Z2/X3/YA/YB**（`machine.yaml:206–223`）；其余 **14 轴（YC/YD/YE/ZA1/ZA2/RA/ZB/FB1/FB2/ZC/FC/RC/ZD/KE）即使链路在线也无回读 ⇒ 恒显「—」＋如实标注「点表未收」**，⛔ **不得因「在线」就显示 0.0**；离线时全部「—」
   - `axes[].pending: true` 的轴加「待回执」徽标（yaml 真字段）
   - 轴点动区（−/＋/回零/速度）**整块禁用**＋「待后续版本」（Δ-7；点动＝写命令，`CMD_HOME` 位在册但模拟器明写「不建模回零轨迹…猜轨迹即越权」`comm/plc_logic.py:104–106`）
   - 软限位提示行（达到限位值的轴数，黄）：数据源＝`axes[].travel` 与回读值；**无回读的轴不计入**（不得把「—」当越限或当正常）；无数据「—」
7. `app/shell.py` 重排：顶栏＋（左 tabshell｜中 viewpane｜右双面板）＋底栏；StepBar 不再进布局（步骤 1）；**五臂选定锁步骤②的机制改为锁「编程页签取点区」（语义不变、挂点变）**；`_toggle_left`/`_toggle_right`（现 `:279–287`）**保留不删**（Δ-10，反向差异，收单不判「与演示稿不一致」），位置避让演示稿元素即可。视口保持**单视口**（双视口＝期 2.5 备选，Δ-3）；⛔ **不做视口工具行占位空条**（期 3）
8. **挂点预埋（波次 3 并行的前提，本单必须做好）**：
   - ① 顶栏「三维导入」按钮**可用**，点击默认弹「待后续版本」提示框；`topbar` 暴露 `import_requested` 信号——T14 连接该信号即替换为真浮层，**不需回改 `topbar.py`**
   - ② `tabshell` 实例化 `prog_tab.ProgTab`／`sim_tab.SimTab`（**import 类名固定**），T14/T15 重写两文件内容即可换体，**不需回改 `tabshell.py`**
   - ③ `sim_tab` 暂挂初版里为「干涉清单区」留空容器＋注释挂点（T16 填）；step4 三态卡暂挂其中（T16 退役吸收，T15 重写时不迁入）
9. e2e 适配：导航从步骤条点击改为页签切换，判据**逐条对应迁移（编号不变、行为不变）**；页签导航判据落新文件 `tools/e2e_tabs.py`（L-2 预授权）；`publish_interval_ms` 守卫等非 UI 判据原样；角标 Hz/fps **双数同时在场**守卫不得丢

## 完成标准
- [ ] 全流程 e2e rc=0（导入→取点→路径→校核→下发→跟随，**页签导航版**；贴原始输出）
- [ ] 顶栏七区（**无模式组**）＋三页签＋右栏双面板截图对照演示稿画面 03（贴 evidence/T13/；**截图用取证档 `--ui-profile evidence`**）
- [ ] **差异清单逐条自证**：Δ-1 无模式组／Δ-7 装配体模式·测量·轴点动·特写跟随 TCP 全禁用＋提示／Δ-8 无 21:9 预览钮／Δ-9 选中件装配全禁用／Δ-10 折叠钮仍在／Δ-11 使能钮禁用＋「待契约回执」／Δ-3 单视口且无工具行空条／Δ-6 无演示数字（每条一张截图或一行 grep 输出）
- [ ] 离线时芯片/轴值/使能全部「—」或灰态，无演示数字（截图佐证）；**在线时 8 轴有值、另 14 轴仍为「—」＋「点表未收」标注**（截图佐证，这是本单最易造假处）；22 轴面板 pending「待回执」徽标可见
- [ ] 装配树工具行三钮实测：清空后视口与件数归零（二次确认在场）；删除选中/整组后路径与校核**如实失效**；未选中时「删除选中」为 disabled（贴输出/截图）
- [ ] **StepBar 薄壳判据（改写自旧「grep 0 命中」）**：①界面上**看不到**步骤条（截图）②`app/stepbar.py` 仍在且 `STEP_LABELS`／`StepBar` 仍可 import ③`app/checkctl.py`／`pathctl.py`／`sendctl.py`／`app/steps/**` **本单零改动**（贴 `git diff --stat` 证明）④3 个冻结取证脚本可照常运行（贴 rc）⑤全仓 grep `stepbar` 命中数与收单时**如实登记**（不要求 0），并列出 T14/T15/T16 各自待清理行号
- [ ] `wc -l` 报数：`app/shell.py` ≤300、新增七件各 ≤300、`core/geometry/` 改动件 ≤300（贴全部数字）
- [ ] pytest 全绿（含 `core/geometry/` 新增纯函数的测试）；`tools/config_check.py` rc=0；`tools/lint_no_magic.py` rc=0——**均贴原始输出**，不得只写「已通过」
- [ ] commit：`feat(T13): 顶栏重构与左右栏范式切换`

## 禁止事项
- 禁落顶栏模式组（Δ-1）；禁启用力 3 功能（Δ-7）；禁造轴中文名/演示值（轴显示 id 即可，中文命名等甲方回执）；**禁把使能钮接 `status` 位**（Δ-11）
- 禁动 `comm/`、`view/js/`（本单为壳层＋`core/geometry/` 纯函数；`core/` 其余件与 `app/theme.py`／`app/statusbar.py`／T12 新增件一律禁改）
- e2e 判据**只迁移不改语义**；⛔ 禁删/改角标 Hz/fps 双数守卫与 `FreqBadge`／`set_freq`
- ⛔ **禁 `git rm app/stepbar.py`**、禁改 `app/checkctl.py`／`pathctl.py`／`sendctl.py`／`app/steps/**`／`evidence/**`（L-7）
- ⛔ 禁自行给 `config/ui.yaml` 加键（白名单校验器会静默丢弃，且该件归 T12）；需要新文案先停下上报
- ⛔ 禁自造未预声明的新源文件名；禁在任何件超 300 行后继续加行（L-8：停下上报）

## 收单记录（指挥侧回填）

- **2026-09-19 收单＝已过（一轮过）**。feat `00911ab` → merge `e641b15`（no-ff 入 main，分支已删）。执行侧以《T13_收单通知》文件代呈（已阅后删）。
- **四项检查指挥侧复跑全绿**：pytest **283 passed**（=T12 的 273＋`test_geometry_edit` 10 例；首轮 1 例 `test_opcua` 回读节奏用例因并行负载时敏抖动失败，单测＋整文件＋全量三重复跑均绿，判非回归）；e2e_smoke **27 PASS·0 FAIL**（A–H 19 条原编号全过＋T1–T8 页签导航版；**T6「22 轴分组·回读 8/22」以运行断言钉死在线 8 轴有值、14 轴恒「—」**——08 图视觉歧义据此排除）；config_check rc=0；lint_no_magic rc=0（扫 72 文件）。
- **StepBar 薄壳（L-7）五判据全过**：①截图＋e2e T2 证不可见②文件在、`STEP_LABELS`/`StepBar` 可 import（按钮数 5）③`checkctl/pathctl/sendctl/steps/**`＋theme/statusbar/splash/stage/config/comm/view-js/evidence-T07T08 **`git diff --stat` 为空**（指挥侧复核）④三冻结脚本复跑 **rc=0×3**（`_drive_step3` 系 T12 改 `StatusBar` 签名波及的冻结件回归——main 上同样失败、非本单引入；指挥侧最小修复＝该脚本补 `load_ui` 实参，详见下方指挥侧处置）⑤全仓 grep `stepbar`＝**36 处**如实登记；待清理行号：T14＝`step1:22,93`／`step2:29,64`／`step3:29,68`，T15＝`step5:30,64`，T16＝`step4:31,67`。
- **目检**：04 顶栏七区（无模式组 Δ-1／使能禁用＋「待契约回执」Δ-11／三芯片「—」Δ-6／装配体模式·测量禁用 Δ-7／无 21:9 钮 Δ-8／ModeBadge＋双灯在场）与 08 右栏在线态（四组分组、8 轴 0.0 真零位、其余「—」＋「点表未收」、「待回执」徽标、软限位黄行、点动整块禁用）逐项判过，无身份类文字；其余七张以 e2e T1–T8 判据＋代码抽查代证；范式全貌另经指挥侧离屏全窗截图直核。
- **行数**：shell 287（净增 6≤19 预算）／wiring 200（`build_controllers` 签名未动）／topbar138·tabshell86·chips121·prog_tab37·sim_tab43·workhead89·joints204·edit_model56·e2e_tabs232·e2e_smoke296 全部 ≤300。**增件九件全部预声明，零预案外**（e2e_rig +15 属卡面归属）。
- **指挥侧处置三件（随收单提交）**：①修复 `evidence/T07/_drive_step3.py`（T12 回归补参）→ 三冻结脚本全 rc=0，复跑重写的 5 张 T07 PNG 已按冻结纪律还原；②`theme.py` 补页签 `state=current/todo` QSS（T13 建件时本表冻结所致协调缺口；离屏截图目检：选中态青色底边条＋区分明显）；③Δ-12 登记（见下）。
- **四项遗留裁决**：①冻结脚本回归→已修复（本节上条），**T12 收单记录补记漏检**；②ModeBadge 留顶栏灯组→**判保留，登记 Δ-12 反向差异**（蓝图 V1.3：`livectl`／G7 依赖 `win.badge`，拆除即断已验收行为）；③页签/报警徽标胶囊→**页签按钮状态样式已由指挥侧补 QSS**；真胶囊徽标（按钮内嵌子件重构）登记 **L-11 期 3**；④软限位「8 个轴已达行程设定值」＝占位行程下界与零位 0.0 重合的边界含入，**如实呈现判不算错**，登记 L-12（回执填真行程自解；对外素材录制时留意）。
- 连带回填：**L-2 闭环**（e2e_tabs.py 已落地，232 行 T1–T8）；L-7 落地；B-2 触发不变。波次 2 收口，波次 3a（T14∥T15）可派。
