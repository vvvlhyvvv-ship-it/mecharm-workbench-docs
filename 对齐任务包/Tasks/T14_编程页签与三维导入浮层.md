# T14 编程页签成形与三维导入浮层（基础版）

## 目标
把暂挂的 step1–3 重排为演示稿「编程」页签形态：四步流水线指示、程序·步骤清单、工具区、校验结论行；顶栏「三维导入」唤出基础版导入浮层。**语义全部沿用 T06/T07 冻结行为，只换容器与呈现**。并清理 step1–3 对 `STEP_LABELS` 的引用（StepBar 退役第一棒，99 台账 L-7）。

## 前置阅读
00 README §五纪律 ｜ 01 蓝图 **V1.2** §2「尺寸与节奏」表、§3.4 编程页签、§3.8 三维导入浮层、§4-B/C（字号与控件高令牌）、§6.1、**§7 Δ-4／Δ-6／Δ-7** ｜ 99 台账「文件归属矩阵」＋**L-7** ｜ 演示稿画面 04/07 ｜ 现读 `app/steps/step1_import.py`、**`app/steps/step2_pick.py`**（⚠️ 旧卡误写 `step2_points.py`，仓内无此件）、`app/steps/step3_path.py`、`app/pathctl.py`、`app/stepbar.py`、`core/project.py`、`app/prog_tab.py`（T13 暂挂初版）、`app/topbar.py`（只读其 `import_requested` 信号）

## 文件归属（唯一写者）
- 改：`app/steps/step1_import.py`（182/300，浮层化＋清 `STEP_LABELS`:22,93）、`app/steps/step2_pick.py`（239/300，迁入页签体重排＋清 `STEP_LABELS`:29,64）、`app/steps/step3_path.py`（242/300，同上＋清 `:29,68`）、`app/pathctl.py`（255/300，「生成并校验轨迹」合并语义接线）、`app/prog_tab.py`（**整体重写** T13 暂挂初版）、`tools/e2e_tabs.py`（追加本单判据）
- 增件：`app/pop_import.py`（导入浮层，连接 `topbar.import_requested` 信号，**⛔ 禁改 `topbar.py`**）、`tests/test_prog_export.py`（CSV/JSON 导出导入往返）
- ⛔ 禁改：`core/path.py`／`core/collision.py`（T15/T16 域，且 collision 已满 300/300）、**`core/project.py`（现 299/300，只剩 1 行余量——本单只可读，需加键/加函数先停下上报）**、`app/topbar.py`／`tabshell.py`／`chips.py`／左右面板件（T13 已收单）、`app/sim_tab.py`／`sendctl.py`／`handshake.py`（**T15 并行域**）、`app/checkctl.py`／`step4_check.py`／`view/js/collision.js`（T16 域）、`app/theme.py`、`config/**`、`comm/**`

> **e2e 归属（L-2）**：`tools/e2e_smoke.py`／`e2e_rig.py` 跨卡按**收单顺序串行合入**；本单判据一律追加到 `tools/e2e_tabs.py`（T13 已预授权拆出），⛔ 禁改 T15 正在写的同名文件段。与 T15 并行时若两单都要动 `e2e_tabs.py`，**后收单者 rebase**。

## 步骤
1. `app/prog_tab.py` 顶部：**四步流水线指示**（①导入模型→②选择面→③生成轨迹→④PLC 输出；done/now 两态由**真实状态**驱动：已导入/已取点/已生成/已输出，**仅指示不可点导航**）。此处即 StepBar 完成态的**新表达载体**（L-7）——徽标/指示必须能被 e2e 判据读到
2. 程序·步骤清单：行＝序号＋名称＋坐标（**等宽字**，走 T11 的字体 helper，⛔ 禁散写字体名）；干涉步红行（读校核结果，T16 前用现三态结果的干涉段标红）；底部提示行「共 N 步」（真实值）
3. 工具区：连续/单步/⏸/⏹复位/倍率——**倍率＝SpeedOverride 接线**（⚠️ T15 的执行控制组也接同一 SpeedOverride：本单只接**编程页签内这一处控件**，数据源与写入路径复用现成件，⛔ 禁自造第二套倍率状态）；连续/单步/复位**期 3 前禁用＋如实提示**（Δ-7）；**插入动作按钮组渲染但禁用**（「待工艺口径」，Δ-7）；选点偏移 mm 输入（并入取点流程：新点位 Z 向偏移，非法值红框）；段型 C-Lin/J-Lin＝现段型控件的演示稿命名（**点位型/轮廓型语义不变**，tooltip 全中文）；保存/导出 JSON/导出 CSV/导入/清空（清空**二次确认**）——数据走 `core/project` proj/v1（**只读其 API，禁改该件**），导出 CSV 列＝序号/名称/X/Y/Z/类型
4. 底部：「生成并校验轨迹」（＝生成路径＋**自动校核**合并语义）→ 校验结论行（通过/不通过 · N 处干涉 · 下发许可 允许/禁止，**三通道呈现**：颜色＋图标＋文字）；「下发执行」按钮＝**跳转路径仿真页签**（Δ-4，⛔ 不做第二套下发）
5. `app/pop_import.py`：顶栏「三维导入」唤出浮层（Esc/遮罩关闭）＝拖拽区＋选文件兜底＋三阶段进度＋结果读数（件/面/耗时，**真实值**）；**复用 step1 现有导入管线与缓存，⛔ 不重写导入逻辑**；「目录命名即认领」「模型规格清单」＝期 3（浮层内留**禁用占位区**＋如实提示）。浮层圆角 8px（§4-C 模态档）
6. step2/step3 原交互（半透明操作态、悬停高亮、红框拒值、超行程校验、失效机制）**逐条保留**；**工作模式未选定→取点区锁定提示不变**（T13 已把锁挂点从「步骤②」移到「编程页签取点区」，语义不变）
7. 清理 `STEP_LABELS`：step1–3 三件内引用改为本页签内的等价表达（或直接删除该依赖），**清理后在收单单里列出剩余命中行号**交 T15/T16 继续（step4:31,67／step5:30,64）；⛔ 禁动 `app/stepbar.py` 本体（L-7：最后清理者或指挥方删件）

## 完成标准
- [ ] e2e rc=0（页签内取点→生成并校核→结论行三态）；导出 CSV→清空→导入→**步骤一致**（贴往返原始输出，含件数/步数对照）
- [ ] 编程页签截图对照演示稿画面 04；导入浮层截图对照画面 07（**截图用取证档 `--ui-profile evidence`**；基础版差异＝认领/规格清单禁用）贴 evidence/T14/
- [ ] 四步指示由**真实状态**驱动（导入前后/生成前后各两态截图）；插入动作组＋连续/单步/复位全禁用可见＋提示文案在场（Δ-7 自证）
- [ ] 「下发执行」为**跳转**而非第二套下发（Δ-4 自证：贴点击后页签切换的截图或 e2e 判据）
- [ ] `STEP_LABELS` 清理如实登记：贴 `grep -rn "STEP_LABELS" app/ tools/` 输出，标明 step4/step5 仍待 T15/T16 清理（**不要求 0 命中**）
- [ ] `wc -l` 报数：本单改动的每个件 ≤300（贴全部数字）；`core/project.py`／`core/path.py`／`core/collision.py` **diff=0**（贴 `git diff --stat` 证明）
- [ ] pytest 全绿（含导出往返）；`tools/config_check.py` rc=0；`tools/lint_no_magic.py` rc=0——**均贴原始输出**，不得只写「已通过」
- [ ] commit：`feat(T14): 编程页签与三维导入浮层`

## 禁止事项
- 禁改 `core/path.py` 与 `core/collision.py` 判定语义（T15/T16 域）；禁实现插入动作/认领规则/规格清单（期 3）；**禁把「生成并校验」做成只生成不校核**
- 上屏文案禁 blending/PTP/LIN 等术语（沿 02 §2 步骤③禁令；C-Lin/J-Lin **仅作段型下拉的短代号显示**，主文案全中文）；⛔ 禁出现 TXX/GXX/电Q-x 等内部编号（§1-5）
- ⛔ 禁造演示数字（26 件/24 步/128 点/18.0 m 等一律 Δ-6）；未生成时一律「—」
- ⛔ 禁改 `app/topbar.py`（挂点已在 T13 预埋，连信号即可）；禁改 `config/ui.yaml`（需加键先停下上报）
- ⛔ 禁改 T15 并行域（`app/sim_tab.py`／`app/sendctl.py`／`comm/opcua_client/handshake.py`／`view/js/path.js`）；禁新增桥 dispatch type

## 收单记录（指挥侧回填）
