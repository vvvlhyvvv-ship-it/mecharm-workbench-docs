# 对齐进度台账（T11–T17）

> 状态以本文件为准；收单记录回填各卡末节。口径：待派／进行中／已合并／阻塞。

## 状态表

| 单 | 内容 | 波次 | 前置 | 状态 | 分支 | 验收轮次 |
|---|---|---|---|---|---|---|
| T11 | 主题令牌与视觉基线 | 1（∥T12） | — | **已合并（收单已过 2026-09-19）** | task/T11（merge `cfe3fdd`，分支已删） | 1（一轮过） |
| T12 | 载入登录与舞台缩放＋ui.yaml | 1（∥T11） | — | **已合并（收单已过 2026-09-19）** | task/T12（merge `0095f02`，分支已删、worktree 已撤） | 1（一轮过；shell 281 偏差判可收） |
| T13 | 顶栏重构与左右栏范式切换（含挂点预埋） | 2 | T12 | **已合并（收单已过 2026-09-19）** | task/T13（merge `e641b15`，分支已删） | 1（一轮过；四遗留已裁） |
| T14 | 编程页签与三维导入浮层 | 3a（∥T15） | T13 | **已合并（收单已过 2026-09-19）** | task/T14（merge `b4d59c5`，feat `dbaf66f`） | 1（一轮过；wiring 12 行越卡胶水判可收） |
| T15 | 路径仿真页签·时间轴·执行控制 | 3a（∥T14） | T13 | **进行中（已迁 wt-t15；wip `6934691` 钉底＝5改1增，290 例收集零 import 错）** | task/T15（worktree `多功能机械臂软件-wt-t15`） | — |
| T16 | 干涉体系对齐 | 3b（∥T17） | T13＋T15 | 待派 | — | — |
| T17 | PLC 输出区与预览模态 | 3b（∥T16） | T13＋T14 | 待派 | — | — |

## 文件归属矩阵（唯一写者裁决，2026-09-18 复核）

> 铁律：一个文件在一波内只有一个写者。**波次 3 两单并行的前提是本表无交集**；有交集的按「前置」列串行。
> 新增源文件名一律在此**预声明**（00 README §五），执行会话不得自造文件名。

| 文件 | 唯一写者 | 约束与备注 |
|---|---|---|
| `app/shell.py` | T12 → T13（**串行**） | T12 后实况 **281/300**（原目标 ≤约220 未达：多页宿主新增与抽件在同件相抵，收单判可收——硬顶未破、T02–T10 已验收槽不搬迁）。T13 改装配以 **`app/wiring.py`（129/300）为主战场**（只可改装配顺序相关行，禁改函数签名）；shell.py 侧范式切换以删为主（StepBar 摘出布局等），净增行预算 ≤19，不够即停下上报 |
| `app/wiring.py` | T12（新增）→ T14 补接线（收单追认） | 由 `app/shell.py` 抽出的控制器构造／信号连接；T13 只可改与装配顺序相关的行，禁改函数签名。T14 实况 **202/300**：`install_t14(win)` 换掉 T13 占位弹窗（pop_import 无其他合法落点，先例 `sendctl.install_send_flow`）＋`_apply_scene` 真值先行修正（e2e 实测竞态）——12 行越卡胶水，收单追认登记 |
| `app/theme.py` | T11 | 179/300。T12/T13 **禁改**（含 docstring `:7–8` 的旧口径行，由 T11 重写） |
| `view/css/app.css`、`view/js/loader.js` | T11 | 视口 `--fs` 推送所需的 CSS 变量入口在 T11 落地 |
| `app/splash.py`、`app/stage.py`、`config/ui.yaml`、`core/config/` 内 ui 加载件、`tests/test_ui_config.py` | T12（新增） | ui.yaml 键表见 01 蓝图 §3.1／§3.7／§6.3。⚠️ 新增键须**同步** `core/config/schema.py` 与 `tools/lint_no_magic.py`（白名单校验器会静默丢弃未登记键） |
| `app/statusbar.py` | T12 | 只加水印位。`FreqBadge`(`:29`)／`set_freq`(`:46`) 是 T10 已收单件，**禁删禁改签名**；版本文字(`:15`)硬编码须迁入 ui.yaml |
| `run.py` | T12 | T10 已授权改此件，本单沿用并在此登记 |
| `app/stepbar.py` | T13（**改造，不 git rm**） | 退役为薄壳，详见 L-7 |
| `app/topbar.py`、`app/tabshell.py`、`app/prog_tab.py`、`app/sim_tab.py`、`app/workhead_panel.py`、`app/joints_panel.py`、`app/chips.py` | T13（新增） | T14/T15 重写 `prog_tab.py`／`sim_tab.py` 内部即可换体，**不回改 `tabshell.py`** |
| `app/mode.py`、`app/assemblytree.py`、`app/panel.py` | T13 | — |
| `core/geometry/**` | T13（**只增纯函数**） | 供装配树工具行「删除选中／删除整组」用。禁改 T05/T06 已收单行为与签名（01 蓝图 §3.4） |
| `tools/e2e_smoke.py`、`tools/e2e_rig.py` | 同卡内独占；跨卡按收单顺序串行合入 | **296/300、201/300**；smoke **冻结拼接**（余 4 行留指挥侧应急，不再受理判据）——T14 已按卡面「跨卡串行合入」把 e2e_tabs 的 T9–T12 接进 PLAN（改 2 行、净增 0，收单追认），全量 **31 判据**；rig 波次 3a 内归 T15 可小改（T14 不涉），3b 起恢复跨卡串行 |
| `tools/e2e_tabs.py` | T13（建）→ T14 写满 | **300/300 冻结**（T1–T8＋T9–T12 在册），不再受理任何追加（2026-09-19） |
| `tools/e2e_sim.py` | T15（新增，2026-09-19 预授权） | 仿真/执行控制判据 standalone 件：自起 Rig＋本机模拟器（仿 `evidence/T13/_shots_online.py`），复用 `e2e_rig.Rig` 只 import 不改；≤300 |
| `tools/e2e_collision.py` | T16（新增，预声明） | 干涉判据 standalone 件，同 e2e_sim 模式；≤300 |
| `tools/e2e_plc.py` | T17（新增，预声明） | PLC 输出判据 standalone 件，同模式；≤300 |
| `app/steps/step1_import.py`／`step2_pick.py`／`step3_path.py` | T14 | **已收单**：`STEP_LABELS` 引用已清（L-7 实绩）；step1 183／step2 271／step3 254 |
| `app/steps/step5_send.py` | T15 | 259/300；清 `STEP_LABELS`(`:30,64`)＋执行控制组化 |
| `app/steps/step4_check.py` | T16（**保留，不删**） | 188/300；三态卡被 `sim_tab`／模态／KPI 吸收后此件按**薄适配**处理，⛔ 旧卡「删除」一句作废（`app/checkctl.py` 等仍引它）；清 `STEP_LABELS`(`:31,67`)；收单时如实登记 |
| `comm/opcua_client/handshake.py` | T15 | 178/300。只增 `pause()`／`resume()`／`_await_bits_cleared()` 三件（约 25 行内，命令字**必须 finally 清零**）；**禁改**既有 `stop_motion()`(`:157`)／`reset()`(`:161`)／`_pulse()`(`:96`)／`_await_bits()`(`:76`) 与九步握手（01 蓝图 §6.2） |
| `core/path.py` | T15 | 233/300，余量 67 行；只增 `estimate_duration()` |
| `core/project.py` | —（**299/300，余 1 行**） | ⛔ T14/T17 **只可读**（proj/v1 数据与导出同源）；需加键/加函数先停下上报，未预声明拆件（见 L-8） |
| `core/collision.py` | —（**已满 300/300**） | ⛔ T16 禁在此件加任何行 |
| `core/collision_report.py` | T16（新增） | 承接干涉清单／报告聚合，替代在 `core/collision.py` 内扩写 |
| `app/checkctl.py` | T16 | 264/300，余量 36 行；`:35 from app.steps.step4_check import Step4Pane` ⇒ **删 step4_check 会当场断本件与 `app/panel.py:23`**（后者是 T13 已收单件），故保留 |
| `config/ui.yaml`＋`core/config/schema.py` | T12（建）→ 波次 3b 追加归 **T17** | T17 追加 §6.3 三串声明文案键；**T16 ⛔禁改 `config/**`**（其界面文案属普通 UI 串，不在 §1-8「水印/品牌/登录预填」范围内）。加键须同步 schema 白名单（L-9） |
| `core/process.py`、`app/plc_out.py` | T17（新增） | `core/process.py` 必须 GUI-free（无 PySide6 import），收单核 |
| `view/js/path.js` | T15 | 只可扩展现有 `path.show`／`pose.update` 的 payload，⛔ 禁新增 dispatch type |
| `view/js/collision.js` | T16 | 同上（collision 通道 payload 扩展，收单核）；T15 禁改 |
| `view/js/**`（其余件，`loader.js` 除外） | 冻结 | 桥接 10 类下发**不新增**。视口 `--fs` 走 `app/bridge.py:54` 既有 `runJavaScript` 通道（不是桥接消息 ⇒ 不算新增下发类型）；T13/T14/T17 禁改 `view/js/` |
| `前端任务包/**`、`CR-*.md` | ⛔ 全员禁改 | 只读母本；需改由指挥方走升版 |

## 连带回填清单（指挥方专办，执行会话不得代改）

| # | 事项 | 依据 | 状态 |
|---|---|---|---|
| B-1 | 前端任务包 02 升版 V2.0 | 01 蓝图 §4-B/C（字号 13px、控件高 26/21px、圆角分层 5/4/0/8px）与 02 §4 旧数值直接冲突；另需吸收「五步向导 ⇒ 三页签」「舞台档位」「取证档」 | **已办（2026-09-18）**：02 升 V2.0（范式升版＋硬指标切演示稿基线＋T06/T07/T08/G16 冻结语义逐条保留＋细目授权蓝图、冲突以蓝图为准）；T02–T10 旧形态按其 §6 修订记录被取代 |
| B-2 | 《项目阶段性汇报报告-V1.0》§6.1 七项重新评级 | 01 蓝图 §8「依据更正」：红线 12＝交付前剥离他方数据（🔴 未达）、红线 10＝交付版无使用限制机制（✅ 已达）。改判内容＝把 §6.1 中双视口/可拖分界线/视图预设/测量/报警与日志面板/程序面板 7 种动作/水印烧录截图等 7 项由 ✅ 改「界面已对齐、能力待接入」并升版报告 | 待办（**触发条件＝T14–T17 收口、16(4)①③ 素材切换前**。⚠️ 现阶段不可提前：报告编制纪律「只用源文件已有事实」，对齐未完成前写入「界面已对齐」即违反其自身纪律） |
| B-3 | `design/ui-concepts/` 处置（归档或删除） | ⛔ 禁作令牌来源（01 蓝图 §4-C 末条） | **已办（V1.2，2026-09-18）**：整目录移出仓外归档至 `D:\ZCode项目\design_原型归档_20260918\`（含 ui-concepts 6 件），仓内不再存在同名令牌干扰源 |
| B-4 | `对齐任务包/` 入库 | `design/` 已按 B-3 移出仓外；本包随 V1.2 修订一并提交（中文路径按 `evidence/README.md` 口径以 `-c core.quotepath=false` 核过）。⚠️ 包内文档含招标编号/甲方名称等引用属**工作文档**（仓根既有 docs 同口径），非 `evidence/` 取证物，不触该目录边界 | **已办（V1.2，2026-09-18）** |
| B-5 | ui.yaml 内甲方／投标标识的取值与剥离口径 | 演示稿含「通裕重工股份有限公司／TONGYU HEAVY INDUSTRY CO., LTD.」「招标编号 GXTC-A1-26170051」「本系统仅限本次投标使用」三类标识（HTML:702）；须全走 ui.yaml 且 `--ui-profile evidence` 可一键剥离 | **已办（T12 收单，2026-09-19）**：身份值全部与演示稿逐字一致（指挥侧对原件核对）；取证档八键强制覆盖＋漏键/空值 fail-fast；e2e S3 实判据常驻；普通档对照图（含真实标识）留仓外 `D:\ZCode项目\_t12_普通档对照_仓外留存` |
| B-6 | 01 蓝图 §7 编号 `D-x` → `Δ-x` 的各卡连带引用 | 已随本轮修订同步进 T11–T17 卡面 | 已办 |

## 验收记录（收单时逐单追加）

| 单 | merge commit | 判据输出摘要（指挥侧复跑） | 增件追认 | 连带回填／备注 |
|---|---|---|---|---|
| T11 | `cfe3fdd`（feat `9082609`，2026-09-19） | 三表逐行一致；pytest 258 绿／e2e 16 PASS·0 FAIL（G5 角标两数在场）／config_check rc=0／lint_no_magic rc=0；取证五图目检过（自造模型、无甲方数据） | `tests/test_theme_tokens.py`（预声明✓）＋`evidence/T11/_shots.py`＋五 PNG；零预案外增件 | 无新增连带；css `--fs:1` 入口判不越界（T11 是 css 唯一写者）；「T02」版本号上屏遗留归 T12 迁 ui.yaml（已随 T12 落地）；theme.py docstring 尾注 V2.0 时点由指挥侧一行修正 |
| T12 | `0095f02`（feat `78222ab`，2026-09-19） | 合并态复跑：pytest 273 绿／e2e **19 PASS**（S1–S3 新判据，S3 取证档剥离为实判据）／config rc=0／lint rc=0；六图目检过（带鱼屏登录 16:9＋留边、wide 460/490）；B-5 值全演示稿原文 | splash/stage/wiring/ui.yaml/ui_config/test_ui_config 全预声明✓；evidence/T12/_shots.py＋六 PNG；e2e_rig +4 属矩阵授权；零预案外增件 | **B-5 已办**；普通档对照图留仓外（红线 12）；**偏差：shell.py 281/300 vs 目标 ≤约220 判可收**——T13 主战场 wiring.py（129/300），shell 侧以删为主；e2e_smoke 294/300 ⇒ T13 开工先拆 e2e_tabs.py（L-2） |
| T13 | `e641b15`（feat `00911ab`，2026-09-19） | pytest 283（1 例时敏抖动三重复跑绿）／e2e **27 PASS**（T1–T8 页签导航版；T6 钉死回读 8/22）／config rc=0／lint rc=0；三冻结脚本 rc=0×3（step3 修复后）；04/08 图逐项判过＋全窗离屏直核 | topbar/tabshell/chips/prog_tab/sim_tab/workhead/joints/edit_model/e2e_tabs 九件全预声明✓＋evidence/T13 双脚本九 PNG；e2e_rig+15 属卡面归属；零预案外增件 | **L-7 落地**（stepbar 36 处登记，T14/T15/T16 待清行号在卡）；**L-2 闭环**；四遗留已裁：Δ-12 登记（蓝图 V1.3）、页签 QSS 指挥侧补、真胶囊→L-11 期 3、软限位边界含入→L-12；T12 冻结脚本漏检补记 |
| T14 | `b4d59c5`（feat `dbaf66f`，2026-09-19） | 合并态复跑：pytest **291 绿**／e2e **31 PASS·0 FAIL**（G/H 16＋T1–T8＋T9–T12 首次在册）／config rc=0（machine；ui.yaml 走 load_ui 判据，加载 OK）／lint rc=0（73 件）；三冻结脚本 rc=0×3（复写 PNG 已按冻结纪律还原）；五图目检过（四步真值 1 件/3 点/2 段、干涉 23 处禁发三通道、插入动作组灰禁、浮层占位灰置、取证档占位全） | pop_import／test_prog_export／evidence/T14 五 PNG＋_shots.py 全预声明✓；**预案外增件一件＝`app/wiring.py` 12 行**（install_t14 胶水＋_apply_scene 真值先行，判可收已登记矩阵） | **L-7 实绩：step1–3 已清**；剩余＝shell.py:29,128（T13 既有，本次新登记）＋step4:31,67（T16）＋step5:30,64（T15）；e2e_smoke.txt +96＝T9–T12 随 PLAN 合入的配套实测输出（非越界） |

## 遗留与风险登记

| # | 事项 | 影响 | 状态 |
|---|---|---|---|
| L-1 | 前端任务包 02 **已升版 V2.0**（2026-09-18，见 B-1） | 两份布局文档并存的冲突已收口：02 V2.0＝纲要、01 蓝图 V1.2＝细目权威（02 §0 联动声明），冲突以蓝图为准并回报指挥方收敛。对齐单 T11–T17 依据不变 | **已闭环** |
| L-2 | `tools/e2e_smoke.py`(245/300)＋`tools/e2e_rig.py`(190/300) 在波次 3 两单并行时既冲突又**余量不足** | 判据追加按收单顺序串行合入（00 README §五）；**预授权**把页签导航判据拆到新文件 `tools/e2e_tabs.py`，任何一单可先拆、拆后各单只追加本单判据；tabshell/topbar 无并行冲突 | 规则已立＋拆件已预授权 → **已闭环（T13 收单）→ 波次 3a 实况续裁（2026-09-19）**：T14 将 e2e_tabs 写满（300/300，T9–T12 在册）⇒ e2e_tabs **冻结**、e2e_smoke 296/300 **冻结拼接**；T15/T16/T17 判据各落**独立新件**（e2e_sim／e2e_collision／e2e_plc，已预授权并改写三卡）——「共享 e2e 文件串行」以**每单一件**根治 |
| L-3 | 期 3 备忘清单未派单（01 蓝图 §8） | 测量/报警面板/装配体模式/插入动作/关节绑定真行为(Δ-9)/使能按钮接真数据(Δ-11) 等 | 投标后 |
| L-4 | 16(4)①③ 截图与视频重录（对齐完成后） | 三场景截图＋≥3min 视频须从对齐后产品录制；**一律用取证档** `--ui-profile evidence` 启动（裁决 7），否则画面带甲方/投标标识即触红线 12 | 待 T14–T17 合并后 |
| L-5 | 双视口＋分界线（演示稿默认形态）列**期 2.5 备选**（裁决 4，Δ-3） | 依赖「冻结态 WebEngine 单进程」追认（T10 待追认项）；未追认前不得开双视口，期 2 一律单视口。**验证法（V1.2 补）**：若决定提前，先在 dev 下临时并置两个 `QWebEngineView` 跑通 e2e（渲染＋桥 echo 双份），凭输出由指挥方追认后再派单 | 待追认 |
| L-6 | 《汇报报告》§6.1 七项评级改判（红线 10/12 引反的连带修正） | 见 B-2（触发条件＝T14–T17 收口、素材切换前）；对外口径以【汇报报告】为准，故须先改报告再对外 | 待办（触发条件已钉死，见 B-2） |
| L-7 | **StepBar 退役方式由「git rm」改为「薄壳保留」** | `git rm app/stepbar.py` 会当场断掉 T13 并不拥有的 8 个源文件（`app/checkctl.py`、`app/pathctl.py`、`app/sendctl.py`＋`app/steps/step1–5`）、`tools/README.md`，以及 3 个**已冻结**取证脚本（`evidence/T07/_drive_step3.py`、`evidence/T07/_drive_shell_path.py`、`evidence/T08/_rig.py`）。处置：T13 保留 `STEP_LABELS` 与 `StepBar` 类，`self.stepbar` 仍实例化但**不加入任何布局**（不再显示），使 `mark_completed()`／`step_clicked.emit(n)`／`stepbar._completed[4]` 全部照常；step1–5 的 `STEP_LABELS` 引用由 T14/T15/T16 各自清理，**最后清理者（或指挥方）删件**。T13「grep 0 命中」判据据此改写 | **已落地（T13 收单 2026-09-19）**：五判据全过（不可见/可 import/依赖件零改/三冻结脚本 rc0/36 处登记）；清理进度：**T14＝step1–3 已清✓（2026-09-19 收单）**；待清理：T15＝step5:30,64，T16＝step4:31,67，另有 **shell.py:29,128（T13 既有，T14 收单时新登记）**；**最后清理者（或指挥方）删件** |
| L-8 | `app/shell.py`、`core/collision.py` 均 **300/300 零余量**；`core/project.py` **299/300 只剩 1 行** | 任何加行都会破硬约束。已按文件归属矩阵预声明 `app/wiring.py`(T12)、`core/collision_report.py`(T16) 承接；`core/project.py` **未预声明拆件** ⇒ T14/T17 只可读，需加函数先停下上报由指挥方裁。执行会话开工前先 `wc -l` 报数，超线即停下，不得自行拆件 | 拆件已预授权（project.py 除外） |
| L-9 | ui.yaml 新增键会被白名单校验器**静默丢弃** | 加键必须同步改 `core/config/schema.py` 与 `tools/lint_no_magic.py`，否则「配置已写但界面读不到」且不报错（G17 同类教训） | 规则已立 |
| L-10 | 顶栏「使能按钮」无真数据源 | 点表无 `enable_mask`（有意缺），CR-2026-03 §三-5 悬置 ⇒ 裁决为**渲染但恒禁用＋提示「待契约回执」**（Δ-11）；⛔ 不得拿 `status` 位（ST_READY 等）推导「使能」 | 已裁，待契约回执 |
| L-11 | 页签/报警/导入徽标**真胶囊样式**（按钮内嵌子件重构，r_pill/r_tab_badge 令牌已在册未用） | T13 时 theme.py 冻结做不了；页签按钮 state 样式已由指挥侧 2026-09-19 补 QSS，真胶囊需重构 `tabshell.py`/`topbar.py`（均已收口冻结）⇒ 归期 3 统一处置 | 期 3（T13 收单裁决③） |
| L-12 | 在线态软限位行显示「8 个轴已达行程设定值」＝模拟器零位 0.0 恰在**占位行程下界**的边界含入计数 | 如实呈现不算错（数据源就是占位值）；**回执填真行程后自然消解**。⚠️ 对外素材（16(4)①③ 截图/视频）录制时该黄条会在场——录前与指挥确认口径（保留如实提示或注素材说明） | 已登记（T13 收单裁决④）；随契约回执消解 |
| L-13 | **波次 3a 两会话同写主树**：T14／T15 均未按 §四开 worktree，两会话未提交改动混在同一工作区（仅靠文件集不相交未出事；e2e_tabs 差点互踩） | T15 会话自查上报（予以记载）。裁决：①T15 整体迁 `wt-t15`（指挥侧已建树），T14 留主树收尾；②T14 提交用**显式路径**禁 `add -A`，直至主树 status 中 T15 五件消失；③**规则加严**：并行波次派单时指挥侧先建 worktree 并在派单消息给路径（README §六已改），执行会话开工第一步 `git branch --show-current` 自查 | 已裁并执行（2026-09-19；T14 显式路径提交核过✓，T15 搬移由指挥侧代执行：patch 799 行落 wt-t15 钉 `6934691`、主树已还原，双树各归各位） |
